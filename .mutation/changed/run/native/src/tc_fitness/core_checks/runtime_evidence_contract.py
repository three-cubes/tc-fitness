"""CORE check for digest-bound, independently identified runtime evidence."""

from __future__ import annotations

import hashlib
import re
from collections.abc import Mapping, Sequence
from datetime import UTC, datetime
from pathlib import Path, PurePosixPath
from typing import Any, cast

from tc_fitness.check_evidence import report_finding
from tc_fitness.core_checks import run_core_check
from tc_fitness.core_checks._runtime_contracts import (
    ContractDocuments,
    ContractFinding,
    RuntimeContractRule,
    canonical_json_bytes,
    is_integer_identity,
    is_sha256_digest,
    render_findings,
    sort_findings,
)
from tc_fitness.lib import remediation as _remediation

_SHA256_RE = re.compile(r"sha256:[0-9a-f]{64}\Z")
_SOURCE_SHA_RE = re.compile(r"[0-9a-f]{40}(?:[0-9a-f]{24})?\Z")
_BASE_IDENTITY_FIELDS = ("source_sha", "image_digest", "host_id", "runtime_user")
_STRING_RECEIPT_IDENTITIES = ("deployment_id", "configuration_identity")
_INTEGER_RECEIPT_IDENTITIES = ("run_id", "attempt_id")
_IDENTITY_FIELDS = (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES, *_INTEGER_RECEIPT_IDENTITIES)
_CHECK_STATUSES = frozenset({"passed", "failed", "skipped", "expected-denial"})
_FAILURE_OBSERVATIONS = frozenset({"denied", "error", "failed", "failure", "false", "rejected", "unhealthy"})

REMEDIATION = _remediation(
    fix=(
        "collect a complete runtime receipt for the exact selected contract, source commit, immutable "
        "artifact, host and runtime user; retain and hash every referenced diagnostic artifact"
    ),
    nxt="re-run evidence verification with independently supplied expected identity values",
    run="tc-fitness-runtime-contract verify-evidence --help",
    passing="fresh evidence with matching identities, observed required checks and verified artifact bytes",
    forbidden="a stale, skipped, exit-code-only or identity-mismatched receipt",
)


from mutmut.mutation.trampoline import wrap_in_trampoline as _mutmut_mutated, MutantDict
mutants_x__finding__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__finding__mutmut)
def _finding(source: Path, pointer: str, code: str, message: str, fix: str) -> ContractFinding:
    return ContractFinding(source=source, pointer=pointer, code=code, message=message, fix=fix)


def x__finding__mutmut_orig(source: Path, pointer: str, code: str, message: str, fix: str) -> ContractFinding:
    return ContractFinding(source=source, pointer=pointer, code=code, message=message, fix=fix)


def x__finding__mutmut_1(source: Path, pointer: str, code: str, message: str, fix: str) -> ContractFinding:
    return ContractFinding(source=None, pointer=pointer, code=code, message=message, fix=fix)


def x__finding__mutmut_2(source: Path, pointer: str, code: str, message: str, fix: str) -> ContractFinding:
    return ContractFinding(source=source, pointer=None, code=code, message=message, fix=fix)


def x__finding__mutmut_3(source: Path, pointer: str, code: str, message: str, fix: str) -> ContractFinding:
    return ContractFinding(source=source, pointer=pointer, code=None, message=message, fix=fix)


def x__finding__mutmut_4(source: Path, pointer: str, code: str, message: str, fix: str) -> ContractFinding:
    return ContractFinding(source=source, pointer=pointer, code=code, message=None, fix=fix)


def x__finding__mutmut_5(source: Path, pointer: str, code: str, message: str, fix: str) -> ContractFinding:
    return ContractFinding(source=source, pointer=pointer, code=code, message=message, fix=None)


def x__finding__mutmut_6(source: Path, pointer: str, code: str, message: str, fix: str) -> ContractFinding:
    return ContractFinding(pointer=pointer, code=code, message=message, fix=fix)


def x__finding__mutmut_7(source: Path, pointer: str, code: str, message: str, fix: str) -> ContractFinding:
    return ContractFinding(source=source, code=code, message=message, fix=fix)


def x__finding__mutmut_8(source: Path, pointer: str, code: str, message: str, fix: str) -> ContractFinding:
    return ContractFinding(source=source, pointer=pointer, message=message, fix=fix)


def x__finding__mutmut_9(source: Path, pointer: str, code: str, message: str, fix: str) -> ContractFinding:
    return ContractFinding(source=source, pointer=pointer, code=code, fix=fix)


def x__finding__mutmut_10(source: Path, pointer: str, code: str, message: str, fix: str) -> ContractFinding:
    return ContractFinding(source=source, pointer=pointer, code=code, message=message, )

mutants_x__finding__mutmut['_mutmut_orig'] = x__finding__mutmut_orig # type: ignore # mutmut generated
mutants_x__finding__mutmut['x__finding__mutmut_1'] = x__finding__mutmut_1 # type: ignore # mutmut generated
mutants_x__finding__mutmut['x__finding__mutmut_2'] = x__finding__mutmut_2 # type: ignore # mutmut generated
mutants_x__finding__mutmut['x__finding__mutmut_3'] = x__finding__mutmut_3 # type: ignore # mutmut generated
mutants_x__finding__mutmut['x__finding__mutmut_4'] = x__finding__mutmut_4 # type: ignore # mutmut generated
mutants_x__finding__mutmut['x__finding__mutmut_5'] = x__finding__mutmut_5 # type: ignore # mutmut generated
mutants_x__finding__mutmut['x__finding__mutmut_6'] = x__finding__mutmut_6 # type: ignore # mutmut generated
mutants_x__finding__mutmut['x__finding__mutmut_7'] = x__finding__mutmut_7 # type: ignore # mutmut generated
mutants_x__finding__mutmut['x__finding__mutmut_8'] = x__finding__mutmut_8 # type: ignore # mutmut generated
mutants_x__finding__mutmut['x__finding__mutmut_9'] = x__finding__mutmut_9 # type: ignore # mutmut generated
mutants_x__finding__mutmut['x__finding__mutmut_10'] = x__finding__mutmut_10 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__expected_identity_findings__mutmut)
def _expected_identity_findings(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_orig(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_1(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = None
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_2(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = None
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_3(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(None)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_4(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) and not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_5(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_6(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_7(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                None
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_8(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    None,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_9(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    None,
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_10(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    None,
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_11(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    None,
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_12(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    None,
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_13(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_14(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_15(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_16(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_17(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_18(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "XXmissing-expected-identityXX",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_19(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "MISSING-EXPECTED-IDENTITY",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_20(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = None
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_21(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(None)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_22(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_23(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(None):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_24(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                None
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_25(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    None,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_26(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    None,
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_27(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    None,
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_28(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    None,
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_29(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    None,
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_30(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_31(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_32(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_33(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_34(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_35(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "XXinvalid-expected-identityXX",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_36(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "INVALID-EXPECTED-IDENTITY",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_37(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = None
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_38(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get(None)
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_39(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("XXsource_shaXX")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_40(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("SOURCE_SHA")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_41(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha or _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_42(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) or source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_43(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(None) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_44(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is not None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_45(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            None
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_46(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                None,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_47(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                None,
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_48(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                None,
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_49(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                None,
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_50(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                None,
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_51(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_52(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_53(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_54(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_55(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_56(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "XX/expected_identity/source_shaXX",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_57(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/EXPECTED_IDENTITY/SOURCE_SHA",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_58(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "XXinvalid-source-shaXX",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_59(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "INVALID-SOURCE-SHA",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_60(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "XXexpected source SHA must be 40 or 64 lowercase hexadecimal charactersXX",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_61(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source sha must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_62(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "EXPECTED SOURCE SHA MUST BE 40 OR 64 LOWERCASE HEXADECIMAL CHARACTERS",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_63(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "XXsupply the full lowercase source commit SHAXX",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_64(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit sha",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_65(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "SUPPLY THE FULL LOWERCASE SOURCE COMMIT SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_66(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = None
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_67(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get(None)
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_68(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("XXimage_digestXX")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_69(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("IMAGE_DIGEST")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_70(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest or _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_71(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) or image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_72(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(None) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_73(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is not None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_74(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            None
        )
    return findings


def x__expected_identity_findings__mutmut_75(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                None,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_76(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                None,
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_77(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                None,
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_78(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                None,
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_79(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                None,
            )
        )
    return findings


def x__expected_identity_findings__mutmut_80(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_81(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_82(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_83(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_84(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                )
        )
    return findings


def x__expected_identity_findings__mutmut_85(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "XX/expected_identity/image_digestXX",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_86(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/EXPECTED_IDENTITY/IMAGE_DIGEST",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_87(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "XXinvalid-image-digestXX",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_88(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "INVALID-IMAGE-DIGEST",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_89(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "XXexpected image digest must use sha256 followed by 64 lowercase hexadecimal charactersXX",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_90(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "EXPECTED IMAGE DIGEST MUST USE SHA256 FOLLOWED BY 64 LOWERCASE HEXADECIMAL CHARACTERS",
                "supply the immutable sha256 image or artifact digest",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_91(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "XXsupply the immutable sha256 image or artifact digestXX",
            )
        )
    return findings


def x__expected_identity_findings__mutmut_92(
    expected_identity: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = expected_identity.get(field)
        if not isinstance(value, str) or not value.strip():
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "missing-expected-identity",
                    f"independent expected {field} is required",
                    f"supply expected {field} outside the evidence document",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = expected_identity.get(field)
        if not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/expected_identity/{field}",
                    "invalid-expected-identity",
                    f"independent expected {field} must be a non-negative integer, not a boolean",
                    f"supply expected {field} outside the evidence document as an integer",
                )
            )
    source_sha = expected_identity.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/source_sha",
                "invalid-source-sha",
                "expected source SHA must be 40 or 64 lowercase hexadecimal characters",
                "supply the full lowercase source commit SHA",
            )
        )
    image_digest = expected_identity.get("image_digest")
    if isinstance(image_digest, str) and image_digest and _SHA256_RE.fullmatch(image_digest) is None:
        findings.append(
            _finding(
                source,
                "/expected_identity/image_digest",
                "invalid-image-digest",
                "expected image digest must use sha256 followed by 64 lowercase hexadecimal characters",
                "SUPPLY THE IMMUTABLE SHA256 IMAGE OR ARTIFACT DIGEST",
            )
        )
    return findings

mutants_x__expected_identity_findings__mutmut['_mutmut_orig'] = x__expected_identity_findings__mutmut_orig # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_1'] = x__expected_identity_findings__mutmut_1 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_2'] = x__expected_identity_findings__mutmut_2 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_3'] = x__expected_identity_findings__mutmut_3 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_4'] = x__expected_identity_findings__mutmut_4 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_5'] = x__expected_identity_findings__mutmut_5 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_6'] = x__expected_identity_findings__mutmut_6 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_7'] = x__expected_identity_findings__mutmut_7 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_8'] = x__expected_identity_findings__mutmut_8 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_9'] = x__expected_identity_findings__mutmut_9 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_10'] = x__expected_identity_findings__mutmut_10 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_11'] = x__expected_identity_findings__mutmut_11 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_12'] = x__expected_identity_findings__mutmut_12 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_13'] = x__expected_identity_findings__mutmut_13 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_14'] = x__expected_identity_findings__mutmut_14 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_15'] = x__expected_identity_findings__mutmut_15 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_16'] = x__expected_identity_findings__mutmut_16 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_17'] = x__expected_identity_findings__mutmut_17 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_18'] = x__expected_identity_findings__mutmut_18 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_19'] = x__expected_identity_findings__mutmut_19 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_20'] = x__expected_identity_findings__mutmut_20 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_21'] = x__expected_identity_findings__mutmut_21 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_22'] = x__expected_identity_findings__mutmut_22 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_23'] = x__expected_identity_findings__mutmut_23 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_24'] = x__expected_identity_findings__mutmut_24 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_25'] = x__expected_identity_findings__mutmut_25 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_26'] = x__expected_identity_findings__mutmut_26 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_27'] = x__expected_identity_findings__mutmut_27 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_28'] = x__expected_identity_findings__mutmut_28 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_29'] = x__expected_identity_findings__mutmut_29 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_30'] = x__expected_identity_findings__mutmut_30 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_31'] = x__expected_identity_findings__mutmut_31 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_32'] = x__expected_identity_findings__mutmut_32 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_33'] = x__expected_identity_findings__mutmut_33 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_34'] = x__expected_identity_findings__mutmut_34 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_35'] = x__expected_identity_findings__mutmut_35 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_36'] = x__expected_identity_findings__mutmut_36 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_37'] = x__expected_identity_findings__mutmut_37 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_38'] = x__expected_identity_findings__mutmut_38 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_39'] = x__expected_identity_findings__mutmut_39 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_40'] = x__expected_identity_findings__mutmut_40 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_41'] = x__expected_identity_findings__mutmut_41 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_42'] = x__expected_identity_findings__mutmut_42 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_43'] = x__expected_identity_findings__mutmut_43 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_44'] = x__expected_identity_findings__mutmut_44 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_45'] = x__expected_identity_findings__mutmut_45 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_46'] = x__expected_identity_findings__mutmut_46 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_47'] = x__expected_identity_findings__mutmut_47 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_48'] = x__expected_identity_findings__mutmut_48 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_49'] = x__expected_identity_findings__mutmut_49 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_50'] = x__expected_identity_findings__mutmut_50 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_51'] = x__expected_identity_findings__mutmut_51 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_52'] = x__expected_identity_findings__mutmut_52 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_53'] = x__expected_identity_findings__mutmut_53 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_54'] = x__expected_identity_findings__mutmut_54 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_55'] = x__expected_identity_findings__mutmut_55 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_56'] = x__expected_identity_findings__mutmut_56 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_57'] = x__expected_identity_findings__mutmut_57 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_58'] = x__expected_identity_findings__mutmut_58 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_59'] = x__expected_identity_findings__mutmut_59 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_60'] = x__expected_identity_findings__mutmut_60 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_61'] = x__expected_identity_findings__mutmut_61 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_62'] = x__expected_identity_findings__mutmut_62 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_63'] = x__expected_identity_findings__mutmut_63 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_64'] = x__expected_identity_findings__mutmut_64 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_65'] = x__expected_identity_findings__mutmut_65 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_66'] = x__expected_identity_findings__mutmut_66 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_67'] = x__expected_identity_findings__mutmut_67 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_68'] = x__expected_identity_findings__mutmut_68 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_69'] = x__expected_identity_findings__mutmut_69 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_70'] = x__expected_identity_findings__mutmut_70 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_71'] = x__expected_identity_findings__mutmut_71 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_72'] = x__expected_identity_findings__mutmut_72 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_73'] = x__expected_identity_findings__mutmut_73 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_74'] = x__expected_identity_findings__mutmut_74 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_75'] = x__expected_identity_findings__mutmut_75 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_76'] = x__expected_identity_findings__mutmut_76 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_77'] = x__expected_identity_findings__mutmut_77 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_78'] = x__expected_identity_findings__mutmut_78 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_79'] = x__expected_identity_findings__mutmut_79 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_80'] = x__expected_identity_findings__mutmut_80 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_81'] = x__expected_identity_findings__mutmut_81 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_82'] = x__expected_identity_findings__mutmut_82 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_83'] = x__expected_identity_findings__mutmut_83 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_84'] = x__expected_identity_findings__mutmut_84 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_85'] = x__expected_identity_findings__mutmut_85 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_86'] = x__expected_identity_findings__mutmut_86 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_87'] = x__expected_identity_findings__mutmut_87 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_88'] = x__expected_identity_findings__mutmut_88 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_89'] = x__expected_identity_findings__mutmut_89 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_90'] = x__expected_identity_findings__mutmut_90 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_91'] = x__expected_identity_findings__mutmut_91 # type: ignore # mutmut generated
mutants_x__expected_identity_findings__mutmut['x__expected_identity_findings__mutmut_92'] = x__expected_identity_findings__mutmut_92 # type: ignore # mutmut generated
mutants_x__identity_findings__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__identity_findings__mutmut)
def _identity_findings(
    evidence: Mapping[str, object],
    *,
    expected_identity: Mapping[str, object],
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        expected = expected_identity.get(field)
        actual = evidence.get(field)
        expected_is_valid = (
            isinstance(expected, str) and bool(expected.strip())
            if field not in _INTEGER_RECEIPT_IDENTITIES
            else is_integer_identity(expected)
        )
        if expected_is_valid and actual != expected:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    f"{field.replace('_', '-')}-mismatch",
                    f"evidence {field} does not match the independently expected value",
                    f"collect evidence from the expected {field}",
                )
            )
    return findings


def x__identity_findings__mutmut_orig(
    evidence: Mapping[str, object],
    *,
    expected_identity: Mapping[str, object],
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        expected = expected_identity.get(field)
        actual = evidence.get(field)
        expected_is_valid = (
            isinstance(expected, str) and bool(expected.strip())
            if field not in _INTEGER_RECEIPT_IDENTITIES
            else is_integer_identity(expected)
        )
        if expected_is_valid and actual != expected:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    f"{field.replace('_', '-')}-mismatch",
                    f"evidence {field} does not match the independently expected value",
                    f"collect evidence from the expected {field}",
                )
            )
    return findings


def x__identity_findings__mutmut_1(
    evidence: Mapping[str, object],
    *,
    expected_identity: Mapping[str, object],
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = None
    for field in _IDENTITY_FIELDS:
        expected = expected_identity.get(field)
        actual = evidence.get(field)
        expected_is_valid = (
            isinstance(expected, str) and bool(expected.strip())
            if field not in _INTEGER_RECEIPT_IDENTITIES
            else is_integer_identity(expected)
        )
        if expected_is_valid and actual != expected:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    f"{field.replace('_', '-')}-mismatch",
                    f"evidence {field} does not match the independently expected value",
                    f"collect evidence from the expected {field}",
                )
            )
    return findings


def x__identity_findings__mutmut_2(
    evidence: Mapping[str, object],
    *,
    expected_identity: Mapping[str, object],
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        expected = None
        actual = evidence.get(field)
        expected_is_valid = (
            isinstance(expected, str) and bool(expected.strip())
            if field not in _INTEGER_RECEIPT_IDENTITIES
            else is_integer_identity(expected)
        )
        if expected_is_valid and actual != expected:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    f"{field.replace('_', '-')}-mismatch",
                    f"evidence {field} does not match the independently expected value",
                    f"collect evidence from the expected {field}",
                )
            )
    return findings


def x__identity_findings__mutmut_3(
    evidence: Mapping[str, object],
    *,
    expected_identity: Mapping[str, object],
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        expected = expected_identity.get(None)
        actual = evidence.get(field)
        expected_is_valid = (
            isinstance(expected, str) and bool(expected.strip())
            if field not in _INTEGER_RECEIPT_IDENTITIES
            else is_integer_identity(expected)
        )
        if expected_is_valid and actual != expected:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    f"{field.replace('_', '-')}-mismatch",
                    f"evidence {field} does not match the independently expected value",
                    f"collect evidence from the expected {field}",
                )
            )
    return findings


def x__identity_findings__mutmut_4(
    evidence: Mapping[str, object],
    *,
    expected_identity: Mapping[str, object],
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        expected = expected_identity.get(field)
        actual = None
        expected_is_valid = (
            isinstance(expected, str) and bool(expected.strip())
            if field not in _INTEGER_RECEIPT_IDENTITIES
            else is_integer_identity(expected)
        )
        if expected_is_valid and actual != expected:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    f"{field.replace('_', '-')}-mismatch",
                    f"evidence {field} does not match the independently expected value",
                    f"collect evidence from the expected {field}",
                )
            )
    return findings


def x__identity_findings__mutmut_5(
    evidence: Mapping[str, object],
    *,
    expected_identity: Mapping[str, object],
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        expected = expected_identity.get(field)
        actual = evidence.get(None)
        expected_is_valid = (
            isinstance(expected, str) and bool(expected.strip())
            if field not in _INTEGER_RECEIPT_IDENTITIES
            else is_integer_identity(expected)
        )
        if expected_is_valid and actual != expected:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    f"{field.replace('_', '-')}-mismatch",
                    f"evidence {field} does not match the independently expected value",
                    f"collect evidence from the expected {field}",
                )
            )
    return findings


def x__identity_findings__mutmut_6(
    evidence: Mapping[str, object],
    *,
    expected_identity: Mapping[str, object],
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        expected = expected_identity.get(field)
        actual = evidence.get(field)
        expected_is_valid = None
        if expected_is_valid and actual != expected:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    f"{field.replace('_', '-')}-mismatch",
                    f"evidence {field} does not match the independently expected value",
                    f"collect evidence from the expected {field}",
                )
            )
    return findings


def x__identity_findings__mutmut_7(
    evidence: Mapping[str, object],
    *,
    expected_identity: Mapping[str, object],
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        expected = expected_identity.get(field)
        actual = evidence.get(field)
        expected_is_valid = (
            isinstance(expected, str) or bool(expected.strip())
            if field not in _INTEGER_RECEIPT_IDENTITIES
            else is_integer_identity(expected)
        )
        if expected_is_valid and actual != expected:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    f"{field.replace('_', '-')}-mismatch",
                    f"evidence {field} does not match the independently expected value",
                    f"collect evidence from the expected {field}",
                )
            )
    return findings


def x__identity_findings__mutmut_8(
    evidence: Mapping[str, object],
    *,
    expected_identity: Mapping[str, object],
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        expected = expected_identity.get(field)
        actual = evidence.get(field)
        expected_is_valid = (
            isinstance(expected, str) and bool(None)
            if field not in _INTEGER_RECEIPT_IDENTITIES
            else is_integer_identity(expected)
        )
        if expected_is_valid and actual != expected:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    f"{field.replace('_', '-')}-mismatch",
                    f"evidence {field} does not match the independently expected value",
                    f"collect evidence from the expected {field}",
                )
            )
    return findings


def x__identity_findings__mutmut_9(
    evidence: Mapping[str, object],
    *,
    expected_identity: Mapping[str, object],
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        expected = expected_identity.get(field)
        actual = evidence.get(field)
        expected_is_valid = (
            isinstance(expected, str) and bool(expected.strip())
            if field in _INTEGER_RECEIPT_IDENTITIES
            else is_integer_identity(expected)
        )
        if expected_is_valid and actual != expected:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    f"{field.replace('_', '-')}-mismatch",
                    f"evidence {field} does not match the independently expected value",
                    f"collect evidence from the expected {field}",
                )
            )
    return findings


def x__identity_findings__mutmut_10(
    evidence: Mapping[str, object],
    *,
    expected_identity: Mapping[str, object],
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        expected = expected_identity.get(field)
        actual = evidence.get(field)
        expected_is_valid = (
            isinstance(expected, str) and bool(expected.strip())
            if field not in _INTEGER_RECEIPT_IDENTITIES
            else is_integer_identity(None)
        )
        if expected_is_valid and actual != expected:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    f"{field.replace('_', '-')}-mismatch",
                    f"evidence {field} does not match the independently expected value",
                    f"collect evidence from the expected {field}",
                )
            )
    return findings


def x__identity_findings__mutmut_11(
    evidence: Mapping[str, object],
    *,
    expected_identity: Mapping[str, object],
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        expected = expected_identity.get(field)
        actual = evidence.get(field)
        expected_is_valid = (
            isinstance(expected, str) and bool(expected.strip())
            if field not in _INTEGER_RECEIPT_IDENTITIES
            else is_integer_identity(expected)
        )
        if expected_is_valid or actual != expected:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    f"{field.replace('_', '-')}-mismatch",
                    f"evidence {field} does not match the independently expected value",
                    f"collect evidence from the expected {field}",
                )
            )
    return findings


def x__identity_findings__mutmut_12(
    evidence: Mapping[str, object],
    *,
    expected_identity: Mapping[str, object],
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        expected = expected_identity.get(field)
        actual = evidence.get(field)
        expected_is_valid = (
            isinstance(expected, str) and bool(expected.strip())
            if field not in _INTEGER_RECEIPT_IDENTITIES
            else is_integer_identity(expected)
        )
        if expected_is_valid and actual == expected:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    f"{field.replace('_', '-')}-mismatch",
                    f"evidence {field} does not match the independently expected value",
                    f"collect evidence from the expected {field}",
                )
            )
    return findings


def x__identity_findings__mutmut_13(
    evidence: Mapping[str, object],
    *,
    expected_identity: Mapping[str, object],
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        expected = expected_identity.get(field)
        actual = evidence.get(field)
        expected_is_valid = (
            isinstance(expected, str) and bool(expected.strip())
            if field not in _INTEGER_RECEIPT_IDENTITIES
            else is_integer_identity(expected)
        )
        if expected_is_valid and actual != expected:
            findings.append(
                None
            )
    return findings


def x__identity_findings__mutmut_14(
    evidence: Mapping[str, object],
    *,
    expected_identity: Mapping[str, object],
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        expected = expected_identity.get(field)
        actual = evidence.get(field)
        expected_is_valid = (
            isinstance(expected, str) and bool(expected.strip())
            if field not in _INTEGER_RECEIPT_IDENTITIES
            else is_integer_identity(expected)
        )
        if expected_is_valid and actual != expected:
            findings.append(
                _finding(
                    None,
                    f"/{field}",
                    f"{field.replace('_', '-')}-mismatch",
                    f"evidence {field} does not match the independently expected value",
                    f"collect evidence from the expected {field}",
                )
            )
    return findings


def x__identity_findings__mutmut_15(
    evidence: Mapping[str, object],
    *,
    expected_identity: Mapping[str, object],
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        expected = expected_identity.get(field)
        actual = evidence.get(field)
        expected_is_valid = (
            isinstance(expected, str) and bool(expected.strip())
            if field not in _INTEGER_RECEIPT_IDENTITIES
            else is_integer_identity(expected)
        )
        if expected_is_valid and actual != expected:
            findings.append(
                _finding(
                    source,
                    None,
                    f"{field.replace('_', '-')}-mismatch",
                    f"evidence {field} does not match the independently expected value",
                    f"collect evidence from the expected {field}",
                )
            )
    return findings


def x__identity_findings__mutmut_16(
    evidence: Mapping[str, object],
    *,
    expected_identity: Mapping[str, object],
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        expected = expected_identity.get(field)
        actual = evidence.get(field)
        expected_is_valid = (
            isinstance(expected, str) and bool(expected.strip())
            if field not in _INTEGER_RECEIPT_IDENTITIES
            else is_integer_identity(expected)
        )
        if expected_is_valid and actual != expected:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    None,
                    f"evidence {field} does not match the independently expected value",
                    f"collect evidence from the expected {field}",
                )
            )
    return findings


def x__identity_findings__mutmut_17(
    evidence: Mapping[str, object],
    *,
    expected_identity: Mapping[str, object],
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        expected = expected_identity.get(field)
        actual = evidence.get(field)
        expected_is_valid = (
            isinstance(expected, str) and bool(expected.strip())
            if field not in _INTEGER_RECEIPT_IDENTITIES
            else is_integer_identity(expected)
        )
        if expected_is_valid and actual != expected:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    f"{field.replace('_', '-')}-mismatch",
                    None,
                    f"collect evidence from the expected {field}",
                )
            )
    return findings


def x__identity_findings__mutmut_18(
    evidence: Mapping[str, object],
    *,
    expected_identity: Mapping[str, object],
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        expected = expected_identity.get(field)
        actual = evidence.get(field)
        expected_is_valid = (
            isinstance(expected, str) and bool(expected.strip())
            if field not in _INTEGER_RECEIPT_IDENTITIES
            else is_integer_identity(expected)
        )
        if expected_is_valid and actual != expected:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    f"{field.replace('_', '-')}-mismatch",
                    f"evidence {field} does not match the independently expected value",
                    None,
                )
            )
    return findings


def x__identity_findings__mutmut_19(
    evidence: Mapping[str, object],
    *,
    expected_identity: Mapping[str, object],
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        expected = expected_identity.get(field)
        actual = evidence.get(field)
        expected_is_valid = (
            isinstance(expected, str) and bool(expected.strip())
            if field not in _INTEGER_RECEIPT_IDENTITIES
            else is_integer_identity(expected)
        )
        if expected_is_valid and actual != expected:
            findings.append(
                _finding(
                    f"/{field}",
                    f"{field.replace('_', '-')}-mismatch",
                    f"evidence {field} does not match the independently expected value",
                    f"collect evidence from the expected {field}",
                )
            )
    return findings


def x__identity_findings__mutmut_20(
    evidence: Mapping[str, object],
    *,
    expected_identity: Mapping[str, object],
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        expected = expected_identity.get(field)
        actual = evidence.get(field)
        expected_is_valid = (
            isinstance(expected, str) and bool(expected.strip())
            if field not in _INTEGER_RECEIPT_IDENTITIES
            else is_integer_identity(expected)
        )
        if expected_is_valid and actual != expected:
            findings.append(
                _finding(
                    source,
                    f"{field.replace('_', '-')}-mismatch",
                    f"evidence {field} does not match the independently expected value",
                    f"collect evidence from the expected {field}",
                )
            )
    return findings


def x__identity_findings__mutmut_21(
    evidence: Mapping[str, object],
    *,
    expected_identity: Mapping[str, object],
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        expected = expected_identity.get(field)
        actual = evidence.get(field)
        expected_is_valid = (
            isinstance(expected, str) and bool(expected.strip())
            if field not in _INTEGER_RECEIPT_IDENTITIES
            else is_integer_identity(expected)
        )
        if expected_is_valid and actual != expected:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    f"evidence {field} does not match the independently expected value",
                    f"collect evidence from the expected {field}",
                )
            )
    return findings


def x__identity_findings__mutmut_22(
    evidence: Mapping[str, object],
    *,
    expected_identity: Mapping[str, object],
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        expected = expected_identity.get(field)
        actual = evidence.get(field)
        expected_is_valid = (
            isinstance(expected, str) and bool(expected.strip())
            if field not in _INTEGER_RECEIPT_IDENTITIES
            else is_integer_identity(expected)
        )
        if expected_is_valid and actual != expected:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    f"{field.replace('_', '-')}-mismatch",
                    f"collect evidence from the expected {field}",
                )
            )
    return findings


def x__identity_findings__mutmut_23(
    evidence: Mapping[str, object],
    *,
    expected_identity: Mapping[str, object],
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        expected = expected_identity.get(field)
        actual = evidence.get(field)
        expected_is_valid = (
            isinstance(expected, str) and bool(expected.strip())
            if field not in _INTEGER_RECEIPT_IDENTITIES
            else is_integer_identity(expected)
        )
        if expected_is_valid and actual != expected:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    f"{field.replace('_', '-')}-mismatch",
                    f"evidence {field} does not match the independently expected value",
                    )
            )
    return findings


def x__identity_findings__mutmut_24(
    evidence: Mapping[str, object],
    *,
    expected_identity: Mapping[str, object],
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        expected = expected_identity.get(field)
        actual = evidence.get(field)
        expected_is_valid = (
            isinstance(expected, str) and bool(expected.strip())
            if field not in _INTEGER_RECEIPT_IDENTITIES
            else is_integer_identity(expected)
        )
        if expected_is_valid and actual != expected:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    f"{field.replace(None, '-')}-mismatch",
                    f"evidence {field} does not match the independently expected value",
                    f"collect evidence from the expected {field}",
                )
            )
    return findings


def x__identity_findings__mutmut_25(
    evidence: Mapping[str, object],
    *,
    expected_identity: Mapping[str, object],
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        expected = expected_identity.get(field)
        actual = evidence.get(field)
        expected_is_valid = (
            isinstance(expected, str) and bool(expected.strip())
            if field not in _INTEGER_RECEIPT_IDENTITIES
            else is_integer_identity(expected)
        )
        if expected_is_valid and actual != expected:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    f"{field.replace('_', None)}-mismatch",
                    f"evidence {field} does not match the independently expected value",
                    f"collect evidence from the expected {field}",
                )
            )
    return findings


def x__identity_findings__mutmut_26(
    evidence: Mapping[str, object],
    *,
    expected_identity: Mapping[str, object],
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        expected = expected_identity.get(field)
        actual = evidence.get(field)
        expected_is_valid = (
            isinstance(expected, str) and bool(expected.strip())
            if field not in _INTEGER_RECEIPT_IDENTITIES
            else is_integer_identity(expected)
        )
        if expected_is_valid and actual != expected:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    f"{field.replace('-')}-mismatch",
                    f"evidence {field} does not match the independently expected value",
                    f"collect evidence from the expected {field}",
                )
            )
    return findings


def x__identity_findings__mutmut_27(
    evidence: Mapping[str, object],
    *,
    expected_identity: Mapping[str, object],
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        expected = expected_identity.get(field)
        actual = evidence.get(field)
        expected_is_valid = (
            isinstance(expected, str) and bool(expected.strip())
            if field not in _INTEGER_RECEIPT_IDENTITIES
            else is_integer_identity(expected)
        )
        if expected_is_valid and actual != expected:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    f"{field.replace('_', )}-mismatch",
                    f"evidence {field} does not match the independently expected value",
                    f"collect evidence from the expected {field}",
                )
            )
    return findings


def x__identity_findings__mutmut_28(
    evidence: Mapping[str, object],
    *,
    expected_identity: Mapping[str, object],
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        expected = expected_identity.get(field)
        actual = evidence.get(field)
        expected_is_valid = (
            isinstance(expected, str) and bool(expected.strip())
            if field not in _INTEGER_RECEIPT_IDENTITIES
            else is_integer_identity(expected)
        )
        if expected_is_valid and actual != expected:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    f"{field.replace('XX_XX', '-')}-mismatch",
                    f"evidence {field} does not match the independently expected value",
                    f"collect evidence from the expected {field}",
                )
            )
    return findings


def x__identity_findings__mutmut_29(
    evidence: Mapping[str, object],
    *,
    expected_identity: Mapping[str, object],
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        expected = expected_identity.get(field)
        actual = evidence.get(field)
        expected_is_valid = (
            isinstance(expected, str) and bool(expected.strip())
            if field not in _INTEGER_RECEIPT_IDENTITIES
            else is_integer_identity(expected)
        )
        if expected_is_valid and actual != expected:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    f"{field.replace('_', 'XX-XX')}-mismatch",
                    f"evidence {field} does not match the independently expected value",
                    f"collect evidence from the expected {field}",
                )
            )
    return findings

mutants_x__identity_findings__mutmut['_mutmut_orig'] = x__identity_findings__mutmut_orig # type: ignore # mutmut generated
mutants_x__identity_findings__mutmut['x__identity_findings__mutmut_1'] = x__identity_findings__mutmut_1 # type: ignore # mutmut generated
mutants_x__identity_findings__mutmut['x__identity_findings__mutmut_2'] = x__identity_findings__mutmut_2 # type: ignore # mutmut generated
mutants_x__identity_findings__mutmut['x__identity_findings__mutmut_3'] = x__identity_findings__mutmut_3 # type: ignore # mutmut generated
mutants_x__identity_findings__mutmut['x__identity_findings__mutmut_4'] = x__identity_findings__mutmut_4 # type: ignore # mutmut generated
mutants_x__identity_findings__mutmut['x__identity_findings__mutmut_5'] = x__identity_findings__mutmut_5 # type: ignore # mutmut generated
mutants_x__identity_findings__mutmut['x__identity_findings__mutmut_6'] = x__identity_findings__mutmut_6 # type: ignore # mutmut generated
mutants_x__identity_findings__mutmut['x__identity_findings__mutmut_7'] = x__identity_findings__mutmut_7 # type: ignore # mutmut generated
mutants_x__identity_findings__mutmut['x__identity_findings__mutmut_8'] = x__identity_findings__mutmut_8 # type: ignore # mutmut generated
mutants_x__identity_findings__mutmut['x__identity_findings__mutmut_9'] = x__identity_findings__mutmut_9 # type: ignore # mutmut generated
mutants_x__identity_findings__mutmut['x__identity_findings__mutmut_10'] = x__identity_findings__mutmut_10 # type: ignore # mutmut generated
mutants_x__identity_findings__mutmut['x__identity_findings__mutmut_11'] = x__identity_findings__mutmut_11 # type: ignore # mutmut generated
mutants_x__identity_findings__mutmut['x__identity_findings__mutmut_12'] = x__identity_findings__mutmut_12 # type: ignore # mutmut generated
mutants_x__identity_findings__mutmut['x__identity_findings__mutmut_13'] = x__identity_findings__mutmut_13 # type: ignore # mutmut generated
mutants_x__identity_findings__mutmut['x__identity_findings__mutmut_14'] = x__identity_findings__mutmut_14 # type: ignore # mutmut generated
mutants_x__identity_findings__mutmut['x__identity_findings__mutmut_15'] = x__identity_findings__mutmut_15 # type: ignore # mutmut generated
mutants_x__identity_findings__mutmut['x__identity_findings__mutmut_16'] = x__identity_findings__mutmut_16 # type: ignore # mutmut generated
mutants_x__identity_findings__mutmut['x__identity_findings__mutmut_17'] = x__identity_findings__mutmut_17 # type: ignore # mutmut generated
mutants_x__identity_findings__mutmut['x__identity_findings__mutmut_18'] = x__identity_findings__mutmut_18 # type: ignore # mutmut generated
mutants_x__identity_findings__mutmut['x__identity_findings__mutmut_19'] = x__identity_findings__mutmut_19 # type: ignore # mutmut generated
mutants_x__identity_findings__mutmut['x__identity_findings__mutmut_20'] = x__identity_findings__mutmut_20 # type: ignore # mutmut generated
mutants_x__identity_findings__mutmut['x__identity_findings__mutmut_21'] = x__identity_findings__mutmut_21 # type: ignore # mutmut generated
mutants_x__identity_findings__mutmut['x__identity_findings__mutmut_22'] = x__identity_findings__mutmut_22 # type: ignore # mutmut generated
mutants_x__identity_findings__mutmut['x__identity_findings__mutmut_23'] = x__identity_findings__mutmut_23 # type: ignore # mutmut generated
mutants_x__identity_findings__mutmut['x__identity_findings__mutmut_24'] = x__identity_findings__mutmut_24 # type: ignore # mutmut generated
mutants_x__identity_findings__mutmut['x__identity_findings__mutmut_25'] = x__identity_findings__mutmut_25 # type: ignore # mutmut generated
mutants_x__identity_findings__mutmut['x__identity_findings__mutmut_26'] = x__identity_findings__mutmut_26 # type: ignore # mutmut generated
mutants_x__identity_findings__mutmut['x__identity_findings__mutmut_27'] = x__identity_findings__mutmut_27 # type: ignore # mutmut generated
mutants_x__identity_findings__mutmut['x__identity_findings__mutmut_28'] = x__identity_findings__mutmut_28 # type: ignore # mutmut generated
mutants_x__identity_findings__mutmut['x__identity_findings__mutmut_29'] = x__identity_findings__mutmut_29 # type: ignore # mutmut generated
mutants_x__normalise_required_checks__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__normalise_required_checks__mutmut)
def _normalise_required_checks(
    value: object,
    *,
    source: Path,
) -> tuple[tuple[str, ...], tuple[ContractFinding, ...]]:
    if not isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return (), (
            _finding(
                source,
                "/required_checks",
                "invalid-required-checks",
                "required_checks must be a list of unique non-empty strings",
                "configure required_checks as an array of check ids",
            ),
        )
    valid: list[str] = []
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, item in enumerate(value):
        if not isinstance(item, str) or not item.strip():
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    "each required check id must be a non-empty string",
                    "replace the member with one declared non-empty check id",
                )
            )
            continue
        if item in seen:
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    f"required check {item!r} is listed more than once",
                    "keep each required check id exactly once",
                )
            )
            continue
        seen.add(item)
        valid.append(item)
    return tuple(valid), sort_findings(findings)


def x__normalise_required_checks__mutmut_orig(
    value: object,
    *,
    source: Path,
) -> tuple[tuple[str, ...], tuple[ContractFinding, ...]]:
    if not isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return (), (
            _finding(
                source,
                "/required_checks",
                "invalid-required-checks",
                "required_checks must be a list of unique non-empty strings",
                "configure required_checks as an array of check ids",
            ),
        )
    valid: list[str] = []
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, item in enumerate(value):
        if not isinstance(item, str) or not item.strip():
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    "each required check id must be a non-empty string",
                    "replace the member with one declared non-empty check id",
                )
            )
            continue
        if item in seen:
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    f"required check {item!r} is listed more than once",
                    "keep each required check id exactly once",
                )
            )
            continue
        seen.add(item)
        valid.append(item)
    return tuple(valid), sort_findings(findings)


def x__normalise_required_checks__mutmut_1(
    value: object,
    *,
    source: Path,
) -> tuple[tuple[str, ...], tuple[ContractFinding, ...]]:
    if not isinstance(value, Sequence) and isinstance(value, (str, bytes)):
        return (), (
            _finding(
                source,
                "/required_checks",
                "invalid-required-checks",
                "required_checks must be a list of unique non-empty strings",
                "configure required_checks as an array of check ids",
            ),
        )
    valid: list[str] = []
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, item in enumerate(value):
        if not isinstance(item, str) or not item.strip():
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    "each required check id must be a non-empty string",
                    "replace the member with one declared non-empty check id",
                )
            )
            continue
        if item in seen:
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    f"required check {item!r} is listed more than once",
                    "keep each required check id exactly once",
                )
            )
            continue
        seen.add(item)
        valid.append(item)
    return tuple(valid), sort_findings(findings)


def x__normalise_required_checks__mutmut_2(
    value: object,
    *,
    source: Path,
) -> tuple[tuple[str, ...], tuple[ContractFinding, ...]]:
    if isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return (), (
            _finding(
                source,
                "/required_checks",
                "invalid-required-checks",
                "required_checks must be a list of unique non-empty strings",
                "configure required_checks as an array of check ids",
            ),
        )
    valid: list[str] = []
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, item in enumerate(value):
        if not isinstance(item, str) or not item.strip():
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    "each required check id must be a non-empty string",
                    "replace the member with one declared non-empty check id",
                )
            )
            continue
        if item in seen:
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    f"required check {item!r} is listed more than once",
                    "keep each required check id exactly once",
                )
            )
            continue
        seen.add(item)
        valid.append(item)
    return tuple(valid), sort_findings(findings)


def x__normalise_required_checks__mutmut_3(
    value: object,
    *,
    source: Path,
) -> tuple[tuple[str, ...], tuple[ContractFinding, ...]]:
    if not isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return (), (
            _finding(
                None,
                "/required_checks",
                "invalid-required-checks",
                "required_checks must be a list of unique non-empty strings",
                "configure required_checks as an array of check ids",
            ),
        )
    valid: list[str] = []
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, item in enumerate(value):
        if not isinstance(item, str) or not item.strip():
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    "each required check id must be a non-empty string",
                    "replace the member with one declared non-empty check id",
                )
            )
            continue
        if item in seen:
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    f"required check {item!r} is listed more than once",
                    "keep each required check id exactly once",
                )
            )
            continue
        seen.add(item)
        valid.append(item)
    return tuple(valid), sort_findings(findings)


def x__normalise_required_checks__mutmut_4(
    value: object,
    *,
    source: Path,
) -> tuple[tuple[str, ...], tuple[ContractFinding, ...]]:
    if not isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return (), (
            _finding(
                source,
                None,
                "invalid-required-checks",
                "required_checks must be a list of unique non-empty strings",
                "configure required_checks as an array of check ids",
            ),
        )
    valid: list[str] = []
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, item in enumerate(value):
        if not isinstance(item, str) or not item.strip():
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    "each required check id must be a non-empty string",
                    "replace the member with one declared non-empty check id",
                )
            )
            continue
        if item in seen:
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    f"required check {item!r} is listed more than once",
                    "keep each required check id exactly once",
                )
            )
            continue
        seen.add(item)
        valid.append(item)
    return tuple(valid), sort_findings(findings)


def x__normalise_required_checks__mutmut_5(
    value: object,
    *,
    source: Path,
) -> tuple[tuple[str, ...], tuple[ContractFinding, ...]]:
    if not isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return (), (
            _finding(
                source,
                "/required_checks",
                None,
                "required_checks must be a list of unique non-empty strings",
                "configure required_checks as an array of check ids",
            ),
        )
    valid: list[str] = []
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, item in enumerate(value):
        if not isinstance(item, str) or not item.strip():
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    "each required check id must be a non-empty string",
                    "replace the member with one declared non-empty check id",
                )
            )
            continue
        if item in seen:
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    f"required check {item!r} is listed more than once",
                    "keep each required check id exactly once",
                )
            )
            continue
        seen.add(item)
        valid.append(item)
    return tuple(valid), sort_findings(findings)


def x__normalise_required_checks__mutmut_6(
    value: object,
    *,
    source: Path,
) -> tuple[tuple[str, ...], tuple[ContractFinding, ...]]:
    if not isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return (), (
            _finding(
                source,
                "/required_checks",
                "invalid-required-checks",
                None,
                "configure required_checks as an array of check ids",
            ),
        )
    valid: list[str] = []
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, item in enumerate(value):
        if not isinstance(item, str) or not item.strip():
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    "each required check id must be a non-empty string",
                    "replace the member with one declared non-empty check id",
                )
            )
            continue
        if item in seen:
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    f"required check {item!r} is listed more than once",
                    "keep each required check id exactly once",
                )
            )
            continue
        seen.add(item)
        valid.append(item)
    return tuple(valid), sort_findings(findings)


def x__normalise_required_checks__mutmut_7(
    value: object,
    *,
    source: Path,
) -> tuple[tuple[str, ...], tuple[ContractFinding, ...]]:
    if not isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return (), (
            _finding(
                source,
                "/required_checks",
                "invalid-required-checks",
                "required_checks must be a list of unique non-empty strings",
                None,
            ),
        )
    valid: list[str] = []
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, item in enumerate(value):
        if not isinstance(item, str) or not item.strip():
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    "each required check id must be a non-empty string",
                    "replace the member with one declared non-empty check id",
                )
            )
            continue
        if item in seen:
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    f"required check {item!r} is listed more than once",
                    "keep each required check id exactly once",
                )
            )
            continue
        seen.add(item)
        valid.append(item)
    return tuple(valid), sort_findings(findings)


def x__normalise_required_checks__mutmut_8(
    value: object,
    *,
    source: Path,
) -> tuple[tuple[str, ...], tuple[ContractFinding, ...]]:
    if not isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return (), (
            _finding(
                "/required_checks",
                "invalid-required-checks",
                "required_checks must be a list of unique non-empty strings",
                "configure required_checks as an array of check ids",
            ),
        )
    valid: list[str] = []
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, item in enumerate(value):
        if not isinstance(item, str) or not item.strip():
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    "each required check id must be a non-empty string",
                    "replace the member with one declared non-empty check id",
                )
            )
            continue
        if item in seen:
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    f"required check {item!r} is listed more than once",
                    "keep each required check id exactly once",
                )
            )
            continue
        seen.add(item)
        valid.append(item)
    return tuple(valid), sort_findings(findings)


def x__normalise_required_checks__mutmut_9(
    value: object,
    *,
    source: Path,
) -> tuple[tuple[str, ...], tuple[ContractFinding, ...]]:
    if not isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return (), (
            _finding(
                source,
                "invalid-required-checks",
                "required_checks must be a list of unique non-empty strings",
                "configure required_checks as an array of check ids",
            ),
        )
    valid: list[str] = []
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, item in enumerate(value):
        if not isinstance(item, str) or not item.strip():
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    "each required check id must be a non-empty string",
                    "replace the member with one declared non-empty check id",
                )
            )
            continue
        if item in seen:
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    f"required check {item!r} is listed more than once",
                    "keep each required check id exactly once",
                )
            )
            continue
        seen.add(item)
        valid.append(item)
    return tuple(valid), sort_findings(findings)


def x__normalise_required_checks__mutmut_10(
    value: object,
    *,
    source: Path,
) -> tuple[tuple[str, ...], tuple[ContractFinding, ...]]:
    if not isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return (), (
            _finding(
                source,
                "/required_checks",
                "required_checks must be a list of unique non-empty strings",
                "configure required_checks as an array of check ids",
            ),
        )
    valid: list[str] = []
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, item in enumerate(value):
        if not isinstance(item, str) or not item.strip():
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    "each required check id must be a non-empty string",
                    "replace the member with one declared non-empty check id",
                )
            )
            continue
        if item in seen:
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    f"required check {item!r} is listed more than once",
                    "keep each required check id exactly once",
                )
            )
            continue
        seen.add(item)
        valid.append(item)
    return tuple(valid), sort_findings(findings)


def x__normalise_required_checks__mutmut_11(
    value: object,
    *,
    source: Path,
) -> tuple[tuple[str, ...], tuple[ContractFinding, ...]]:
    if not isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return (), (
            _finding(
                source,
                "/required_checks",
                "invalid-required-checks",
                "configure required_checks as an array of check ids",
            ),
        )
    valid: list[str] = []
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, item in enumerate(value):
        if not isinstance(item, str) or not item.strip():
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    "each required check id must be a non-empty string",
                    "replace the member with one declared non-empty check id",
                )
            )
            continue
        if item in seen:
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    f"required check {item!r} is listed more than once",
                    "keep each required check id exactly once",
                )
            )
            continue
        seen.add(item)
        valid.append(item)
    return tuple(valid), sort_findings(findings)


def x__normalise_required_checks__mutmut_12(
    value: object,
    *,
    source: Path,
) -> tuple[tuple[str, ...], tuple[ContractFinding, ...]]:
    if not isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return (), (
            _finding(
                source,
                "/required_checks",
                "invalid-required-checks",
                "required_checks must be a list of unique non-empty strings",
                ),
        )
    valid: list[str] = []
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, item in enumerate(value):
        if not isinstance(item, str) or not item.strip():
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    "each required check id must be a non-empty string",
                    "replace the member with one declared non-empty check id",
                )
            )
            continue
        if item in seen:
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    f"required check {item!r} is listed more than once",
                    "keep each required check id exactly once",
                )
            )
            continue
        seen.add(item)
        valid.append(item)
    return tuple(valid), sort_findings(findings)


def x__normalise_required_checks__mutmut_13(
    value: object,
    *,
    source: Path,
) -> tuple[tuple[str, ...], tuple[ContractFinding, ...]]:
    if not isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return (), (
            _finding(
                source,
                "XX/required_checksXX",
                "invalid-required-checks",
                "required_checks must be a list of unique non-empty strings",
                "configure required_checks as an array of check ids",
            ),
        )
    valid: list[str] = []
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, item in enumerate(value):
        if not isinstance(item, str) or not item.strip():
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    "each required check id must be a non-empty string",
                    "replace the member with one declared non-empty check id",
                )
            )
            continue
        if item in seen:
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    f"required check {item!r} is listed more than once",
                    "keep each required check id exactly once",
                )
            )
            continue
        seen.add(item)
        valid.append(item)
    return tuple(valid), sort_findings(findings)


def x__normalise_required_checks__mutmut_14(
    value: object,
    *,
    source: Path,
) -> tuple[tuple[str, ...], tuple[ContractFinding, ...]]:
    if not isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return (), (
            _finding(
                source,
                "/REQUIRED_CHECKS",
                "invalid-required-checks",
                "required_checks must be a list of unique non-empty strings",
                "configure required_checks as an array of check ids",
            ),
        )
    valid: list[str] = []
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, item in enumerate(value):
        if not isinstance(item, str) or not item.strip():
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    "each required check id must be a non-empty string",
                    "replace the member with one declared non-empty check id",
                )
            )
            continue
        if item in seen:
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    f"required check {item!r} is listed more than once",
                    "keep each required check id exactly once",
                )
            )
            continue
        seen.add(item)
        valid.append(item)
    return tuple(valid), sort_findings(findings)


def x__normalise_required_checks__mutmut_15(
    value: object,
    *,
    source: Path,
) -> tuple[tuple[str, ...], tuple[ContractFinding, ...]]:
    if not isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return (), (
            _finding(
                source,
                "/required_checks",
                "XXinvalid-required-checksXX",
                "required_checks must be a list of unique non-empty strings",
                "configure required_checks as an array of check ids",
            ),
        )
    valid: list[str] = []
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, item in enumerate(value):
        if not isinstance(item, str) or not item.strip():
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    "each required check id must be a non-empty string",
                    "replace the member with one declared non-empty check id",
                )
            )
            continue
        if item in seen:
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    f"required check {item!r} is listed more than once",
                    "keep each required check id exactly once",
                )
            )
            continue
        seen.add(item)
        valid.append(item)
    return tuple(valid), sort_findings(findings)


def x__normalise_required_checks__mutmut_16(
    value: object,
    *,
    source: Path,
) -> tuple[tuple[str, ...], tuple[ContractFinding, ...]]:
    if not isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return (), (
            _finding(
                source,
                "/required_checks",
                "INVALID-REQUIRED-CHECKS",
                "required_checks must be a list of unique non-empty strings",
                "configure required_checks as an array of check ids",
            ),
        )
    valid: list[str] = []
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, item in enumerate(value):
        if not isinstance(item, str) or not item.strip():
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    "each required check id must be a non-empty string",
                    "replace the member with one declared non-empty check id",
                )
            )
            continue
        if item in seen:
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    f"required check {item!r} is listed more than once",
                    "keep each required check id exactly once",
                )
            )
            continue
        seen.add(item)
        valid.append(item)
    return tuple(valid), sort_findings(findings)


def x__normalise_required_checks__mutmut_17(
    value: object,
    *,
    source: Path,
) -> tuple[tuple[str, ...], tuple[ContractFinding, ...]]:
    if not isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return (), (
            _finding(
                source,
                "/required_checks",
                "invalid-required-checks",
                "XXrequired_checks must be a list of unique non-empty stringsXX",
                "configure required_checks as an array of check ids",
            ),
        )
    valid: list[str] = []
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, item in enumerate(value):
        if not isinstance(item, str) or not item.strip():
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    "each required check id must be a non-empty string",
                    "replace the member with one declared non-empty check id",
                )
            )
            continue
        if item in seen:
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    f"required check {item!r} is listed more than once",
                    "keep each required check id exactly once",
                )
            )
            continue
        seen.add(item)
        valid.append(item)
    return tuple(valid), sort_findings(findings)


def x__normalise_required_checks__mutmut_18(
    value: object,
    *,
    source: Path,
) -> tuple[tuple[str, ...], tuple[ContractFinding, ...]]:
    if not isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return (), (
            _finding(
                source,
                "/required_checks",
                "invalid-required-checks",
                "REQUIRED_CHECKS MUST BE A LIST OF UNIQUE NON-EMPTY STRINGS",
                "configure required_checks as an array of check ids",
            ),
        )
    valid: list[str] = []
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, item in enumerate(value):
        if not isinstance(item, str) or not item.strip():
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    "each required check id must be a non-empty string",
                    "replace the member with one declared non-empty check id",
                )
            )
            continue
        if item in seen:
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    f"required check {item!r} is listed more than once",
                    "keep each required check id exactly once",
                )
            )
            continue
        seen.add(item)
        valid.append(item)
    return tuple(valid), sort_findings(findings)


def x__normalise_required_checks__mutmut_19(
    value: object,
    *,
    source: Path,
) -> tuple[tuple[str, ...], tuple[ContractFinding, ...]]:
    if not isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return (), (
            _finding(
                source,
                "/required_checks",
                "invalid-required-checks",
                "required_checks must be a list of unique non-empty strings",
                "XXconfigure required_checks as an array of check idsXX",
            ),
        )
    valid: list[str] = []
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, item in enumerate(value):
        if not isinstance(item, str) or not item.strip():
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    "each required check id must be a non-empty string",
                    "replace the member with one declared non-empty check id",
                )
            )
            continue
        if item in seen:
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    f"required check {item!r} is listed more than once",
                    "keep each required check id exactly once",
                )
            )
            continue
        seen.add(item)
        valid.append(item)
    return tuple(valid), sort_findings(findings)


def x__normalise_required_checks__mutmut_20(
    value: object,
    *,
    source: Path,
) -> tuple[tuple[str, ...], tuple[ContractFinding, ...]]:
    if not isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return (), (
            _finding(
                source,
                "/required_checks",
                "invalid-required-checks",
                "required_checks must be a list of unique non-empty strings",
                "CONFIGURE REQUIRED_CHECKS AS AN ARRAY OF CHECK IDS",
            ),
        )
    valid: list[str] = []
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, item in enumerate(value):
        if not isinstance(item, str) or not item.strip():
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    "each required check id must be a non-empty string",
                    "replace the member with one declared non-empty check id",
                )
            )
            continue
        if item in seen:
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    f"required check {item!r} is listed more than once",
                    "keep each required check id exactly once",
                )
            )
            continue
        seen.add(item)
        valid.append(item)
    return tuple(valid), sort_findings(findings)


def x__normalise_required_checks__mutmut_21(
    value: object,
    *,
    source: Path,
) -> tuple[tuple[str, ...], tuple[ContractFinding, ...]]:
    if not isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return (), (
            _finding(
                source,
                "/required_checks",
                "invalid-required-checks",
                "required_checks must be a list of unique non-empty strings",
                "configure required_checks as an array of check ids",
            ),
        )
    valid: list[str] = None
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, item in enumerate(value):
        if not isinstance(item, str) or not item.strip():
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    "each required check id must be a non-empty string",
                    "replace the member with one declared non-empty check id",
                )
            )
            continue
        if item in seen:
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    f"required check {item!r} is listed more than once",
                    "keep each required check id exactly once",
                )
            )
            continue
        seen.add(item)
        valid.append(item)
    return tuple(valid), sort_findings(findings)


def x__normalise_required_checks__mutmut_22(
    value: object,
    *,
    source: Path,
) -> tuple[tuple[str, ...], tuple[ContractFinding, ...]]:
    if not isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return (), (
            _finding(
                source,
                "/required_checks",
                "invalid-required-checks",
                "required_checks must be a list of unique non-empty strings",
                "configure required_checks as an array of check ids",
            ),
        )
    valid: list[str] = []
    findings: list[ContractFinding] = None
    seen: set[str] = set()
    for index, item in enumerate(value):
        if not isinstance(item, str) or not item.strip():
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    "each required check id must be a non-empty string",
                    "replace the member with one declared non-empty check id",
                )
            )
            continue
        if item in seen:
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    f"required check {item!r} is listed more than once",
                    "keep each required check id exactly once",
                )
            )
            continue
        seen.add(item)
        valid.append(item)
    return tuple(valid), sort_findings(findings)


def x__normalise_required_checks__mutmut_23(
    value: object,
    *,
    source: Path,
) -> tuple[tuple[str, ...], tuple[ContractFinding, ...]]:
    if not isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return (), (
            _finding(
                source,
                "/required_checks",
                "invalid-required-checks",
                "required_checks must be a list of unique non-empty strings",
                "configure required_checks as an array of check ids",
            ),
        )
    valid: list[str] = []
    findings: list[ContractFinding] = []
    seen: set[str] = None
    for index, item in enumerate(value):
        if not isinstance(item, str) or not item.strip():
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    "each required check id must be a non-empty string",
                    "replace the member with one declared non-empty check id",
                )
            )
            continue
        if item in seen:
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    f"required check {item!r} is listed more than once",
                    "keep each required check id exactly once",
                )
            )
            continue
        seen.add(item)
        valid.append(item)
    return tuple(valid), sort_findings(findings)


def x__normalise_required_checks__mutmut_24(
    value: object,
    *,
    source: Path,
) -> tuple[tuple[str, ...], tuple[ContractFinding, ...]]:
    if not isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return (), (
            _finding(
                source,
                "/required_checks",
                "invalid-required-checks",
                "required_checks must be a list of unique non-empty strings",
                "configure required_checks as an array of check ids",
            ),
        )
    valid: list[str] = []
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, item in enumerate(None):
        if not isinstance(item, str) or not item.strip():
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    "each required check id must be a non-empty string",
                    "replace the member with one declared non-empty check id",
                )
            )
            continue
        if item in seen:
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    f"required check {item!r} is listed more than once",
                    "keep each required check id exactly once",
                )
            )
            continue
        seen.add(item)
        valid.append(item)
    return tuple(valid), sort_findings(findings)


def x__normalise_required_checks__mutmut_25(
    value: object,
    *,
    source: Path,
) -> tuple[tuple[str, ...], tuple[ContractFinding, ...]]:
    if not isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return (), (
            _finding(
                source,
                "/required_checks",
                "invalid-required-checks",
                "required_checks must be a list of unique non-empty strings",
                "configure required_checks as an array of check ids",
            ),
        )
    valid: list[str] = []
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, item in enumerate(value):
        if not isinstance(item, str) and not item.strip():
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    "each required check id must be a non-empty string",
                    "replace the member with one declared non-empty check id",
                )
            )
            continue
        if item in seen:
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    f"required check {item!r} is listed more than once",
                    "keep each required check id exactly once",
                )
            )
            continue
        seen.add(item)
        valid.append(item)
    return tuple(valid), sort_findings(findings)


def x__normalise_required_checks__mutmut_26(
    value: object,
    *,
    source: Path,
) -> tuple[tuple[str, ...], tuple[ContractFinding, ...]]:
    if not isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return (), (
            _finding(
                source,
                "/required_checks",
                "invalid-required-checks",
                "required_checks must be a list of unique non-empty strings",
                "configure required_checks as an array of check ids",
            ),
        )
    valid: list[str] = []
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, item in enumerate(value):
        if isinstance(item, str) or not item.strip():
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    "each required check id must be a non-empty string",
                    "replace the member with one declared non-empty check id",
                )
            )
            continue
        if item in seen:
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    f"required check {item!r} is listed more than once",
                    "keep each required check id exactly once",
                )
            )
            continue
        seen.add(item)
        valid.append(item)
    return tuple(valid), sort_findings(findings)


def x__normalise_required_checks__mutmut_27(
    value: object,
    *,
    source: Path,
) -> tuple[tuple[str, ...], tuple[ContractFinding, ...]]:
    if not isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return (), (
            _finding(
                source,
                "/required_checks",
                "invalid-required-checks",
                "required_checks must be a list of unique non-empty strings",
                "configure required_checks as an array of check ids",
            ),
        )
    valid: list[str] = []
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, item in enumerate(value):
        if not isinstance(item, str) or item.strip():
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    "each required check id must be a non-empty string",
                    "replace the member with one declared non-empty check id",
                )
            )
            continue
        if item in seen:
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    f"required check {item!r} is listed more than once",
                    "keep each required check id exactly once",
                )
            )
            continue
        seen.add(item)
        valid.append(item)
    return tuple(valid), sort_findings(findings)


def x__normalise_required_checks__mutmut_28(
    value: object,
    *,
    source: Path,
) -> tuple[tuple[str, ...], tuple[ContractFinding, ...]]:
    if not isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return (), (
            _finding(
                source,
                "/required_checks",
                "invalid-required-checks",
                "required_checks must be a list of unique non-empty strings",
                "configure required_checks as an array of check ids",
            ),
        )
    valid: list[str] = []
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, item in enumerate(value):
        if not isinstance(item, str) or not item.strip():
            findings.append(
                None
            )
            continue
        if item in seen:
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    f"required check {item!r} is listed more than once",
                    "keep each required check id exactly once",
                )
            )
            continue
        seen.add(item)
        valid.append(item)
    return tuple(valid), sort_findings(findings)


def x__normalise_required_checks__mutmut_29(
    value: object,
    *,
    source: Path,
) -> tuple[tuple[str, ...], tuple[ContractFinding, ...]]:
    if not isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return (), (
            _finding(
                source,
                "/required_checks",
                "invalid-required-checks",
                "required_checks must be a list of unique non-empty strings",
                "configure required_checks as an array of check ids",
            ),
        )
    valid: list[str] = []
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, item in enumerate(value):
        if not isinstance(item, str) or not item.strip():
            findings.append(
                _finding(
                    None,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    "each required check id must be a non-empty string",
                    "replace the member with one declared non-empty check id",
                )
            )
            continue
        if item in seen:
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    f"required check {item!r} is listed more than once",
                    "keep each required check id exactly once",
                )
            )
            continue
        seen.add(item)
        valid.append(item)
    return tuple(valid), sort_findings(findings)


def x__normalise_required_checks__mutmut_30(
    value: object,
    *,
    source: Path,
) -> tuple[tuple[str, ...], tuple[ContractFinding, ...]]:
    if not isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return (), (
            _finding(
                source,
                "/required_checks",
                "invalid-required-checks",
                "required_checks must be a list of unique non-empty strings",
                "configure required_checks as an array of check ids",
            ),
        )
    valid: list[str] = []
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, item in enumerate(value):
        if not isinstance(item, str) or not item.strip():
            findings.append(
                _finding(
                    source,
                    None,
                    "invalid-required-checks",
                    "each required check id must be a non-empty string",
                    "replace the member with one declared non-empty check id",
                )
            )
            continue
        if item in seen:
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    f"required check {item!r} is listed more than once",
                    "keep each required check id exactly once",
                )
            )
            continue
        seen.add(item)
        valid.append(item)
    return tuple(valid), sort_findings(findings)


def x__normalise_required_checks__mutmut_31(
    value: object,
    *,
    source: Path,
) -> tuple[tuple[str, ...], tuple[ContractFinding, ...]]:
    if not isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return (), (
            _finding(
                source,
                "/required_checks",
                "invalid-required-checks",
                "required_checks must be a list of unique non-empty strings",
                "configure required_checks as an array of check ids",
            ),
        )
    valid: list[str] = []
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, item in enumerate(value):
        if not isinstance(item, str) or not item.strip():
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    None,
                    "each required check id must be a non-empty string",
                    "replace the member with one declared non-empty check id",
                )
            )
            continue
        if item in seen:
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    f"required check {item!r} is listed more than once",
                    "keep each required check id exactly once",
                )
            )
            continue
        seen.add(item)
        valid.append(item)
    return tuple(valid), sort_findings(findings)


def x__normalise_required_checks__mutmut_32(
    value: object,
    *,
    source: Path,
) -> tuple[tuple[str, ...], tuple[ContractFinding, ...]]:
    if not isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return (), (
            _finding(
                source,
                "/required_checks",
                "invalid-required-checks",
                "required_checks must be a list of unique non-empty strings",
                "configure required_checks as an array of check ids",
            ),
        )
    valid: list[str] = []
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, item in enumerate(value):
        if not isinstance(item, str) or not item.strip():
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    None,
                    "replace the member with one declared non-empty check id",
                )
            )
            continue
        if item in seen:
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    f"required check {item!r} is listed more than once",
                    "keep each required check id exactly once",
                )
            )
            continue
        seen.add(item)
        valid.append(item)
    return tuple(valid), sort_findings(findings)


def x__normalise_required_checks__mutmut_33(
    value: object,
    *,
    source: Path,
) -> tuple[tuple[str, ...], tuple[ContractFinding, ...]]:
    if not isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return (), (
            _finding(
                source,
                "/required_checks",
                "invalid-required-checks",
                "required_checks must be a list of unique non-empty strings",
                "configure required_checks as an array of check ids",
            ),
        )
    valid: list[str] = []
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, item in enumerate(value):
        if not isinstance(item, str) or not item.strip():
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    "each required check id must be a non-empty string",
                    None,
                )
            )
            continue
        if item in seen:
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    f"required check {item!r} is listed more than once",
                    "keep each required check id exactly once",
                )
            )
            continue
        seen.add(item)
        valid.append(item)
    return tuple(valid), sort_findings(findings)


def x__normalise_required_checks__mutmut_34(
    value: object,
    *,
    source: Path,
) -> tuple[tuple[str, ...], tuple[ContractFinding, ...]]:
    if not isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return (), (
            _finding(
                source,
                "/required_checks",
                "invalid-required-checks",
                "required_checks must be a list of unique non-empty strings",
                "configure required_checks as an array of check ids",
            ),
        )
    valid: list[str] = []
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, item in enumerate(value):
        if not isinstance(item, str) or not item.strip():
            findings.append(
                _finding(
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    "each required check id must be a non-empty string",
                    "replace the member with one declared non-empty check id",
                )
            )
            continue
        if item in seen:
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    f"required check {item!r} is listed more than once",
                    "keep each required check id exactly once",
                )
            )
            continue
        seen.add(item)
        valid.append(item)
    return tuple(valid), sort_findings(findings)


def x__normalise_required_checks__mutmut_35(
    value: object,
    *,
    source: Path,
) -> tuple[tuple[str, ...], tuple[ContractFinding, ...]]:
    if not isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return (), (
            _finding(
                source,
                "/required_checks",
                "invalid-required-checks",
                "required_checks must be a list of unique non-empty strings",
                "configure required_checks as an array of check ids",
            ),
        )
    valid: list[str] = []
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, item in enumerate(value):
        if not isinstance(item, str) or not item.strip():
            findings.append(
                _finding(
                    source,
                    "invalid-required-checks",
                    "each required check id must be a non-empty string",
                    "replace the member with one declared non-empty check id",
                )
            )
            continue
        if item in seen:
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    f"required check {item!r} is listed more than once",
                    "keep each required check id exactly once",
                )
            )
            continue
        seen.add(item)
        valid.append(item)
    return tuple(valid), sort_findings(findings)


def x__normalise_required_checks__mutmut_36(
    value: object,
    *,
    source: Path,
) -> tuple[tuple[str, ...], tuple[ContractFinding, ...]]:
    if not isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return (), (
            _finding(
                source,
                "/required_checks",
                "invalid-required-checks",
                "required_checks must be a list of unique non-empty strings",
                "configure required_checks as an array of check ids",
            ),
        )
    valid: list[str] = []
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, item in enumerate(value):
        if not isinstance(item, str) or not item.strip():
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "each required check id must be a non-empty string",
                    "replace the member with one declared non-empty check id",
                )
            )
            continue
        if item in seen:
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    f"required check {item!r} is listed more than once",
                    "keep each required check id exactly once",
                )
            )
            continue
        seen.add(item)
        valid.append(item)
    return tuple(valid), sort_findings(findings)


def x__normalise_required_checks__mutmut_37(
    value: object,
    *,
    source: Path,
) -> tuple[tuple[str, ...], tuple[ContractFinding, ...]]:
    if not isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return (), (
            _finding(
                source,
                "/required_checks",
                "invalid-required-checks",
                "required_checks must be a list of unique non-empty strings",
                "configure required_checks as an array of check ids",
            ),
        )
    valid: list[str] = []
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, item in enumerate(value):
        if not isinstance(item, str) or not item.strip():
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    "replace the member with one declared non-empty check id",
                )
            )
            continue
        if item in seen:
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    f"required check {item!r} is listed more than once",
                    "keep each required check id exactly once",
                )
            )
            continue
        seen.add(item)
        valid.append(item)
    return tuple(valid), sort_findings(findings)


def x__normalise_required_checks__mutmut_38(
    value: object,
    *,
    source: Path,
) -> tuple[tuple[str, ...], tuple[ContractFinding, ...]]:
    if not isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return (), (
            _finding(
                source,
                "/required_checks",
                "invalid-required-checks",
                "required_checks must be a list of unique non-empty strings",
                "configure required_checks as an array of check ids",
            ),
        )
    valid: list[str] = []
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, item in enumerate(value):
        if not isinstance(item, str) or not item.strip():
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    "each required check id must be a non-empty string",
                    )
            )
            continue
        if item in seen:
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    f"required check {item!r} is listed more than once",
                    "keep each required check id exactly once",
                )
            )
            continue
        seen.add(item)
        valid.append(item)
    return tuple(valid), sort_findings(findings)


def x__normalise_required_checks__mutmut_39(
    value: object,
    *,
    source: Path,
) -> tuple[tuple[str, ...], tuple[ContractFinding, ...]]:
    if not isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return (), (
            _finding(
                source,
                "/required_checks",
                "invalid-required-checks",
                "required_checks must be a list of unique non-empty strings",
                "configure required_checks as an array of check ids",
            ),
        )
    valid: list[str] = []
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, item in enumerate(value):
        if not isinstance(item, str) or not item.strip():
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "XXinvalid-required-checksXX",
                    "each required check id must be a non-empty string",
                    "replace the member with one declared non-empty check id",
                )
            )
            continue
        if item in seen:
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    f"required check {item!r} is listed more than once",
                    "keep each required check id exactly once",
                )
            )
            continue
        seen.add(item)
        valid.append(item)
    return tuple(valid), sort_findings(findings)


def x__normalise_required_checks__mutmut_40(
    value: object,
    *,
    source: Path,
) -> tuple[tuple[str, ...], tuple[ContractFinding, ...]]:
    if not isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return (), (
            _finding(
                source,
                "/required_checks",
                "invalid-required-checks",
                "required_checks must be a list of unique non-empty strings",
                "configure required_checks as an array of check ids",
            ),
        )
    valid: list[str] = []
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, item in enumerate(value):
        if not isinstance(item, str) or not item.strip():
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "INVALID-REQUIRED-CHECKS",
                    "each required check id must be a non-empty string",
                    "replace the member with one declared non-empty check id",
                )
            )
            continue
        if item in seen:
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    f"required check {item!r} is listed more than once",
                    "keep each required check id exactly once",
                )
            )
            continue
        seen.add(item)
        valid.append(item)
    return tuple(valid), sort_findings(findings)


def x__normalise_required_checks__mutmut_41(
    value: object,
    *,
    source: Path,
) -> tuple[tuple[str, ...], tuple[ContractFinding, ...]]:
    if not isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return (), (
            _finding(
                source,
                "/required_checks",
                "invalid-required-checks",
                "required_checks must be a list of unique non-empty strings",
                "configure required_checks as an array of check ids",
            ),
        )
    valid: list[str] = []
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, item in enumerate(value):
        if not isinstance(item, str) or not item.strip():
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    "XXeach required check id must be a non-empty stringXX",
                    "replace the member with one declared non-empty check id",
                )
            )
            continue
        if item in seen:
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    f"required check {item!r} is listed more than once",
                    "keep each required check id exactly once",
                )
            )
            continue
        seen.add(item)
        valid.append(item)
    return tuple(valid), sort_findings(findings)


def x__normalise_required_checks__mutmut_42(
    value: object,
    *,
    source: Path,
) -> tuple[tuple[str, ...], tuple[ContractFinding, ...]]:
    if not isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return (), (
            _finding(
                source,
                "/required_checks",
                "invalid-required-checks",
                "required_checks must be a list of unique non-empty strings",
                "configure required_checks as an array of check ids",
            ),
        )
    valid: list[str] = []
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, item in enumerate(value):
        if not isinstance(item, str) or not item.strip():
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    "EACH REQUIRED CHECK ID MUST BE A NON-EMPTY STRING",
                    "replace the member with one declared non-empty check id",
                )
            )
            continue
        if item in seen:
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    f"required check {item!r} is listed more than once",
                    "keep each required check id exactly once",
                )
            )
            continue
        seen.add(item)
        valid.append(item)
    return tuple(valid), sort_findings(findings)


def x__normalise_required_checks__mutmut_43(
    value: object,
    *,
    source: Path,
) -> tuple[tuple[str, ...], tuple[ContractFinding, ...]]:
    if not isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return (), (
            _finding(
                source,
                "/required_checks",
                "invalid-required-checks",
                "required_checks must be a list of unique non-empty strings",
                "configure required_checks as an array of check ids",
            ),
        )
    valid: list[str] = []
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, item in enumerate(value):
        if not isinstance(item, str) or not item.strip():
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    "each required check id must be a non-empty string",
                    "XXreplace the member with one declared non-empty check idXX",
                )
            )
            continue
        if item in seen:
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    f"required check {item!r} is listed more than once",
                    "keep each required check id exactly once",
                )
            )
            continue
        seen.add(item)
        valid.append(item)
    return tuple(valid), sort_findings(findings)


def x__normalise_required_checks__mutmut_44(
    value: object,
    *,
    source: Path,
) -> tuple[tuple[str, ...], tuple[ContractFinding, ...]]:
    if not isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return (), (
            _finding(
                source,
                "/required_checks",
                "invalid-required-checks",
                "required_checks must be a list of unique non-empty strings",
                "configure required_checks as an array of check ids",
            ),
        )
    valid: list[str] = []
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, item in enumerate(value):
        if not isinstance(item, str) or not item.strip():
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    "each required check id must be a non-empty string",
                    "REPLACE THE MEMBER WITH ONE DECLARED NON-EMPTY CHECK ID",
                )
            )
            continue
        if item in seen:
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    f"required check {item!r} is listed more than once",
                    "keep each required check id exactly once",
                )
            )
            continue
        seen.add(item)
        valid.append(item)
    return tuple(valid), sort_findings(findings)


def x__normalise_required_checks__mutmut_45(
    value: object,
    *,
    source: Path,
) -> tuple[tuple[str, ...], tuple[ContractFinding, ...]]:
    if not isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return (), (
            _finding(
                source,
                "/required_checks",
                "invalid-required-checks",
                "required_checks must be a list of unique non-empty strings",
                "configure required_checks as an array of check ids",
            ),
        )
    valid: list[str] = []
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, item in enumerate(value):
        if not isinstance(item, str) or not item.strip():
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    "each required check id must be a non-empty string",
                    "replace the member with one declared non-empty check id",
                )
            )
            break
        if item in seen:
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    f"required check {item!r} is listed more than once",
                    "keep each required check id exactly once",
                )
            )
            continue
        seen.add(item)
        valid.append(item)
    return tuple(valid), sort_findings(findings)


def x__normalise_required_checks__mutmut_46(
    value: object,
    *,
    source: Path,
) -> tuple[tuple[str, ...], tuple[ContractFinding, ...]]:
    if not isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return (), (
            _finding(
                source,
                "/required_checks",
                "invalid-required-checks",
                "required_checks must be a list of unique non-empty strings",
                "configure required_checks as an array of check ids",
            ),
        )
    valid: list[str] = []
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, item in enumerate(value):
        if not isinstance(item, str) or not item.strip():
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    "each required check id must be a non-empty string",
                    "replace the member with one declared non-empty check id",
                )
            )
            continue
        if item not in seen:
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    f"required check {item!r} is listed more than once",
                    "keep each required check id exactly once",
                )
            )
            continue
        seen.add(item)
        valid.append(item)
    return tuple(valid), sort_findings(findings)


def x__normalise_required_checks__mutmut_47(
    value: object,
    *,
    source: Path,
) -> tuple[tuple[str, ...], tuple[ContractFinding, ...]]:
    if not isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return (), (
            _finding(
                source,
                "/required_checks",
                "invalid-required-checks",
                "required_checks must be a list of unique non-empty strings",
                "configure required_checks as an array of check ids",
            ),
        )
    valid: list[str] = []
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, item in enumerate(value):
        if not isinstance(item, str) or not item.strip():
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    "each required check id must be a non-empty string",
                    "replace the member with one declared non-empty check id",
                )
            )
            continue
        if item in seen:
            findings.append(
                None
            )
            continue
        seen.add(item)
        valid.append(item)
    return tuple(valid), sort_findings(findings)


def x__normalise_required_checks__mutmut_48(
    value: object,
    *,
    source: Path,
) -> tuple[tuple[str, ...], tuple[ContractFinding, ...]]:
    if not isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return (), (
            _finding(
                source,
                "/required_checks",
                "invalid-required-checks",
                "required_checks must be a list of unique non-empty strings",
                "configure required_checks as an array of check ids",
            ),
        )
    valid: list[str] = []
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, item in enumerate(value):
        if not isinstance(item, str) or not item.strip():
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    "each required check id must be a non-empty string",
                    "replace the member with one declared non-empty check id",
                )
            )
            continue
        if item in seen:
            findings.append(
                _finding(
                    None,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    f"required check {item!r} is listed more than once",
                    "keep each required check id exactly once",
                )
            )
            continue
        seen.add(item)
        valid.append(item)
    return tuple(valid), sort_findings(findings)


def x__normalise_required_checks__mutmut_49(
    value: object,
    *,
    source: Path,
) -> tuple[tuple[str, ...], tuple[ContractFinding, ...]]:
    if not isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return (), (
            _finding(
                source,
                "/required_checks",
                "invalid-required-checks",
                "required_checks must be a list of unique non-empty strings",
                "configure required_checks as an array of check ids",
            ),
        )
    valid: list[str] = []
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, item in enumerate(value):
        if not isinstance(item, str) or not item.strip():
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    "each required check id must be a non-empty string",
                    "replace the member with one declared non-empty check id",
                )
            )
            continue
        if item in seen:
            findings.append(
                _finding(
                    source,
                    None,
                    "invalid-required-checks",
                    f"required check {item!r} is listed more than once",
                    "keep each required check id exactly once",
                )
            )
            continue
        seen.add(item)
        valid.append(item)
    return tuple(valid), sort_findings(findings)


def x__normalise_required_checks__mutmut_50(
    value: object,
    *,
    source: Path,
) -> tuple[tuple[str, ...], tuple[ContractFinding, ...]]:
    if not isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return (), (
            _finding(
                source,
                "/required_checks",
                "invalid-required-checks",
                "required_checks must be a list of unique non-empty strings",
                "configure required_checks as an array of check ids",
            ),
        )
    valid: list[str] = []
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, item in enumerate(value):
        if not isinstance(item, str) or not item.strip():
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    "each required check id must be a non-empty string",
                    "replace the member with one declared non-empty check id",
                )
            )
            continue
        if item in seen:
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    None,
                    f"required check {item!r} is listed more than once",
                    "keep each required check id exactly once",
                )
            )
            continue
        seen.add(item)
        valid.append(item)
    return tuple(valid), sort_findings(findings)


def x__normalise_required_checks__mutmut_51(
    value: object,
    *,
    source: Path,
) -> tuple[tuple[str, ...], tuple[ContractFinding, ...]]:
    if not isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return (), (
            _finding(
                source,
                "/required_checks",
                "invalid-required-checks",
                "required_checks must be a list of unique non-empty strings",
                "configure required_checks as an array of check ids",
            ),
        )
    valid: list[str] = []
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, item in enumerate(value):
        if not isinstance(item, str) or not item.strip():
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    "each required check id must be a non-empty string",
                    "replace the member with one declared non-empty check id",
                )
            )
            continue
        if item in seen:
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    None,
                    "keep each required check id exactly once",
                )
            )
            continue
        seen.add(item)
        valid.append(item)
    return tuple(valid), sort_findings(findings)


def x__normalise_required_checks__mutmut_52(
    value: object,
    *,
    source: Path,
) -> tuple[tuple[str, ...], tuple[ContractFinding, ...]]:
    if not isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return (), (
            _finding(
                source,
                "/required_checks",
                "invalid-required-checks",
                "required_checks must be a list of unique non-empty strings",
                "configure required_checks as an array of check ids",
            ),
        )
    valid: list[str] = []
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, item in enumerate(value):
        if not isinstance(item, str) or not item.strip():
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    "each required check id must be a non-empty string",
                    "replace the member with one declared non-empty check id",
                )
            )
            continue
        if item in seen:
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    f"required check {item!r} is listed more than once",
                    None,
                )
            )
            continue
        seen.add(item)
        valid.append(item)
    return tuple(valid), sort_findings(findings)


def x__normalise_required_checks__mutmut_53(
    value: object,
    *,
    source: Path,
) -> tuple[tuple[str, ...], tuple[ContractFinding, ...]]:
    if not isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return (), (
            _finding(
                source,
                "/required_checks",
                "invalid-required-checks",
                "required_checks must be a list of unique non-empty strings",
                "configure required_checks as an array of check ids",
            ),
        )
    valid: list[str] = []
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, item in enumerate(value):
        if not isinstance(item, str) or not item.strip():
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    "each required check id must be a non-empty string",
                    "replace the member with one declared non-empty check id",
                )
            )
            continue
        if item in seen:
            findings.append(
                _finding(
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    f"required check {item!r} is listed more than once",
                    "keep each required check id exactly once",
                )
            )
            continue
        seen.add(item)
        valid.append(item)
    return tuple(valid), sort_findings(findings)


def x__normalise_required_checks__mutmut_54(
    value: object,
    *,
    source: Path,
) -> tuple[tuple[str, ...], tuple[ContractFinding, ...]]:
    if not isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return (), (
            _finding(
                source,
                "/required_checks",
                "invalid-required-checks",
                "required_checks must be a list of unique non-empty strings",
                "configure required_checks as an array of check ids",
            ),
        )
    valid: list[str] = []
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, item in enumerate(value):
        if not isinstance(item, str) or not item.strip():
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    "each required check id must be a non-empty string",
                    "replace the member with one declared non-empty check id",
                )
            )
            continue
        if item in seen:
            findings.append(
                _finding(
                    source,
                    "invalid-required-checks",
                    f"required check {item!r} is listed more than once",
                    "keep each required check id exactly once",
                )
            )
            continue
        seen.add(item)
        valid.append(item)
    return tuple(valid), sort_findings(findings)


def x__normalise_required_checks__mutmut_55(
    value: object,
    *,
    source: Path,
) -> tuple[tuple[str, ...], tuple[ContractFinding, ...]]:
    if not isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return (), (
            _finding(
                source,
                "/required_checks",
                "invalid-required-checks",
                "required_checks must be a list of unique non-empty strings",
                "configure required_checks as an array of check ids",
            ),
        )
    valid: list[str] = []
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, item in enumerate(value):
        if not isinstance(item, str) or not item.strip():
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    "each required check id must be a non-empty string",
                    "replace the member with one declared non-empty check id",
                )
            )
            continue
        if item in seen:
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    f"required check {item!r} is listed more than once",
                    "keep each required check id exactly once",
                )
            )
            continue
        seen.add(item)
        valid.append(item)
    return tuple(valid), sort_findings(findings)


def x__normalise_required_checks__mutmut_56(
    value: object,
    *,
    source: Path,
) -> tuple[tuple[str, ...], tuple[ContractFinding, ...]]:
    if not isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return (), (
            _finding(
                source,
                "/required_checks",
                "invalid-required-checks",
                "required_checks must be a list of unique non-empty strings",
                "configure required_checks as an array of check ids",
            ),
        )
    valid: list[str] = []
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, item in enumerate(value):
        if not isinstance(item, str) or not item.strip():
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    "each required check id must be a non-empty string",
                    "replace the member with one declared non-empty check id",
                )
            )
            continue
        if item in seen:
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    "keep each required check id exactly once",
                )
            )
            continue
        seen.add(item)
        valid.append(item)
    return tuple(valid), sort_findings(findings)


def x__normalise_required_checks__mutmut_57(
    value: object,
    *,
    source: Path,
) -> tuple[tuple[str, ...], tuple[ContractFinding, ...]]:
    if not isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return (), (
            _finding(
                source,
                "/required_checks",
                "invalid-required-checks",
                "required_checks must be a list of unique non-empty strings",
                "configure required_checks as an array of check ids",
            ),
        )
    valid: list[str] = []
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, item in enumerate(value):
        if not isinstance(item, str) or not item.strip():
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    "each required check id must be a non-empty string",
                    "replace the member with one declared non-empty check id",
                )
            )
            continue
        if item in seen:
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    f"required check {item!r} is listed more than once",
                    )
            )
            continue
        seen.add(item)
        valid.append(item)
    return tuple(valid), sort_findings(findings)


def x__normalise_required_checks__mutmut_58(
    value: object,
    *,
    source: Path,
) -> tuple[tuple[str, ...], tuple[ContractFinding, ...]]:
    if not isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return (), (
            _finding(
                source,
                "/required_checks",
                "invalid-required-checks",
                "required_checks must be a list of unique non-empty strings",
                "configure required_checks as an array of check ids",
            ),
        )
    valid: list[str] = []
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, item in enumerate(value):
        if not isinstance(item, str) or not item.strip():
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    "each required check id must be a non-empty string",
                    "replace the member with one declared non-empty check id",
                )
            )
            continue
        if item in seen:
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "XXinvalid-required-checksXX",
                    f"required check {item!r} is listed more than once",
                    "keep each required check id exactly once",
                )
            )
            continue
        seen.add(item)
        valid.append(item)
    return tuple(valid), sort_findings(findings)


def x__normalise_required_checks__mutmut_59(
    value: object,
    *,
    source: Path,
) -> tuple[tuple[str, ...], tuple[ContractFinding, ...]]:
    if not isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return (), (
            _finding(
                source,
                "/required_checks",
                "invalid-required-checks",
                "required_checks must be a list of unique non-empty strings",
                "configure required_checks as an array of check ids",
            ),
        )
    valid: list[str] = []
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, item in enumerate(value):
        if not isinstance(item, str) or not item.strip():
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    "each required check id must be a non-empty string",
                    "replace the member with one declared non-empty check id",
                )
            )
            continue
        if item in seen:
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "INVALID-REQUIRED-CHECKS",
                    f"required check {item!r} is listed more than once",
                    "keep each required check id exactly once",
                )
            )
            continue
        seen.add(item)
        valid.append(item)
    return tuple(valid), sort_findings(findings)


def x__normalise_required_checks__mutmut_60(
    value: object,
    *,
    source: Path,
) -> tuple[tuple[str, ...], tuple[ContractFinding, ...]]:
    if not isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return (), (
            _finding(
                source,
                "/required_checks",
                "invalid-required-checks",
                "required_checks must be a list of unique non-empty strings",
                "configure required_checks as an array of check ids",
            ),
        )
    valid: list[str] = []
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, item in enumerate(value):
        if not isinstance(item, str) or not item.strip():
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    "each required check id must be a non-empty string",
                    "replace the member with one declared non-empty check id",
                )
            )
            continue
        if item in seen:
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    f"required check {item!r} is listed more than once",
                    "XXkeep each required check id exactly onceXX",
                )
            )
            continue
        seen.add(item)
        valid.append(item)
    return tuple(valid), sort_findings(findings)


def x__normalise_required_checks__mutmut_61(
    value: object,
    *,
    source: Path,
) -> tuple[tuple[str, ...], tuple[ContractFinding, ...]]:
    if not isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return (), (
            _finding(
                source,
                "/required_checks",
                "invalid-required-checks",
                "required_checks must be a list of unique non-empty strings",
                "configure required_checks as an array of check ids",
            ),
        )
    valid: list[str] = []
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, item in enumerate(value):
        if not isinstance(item, str) or not item.strip():
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    "each required check id must be a non-empty string",
                    "replace the member with one declared non-empty check id",
                )
            )
            continue
        if item in seen:
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    f"required check {item!r} is listed more than once",
                    "KEEP EACH REQUIRED CHECK ID EXACTLY ONCE",
                )
            )
            continue
        seen.add(item)
        valid.append(item)
    return tuple(valid), sort_findings(findings)


def x__normalise_required_checks__mutmut_62(
    value: object,
    *,
    source: Path,
) -> tuple[tuple[str, ...], tuple[ContractFinding, ...]]:
    if not isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return (), (
            _finding(
                source,
                "/required_checks",
                "invalid-required-checks",
                "required_checks must be a list of unique non-empty strings",
                "configure required_checks as an array of check ids",
            ),
        )
    valid: list[str] = []
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, item in enumerate(value):
        if not isinstance(item, str) or not item.strip():
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    "each required check id must be a non-empty string",
                    "replace the member with one declared non-empty check id",
                )
            )
            continue
        if item in seen:
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    f"required check {item!r} is listed more than once",
                    "keep each required check id exactly once",
                )
            )
            break
        seen.add(item)
        valid.append(item)
    return tuple(valid), sort_findings(findings)


def x__normalise_required_checks__mutmut_63(
    value: object,
    *,
    source: Path,
) -> tuple[tuple[str, ...], tuple[ContractFinding, ...]]:
    if not isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return (), (
            _finding(
                source,
                "/required_checks",
                "invalid-required-checks",
                "required_checks must be a list of unique non-empty strings",
                "configure required_checks as an array of check ids",
            ),
        )
    valid: list[str] = []
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, item in enumerate(value):
        if not isinstance(item, str) or not item.strip():
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    "each required check id must be a non-empty string",
                    "replace the member with one declared non-empty check id",
                )
            )
            continue
        if item in seen:
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    f"required check {item!r} is listed more than once",
                    "keep each required check id exactly once",
                )
            )
            continue
        seen.add(None)
        valid.append(item)
    return tuple(valid), sort_findings(findings)


def x__normalise_required_checks__mutmut_64(
    value: object,
    *,
    source: Path,
) -> tuple[tuple[str, ...], tuple[ContractFinding, ...]]:
    if not isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return (), (
            _finding(
                source,
                "/required_checks",
                "invalid-required-checks",
                "required_checks must be a list of unique non-empty strings",
                "configure required_checks as an array of check ids",
            ),
        )
    valid: list[str] = []
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, item in enumerate(value):
        if not isinstance(item, str) or not item.strip():
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    "each required check id must be a non-empty string",
                    "replace the member with one declared non-empty check id",
                )
            )
            continue
        if item in seen:
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    f"required check {item!r} is listed more than once",
                    "keep each required check id exactly once",
                )
            )
            continue
        seen.add(item)
        valid.append(None)
    return tuple(valid), sort_findings(findings)


def x__normalise_required_checks__mutmut_65(
    value: object,
    *,
    source: Path,
) -> tuple[tuple[str, ...], tuple[ContractFinding, ...]]:
    if not isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return (), (
            _finding(
                source,
                "/required_checks",
                "invalid-required-checks",
                "required_checks must be a list of unique non-empty strings",
                "configure required_checks as an array of check ids",
            ),
        )
    valid: list[str] = []
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, item in enumerate(value):
        if not isinstance(item, str) or not item.strip():
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    "each required check id must be a non-empty string",
                    "replace the member with one declared non-empty check id",
                )
            )
            continue
        if item in seen:
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    f"required check {item!r} is listed more than once",
                    "keep each required check id exactly once",
                )
            )
            continue
        seen.add(item)
        valid.append(item)
    return tuple(None), sort_findings(findings)


def x__normalise_required_checks__mutmut_66(
    value: object,
    *,
    source: Path,
) -> tuple[tuple[str, ...], tuple[ContractFinding, ...]]:
    if not isinstance(value, Sequence) or isinstance(value, (str, bytes)):
        return (), (
            _finding(
                source,
                "/required_checks",
                "invalid-required-checks",
                "required_checks must be a list of unique non-empty strings",
                "configure required_checks as an array of check ids",
            ),
        )
    valid: list[str] = []
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, item in enumerate(value):
        if not isinstance(item, str) or not item.strip():
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    "each required check id must be a non-empty string",
                    "replace the member with one declared non-empty check id",
                )
            )
            continue
        if item in seen:
            findings.append(
                _finding(
                    source,
                    f"/required_checks/{index}",
                    "invalid-required-checks",
                    f"required check {item!r} is listed more than once",
                    "keep each required check id exactly once",
                )
            )
            continue
        seen.add(item)
        valid.append(item)
    return tuple(valid), sort_findings(None)

mutants_x__normalise_required_checks__mutmut['_mutmut_orig'] = x__normalise_required_checks__mutmut_orig # type: ignore # mutmut generated
mutants_x__normalise_required_checks__mutmut['x__normalise_required_checks__mutmut_1'] = x__normalise_required_checks__mutmut_1 # type: ignore # mutmut generated
mutants_x__normalise_required_checks__mutmut['x__normalise_required_checks__mutmut_2'] = x__normalise_required_checks__mutmut_2 # type: ignore # mutmut generated
mutants_x__normalise_required_checks__mutmut['x__normalise_required_checks__mutmut_3'] = x__normalise_required_checks__mutmut_3 # type: ignore # mutmut generated
mutants_x__normalise_required_checks__mutmut['x__normalise_required_checks__mutmut_4'] = x__normalise_required_checks__mutmut_4 # type: ignore # mutmut generated
mutants_x__normalise_required_checks__mutmut['x__normalise_required_checks__mutmut_5'] = x__normalise_required_checks__mutmut_5 # type: ignore # mutmut generated
mutants_x__normalise_required_checks__mutmut['x__normalise_required_checks__mutmut_6'] = x__normalise_required_checks__mutmut_6 # type: ignore # mutmut generated
mutants_x__normalise_required_checks__mutmut['x__normalise_required_checks__mutmut_7'] = x__normalise_required_checks__mutmut_7 # type: ignore # mutmut generated
mutants_x__normalise_required_checks__mutmut['x__normalise_required_checks__mutmut_8'] = x__normalise_required_checks__mutmut_8 # type: ignore # mutmut generated
mutants_x__normalise_required_checks__mutmut['x__normalise_required_checks__mutmut_9'] = x__normalise_required_checks__mutmut_9 # type: ignore # mutmut generated
mutants_x__normalise_required_checks__mutmut['x__normalise_required_checks__mutmut_10'] = x__normalise_required_checks__mutmut_10 # type: ignore # mutmut generated
mutants_x__normalise_required_checks__mutmut['x__normalise_required_checks__mutmut_11'] = x__normalise_required_checks__mutmut_11 # type: ignore # mutmut generated
mutants_x__normalise_required_checks__mutmut['x__normalise_required_checks__mutmut_12'] = x__normalise_required_checks__mutmut_12 # type: ignore # mutmut generated
mutants_x__normalise_required_checks__mutmut['x__normalise_required_checks__mutmut_13'] = x__normalise_required_checks__mutmut_13 # type: ignore # mutmut generated
mutants_x__normalise_required_checks__mutmut['x__normalise_required_checks__mutmut_14'] = x__normalise_required_checks__mutmut_14 # type: ignore # mutmut generated
mutants_x__normalise_required_checks__mutmut['x__normalise_required_checks__mutmut_15'] = x__normalise_required_checks__mutmut_15 # type: ignore # mutmut generated
mutants_x__normalise_required_checks__mutmut['x__normalise_required_checks__mutmut_16'] = x__normalise_required_checks__mutmut_16 # type: ignore # mutmut generated
mutants_x__normalise_required_checks__mutmut['x__normalise_required_checks__mutmut_17'] = x__normalise_required_checks__mutmut_17 # type: ignore # mutmut generated
mutants_x__normalise_required_checks__mutmut['x__normalise_required_checks__mutmut_18'] = x__normalise_required_checks__mutmut_18 # type: ignore # mutmut generated
mutants_x__normalise_required_checks__mutmut['x__normalise_required_checks__mutmut_19'] = x__normalise_required_checks__mutmut_19 # type: ignore # mutmut generated
mutants_x__normalise_required_checks__mutmut['x__normalise_required_checks__mutmut_20'] = x__normalise_required_checks__mutmut_20 # type: ignore # mutmut generated
mutants_x__normalise_required_checks__mutmut['x__normalise_required_checks__mutmut_21'] = x__normalise_required_checks__mutmut_21 # type: ignore # mutmut generated
mutants_x__normalise_required_checks__mutmut['x__normalise_required_checks__mutmut_22'] = x__normalise_required_checks__mutmut_22 # type: ignore # mutmut generated
mutants_x__normalise_required_checks__mutmut['x__normalise_required_checks__mutmut_23'] = x__normalise_required_checks__mutmut_23 # type: ignore # mutmut generated
mutants_x__normalise_required_checks__mutmut['x__normalise_required_checks__mutmut_24'] = x__normalise_required_checks__mutmut_24 # type: ignore # mutmut generated
mutants_x__normalise_required_checks__mutmut['x__normalise_required_checks__mutmut_25'] = x__normalise_required_checks__mutmut_25 # type: ignore # mutmut generated
mutants_x__normalise_required_checks__mutmut['x__normalise_required_checks__mutmut_26'] = x__normalise_required_checks__mutmut_26 # type: ignore # mutmut generated
mutants_x__normalise_required_checks__mutmut['x__normalise_required_checks__mutmut_27'] = x__normalise_required_checks__mutmut_27 # type: ignore # mutmut generated
mutants_x__normalise_required_checks__mutmut['x__normalise_required_checks__mutmut_28'] = x__normalise_required_checks__mutmut_28 # type: ignore # mutmut generated
mutants_x__normalise_required_checks__mutmut['x__normalise_required_checks__mutmut_29'] = x__normalise_required_checks__mutmut_29 # type: ignore # mutmut generated
mutants_x__normalise_required_checks__mutmut['x__normalise_required_checks__mutmut_30'] = x__normalise_required_checks__mutmut_30 # type: ignore # mutmut generated
mutants_x__normalise_required_checks__mutmut['x__normalise_required_checks__mutmut_31'] = x__normalise_required_checks__mutmut_31 # type: ignore # mutmut generated
mutants_x__normalise_required_checks__mutmut['x__normalise_required_checks__mutmut_32'] = x__normalise_required_checks__mutmut_32 # type: ignore # mutmut generated
mutants_x__normalise_required_checks__mutmut['x__normalise_required_checks__mutmut_33'] = x__normalise_required_checks__mutmut_33 # type: ignore # mutmut generated
mutants_x__normalise_required_checks__mutmut['x__normalise_required_checks__mutmut_34'] = x__normalise_required_checks__mutmut_34 # type: ignore # mutmut generated
mutants_x__normalise_required_checks__mutmut['x__normalise_required_checks__mutmut_35'] = x__normalise_required_checks__mutmut_35 # type: ignore # mutmut generated
mutants_x__normalise_required_checks__mutmut['x__normalise_required_checks__mutmut_36'] = x__normalise_required_checks__mutmut_36 # type: ignore # mutmut generated
mutants_x__normalise_required_checks__mutmut['x__normalise_required_checks__mutmut_37'] = x__normalise_required_checks__mutmut_37 # type: ignore # mutmut generated
mutants_x__normalise_required_checks__mutmut['x__normalise_required_checks__mutmut_38'] = x__normalise_required_checks__mutmut_38 # type: ignore # mutmut generated
mutants_x__normalise_required_checks__mutmut['x__normalise_required_checks__mutmut_39'] = x__normalise_required_checks__mutmut_39 # type: ignore # mutmut generated
mutants_x__normalise_required_checks__mutmut['x__normalise_required_checks__mutmut_40'] = x__normalise_required_checks__mutmut_40 # type: ignore # mutmut generated
mutants_x__normalise_required_checks__mutmut['x__normalise_required_checks__mutmut_41'] = x__normalise_required_checks__mutmut_41 # type: ignore # mutmut generated
mutants_x__normalise_required_checks__mutmut['x__normalise_required_checks__mutmut_42'] = x__normalise_required_checks__mutmut_42 # type: ignore # mutmut generated
mutants_x__normalise_required_checks__mutmut['x__normalise_required_checks__mutmut_43'] = x__normalise_required_checks__mutmut_43 # type: ignore # mutmut generated
mutants_x__normalise_required_checks__mutmut['x__normalise_required_checks__mutmut_44'] = x__normalise_required_checks__mutmut_44 # type: ignore # mutmut generated
mutants_x__normalise_required_checks__mutmut['x__normalise_required_checks__mutmut_45'] = x__normalise_required_checks__mutmut_45 # type: ignore # mutmut generated
mutants_x__normalise_required_checks__mutmut['x__normalise_required_checks__mutmut_46'] = x__normalise_required_checks__mutmut_46 # type: ignore # mutmut generated
mutants_x__normalise_required_checks__mutmut['x__normalise_required_checks__mutmut_47'] = x__normalise_required_checks__mutmut_47 # type: ignore # mutmut generated
mutants_x__normalise_required_checks__mutmut['x__normalise_required_checks__mutmut_48'] = x__normalise_required_checks__mutmut_48 # type: ignore # mutmut generated
mutants_x__normalise_required_checks__mutmut['x__normalise_required_checks__mutmut_49'] = x__normalise_required_checks__mutmut_49 # type: ignore # mutmut generated
mutants_x__normalise_required_checks__mutmut['x__normalise_required_checks__mutmut_50'] = x__normalise_required_checks__mutmut_50 # type: ignore # mutmut generated
mutants_x__normalise_required_checks__mutmut['x__normalise_required_checks__mutmut_51'] = x__normalise_required_checks__mutmut_51 # type: ignore # mutmut generated
mutants_x__normalise_required_checks__mutmut['x__normalise_required_checks__mutmut_52'] = x__normalise_required_checks__mutmut_52 # type: ignore # mutmut generated
mutants_x__normalise_required_checks__mutmut['x__normalise_required_checks__mutmut_53'] = x__normalise_required_checks__mutmut_53 # type: ignore # mutmut generated
mutants_x__normalise_required_checks__mutmut['x__normalise_required_checks__mutmut_54'] = x__normalise_required_checks__mutmut_54 # type: ignore # mutmut generated
mutants_x__normalise_required_checks__mutmut['x__normalise_required_checks__mutmut_55'] = x__normalise_required_checks__mutmut_55 # type: ignore # mutmut generated
mutants_x__normalise_required_checks__mutmut['x__normalise_required_checks__mutmut_56'] = x__normalise_required_checks__mutmut_56 # type: ignore # mutmut generated
mutants_x__normalise_required_checks__mutmut['x__normalise_required_checks__mutmut_57'] = x__normalise_required_checks__mutmut_57 # type: ignore # mutmut generated
mutants_x__normalise_required_checks__mutmut['x__normalise_required_checks__mutmut_58'] = x__normalise_required_checks__mutmut_58 # type: ignore # mutmut generated
mutants_x__normalise_required_checks__mutmut['x__normalise_required_checks__mutmut_59'] = x__normalise_required_checks__mutmut_59 # type: ignore # mutmut generated
mutants_x__normalise_required_checks__mutmut['x__normalise_required_checks__mutmut_60'] = x__normalise_required_checks__mutmut_60 # type: ignore # mutmut generated
mutants_x__normalise_required_checks__mutmut['x__normalise_required_checks__mutmut_61'] = x__normalise_required_checks__mutmut_61 # type: ignore # mutmut generated
mutants_x__normalise_required_checks__mutmut['x__normalise_required_checks__mutmut_62'] = x__normalise_required_checks__mutmut_62 # type: ignore # mutmut generated
mutants_x__normalise_required_checks__mutmut['x__normalise_required_checks__mutmut_63'] = x__normalise_required_checks__mutmut_63 # type: ignore # mutmut generated
mutants_x__normalise_required_checks__mutmut['x__normalise_required_checks__mutmut_64'] = x__normalise_required_checks__mutmut_64 # type: ignore # mutmut generated
mutants_x__normalise_required_checks__mutmut['x__normalise_required_checks__mutmut_65'] = x__normalise_required_checks__mutmut_65 # type: ignore # mutmut generated
mutants_x__normalise_required_checks__mutmut['x__normalise_required_checks__mutmut_66'] = x__normalise_required_checks__mutmut_66 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__receipt_identity_findings__mutmut)
def _receipt_identity_findings(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_orig(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_1(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = None
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_2(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_3(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                None
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_4(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    None,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_5(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    None,
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_6(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    None,
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_7(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    None,
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_8(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    None,
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_9(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_10(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_11(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_12(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_13(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_14(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "XXmissing-receipt-identityXX",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_15(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "MISSING-RECEIPT-IDENTITY",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_16(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = None
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_17(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(None)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_18(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence or (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_19(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field not in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_20(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) and not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_21(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_22(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_23(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                None
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_24(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    None,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_25(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    None,
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_26(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    None,
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_27(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    None,
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_28(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    None,
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_29(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_30(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_31(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_32(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_33(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_34(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "XXinvalid-receipt-identityXX",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_35(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "INVALID-RECEIPT-IDENTITY",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_36(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = None
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_37(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(None)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_38(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence or not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_39(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field not in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_40(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_41(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(None):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_42(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                None
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_43(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    None,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_44(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    None,
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_45(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    None,
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_46(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    None,
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_47(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    None,
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_48(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_49(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_50(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_51(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_52(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_53(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "XXinvalid-receipt-identityXX",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_54(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "INVALID-RECEIPT-IDENTITY",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_55(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = None
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_56(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get(None)
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_57(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("XXsource_shaXX")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_58(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("SOURCE_SHA")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_59(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha or _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_60(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) or source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_61(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(None) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_62(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is not None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_63(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            None
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_64(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                None,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_65(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                None,
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_66(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                None,
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_67(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                None,
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_68(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                None,
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_69(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_70(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_71(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_72(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_73(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_74(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "XX/source_shaXX",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_75(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/SOURCE_SHA",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_76(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "XXinvalid-receipt-identityXX",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_77(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "INVALID-RECEIPT-IDENTITY",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_78(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "XXreceipt source_sha must be 40 or 64 lowercase hexadecimal charactersXX",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_79(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "RECEIPT SOURCE_SHA MUST BE 40 OR 64 LOWERCASE HEXADECIMAL CHARACTERS",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_80(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "XXrecord the full lowercase source commit SHAXX",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_81(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit sha",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_82(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "RECORD THE FULL LOWERCASE SOURCE COMMIT SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_83(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = None
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_84(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get(None)
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_85(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("XXimage_digestXX")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_86(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("IMAGE_DIGEST")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_87(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest or not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_88(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) or image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_89(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_90(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(None):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_91(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            None
        )
    return findings


def x__receipt_identity_findings__mutmut_92(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                None,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_93(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                None,
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_94(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                None,
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_95(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                None,
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_96(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                None,
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_97(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_98(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_99(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_100(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_101(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                )
        )
    return findings


def x__receipt_identity_findings__mutmut_102(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "XX/image_digestXX",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_103(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/IMAGE_DIGEST",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_104(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "XXinvalid-receipt-identityXX",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_105(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "INVALID-RECEIPT-IDENTITY",
                "receipt image_digest must use canonical sha256 notation",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_106(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "XXreceipt image_digest must use canonical sha256 notationXX",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_107(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "RECEIPT IMAGE_DIGEST MUST USE CANONICAL SHA256 NOTATION",
                "record sha256 followed by 64 lowercase hexadecimal characters",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_108(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "XXrecord sha256 followed by 64 lowercase hexadecimal charactersXX",
            )
        )
    return findings


def x__receipt_identity_findings__mutmut_109(
    evidence: Mapping[str, object],
    *,
    source: Path,
) -> list[ContractFinding]:
    findings: list[ContractFinding] = []
    for field in _IDENTITY_FIELDS:
        if field not in evidence:
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "missing-receipt-identity",
                    f"receipt identity {field} is required",
                    f"record the observed {field} for this deployment attempt",
                )
            )
    for field in (*_BASE_IDENTITY_FIELDS, *_STRING_RECEIPT_IDENTITIES):
        value = evidence.get(field)
        if field in evidence and (not isinstance(value, str) or not value.strip()):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-empty string",
                    f"record a non-empty observed {field}",
                )
            )
    for field in _INTEGER_RECEIPT_IDENTITIES:
        value = evidence.get(field)
        if field in evidence and not is_integer_identity(value):
            findings.append(
                _finding(
                    source,
                    f"/{field}",
                    "invalid-receipt-identity",
                    f"receipt identity {field} must be a non-negative integer, not a boolean",
                    f"record {field} as a JSON integer",
                )
            )
    source_sha = evidence.get("source_sha")
    if isinstance(source_sha, str) and source_sha and _SOURCE_SHA_RE.fullmatch(source_sha) is None:
        findings.append(
            _finding(
                source,
                "/source_sha",
                "invalid-receipt-identity",
                "receipt source_sha must be 40 or 64 lowercase hexadecimal characters",
                "record the full lowercase source commit SHA",
            )
        )
    image_digest = evidence.get("image_digest")
    if isinstance(image_digest, str) and image_digest and not is_sha256_digest(image_digest):
        findings.append(
            _finding(
                source,
                "/image_digest",
                "invalid-receipt-identity",
                "receipt image_digest must use canonical sha256 notation",
                "RECORD SHA256 FOLLOWED BY 64 LOWERCASE HEXADECIMAL CHARACTERS",
            )
        )
    return findings

mutants_x__receipt_identity_findings__mutmut['_mutmut_orig'] = x__receipt_identity_findings__mutmut_orig # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_1'] = x__receipt_identity_findings__mutmut_1 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_2'] = x__receipt_identity_findings__mutmut_2 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_3'] = x__receipt_identity_findings__mutmut_3 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_4'] = x__receipt_identity_findings__mutmut_4 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_5'] = x__receipt_identity_findings__mutmut_5 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_6'] = x__receipt_identity_findings__mutmut_6 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_7'] = x__receipt_identity_findings__mutmut_7 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_8'] = x__receipt_identity_findings__mutmut_8 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_9'] = x__receipt_identity_findings__mutmut_9 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_10'] = x__receipt_identity_findings__mutmut_10 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_11'] = x__receipt_identity_findings__mutmut_11 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_12'] = x__receipt_identity_findings__mutmut_12 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_13'] = x__receipt_identity_findings__mutmut_13 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_14'] = x__receipt_identity_findings__mutmut_14 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_15'] = x__receipt_identity_findings__mutmut_15 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_16'] = x__receipt_identity_findings__mutmut_16 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_17'] = x__receipt_identity_findings__mutmut_17 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_18'] = x__receipt_identity_findings__mutmut_18 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_19'] = x__receipt_identity_findings__mutmut_19 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_20'] = x__receipt_identity_findings__mutmut_20 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_21'] = x__receipt_identity_findings__mutmut_21 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_22'] = x__receipt_identity_findings__mutmut_22 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_23'] = x__receipt_identity_findings__mutmut_23 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_24'] = x__receipt_identity_findings__mutmut_24 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_25'] = x__receipt_identity_findings__mutmut_25 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_26'] = x__receipt_identity_findings__mutmut_26 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_27'] = x__receipt_identity_findings__mutmut_27 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_28'] = x__receipt_identity_findings__mutmut_28 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_29'] = x__receipt_identity_findings__mutmut_29 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_30'] = x__receipt_identity_findings__mutmut_30 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_31'] = x__receipt_identity_findings__mutmut_31 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_32'] = x__receipt_identity_findings__mutmut_32 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_33'] = x__receipt_identity_findings__mutmut_33 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_34'] = x__receipt_identity_findings__mutmut_34 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_35'] = x__receipt_identity_findings__mutmut_35 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_36'] = x__receipt_identity_findings__mutmut_36 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_37'] = x__receipt_identity_findings__mutmut_37 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_38'] = x__receipt_identity_findings__mutmut_38 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_39'] = x__receipt_identity_findings__mutmut_39 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_40'] = x__receipt_identity_findings__mutmut_40 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_41'] = x__receipt_identity_findings__mutmut_41 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_42'] = x__receipt_identity_findings__mutmut_42 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_43'] = x__receipt_identity_findings__mutmut_43 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_44'] = x__receipt_identity_findings__mutmut_44 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_45'] = x__receipt_identity_findings__mutmut_45 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_46'] = x__receipt_identity_findings__mutmut_46 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_47'] = x__receipt_identity_findings__mutmut_47 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_48'] = x__receipt_identity_findings__mutmut_48 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_49'] = x__receipt_identity_findings__mutmut_49 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_50'] = x__receipt_identity_findings__mutmut_50 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_51'] = x__receipt_identity_findings__mutmut_51 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_52'] = x__receipt_identity_findings__mutmut_52 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_53'] = x__receipt_identity_findings__mutmut_53 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_54'] = x__receipt_identity_findings__mutmut_54 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_55'] = x__receipt_identity_findings__mutmut_55 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_56'] = x__receipt_identity_findings__mutmut_56 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_57'] = x__receipt_identity_findings__mutmut_57 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_58'] = x__receipt_identity_findings__mutmut_58 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_59'] = x__receipt_identity_findings__mutmut_59 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_60'] = x__receipt_identity_findings__mutmut_60 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_61'] = x__receipt_identity_findings__mutmut_61 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_62'] = x__receipt_identity_findings__mutmut_62 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_63'] = x__receipt_identity_findings__mutmut_63 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_64'] = x__receipt_identity_findings__mutmut_64 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_65'] = x__receipt_identity_findings__mutmut_65 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_66'] = x__receipt_identity_findings__mutmut_66 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_67'] = x__receipt_identity_findings__mutmut_67 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_68'] = x__receipt_identity_findings__mutmut_68 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_69'] = x__receipt_identity_findings__mutmut_69 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_70'] = x__receipt_identity_findings__mutmut_70 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_71'] = x__receipt_identity_findings__mutmut_71 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_72'] = x__receipt_identity_findings__mutmut_72 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_73'] = x__receipt_identity_findings__mutmut_73 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_74'] = x__receipt_identity_findings__mutmut_74 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_75'] = x__receipt_identity_findings__mutmut_75 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_76'] = x__receipt_identity_findings__mutmut_76 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_77'] = x__receipt_identity_findings__mutmut_77 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_78'] = x__receipt_identity_findings__mutmut_78 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_79'] = x__receipt_identity_findings__mutmut_79 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_80'] = x__receipt_identity_findings__mutmut_80 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_81'] = x__receipt_identity_findings__mutmut_81 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_82'] = x__receipt_identity_findings__mutmut_82 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_83'] = x__receipt_identity_findings__mutmut_83 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_84'] = x__receipt_identity_findings__mutmut_84 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_85'] = x__receipt_identity_findings__mutmut_85 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_86'] = x__receipt_identity_findings__mutmut_86 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_87'] = x__receipt_identity_findings__mutmut_87 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_88'] = x__receipt_identity_findings__mutmut_88 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_89'] = x__receipt_identity_findings__mutmut_89 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_90'] = x__receipt_identity_findings__mutmut_90 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_91'] = x__receipt_identity_findings__mutmut_91 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_92'] = x__receipt_identity_findings__mutmut_92 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_93'] = x__receipt_identity_findings__mutmut_93 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_94'] = x__receipt_identity_findings__mutmut_94 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_95'] = x__receipt_identity_findings__mutmut_95 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_96'] = x__receipt_identity_findings__mutmut_96 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_97'] = x__receipt_identity_findings__mutmut_97 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_98'] = x__receipt_identity_findings__mutmut_98 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_99'] = x__receipt_identity_findings__mutmut_99 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_100'] = x__receipt_identity_findings__mutmut_100 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_101'] = x__receipt_identity_findings__mutmut_101 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_102'] = x__receipt_identity_findings__mutmut_102 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_103'] = x__receipt_identity_findings__mutmut_103 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_104'] = x__receipt_identity_findings__mutmut_104 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_105'] = x__receipt_identity_findings__mutmut_105 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_106'] = x__receipt_identity_findings__mutmut_106 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_107'] = x__receipt_identity_findings__mutmut_107 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_108'] = x__receipt_identity_findings__mutmut_108 # type: ignore # mutmut generated
mutants_x__receipt_identity_findings__mutmut['x__receipt_identity_findings__mutmut_109'] = x__receipt_identity_findings__mutmut_109 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__freshness_findings__mutmut)
def _freshness_findings(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_orig(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_1(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = None
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_2(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get(None)
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_3(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("XXcaptured_atXX")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_4(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("CAPTURED_AT")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_5(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_6(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                None,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_7(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                None,
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_8(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                None,
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_9(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                None,
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_10(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                None,
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_11(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_12(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_13(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_14(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_15(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_16(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "XX/captured_atXX",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_17(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/CAPTURED_AT",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_18(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "XXmissing-capture-timeXX",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_19(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "MISSING-CAPTURE-TIME",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_20(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "XXevidence requires a timezone-aware capture timestampXX",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_21(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "EVIDENCE REQUIRES A TIMEZONE-AWARE CAPTURE TIMESTAMP",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_22(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "XXrecord captured_at as an ISO 8601 timestamp with a timezoneXX",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_23(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an iso 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_24(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "RECORD CAPTURED_AT AS AN ISO 8601 TIMESTAMP WITH A TIMEZONE",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_25(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = None
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_26(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(None)
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_27(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace(None, "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_28(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", None))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_29(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_30(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", ))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_31(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("XXZXX", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_32(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_33(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "XX+00:00XX"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_34(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = ""
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_35(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None and captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_36(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None and captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_37(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is not None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_38(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is not None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_39(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is not None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_40(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                None,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_41(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                None,
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_42(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                None,
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_43(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                None,
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_44(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                None,
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_45(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_46(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_47(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_48(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_49(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_50(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "XX/captured_atXX",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_51(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/CAPTURED_AT",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_52(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "XXinvalid-capture-timeXX",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_53(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "INVALID-CAPTURE-TIME",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_54(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "XXcaptured_at must be a valid timezone-aware ISO 8601 timestampXX",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_55(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware iso 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_56(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "CAPTURED_AT MUST BE A VALID TIMEZONE-AWARE ISO 8601 TIMESTAMP",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_57(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "XXrecord captured_at with an explicit UTC offsetXX",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_58(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit utc offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_59(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "RECORD CAPTURED_AT WITH AN EXPLICIT UTC OFFSET",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_60(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None and now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_61(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is not None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_62(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is not None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_63(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                None,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_64(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                None,
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_65(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                None,
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_66(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                None,
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_67(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                None,
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_68(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_69(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_70(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_71(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_72(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_73(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "XX/captured_atXX",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_74(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/CAPTURED_AT",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_75(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "XXinvalid-verification-timeXX",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_76(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "INVALID-VERIFICATION-TIME",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_77(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "XXverification time must be timezone-awareXX",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_78(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "VERIFICATION TIME MUST BE TIMEZONE-AWARE",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_79(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "XXverify using a timezone-aware current timeXX",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_80(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "VERIFY USING A TIMEZONE-AWARE CURRENT TIME",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_81(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = None
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_82(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) + captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_83(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(None) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_84(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(None)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_85(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds <= 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_86(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 1:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_87(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                None,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_88(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                None,
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_89(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                None,
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_90(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                None,
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_91(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                None,
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_92(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_93(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_94(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_95(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_96(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_97(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "XX/captured_atXX",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_98(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/CAPTURED_AT",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_99(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "XXfuture-evidenceXX",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_100(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "FUTURE-EVIDENCE",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_101(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "XXevidence capture time is later than the verifier clockXX",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_102(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "EVIDENCE CAPTURE TIME IS LATER THAN THE VERIFIER CLOCK",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_103(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "XXsynchronise clocks and collect a new receiptXX",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_104(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "SYNCHRONISE CLOCKS AND COLLECT A NEW RECEIPT",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_105(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds >= max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_106(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                None,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_107(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                None,
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_108(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                None,
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_109(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                None,
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_110(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                None,
            )
        ]
    return []


def x__freshness_findings__mutmut_111(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_112(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_113(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_114(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_115(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                )
        ]
    return []


def x__freshness_findings__mutmut_116(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "XX/captured_atXX",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_117(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/CAPTURED_AT",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_118(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "XXstale-evidenceXX",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_119(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "STALE-EVIDENCE",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "collect a fresh receipt for this deployment attempt",
            )
        ]
    return []


def x__freshness_findings__mutmut_120(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "XXcollect a fresh receipt for this deployment attemptXX",
            )
        ]
    return []


def x__freshness_findings__mutmut_121(
    evidence: Mapping[str, object],
    *,
    source: Path,
    now: datetime,
    max_age_seconds: int,
) -> list[ContractFinding]:
    captured = evidence.get("captured_at")
    if not isinstance(captured, str):
        return [
            _finding(
                source,
                "/captured_at",
                "missing-capture-time",
                "evidence requires a timezone-aware capture timestamp",
                "record captured_at as an ISO 8601 timestamp with a timezone",
            )
        ]
    try:
        captured_at = datetime.fromisoformat(captured.replace("Z", "+00:00"))
    except ValueError:
        captured_at = None
    if captured_at is None or captured_at.tzinfo is None or captured_at.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-capture-time",
                "captured_at must be a valid timezone-aware ISO 8601 timestamp",
                "record captured_at with an explicit UTC offset",
            )
        ]
    if now.tzinfo is None or now.utcoffset() is None:
        return [
            _finding(
                source,
                "/captured_at",
                "invalid-verification-time",
                "verification time must be timezone-aware",
                "verify using a timezone-aware current time",
            )
        ]
    age_seconds = (now.astimezone(UTC) - captured_at.astimezone(UTC)).total_seconds()
    if age_seconds < 0:
        return [
            _finding(
                source,
                "/captured_at",
                "future-evidence",
                "evidence capture time is later than the verifier clock",
                "synchronise clocks and collect a new receipt",
            )
        ]
    if age_seconds > max_age_seconds:
        return [
            _finding(
                source,
                "/captured_at",
                "stale-evidence",
                f"evidence age exceeds the {max_age_seconds}-second freshness window",
                "COLLECT A FRESH RECEIPT FOR THIS DEPLOYMENT ATTEMPT",
            )
        ]
    return []

mutants_x__freshness_findings__mutmut['_mutmut_orig'] = x__freshness_findings__mutmut_orig # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_1'] = x__freshness_findings__mutmut_1 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_2'] = x__freshness_findings__mutmut_2 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_3'] = x__freshness_findings__mutmut_3 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_4'] = x__freshness_findings__mutmut_4 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_5'] = x__freshness_findings__mutmut_5 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_6'] = x__freshness_findings__mutmut_6 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_7'] = x__freshness_findings__mutmut_7 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_8'] = x__freshness_findings__mutmut_8 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_9'] = x__freshness_findings__mutmut_9 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_10'] = x__freshness_findings__mutmut_10 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_11'] = x__freshness_findings__mutmut_11 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_12'] = x__freshness_findings__mutmut_12 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_13'] = x__freshness_findings__mutmut_13 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_14'] = x__freshness_findings__mutmut_14 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_15'] = x__freshness_findings__mutmut_15 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_16'] = x__freshness_findings__mutmut_16 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_17'] = x__freshness_findings__mutmut_17 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_18'] = x__freshness_findings__mutmut_18 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_19'] = x__freshness_findings__mutmut_19 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_20'] = x__freshness_findings__mutmut_20 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_21'] = x__freshness_findings__mutmut_21 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_22'] = x__freshness_findings__mutmut_22 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_23'] = x__freshness_findings__mutmut_23 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_24'] = x__freshness_findings__mutmut_24 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_25'] = x__freshness_findings__mutmut_25 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_26'] = x__freshness_findings__mutmut_26 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_27'] = x__freshness_findings__mutmut_27 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_28'] = x__freshness_findings__mutmut_28 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_29'] = x__freshness_findings__mutmut_29 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_30'] = x__freshness_findings__mutmut_30 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_31'] = x__freshness_findings__mutmut_31 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_32'] = x__freshness_findings__mutmut_32 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_33'] = x__freshness_findings__mutmut_33 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_34'] = x__freshness_findings__mutmut_34 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_35'] = x__freshness_findings__mutmut_35 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_36'] = x__freshness_findings__mutmut_36 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_37'] = x__freshness_findings__mutmut_37 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_38'] = x__freshness_findings__mutmut_38 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_39'] = x__freshness_findings__mutmut_39 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_40'] = x__freshness_findings__mutmut_40 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_41'] = x__freshness_findings__mutmut_41 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_42'] = x__freshness_findings__mutmut_42 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_43'] = x__freshness_findings__mutmut_43 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_44'] = x__freshness_findings__mutmut_44 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_45'] = x__freshness_findings__mutmut_45 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_46'] = x__freshness_findings__mutmut_46 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_47'] = x__freshness_findings__mutmut_47 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_48'] = x__freshness_findings__mutmut_48 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_49'] = x__freshness_findings__mutmut_49 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_50'] = x__freshness_findings__mutmut_50 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_51'] = x__freshness_findings__mutmut_51 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_52'] = x__freshness_findings__mutmut_52 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_53'] = x__freshness_findings__mutmut_53 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_54'] = x__freshness_findings__mutmut_54 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_55'] = x__freshness_findings__mutmut_55 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_56'] = x__freshness_findings__mutmut_56 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_57'] = x__freshness_findings__mutmut_57 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_58'] = x__freshness_findings__mutmut_58 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_59'] = x__freshness_findings__mutmut_59 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_60'] = x__freshness_findings__mutmut_60 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_61'] = x__freshness_findings__mutmut_61 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_62'] = x__freshness_findings__mutmut_62 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_63'] = x__freshness_findings__mutmut_63 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_64'] = x__freshness_findings__mutmut_64 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_65'] = x__freshness_findings__mutmut_65 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_66'] = x__freshness_findings__mutmut_66 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_67'] = x__freshness_findings__mutmut_67 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_68'] = x__freshness_findings__mutmut_68 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_69'] = x__freshness_findings__mutmut_69 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_70'] = x__freshness_findings__mutmut_70 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_71'] = x__freshness_findings__mutmut_71 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_72'] = x__freshness_findings__mutmut_72 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_73'] = x__freshness_findings__mutmut_73 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_74'] = x__freshness_findings__mutmut_74 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_75'] = x__freshness_findings__mutmut_75 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_76'] = x__freshness_findings__mutmut_76 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_77'] = x__freshness_findings__mutmut_77 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_78'] = x__freshness_findings__mutmut_78 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_79'] = x__freshness_findings__mutmut_79 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_80'] = x__freshness_findings__mutmut_80 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_81'] = x__freshness_findings__mutmut_81 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_82'] = x__freshness_findings__mutmut_82 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_83'] = x__freshness_findings__mutmut_83 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_84'] = x__freshness_findings__mutmut_84 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_85'] = x__freshness_findings__mutmut_85 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_86'] = x__freshness_findings__mutmut_86 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_87'] = x__freshness_findings__mutmut_87 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_88'] = x__freshness_findings__mutmut_88 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_89'] = x__freshness_findings__mutmut_89 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_90'] = x__freshness_findings__mutmut_90 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_91'] = x__freshness_findings__mutmut_91 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_92'] = x__freshness_findings__mutmut_92 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_93'] = x__freshness_findings__mutmut_93 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_94'] = x__freshness_findings__mutmut_94 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_95'] = x__freshness_findings__mutmut_95 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_96'] = x__freshness_findings__mutmut_96 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_97'] = x__freshness_findings__mutmut_97 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_98'] = x__freshness_findings__mutmut_98 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_99'] = x__freshness_findings__mutmut_99 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_100'] = x__freshness_findings__mutmut_100 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_101'] = x__freshness_findings__mutmut_101 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_102'] = x__freshness_findings__mutmut_102 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_103'] = x__freshness_findings__mutmut_103 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_104'] = x__freshness_findings__mutmut_104 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_105'] = x__freshness_findings__mutmut_105 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_106'] = x__freshness_findings__mutmut_106 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_107'] = x__freshness_findings__mutmut_107 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_108'] = x__freshness_findings__mutmut_108 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_109'] = x__freshness_findings__mutmut_109 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_110'] = x__freshness_findings__mutmut_110 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_111'] = x__freshness_findings__mutmut_111 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_112'] = x__freshness_findings__mutmut_112 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_113'] = x__freshness_findings__mutmut_113 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_114'] = x__freshness_findings__mutmut_114 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_115'] = x__freshness_findings__mutmut_115 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_116'] = x__freshness_findings__mutmut_116 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_117'] = x__freshness_findings__mutmut_117 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_118'] = x__freshness_findings__mutmut_118 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_119'] = x__freshness_findings__mutmut_119 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_120'] = x__freshness_findings__mutmut_120 # type: ignore # mutmut generated
mutants_x__freshness_findings__mutmut['x__freshness_findings__mutmut_121'] = x__freshness_findings__mutmut_121 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__check_findings__mutmut)
def _check_findings(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_orig(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_1(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = None
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_2(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get(None)
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_3(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("XXchecksXX")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_4(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("CHECKS")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_5(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_6(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                None,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_7(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                None,
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_8(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                None,
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_9(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                None,
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_10(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                None,
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_11(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_12(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_13(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_14(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_15(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_16(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "XX/checksXX",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_17(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/CHECKS",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_18(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "XXmissing-checksXX",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_19(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "MISSING-CHECKS",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_20(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "XXevidence checks must be a listXX",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_21(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "EVIDENCE CHECKS MUST BE A LIST",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_22(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "XXrecord every required check observationXX",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_23(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "RECORD EVERY REQUIRED CHECK OBSERVATION",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_24(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = None
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_25(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = None
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_26(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(None):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_27(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_28(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                None
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_29(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    None,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_30(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    None,
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_31(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    None,
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_32(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    None,
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_33(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    None,
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_34(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_35(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_36(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_37(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_38(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_39(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "XXinvalid-checkXX",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_40(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "INVALID-CHECK",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_41(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "XXcheck evidence must be an objectXX",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_42(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "CHECK EVIDENCE MUST BE AN OBJECT",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_43(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "XXrecord a structured check resultXX",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_44(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "RECORD A STRUCTURED CHECK RESULT",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_45(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            break
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_46(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = None
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_47(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(None, value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_48(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], None)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_49(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_50(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], )
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_51(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = None
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_52(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get(None)
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_53(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("XXidXX")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_54(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("ID")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_55(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) and not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_56(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_57(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_58(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                None
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_59(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    None,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_60(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    None,
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_61(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    None,
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_62(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    None,
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_63(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    None,
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_64(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_65(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_66(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_67(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_68(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_69(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "XXmissing-check-idXX",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_70(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "MISSING-CHECK-ID",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_71(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "XXcheck evidence requires a non-empty idXX",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_72(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "CHECK EVIDENCE REQUIRES A NON-EMPTY ID",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_73(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "XXset the declared check idXX",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_74(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "SET THE DECLARED CHECK ID",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_75(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            break
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_76(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id not in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_77(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                None
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_78(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    None,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_79(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    None,
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_80(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    None,
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_81(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    None,
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_82(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    None,
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_83(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_84(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_85(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_86(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_87(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_88(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "XXduplicate-checkXX",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_89(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "DUPLICATE-CHECK",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_90(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "XXrecord exactly one result per check idXX",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_91(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "RECORD EXACTLY ONE RESULT PER CHECK ID",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_92(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = None
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_93(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = None
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_94(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get(None)
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_95(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("XXstatusXX")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_96(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("STATUS")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_97(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) and status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_98(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_99(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_100(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                None
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_101(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    None,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_102(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    None,
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_103(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    None,
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_104(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    None,
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_105(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    None,
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_106(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_107(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_108(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_109(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_110(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_111(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "XXinvalid-check-verdictXX",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_112(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "INVALID-CHECK-VERDICT",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_113(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "XXrecord passed, failed, skipped or expected-denialXX",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_114(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "RECORD PASSED, FAILED, SKIPPED OR EXPECTED-DENIAL",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_115(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status != "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_116(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "XXfailedXX":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_117(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "FAILED":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_118(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                None
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_119(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    None,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_120(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    None,
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_121(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    None,
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_122(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    None,
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_123(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    None,
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_124(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_125(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_126(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_127(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_128(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_129(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "XXfailed-check-verdictXX",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_130(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "FAILED-CHECK-VERDICT",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_131(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "XXcorrect the runtime defect and collect new passing evidenceXX",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_132(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "CORRECT THE RUNTIME DEFECT AND COLLECT NEW PASSING EVIDENCE",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_133(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status != "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_134(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "XXskippedXX":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_135(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "SKIPPED":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_136(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                None
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_137(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    None,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_138(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    None,
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_139(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    None,
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_140(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    None,
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_141(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    None,
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_142(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_143(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_144(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_145(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_146(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_147(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "XXcheck-skippedXX",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_148(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "CHECK-SKIPPED",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_149(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "XXexecute the check and record an observed resultXX",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_150(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "EXECUTE THE CHECK AND RECORD AN OBSERVED RESULT",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_151(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = None
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_152(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get(None)
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_153(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("XXobservationXX")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_154(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("OBSERVATION")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_155(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation and set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_156(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) and not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_157(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_158(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_159(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(None) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_160(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) < {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_161(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"XXexit_codeXX"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_162(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"EXIT_CODE"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_163(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                None
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_164(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    None,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_165(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    None,
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_166(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    None,
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_167(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    None,
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_168(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    None,
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_169(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_170(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_171(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_172(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_173(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_174(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "XXincomplete-observationXX",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_175(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "INCOMPLETE-OBSERVATION",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_176(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "XXrecord the observed runtime state that proves the check outcomeXX",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_177(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "RECORD THE OBSERVED RUNTIME STATE THAT PROVES THE CHECK OUTCOME",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_178(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            break
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_179(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = None
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_180(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(None, observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_181(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], None)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_182(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_183(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], )
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_184(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = None
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_185(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get(None)
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_186(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("XXexit_codeXX")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_187(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("EXIT_CODE")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_188(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = None
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_189(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get(None)
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_190(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("XXexit_codeXX")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_191(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("EXIT_CODE")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_192(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None or not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_193(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_194(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_195(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(None):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_196(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                None
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_197(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    None,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_198(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    None,
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_199(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    None,
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_200(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    None,
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_201(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    None,
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_202(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_203(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_204(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_205(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_206(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_207(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "XXcontradictory-check-verdictXX",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_208(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "CONTRADICTORY-CHECK-VERDICT",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_209(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "XXrecord the actual integer process exit codeXX",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_210(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "RECORD THE ACTUAL INTEGER PROCESS EXIT CODE",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_211(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None or not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_212(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_213(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_214(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(None):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_215(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                None
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_216(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    None,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_217(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    None,
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_218(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    None,
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_219(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    None,
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_220(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    None,
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_221(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_222(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_223(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_224(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_225(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_226(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "XXcontradictory-check-verdictXX",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_227(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "CONTRADICTORY-CHECK-VERDICT",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_228(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "XXrecord the actual integer process exit codeXX",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_229(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "RECORD THE ACTUAL INTEGER PROCESS EXIT CODE",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_230(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None or top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_231(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None or observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_232(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_233(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_234(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code == observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_235(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                None
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_236(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    None,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_237(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    None,
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_238(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    None,
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_239(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    None,
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_240(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    None,
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_241(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_242(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_243(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_244(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_245(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_246(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "XXcontradictory-check-verdictXX",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_247(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "CONTRADICTORY-CHECK-VERDICT",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_248(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "XXrecord one consistent process exit codeXX",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_249(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "RECORD ONE CONSISTENT PROCESS EXIT CODE",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_250(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = None
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_251(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_252(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status != "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_253(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "XXpassedXX":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_254(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "PASSED":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_255(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) or effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_256(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(None) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_257(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_258(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 1:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_259(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    None
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_260(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        None,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_261(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        None,
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_262(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        None,
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_263(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        None,
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_264(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        None,
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_265(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_266(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_267(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_268(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_269(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_270(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "XXcontradictory-check-verdictXX",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_271(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "CONTRADICTORY-CHECK-VERDICT",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_272(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "XXmark the check failed or collect a zero-exit passing observationXX",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_273(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "MARK THE CHECK FAILED OR COLLECT A ZERO-EXIT PASSING OBSERVATION",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_274(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("XXstateXX", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_275(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("STATE", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_276(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "XXstatusXX", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_277(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "STATUS", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_278(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "XXresultXX", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_279(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "RESULT", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_280(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "XXoutcomeXX", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_281(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "OUTCOME", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_282(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "XXverdictXX"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_283(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "VERDICT"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_284(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = None
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_285(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(None)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_286(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) or value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_287(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() not in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_288(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        None
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_289(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            None,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_290(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            None,
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_291(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            None,
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_292(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            None,
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_293(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            None,
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_294(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_295(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_296(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_297(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_298(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_299(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "XXcontradictory-check-verdictXX",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_300(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "CONTRADICTORY-CHECK-VERDICT",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_301(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "XXmark the check failed or record the actual successful observationXX",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_302(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "MARK THE CHECK FAILED OR RECORD THE ACTUAL SUCCESSFUL OBSERVATION",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_303(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("XXsuccessXX", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_304(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("SUCCESS", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_305(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "XXpassedXX", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_306(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "PASSED", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_307(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "XXhealthyXX", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_308(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "HEALTHY", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_309(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "XXallowedXX"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_310(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "ALLOWED"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_311(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(None) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_312(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is not False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_313(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is True:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_314(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        None
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_315(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            None,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_316(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            None,
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_317(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            None,
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_318(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            None,
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_319(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            None,
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_320(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_321(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_322(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_323(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_324(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_325(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "XXcontradictory-check-verdictXX",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_326(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "CONTRADICTORY-CHECK-VERDICT",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_327(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "XXmark the check failed or record the actual successful observationXX",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_328(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "MARK THE CHECK FAILED OR RECORD THE ACTUAL SUCCESSFUL OBSERVATION",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_329(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status != "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_330(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "XXexpected-denialXX":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_331(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "EXPECTED-DENIAL":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_332(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get(None) != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_333(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("XXoutcomeXX") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_334(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("OUTCOME") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_335(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") == "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_336(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "XXdeniedXX":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_337(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "DENIED":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_338(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    None
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_339(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        None,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_340(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        None,
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_341(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        None,
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_342(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        None,
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_343(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        None,
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_344(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_345(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_346(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_347(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_348(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_349(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "XXcontradictory-check-verdictXX",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_350(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "CONTRADICTORY-CHECK-VERDICT",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_351(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "XXrecord outcome as denied or use the status matching the observed resultXX",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_352(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "RECORD OUTCOME AS DENIED OR USE THE STATUS MATCHING THE OBSERVED RESULT",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_353(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) or effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_354(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(None) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_355(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_356(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 1:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_357(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    None
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_358(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        None,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_359(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        None,
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_360(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        None,
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_361(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        None,
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_362(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        None,
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_363(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_364(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_365(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_366(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_367(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_368(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "XXcontradictory-check-verdictXX",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_369(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "CONTRADICTORY-CHECK-VERDICT",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_370(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "XXrecord the non-zero denial exit code or correct the observation statusXX",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_371(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "RECORD THE NON-ZERO DENIAL EXIT CODE OR CORRECT THE OBSERVATION STATUS",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_372(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = None
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_373(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(None)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_374(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is not None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_375(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                None
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_376(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    None,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_377(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    None,
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_378(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    None,
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_379(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    None,
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_380(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    None,
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_381(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_382(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_383(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_384(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_385(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_386(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "XX/checksXX",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_387(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/CHECKS",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_388(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "XXmissing-required-checkXX",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_389(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "MISSING-REQUIRED-CHECK",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_390(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "XXcollect and record the required checkXX",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_391(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "COLLECT AND RECORD THE REQUIRED CHECK",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_392(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            break
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_393(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = None
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_394(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = None
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_395(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get(None)
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_396(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("XXstatusXX")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_397(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("STATUS")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_398(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) and status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_399(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_400(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_401(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"XXpassedXX", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_402(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"PASSED", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_403(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "XXexpected-denialXX", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_404(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "EXPECTED-DENIAL", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_405(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "XXskippedXX"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_406(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "SKIPPED"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_407(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                None
            )
    return findings


def x__check_findings__mutmut_408(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    None,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_409(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    None,
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_410(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    None,
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_411(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    None,
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_412(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    None,
                )
            )
    return findings


def x__check_findings__mutmut_413(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_414(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_415(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_416(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_417(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    )
            )
    return findings


def x__check_findings__mutmut_418(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "XXrequired-check-failedXX",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_419(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "REQUIRED-CHECK-FAILED",
                    f"required check {check_id!r} did not pass",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
    return findings


def x__check_findings__mutmut_420(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "XXcorrect the runtime defect and collect new passing evidenceXX",
                )
            )
    return findings


def x__check_findings__mutmut_421(
    evidence: Mapping[str, object],
    *,
    required_checks: tuple[str, ...],
    source: Path,
) -> list[ContractFinding]:
    checks = evidence.get("checks")
    if not isinstance(checks, list):
        return [
            _finding(
                source,
                "/checks",
                "missing-checks",
                "evidence checks must be a list",
                "record every required check observation",
            )
        ]
    findings: list[ContractFinding] = []
    by_id: dict[str, tuple[int, Mapping[str, object]]] = {}
    for index, value in enumerate(checks):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}",
                    "invalid-check",
                    "check evidence must be an object",
                    "record a structured check result",
                )
            )
            continue
        check = cast(Mapping[str, object], value)
        check_id = check.get("id")
        if not isinstance(check_id, str) or not check_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "missing-check-id",
                    "check evidence requires a non-empty id",
                    "set the declared check id",
                )
            )
            continue
        if check_id in by_id:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/id",
                    "duplicate-check",
                    f"check {check_id!r} appears more than once",
                    "record exactly one result per check id",
                )
            )
        else:
            by_id[check_id] = (index, check)
        status = check.get("status")
        if not isinstance(status, str) or status not in _CHECK_STATUSES:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "invalid-check-verdict",
                    f"check {check_id!r} has an unsupported status",
                    "record passed, failed, skipped or expected-denial",
                )
            )
        elif status == "failed":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "failed-check-verdict",
                    f"emitted check {check_id!r} records a failure",
                    "correct the runtime defect and collect new passing evidence",
                )
            )
        elif status == "skipped":
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "check-skipped",
                    f"check {check_id!r} was skipped",
                    "execute the check and record an observed result",
                )
            )
        observation = check.get("observation")
        if not isinstance(observation, Mapping) or not observation or set(observation) <= {"exit_code"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation",
                    "incomplete-observation",
                    f"check {check_id!r} has no substantive observation beyond an exit code",
                    "record the observed runtime state that proves the check outcome",
                )
            )
            continue
        observation = cast(Mapping[str, object], observation)
        top_exit_code = check.get("exit_code")
        observed_exit_code = observation.get("exit_code")
        if top_exit_code is not None and not is_integer_identity(top_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} exit_code must be a non-negative integer, not a boolean",
                    "record the actual integer process exit code",
                )
            )
        if observed_exit_code is not None and not is_integer_identity(observed_exit_code):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} observation exit_code is not a valid integer",
                    "record the actual integer process exit code",
                )
            )
        if (
            top_exit_code is not None
            and observed_exit_code is not None
            and top_exit_code != observed_exit_code
        ):
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/observation/exit_code",
                    "contradictory-check-verdict",
                    f"check {check_id!r} records conflicting exit codes",
                    "record one consistent process exit code",
                )
            )
        effective_exit_code = top_exit_code if top_exit_code is not None else observed_exit_code
        if status == "passed":
            if is_integer_identity(effective_exit_code) and effective_exit_code != 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} is marked passed but has a non-zero exit code",
                        "mark the check failed or collect a zero-exit passing observation",
                    )
                )
            for field in ("state", "status", "result", "outcome", "verdict"):
                value = observation.get(field)
                if isinstance(value, str) and value.casefold() in _FAILURE_OBSERVATIONS:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but its {field} records {value!r}",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
            for field in ("success", "passed", "healthy", "allowed"):
                if observation.get(field) is False:
                    findings.append(
                        _finding(
                            source,
                            f"/checks/{index}/observation/{field}",
                            "contradictory-check-verdict",
                            f"check {check_id!r} is marked passed but {field} is false",
                            "mark the check failed or record the actual successful observation",
                        )
                    )
        elif status == "expected-denial":
            if observation.get("outcome") != "denied":
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/observation/outcome",
                        "contradictory-check-verdict",
                        f"check {check_id!r} uses expected-denial without observing a denied outcome",
                        "record outcome as denied or use the status matching the observed result",
                    )
                )
            if is_integer_identity(effective_exit_code) and effective_exit_code == 0:
                findings.append(
                    _finding(
                        source,
                        f"/checks/{index}/exit_code",
                        "contradictory-check-verdict",
                        f"check {check_id!r} expected denial but its process exited successfully",
                        "record the non-zero denial exit code or correct the observation status",
                    )
                )

    for check_id in required_checks:
        located = by_id.get(check_id)
        if located is None:
            findings.append(
                _finding(
                    source,
                    "/checks",
                    "missing-required-check",
                    f"required check {check_id!r} is absent",
                    "collect and record the required check",
                )
            )
            continue
        index, check = located
        status = check.get("status")
        if not isinstance(status, str) or status not in {"passed", "expected-denial", "skipped"}:
            findings.append(
                _finding(
                    source,
                    f"/checks/{index}/status",
                    "required-check-failed",
                    f"required check {check_id!r} did not pass",
                    "CORRECT THE RUNTIME DEFECT AND COLLECT NEW PASSING EVIDENCE",
                )
            )
    return findings

mutants_x__check_findings__mutmut['_mutmut_orig'] = x__check_findings__mutmut_orig # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_1'] = x__check_findings__mutmut_1 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_2'] = x__check_findings__mutmut_2 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_3'] = x__check_findings__mutmut_3 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_4'] = x__check_findings__mutmut_4 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_5'] = x__check_findings__mutmut_5 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_6'] = x__check_findings__mutmut_6 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_7'] = x__check_findings__mutmut_7 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_8'] = x__check_findings__mutmut_8 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_9'] = x__check_findings__mutmut_9 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_10'] = x__check_findings__mutmut_10 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_11'] = x__check_findings__mutmut_11 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_12'] = x__check_findings__mutmut_12 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_13'] = x__check_findings__mutmut_13 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_14'] = x__check_findings__mutmut_14 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_15'] = x__check_findings__mutmut_15 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_16'] = x__check_findings__mutmut_16 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_17'] = x__check_findings__mutmut_17 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_18'] = x__check_findings__mutmut_18 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_19'] = x__check_findings__mutmut_19 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_20'] = x__check_findings__mutmut_20 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_21'] = x__check_findings__mutmut_21 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_22'] = x__check_findings__mutmut_22 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_23'] = x__check_findings__mutmut_23 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_24'] = x__check_findings__mutmut_24 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_25'] = x__check_findings__mutmut_25 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_26'] = x__check_findings__mutmut_26 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_27'] = x__check_findings__mutmut_27 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_28'] = x__check_findings__mutmut_28 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_29'] = x__check_findings__mutmut_29 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_30'] = x__check_findings__mutmut_30 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_31'] = x__check_findings__mutmut_31 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_32'] = x__check_findings__mutmut_32 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_33'] = x__check_findings__mutmut_33 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_34'] = x__check_findings__mutmut_34 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_35'] = x__check_findings__mutmut_35 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_36'] = x__check_findings__mutmut_36 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_37'] = x__check_findings__mutmut_37 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_38'] = x__check_findings__mutmut_38 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_39'] = x__check_findings__mutmut_39 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_40'] = x__check_findings__mutmut_40 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_41'] = x__check_findings__mutmut_41 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_42'] = x__check_findings__mutmut_42 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_43'] = x__check_findings__mutmut_43 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_44'] = x__check_findings__mutmut_44 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_45'] = x__check_findings__mutmut_45 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_46'] = x__check_findings__mutmut_46 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_47'] = x__check_findings__mutmut_47 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_48'] = x__check_findings__mutmut_48 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_49'] = x__check_findings__mutmut_49 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_50'] = x__check_findings__mutmut_50 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_51'] = x__check_findings__mutmut_51 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_52'] = x__check_findings__mutmut_52 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_53'] = x__check_findings__mutmut_53 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_54'] = x__check_findings__mutmut_54 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_55'] = x__check_findings__mutmut_55 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_56'] = x__check_findings__mutmut_56 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_57'] = x__check_findings__mutmut_57 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_58'] = x__check_findings__mutmut_58 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_59'] = x__check_findings__mutmut_59 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_60'] = x__check_findings__mutmut_60 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_61'] = x__check_findings__mutmut_61 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_62'] = x__check_findings__mutmut_62 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_63'] = x__check_findings__mutmut_63 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_64'] = x__check_findings__mutmut_64 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_65'] = x__check_findings__mutmut_65 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_66'] = x__check_findings__mutmut_66 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_67'] = x__check_findings__mutmut_67 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_68'] = x__check_findings__mutmut_68 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_69'] = x__check_findings__mutmut_69 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_70'] = x__check_findings__mutmut_70 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_71'] = x__check_findings__mutmut_71 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_72'] = x__check_findings__mutmut_72 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_73'] = x__check_findings__mutmut_73 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_74'] = x__check_findings__mutmut_74 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_75'] = x__check_findings__mutmut_75 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_76'] = x__check_findings__mutmut_76 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_77'] = x__check_findings__mutmut_77 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_78'] = x__check_findings__mutmut_78 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_79'] = x__check_findings__mutmut_79 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_80'] = x__check_findings__mutmut_80 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_81'] = x__check_findings__mutmut_81 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_82'] = x__check_findings__mutmut_82 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_83'] = x__check_findings__mutmut_83 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_84'] = x__check_findings__mutmut_84 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_85'] = x__check_findings__mutmut_85 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_86'] = x__check_findings__mutmut_86 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_87'] = x__check_findings__mutmut_87 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_88'] = x__check_findings__mutmut_88 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_89'] = x__check_findings__mutmut_89 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_90'] = x__check_findings__mutmut_90 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_91'] = x__check_findings__mutmut_91 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_92'] = x__check_findings__mutmut_92 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_93'] = x__check_findings__mutmut_93 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_94'] = x__check_findings__mutmut_94 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_95'] = x__check_findings__mutmut_95 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_96'] = x__check_findings__mutmut_96 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_97'] = x__check_findings__mutmut_97 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_98'] = x__check_findings__mutmut_98 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_99'] = x__check_findings__mutmut_99 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_100'] = x__check_findings__mutmut_100 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_101'] = x__check_findings__mutmut_101 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_102'] = x__check_findings__mutmut_102 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_103'] = x__check_findings__mutmut_103 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_104'] = x__check_findings__mutmut_104 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_105'] = x__check_findings__mutmut_105 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_106'] = x__check_findings__mutmut_106 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_107'] = x__check_findings__mutmut_107 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_108'] = x__check_findings__mutmut_108 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_109'] = x__check_findings__mutmut_109 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_110'] = x__check_findings__mutmut_110 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_111'] = x__check_findings__mutmut_111 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_112'] = x__check_findings__mutmut_112 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_113'] = x__check_findings__mutmut_113 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_114'] = x__check_findings__mutmut_114 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_115'] = x__check_findings__mutmut_115 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_116'] = x__check_findings__mutmut_116 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_117'] = x__check_findings__mutmut_117 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_118'] = x__check_findings__mutmut_118 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_119'] = x__check_findings__mutmut_119 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_120'] = x__check_findings__mutmut_120 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_121'] = x__check_findings__mutmut_121 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_122'] = x__check_findings__mutmut_122 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_123'] = x__check_findings__mutmut_123 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_124'] = x__check_findings__mutmut_124 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_125'] = x__check_findings__mutmut_125 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_126'] = x__check_findings__mutmut_126 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_127'] = x__check_findings__mutmut_127 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_128'] = x__check_findings__mutmut_128 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_129'] = x__check_findings__mutmut_129 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_130'] = x__check_findings__mutmut_130 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_131'] = x__check_findings__mutmut_131 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_132'] = x__check_findings__mutmut_132 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_133'] = x__check_findings__mutmut_133 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_134'] = x__check_findings__mutmut_134 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_135'] = x__check_findings__mutmut_135 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_136'] = x__check_findings__mutmut_136 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_137'] = x__check_findings__mutmut_137 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_138'] = x__check_findings__mutmut_138 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_139'] = x__check_findings__mutmut_139 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_140'] = x__check_findings__mutmut_140 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_141'] = x__check_findings__mutmut_141 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_142'] = x__check_findings__mutmut_142 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_143'] = x__check_findings__mutmut_143 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_144'] = x__check_findings__mutmut_144 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_145'] = x__check_findings__mutmut_145 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_146'] = x__check_findings__mutmut_146 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_147'] = x__check_findings__mutmut_147 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_148'] = x__check_findings__mutmut_148 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_149'] = x__check_findings__mutmut_149 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_150'] = x__check_findings__mutmut_150 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_151'] = x__check_findings__mutmut_151 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_152'] = x__check_findings__mutmut_152 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_153'] = x__check_findings__mutmut_153 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_154'] = x__check_findings__mutmut_154 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_155'] = x__check_findings__mutmut_155 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_156'] = x__check_findings__mutmut_156 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_157'] = x__check_findings__mutmut_157 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_158'] = x__check_findings__mutmut_158 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_159'] = x__check_findings__mutmut_159 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_160'] = x__check_findings__mutmut_160 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_161'] = x__check_findings__mutmut_161 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_162'] = x__check_findings__mutmut_162 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_163'] = x__check_findings__mutmut_163 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_164'] = x__check_findings__mutmut_164 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_165'] = x__check_findings__mutmut_165 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_166'] = x__check_findings__mutmut_166 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_167'] = x__check_findings__mutmut_167 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_168'] = x__check_findings__mutmut_168 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_169'] = x__check_findings__mutmut_169 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_170'] = x__check_findings__mutmut_170 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_171'] = x__check_findings__mutmut_171 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_172'] = x__check_findings__mutmut_172 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_173'] = x__check_findings__mutmut_173 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_174'] = x__check_findings__mutmut_174 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_175'] = x__check_findings__mutmut_175 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_176'] = x__check_findings__mutmut_176 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_177'] = x__check_findings__mutmut_177 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_178'] = x__check_findings__mutmut_178 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_179'] = x__check_findings__mutmut_179 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_180'] = x__check_findings__mutmut_180 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_181'] = x__check_findings__mutmut_181 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_182'] = x__check_findings__mutmut_182 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_183'] = x__check_findings__mutmut_183 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_184'] = x__check_findings__mutmut_184 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_185'] = x__check_findings__mutmut_185 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_186'] = x__check_findings__mutmut_186 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_187'] = x__check_findings__mutmut_187 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_188'] = x__check_findings__mutmut_188 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_189'] = x__check_findings__mutmut_189 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_190'] = x__check_findings__mutmut_190 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_191'] = x__check_findings__mutmut_191 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_192'] = x__check_findings__mutmut_192 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_193'] = x__check_findings__mutmut_193 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_194'] = x__check_findings__mutmut_194 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_195'] = x__check_findings__mutmut_195 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_196'] = x__check_findings__mutmut_196 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_197'] = x__check_findings__mutmut_197 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_198'] = x__check_findings__mutmut_198 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_199'] = x__check_findings__mutmut_199 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_200'] = x__check_findings__mutmut_200 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_201'] = x__check_findings__mutmut_201 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_202'] = x__check_findings__mutmut_202 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_203'] = x__check_findings__mutmut_203 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_204'] = x__check_findings__mutmut_204 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_205'] = x__check_findings__mutmut_205 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_206'] = x__check_findings__mutmut_206 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_207'] = x__check_findings__mutmut_207 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_208'] = x__check_findings__mutmut_208 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_209'] = x__check_findings__mutmut_209 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_210'] = x__check_findings__mutmut_210 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_211'] = x__check_findings__mutmut_211 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_212'] = x__check_findings__mutmut_212 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_213'] = x__check_findings__mutmut_213 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_214'] = x__check_findings__mutmut_214 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_215'] = x__check_findings__mutmut_215 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_216'] = x__check_findings__mutmut_216 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_217'] = x__check_findings__mutmut_217 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_218'] = x__check_findings__mutmut_218 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_219'] = x__check_findings__mutmut_219 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_220'] = x__check_findings__mutmut_220 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_221'] = x__check_findings__mutmut_221 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_222'] = x__check_findings__mutmut_222 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_223'] = x__check_findings__mutmut_223 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_224'] = x__check_findings__mutmut_224 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_225'] = x__check_findings__mutmut_225 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_226'] = x__check_findings__mutmut_226 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_227'] = x__check_findings__mutmut_227 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_228'] = x__check_findings__mutmut_228 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_229'] = x__check_findings__mutmut_229 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_230'] = x__check_findings__mutmut_230 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_231'] = x__check_findings__mutmut_231 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_232'] = x__check_findings__mutmut_232 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_233'] = x__check_findings__mutmut_233 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_234'] = x__check_findings__mutmut_234 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_235'] = x__check_findings__mutmut_235 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_236'] = x__check_findings__mutmut_236 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_237'] = x__check_findings__mutmut_237 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_238'] = x__check_findings__mutmut_238 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_239'] = x__check_findings__mutmut_239 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_240'] = x__check_findings__mutmut_240 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_241'] = x__check_findings__mutmut_241 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_242'] = x__check_findings__mutmut_242 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_243'] = x__check_findings__mutmut_243 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_244'] = x__check_findings__mutmut_244 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_245'] = x__check_findings__mutmut_245 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_246'] = x__check_findings__mutmut_246 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_247'] = x__check_findings__mutmut_247 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_248'] = x__check_findings__mutmut_248 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_249'] = x__check_findings__mutmut_249 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_250'] = x__check_findings__mutmut_250 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_251'] = x__check_findings__mutmut_251 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_252'] = x__check_findings__mutmut_252 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_253'] = x__check_findings__mutmut_253 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_254'] = x__check_findings__mutmut_254 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_255'] = x__check_findings__mutmut_255 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_256'] = x__check_findings__mutmut_256 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_257'] = x__check_findings__mutmut_257 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_258'] = x__check_findings__mutmut_258 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_259'] = x__check_findings__mutmut_259 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_260'] = x__check_findings__mutmut_260 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_261'] = x__check_findings__mutmut_261 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_262'] = x__check_findings__mutmut_262 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_263'] = x__check_findings__mutmut_263 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_264'] = x__check_findings__mutmut_264 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_265'] = x__check_findings__mutmut_265 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_266'] = x__check_findings__mutmut_266 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_267'] = x__check_findings__mutmut_267 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_268'] = x__check_findings__mutmut_268 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_269'] = x__check_findings__mutmut_269 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_270'] = x__check_findings__mutmut_270 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_271'] = x__check_findings__mutmut_271 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_272'] = x__check_findings__mutmut_272 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_273'] = x__check_findings__mutmut_273 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_274'] = x__check_findings__mutmut_274 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_275'] = x__check_findings__mutmut_275 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_276'] = x__check_findings__mutmut_276 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_277'] = x__check_findings__mutmut_277 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_278'] = x__check_findings__mutmut_278 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_279'] = x__check_findings__mutmut_279 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_280'] = x__check_findings__mutmut_280 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_281'] = x__check_findings__mutmut_281 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_282'] = x__check_findings__mutmut_282 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_283'] = x__check_findings__mutmut_283 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_284'] = x__check_findings__mutmut_284 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_285'] = x__check_findings__mutmut_285 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_286'] = x__check_findings__mutmut_286 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_287'] = x__check_findings__mutmut_287 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_288'] = x__check_findings__mutmut_288 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_289'] = x__check_findings__mutmut_289 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_290'] = x__check_findings__mutmut_290 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_291'] = x__check_findings__mutmut_291 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_292'] = x__check_findings__mutmut_292 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_293'] = x__check_findings__mutmut_293 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_294'] = x__check_findings__mutmut_294 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_295'] = x__check_findings__mutmut_295 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_296'] = x__check_findings__mutmut_296 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_297'] = x__check_findings__mutmut_297 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_298'] = x__check_findings__mutmut_298 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_299'] = x__check_findings__mutmut_299 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_300'] = x__check_findings__mutmut_300 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_301'] = x__check_findings__mutmut_301 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_302'] = x__check_findings__mutmut_302 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_303'] = x__check_findings__mutmut_303 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_304'] = x__check_findings__mutmut_304 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_305'] = x__check_findings__mutmut_305 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_306'] = x__check_findings__mutmut_306 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_307'] = x__check_findings__mutmut_307 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_308'] = x__check_findings__mutmut_308 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_309'] = x__check_findings__mutmut_309 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_310'] = x__check_findings__mutmut_310 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_311'] = x__check_findings__mutmut_311 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_312'] = x__check_findings__mutmut_312 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_313'] = x__check_findings__mutmut_313 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_314'] = x__check_findings__mutmut_314 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_315'] = x__check_findings__mutmut_315 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_316'] = x__check_findings__mutmut_316 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_317'] = x__check_findings__mutmut_317 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_318'] = x__check_findings__mutmut_318 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_319'] = x__check_findings__mutmut_319 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_320'] = x__check_findings__mutmut_320 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_321'] = x__check_findings__mutmut_321 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_322'] = x__check_findings__mutmut_322 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_323'] = x__check_findings__mutmut_323 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_324'] = x__check_findings__mutmut_324 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_325'] = x__check_findings__mutmut_325 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_326'] = x__check_findings__mutmut_326 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_327'] = x__check_findings__mutmut_327 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_328'] = x__check_findings__mutmut_328 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_329'] = x__check_findings__mutmut_329 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_330'] = x__check_findings__mutmut_330 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_331'] = x__check_findings__mutmut_331 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_332'] = x__check_findings__mutmut_332 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_333'] = x__check_findings__mutmut_333 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_334'] = x__check_findings__mutmut_334 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_335'] = x__check_findings__mutmut_335 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_336'] = x__check_findings__mutmut_336 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_337'] = x__check_findings__mutmut_337 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_338'] = x__check_findings__mutmut_338 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_339'] = x__check_findings__mutmut_339 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_340'] = x__check_findings__mutmut_340 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_341'] = x__check_findings__mutmut_341 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_342'] = x__check_findings__mutmut_342 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_343'] = x__check_findings__mutmut_343 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_344'] = x__check_findings__mutmut_344 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_345'] = x__check_findings__mutmut_345 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_346'] = x__check_findings__mutmut_346 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_347'] = x__check_findings__mutmut_347 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_348'] = x__check_findings__mutmut_348 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_349'] = x__check_findings__mutmut_349 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_350'] = x__check_findings__mutmut_350 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_351'] = x__check_findings__mutmut_351 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_352'] = x__check_findings__mutmut_352 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_353'] = x__check_findings__mutmut_353 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_354'] = x__check_findings__mutmut_354 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_355'] = x__check_findings__mutmut_355 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_356'] = x__check_findings__mutmut_356 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_357'] = x__check_findings__mutmut_357 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_358'] = x__check_findings__mutmut_358 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_359'] = x__check_findings__mutmut_359 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_360'] = x__check_findings__mutmut_360 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_361'] = x__check_findings__mutmut_361 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_362'] = x__check_findings__mutmut_362 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_363'] = x__check_findings__mutmut_363 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_364'] = x__check_findings__mutmut_364 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_365'] = x__check_findings__mutmut_365 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_366'] = x__check_findings__mutmut_366 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_367'] = x__check_findings__mutmut_367 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_368'] = x__check_findings__mutmut_368 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_369'] = x__check_findings__mutmut_369 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_370'] = x__check_findings__mutmut_370 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_371'] = x__check_findings__mutmut_371 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_372'] = x__check_findings__mutmut_372 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_373'] = x__check_findings__mutmut_373 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_374'] = x__check_findings__mutmut_374 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_375'] = x__check_findings__mutmut_375 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_376'] = x__check_findings__mutmut_376 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_377'] = x__check_findings__mutmut_377 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_378'] = x__check_findings__mutmut_378 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_379'] = x__check_findings__mutmut_379 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_380'] = x__check_findings__mutmut_380 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_381'] = x__check_findings__mutmut_381 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_382'] = x__check_findings__mutmut_382 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_383'] = x__check_findings__mutmut_383 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_384'] = x__check_findings__mutmut_384 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_385'] = x__check_findings__mutmut_385 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_386'] = x__check_findings__mutmut_386 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_387'] = x__check_findings__mutmut_387 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_388'] = x__check_findings__mutmut_388 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_389'] = x__check_findings__mutmut_389 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_390'] = x__check_findings__mutmut_390 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_391'] = x__check_findings__mutmut_391 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_392'] = x__check_findings__mutmut_392 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_393'] = x__check_findings__mutmut_393 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_394'] = x__check_findings__mutmut_394 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_395'] = x__check_findings__mutmut_395 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_396'] = x__check_findings__mutmut_396 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_397'] = x__check_findings__mutmut_397 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_398'] = x__check_findings__mutmut_398 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_399'] = x__check_findings__mutmut_399 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_400'] = x__check_findings__mutmut_400 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_401'] = x__check_findings__mutmut_401 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_402'] = x__check_findings__mutmut_402 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_403'] = x__check_findings__mutmut_403 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_404'] = x__check_findings__mutmut_404 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_405'] = x__check_findings__mutmut_405 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_406'] = x__check_findings__mutmut_406 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_407'] = x__check_findings__mutmut_407 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_408'] = x__check_findings__mutmut_408 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_409'] = x__check_findings__mutmut_409 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_410'] = x__check_findings__mutmut_410 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_411'] = x__check_findings__mutmut_411 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_412'] = x__check_findings__mutmut_412 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_413'] = x__check_findings__mutmut_413 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_414'] = x__check_findings__mutmut_414 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_415'] = x__check_findings__mutmut_415 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_416'] = x__check_findings__mutmut_416 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_417'] = x__check_findings__mutmut_417 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_418'] = x__check_findings__mutmut_418 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_419'] = x__check_findings__mutmut_419 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_420'] = x__check_findings__mutmut_420 # type: ignore # mutmut generated
mutants_x__check_findings__mutmut['x__check_findings__mutmut_421'] = x__check_findings__mutmut_421 # type: ignore # mutmut generated
mutants_x__safe_artifact_path__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__safe_artifact_path__mutmut)
def _safe_artifact_path(base: Path, value: object) -> Path | None:
    if not isinstance(value, str) or not value or "\\" in value or "\x00" in value:
        return None
    relative = PurePosixPath(value)
    if relative.is_absolute() or ".." in relative.parts:
        return None
    root = base.resolve()
    try:
        path = (root / Path(*relative.parts)).resolve()
    except (OSError, RuntimeError, ValueError):
        return None
    try:
        path.relative_to(root)
    except ValueError:
        return None
    return path


def x__safe_artifact_path__mutmut_orig(base: Path, value: object) -> Path | None:
    if not isinstance(value, str) or not value or "\\" in value or "\x00" in value:
        return None
    relative = PurePosixPath(value)
    if relative.is_absolute() or ".." in relative.parts:
        return None
    root = base.resolve()
    try:
        path = (root / Path(*relative.parts)).resolve()
    except (OSError, RuntimeError, ValueError):
        return None
    try:
        path.relative_to(root)
    except ValueError:
        return None
    return path


def x__safe_artifact_path__mutmut_1(base: Path, value: object) -> Path | None:
    if not isinstance(value, str) or not value or "\\" in value and "\x00" in value:
        return None
    relative = PurePosixPath(value)
    if relative.is_absolute() or ".." in relative.parts:
        return None
    root = base.resolve()
    try:
        path = (root / Path(*relative.parts)).resolve()
    except (OSError, RuntimeError, ValueError):
        return None
    try:
        path.relative_to(root)
    except ValueError:
        return None
    return path


def x__safe_artifact_path__mutmut_2(base: Path, value: object) -> Path | None:
    if not isinstance(value, str) or not value and "\\" in value or "\x00" in value:
        return None
    relative = PurePosixPath(value)
    if relative.is_absolute() or ".." in relative.parts:
        return None
    root = base.resolve()
    try:
        path = (root / Path(*relative.parts)).resolve()
    except (OSError, RuntimeError, ValueError):
        return None
    try:
        path.relative_to(root)
    except ValueError:
        return None
    return path


def x__safe_artifact_path__mutmut_3(base: Path, value: object) -> Path | None:
    if not isinstance(value, str) and not value or "\\" in value or "\x00" in value:
        return None
    relative = PurePosixPath(value)
    if relative.is_absolute() or ".." in relative.parts:
        return None
    root = base.resolve()
    try:
        path = (root / Path(*relative.parts)).resolve()
    except (OSError, RuntimeError, ValueError):
        return None
    try:
        path.relative_to(root)
    except ValueError:
        return None
    return path


def x__safe_artifact_path__mutmut_4(base: Path, value: object) -> Path | None:
    if isinstance(value, str) or not value or "\\" in value or "\x00" in value:
        return None
    relative = PurePosixPath(value)
    if relative.is_absolute() or ".." in relative.parts:
        return None
    root = base.resolve()
    try:
        path = (root / Path(*relative.parts)).resolve()
    except (OSError, RuntimeError, ValueError):
        return None
    try:
        path.relative_to(root)
    except ValueError:
        return None
    return path


def x__safe_artifact_path__mutmut_5(base: Path, value: object) -> Path | None:
    if not isinstance(value, str) or value or "\\" in value or "\x00" in value:
        return None
    relative = PurePosixPath(value)
    if relative.is_absolute() or ".." in relative.parts:
        return None
    root = base.resolve()
    try:
        path = (root / Path(*relative.parts)).resolve()
    except (OSError, RuntimeError, ValueError):
        return None
    try:
        path.relative_to(root)
    except ValueError:
        return None
    return path


def x__safe_artifact_path__mutmut_6(base: Path, value: object) -> Path | None:
    if not isinstance(value, str) or not value or "XX\\XX" in value or "\x00" in value:
        return None
    relative = PurePosixPath(value)
    if relative.is_absolute() or ".." in relative.parts:
        return None
    root = base.resolve()
    try:
        path = (root / Path(*relative.parts)).resolve()
    except (OSError, RuntimeError, ValueError):
        return None
    try:
        path.relative_to(root)
    except ValueError:
        return None
    return path


def x__safe_artifact_path__mutmut_7(base: Path, value: object) -> Path | None:
    if not isinstance(value, str) or not value or "\\" not in value or "\x00" in value:
        return None
    relative = PurePosixPath(value)
    if relative.is_absolute() or ".." in relative.parts:
        return None
    root = base.resolve()
    try:
        path = (root / Path(*relative.parts)).resolve()
    except (OSError, RuntimeError, ValueError):
        return None
    try:
        path.relative_to(root)
    except ValueError:
        return None
    return path


def x__safe_artifact_path__mutmut_8(base: Path, value: object) -> Path | None:
    if not isinstance(value, str) or not value or "\\" in value or "XX\x00XX" in value:
        return None
    relative = PurePosixPath(value)
    if relative.is_absolute() or ".." in relative.parts:
        return None
    root = base.resolve()
    try:
        path = (root / Path(*relative.parts)).resolve()
    except (OSError, RuntimeError, ValueError):
        return None
    try:
        path.relative_to(root)
    except ValueError:
        return None
    return path


def x__safe_artifact_path__mutmut_9(base: Path, value: object) -> Path | None:
    if not isinstance(value, str) or not value or "\\" in value or "\x00" not in value:
        return None
    relative = PurePosixPath(value)
    if relative.is_absolute() or ".." in relative.parts:
        return None
    root = base.resolve()
    try:
        path = (root / Path(*relative.parts)).resolve()
    except (OSError, RuntimeError, ValueError):
        return None
    try:
        path.relative_to(root)
    except ValueError:
        return None
    return path


def x__safe_artifact_path__mutmut_10(base: Path, value: object) -> Path | None:
    if not isinstance(value, str) or not value or "\\" in value or "\x00" in value:
        return None
    relative = None
    if relative.is_absolute() or ".." in relative.parts:
        return None
    root = base.resolve()
    try:
        path = (root / Path(*relative.parts)).resolve()
    except (OSError, RuntimeError, ValueError):
        return None
    try:
        path.relative_to(root)
    except ValueError:
        return None
    return path


def x__safe_artifact_path__mutmut_11(base: Path, value: object) -> Path | None:
    if not isinstance(value, str) or not value or "\\" in value or "\x00" in value:
        return None
    relative = PurePosixPath(None)
    if relative.is_absolute() or ".." in relative.parts:
        return None
    root = base.resolve()
    try:
        path = (root / Path(*relative.parts)).resolve()
    except (OSError, RuntimeError, ValueError):
        return None
    try:
        path.relative_to(root)
    except ValueError:
        return None
    return path


def x__safe_artifact_path__mutmut_12(base: Path, value: object) -> Path | None:
    if not isinstance(value, str) or not value or "\\" in value or "\x00" in value:
        return None
    relative = PurePosixPath(value)
    if relative.is_absolute() and ".." in relative.parts:
        return None
    root = base.resolve()
    try:
        path = (root / Path(*relative.parts)).resolve()
    except (OSError, RuntimeError, ValueError):
        return None
    try:
        path.relative_to(root)
    except ValueError:
        return None
    return path


def x__safe_artifact_path__mutmut_13(base: Path, value: object) -> Path | None:
    if not isinstance(value, str) or not value or "\\" in value or "\x00" in value:
        return None
    relative = PurePosixPath(value)
    if relative.is_absolute() or "XX..XX" in relative.parts:
        return None
    root = base.resolve()
    try:
        path = (root / Path(*relative.parts)).resolve()
    except (OSError, RuntimeError, ValueError):
        return None
    try:
        path.relative_to(root)
    except ValueError:
        return None
    return path


def x__safe_artifact_path__mutmut_14(base: Path, value: object) -> Path | None:
    if not isinstance(value, str) or not value or "\\" in value or "\x00" in value:
        return None
    relative = PurePosixPath(value)
    if relative.is_absolute() or ".." not in relative.parts:
        return None
    root = base.resolve()
    try:
        path = (root / Path(*relative.parts)).resolve()
    except (OSError, RuntimeError, ValueError):
        return None
    try:
        path.relative_to(root)
    except ValueError:
        return None
    return path


def x__safe_artifact_path__mutmut_15(base: Path, value: object) -> Path | None:
    if not isinstance(value, str) or not value or "\\" in value or "\x00" in value:
        return None
    relative = PurePosixPath(value)
    if relative.is_absolute() or ".." in relative.parts:
        return None
    root = None
    try:
        path = (root / Path(*relative.parts)).resolve()
    except (OSError, RuntimeError, ValueError):
        return None
    try:
        path.relative_to(root)
    except ValueError:
        return None
    return path


def x__safe_artifact_path__mutmut_16(base: Path, value: object) -> Path | None:
    if not isinstance(value, str) or not value or "\\" in value or "\x00" in value:
        return None
    relative = PurePosixPath(value)
    if relative.is_absolute() or ".." in relative.parts:
        return None
    root = base.resolve()
    try:
        path = None
    except (OSError, RuntimeError, ValueError):
        return None
    try:
        path.relative_to(root)
    except ValueError:
        return None
    return path


def x__safe_artifact_path__mutmut_17(base: Path, value: object) -> Path | None:
    if not isinstance(value, str) or not value or "\\" in value or "\x00" in value:
        return None
    relative = PurePosixPath(value)
    if relative.is_absolute() or ".." in relative.parts:
        return None
    root = base.resolve()
    try:
        path = (root * Path(*relative.parts)).resolve()
    except (OSError, RuntimeError, ValueError):
        return None
    try:
        path.relative_to(root)
    except ValueError:
        return None
    return path


def x__safe_artifact_path__mutmut_18(base: Path, value: object) -> Path | None:
    if not isinstance(value, str) or not value or "\\" in value or "\x00" in value:
        return None
    relative = PurePosixPath(value)
    if relative.is_absolute() or ".." in relative.parts:
        return None
    root = base.resolve()
    try:
        path = (root / Path(*relative.parts)).resolve()
    except (OSError, RuntimeError, ValueError):
        return None
    try:
        path.relative_to(None)
    except ValueError:
        return None
    return path

mutants_x__safe_artifact_path__mutmut['_mutmut_orig'] = x__safe_artifact_path__mutmut_orig # type: ignore # mutmut generated
mutants_x__safe_artifact_path__mutmut['x__safe_artifact_path__mutmut_1'] = x__safe_artifact_path__mutmut_1 # type: ignore # mutmut generated
mutants_x__safe_artifact_path__mutmut['x__safe_artifact_path__mutmut_2'] = x__safe_artifact_path__mutmut_2 # type: ignore # mutmut generated
mutants_x__safe_artifact_path__mutmut['x__safe_artifact_path__mutmut_3'] = x__safe_artifact_path__mutmut_3 # type: ignore # mutmut generated
mutants_x__safe_artifact_path__mutmut['x__safe_artifact_path__mutmut_4'] = x__safe_artifact_path__mutmut_4 # type: ignore # mutmut generated
mutants_x__safe_artifact_path__mutmut['x__safe_artifact_path__mutmut_5'] = x__safe_artifact_path__mutmut_5 # type: ignore # mutmut generated
mutants_x__safe_artifact_path__mutmut['x__safe_artifact_path__mutmut_6'] = x__safe_artifact_path__mutmut_6 # type: ignore # mutmut generated
mutants_x__safe_artifact_path__mutmut['x__safe_artifact_path__mutmut_7'] = x__safe_artifact_path__mutmut_7 # type: ignore # mutmut generated
mutants_x__safe_artifact_path__mutmut['x__safe_artifact_path__mutmut_8'] = x__safe_artifact_path__mutmut_8 # type: ignore # mutmut generated
mutants_x__safe_artifact_path__mutmut['x__safe_artifact_path__mutmut_9'] = x__safe_artifact_path__mutmut_9 # type: ignore # mutmut generated
mutants_x__safe_artifact_path__mutmut['x__safe_artifact_path__mutmut_10'] = x__safe_artifact_path__mutmut_10 # type: ignore # mutmut generated
mutants_x__safe_artifact_path__mutmut['x__safe_artifact_path__mutmut_11'] = x__safe_artifact_path__mutmut_11 # type: ignore # mutmut generated
mutants_x__safe_artifact_path__mutmut['x__safe_artifact_path__mutmut_12'] = x__safe_artifact_path__mutmut_12 # type: ignore # mutmut generated
mutants_x__safe_artifact_path__mutmut['x__safe_artifact_path__mutmut_13'] = x__safe_artifact_path__mutmut_13 # type: ignore # mutmut generated
mutants_x__safe_artifact_path__mutmut['x__safe_artifact_path__mutmut_14'] = x__safe_artifact_path__mutmut_14 # type: ignore # mutmut generated
mutants_x__safe_artifact_path__mutmut['x__safe_artifact_path__mutmut_15'] = x__safe_artifact_path__mutmut_15 # type: ignore # mutmut generated
mutants_x__safe_artifact_path__mutmut['x__safe_artifact_path__mutmut_16'] = x__safe_artifact_path__mutmut_16 # type: ignore # mutmut generated
mutants_x__safe_artifact_path__mutmut['x__safe_artifact_path__mutmut_17'] = x__safe_artifact_path__mutmut_17 # type: ignore # mutmut generated
mutants_x__safe_artifact_path__mutmut['x__safe_artifact_path__mutmut_18'] = x__safe_artifact_path__mutmut_18 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__artifact_findings__mutmut)
def _artifact_findings(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_orig(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_1(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = None
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_2(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get(None)
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_3(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("XXartifactsXX")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_4(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("ARTIFACTS")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_5(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_6(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                None,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_7(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                None,
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_8(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                None,
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_9(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                None,
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_10(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                None,
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_11(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_12(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_13(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_14(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_15(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_16(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "XX/artifactsXX",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_17(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/ARTIFACTS",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_18(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "XXmissing-artifactsXX",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_19(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "MISSING-ARTIFACTS",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_20(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "XXevidence artifacts must be a listXX",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_21(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "EVIDENCE ARTIFACTS MUST BE A LIST",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_22(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "XXrecord an empty list or each referenced artifact and digestXX",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_23(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "RECORD AN EMPTY LIST OR EACH REFERENCED ARTIFACT AND DIGEST",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_24(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = None
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_25(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = None
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_26(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(None):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_27(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_28(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                None
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_29(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    None,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_30(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    None,
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_31(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    None,
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_32(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    None,
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_33(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    None,
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_34(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_35(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_36(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_37(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_38(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_39(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "XXinvalid-artifactXX",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_40(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "INVALID-ARTIFACT",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_41(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "XXartifact reference must be an objectXX",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_42(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "ARTIFACT REFERENCE MUST BE AN OBJECT",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_43(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "XXrecord path and sha256 fieldsXX",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_44(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "RECORD PATH AND SHA256 FIELDS",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_45(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            break
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_46(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = None
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_47(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(None, value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_48(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], None)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_49(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_50(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], )
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_51(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = None
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_52(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get(None)
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_53(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("XXpathXX")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_54(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("PATH")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_55(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = None
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_56(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(None, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_57(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, None)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_58(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_59(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, )
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_60(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is not None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_61(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                None
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_62(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    None,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_63(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    None,
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_64(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    None,
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_65(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    None,
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_66(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    None,
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_67(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_68(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_69(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_70(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_71(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_72(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "XXunsafe-artifact-pathXX",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_73(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "UNSAFE-ARTIFACT-PATH",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_74(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "XXartifact path must remain beneath the evidence directoryXX",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_75(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "ARTIFACT PATH MUST REMAIN BENEATH THE EVIDENCE DIRECTORY",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_76(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "XXuse a relative POSIX path without '..', backslashes or symlink escapeXX",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_77(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative posix path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_78(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "USE A RELATIVE POSIX PATH WITHOUT '..', BACKSLASHES OR SYMLINK ESCAPE",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_79(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            break
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_80(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = None
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_81(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(None, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_82(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, None)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_83(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_84(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, )
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_85(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text not in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_86(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                None
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_87(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    None,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_88(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    None,
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_89(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    None,
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_90(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    None,
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_91(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    None,
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_92(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_93(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_94(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_95(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_96(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_97(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "XXduplicate-artifactXX",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_98(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "DUPLICATE-ARTIFACT",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_99(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "XXrecord one authoritative digest per artifactXX",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_100(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "RECORD ONE AUTHORITATIVE DIGEST PER ARTIFACT",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_101(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            break
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_102(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(None)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_103(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = None
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_104(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get(None)
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_105(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("XXsha256XX")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_106(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("SHA256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_107(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) and _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_108(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_109(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(None) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_110(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is not None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_111(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                None
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_112(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    None,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_113(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    None,
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_114(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    None,
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_115(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    None,
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_116(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    None,
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_117(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_118(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_119(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_120(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_121(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_122(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "XXinvalid-artifact-digestXX",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_123(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "INVALID-ARTIFACT-DIGEST",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_124(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "XXartifact digest must be sha256 followed by 64 lowercase hexadecimal charactersXX",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_125(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "ARTIFACT DIGEST MUST BE SHA256 FOLLOWED BY 64 LOWERCASE HEXADECIMAL CHARACTERS",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_126(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "XXrecord the exact sha256 digest of the artifact bytesXX",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_127(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "RECORD THE EXACT SHA256 DIGEST OF THE ARTIFACT BYTES",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_128(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            break
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_129(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = None
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_130(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open(None) as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_131(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("XXrbXX") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_132(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("RB") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_133(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(None, b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_134(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), None):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_135(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_136(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), ):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_137(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: None, b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_138(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(None), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_139(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 / 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_140(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1025 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_141(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1025), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_142(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b"XXXX"):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_143(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(None)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_144(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = None
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_145(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" - hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_146(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "XXsha256:XX" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_147(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "SHA256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_148(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                None
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_149(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    None,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_150(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    None,
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_151(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    None,
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_152(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    None,
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_153(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    None,
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_154(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_155(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_156(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_157(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_158(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_159(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "XXmissing-artifactXX",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_160(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "MISSING-ARTIFACT",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_161(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "XXretain the referenced artifact beside the evidence receiptXX",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_162(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "RETAIN THE REFERENCED ARTIFACT BESIDE THE EVIDENCE RECEIPT",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_163(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            break
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_164(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual == digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_165(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                None
            )
    return findings


def x__artifact_findings__mutmut_166(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    None,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_167(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    None,
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_168(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    None,
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_169(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    None,
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_170(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    None,
                )
            )
    return findings


def x__artifact_findings__mutmut_171(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_172(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_173(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_174(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_175(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    )
            )
    return findings


def x__artifact_findings__mutmut_176(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "XXartifact-digest-mismatchXX",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_177(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "ARTIFACT-DIGEST-MISMATCH",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "restore the exact retained artifact bytes or collect a new receipt",
                )
            )
    return findings


def x__artifact_findings__mutmut_178(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "XXrestore the exact retained artifact bytes or collect a new receiptXX",
                )
            )
    return findings


def x__artifact_findings__mutmut_179(evidence: Mapping[str, object], *, source: Path) -> list[ContractFinding]:
    artifacts = evidence.get("artifacts")
    if not isinstance(artifacts, list):
        return [
            _finding(
                source,
                "/artifacts",
                "missing-artifacts",
                "evidence artifacts must be a list",
                "record an empty list or each referenced artifact and digest",
            )
        ]
    findings: list[ContractFinding] = []
    seen: set[str] = set()
    for index, value in enumerate(artifacts):
        if not isinstance(value, Mapping):
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}",
                    "invalid-artifact",
                    "artifact reference must be an object",
                    "record path and sha256 fields",
                )
            )
            continue
        artifact = cast(Mapping[str, object], value)
        reference = artifact.get("path")
        path = _safe_artifact_path(source.parent, reference)
        if path is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "unsafe-artifact-path",
                    "artifact path must remain beneath the evidence directory",
                    "use a relative POSIX path without '..', backslashes or symlink escape",
                )
            )
            continue
        reference_text = cast(str, reference)
        if reference_text in seen:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "duplicate-artifact",
                    f"artifact {reference_text!r} is referenced more than once",
                    "record one authoritative digest per artifact",
                )
            )
            continue
        seen.add(reference_text)
        digest = artifact.get("sha256")
        if not isinstance(digest, str) or _SHA256_RE.fullmatch(digest) is None:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "invalid-artifact-digest",
                    "artifact digest must be sha256 followed by 64 lowercase hexadecimal characters",
                    "record the exact sha256 digest of the artifact bytes",
                )
            )
            continue
        try:
            hasher = hashlib.sha256()
            with path.open("rb") as artifact_file:
                for chunk in iter(lambda: artifact_file.read(1024 * 1024), b""):
                    hasher.update(chunk)
            actual = "sha256:" + hasher.hexdigest()
        except OSError:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/path",
                    "missing-artifact",
                    f"referenced artifact {reference_text!r} cannot be read",
                    "retain the referenced artifact beside the evidence receipt",
                )
            )
            continue
        if actual != digest:
            findings.append(
                _finding(
                    source,
                    f"/artifacts/{index}/sha256",
                    "artifact-digest-mismatch",
                    f"referenced artifact {reference_text!r} does not match its digest",
                    "RESTORE THE EXACT RETAINED ARTIFACT BYTES OR COLLECT A NEW RECEIPT",
                )
            )
    return findings

mutants_x__artifact_findings__mutmut['_mutmut_orig'] = x__artifact_findings__mutmut_orig # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_1'] = x__artifact_findings__mutmut_1 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_2'] = x__artifact_findings__mutmut_2 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_3'] = x__artifact_findings__mutmut_3 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_4'] = x__artifact_findings__mutmut_4 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_5'] = x__artifact_findings__mutmut_5 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_6'] = x__artifact_findings__mutmut_6 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_7'] = x__artifact_findings__mutmut_7 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_8'] = x__artifact_findings__mutmut_8 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_9'] = x__artifact_findings__mutmut_9 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_10'] = x__artifact_findings__mutmut_10 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_11'] = x__artifact_findings__mutmut_11 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_12'] = x__artifact_findings__mutmut_12 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_13'] = x__artifact_findings__mutmut_13 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_14'] = x__artifact_findings__mutmut_14 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_15'] = x__artifact_findings__mutmut_15 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_16'] = x__artifact_findings__mutmut_16 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_17'] = x__artifact_findings__mutmut_17 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_18'] = x__artifact_findings__mutmut_18 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_19'] = x__artifact_findings__mutmut_19 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_20'] = x__artifact_findings__mutmut_20 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_21'] = x__artifact_findings__mutmut_21 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_22'] = x__artifact_findings__mutmut_22 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_23'] = x__artifact_findings__mutmut_23 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_24'] = x__artifact_findings__mutmut_24 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_25'] = x__artifact_findings__mutmut_25 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_26'] = x__artifact_findings__mutmut_26 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_27'] = x__artifact_findings__mutmut_27 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_28'] = x__artifact_findings__mutmut_28 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_29'] = x__artifact_findings__mutmut_29 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_30'] = x__artifact_findings__mutmut_30 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_31'] = x__artifact_findings__mutmut_31 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_32'] = x__artifact_findings__mutmut_32 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_33'] = x__artifact_findings__mutmut_33 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_34'] = x__artifact_findings__mutmut_34 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_35'] = x__artifact_findings__mutmut_35 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_36'] = x__artifact_findings__mutmut_36 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_37'] = x__artifact_findings__mutmut_37 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_38'] = x__artifact_findings__mutmut_38 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_39'] = x__artifact_findings__mutmut_39 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_40'] = x__artifact_findings__mutmut_40 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_41'] = x__artifact_findings__mutmut_41 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_42'] = x__artifact_findings__mutmut_42 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_43'] = x__artifact_findings__mutmut_43 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_44'] = x__artifact_findings__mutmut_44 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_45'] = x__artifact_findings__mutmut_45 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_46'] = x__artifact_findings__mutmut_46 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_47'] = x__artifact_findings__mutmut_47 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_48'] = x__artifact_findings__mutmut_48 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_49'] = x__artifact_findings__mutmut_49 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_50'] = x__artifact_findings__mutmut_50 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_51'] = x__artifact_findings__mutmut_51 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_52'] = x__artifact_findings__mutmut_52 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_53'] = x__artifact_findings__mutmut_53 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_54'] = x__artifact_findings__mutmut_54 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_55'] = x__artifact_findings__mutmut_55 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_56'] = x__artifact_findings__mutmut_56 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_57'] = x__artifact_findings__mutmut_57 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_58'] = x__artifact_findings__mutmut_58 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_59'] = x__artifact_findings__mutmut_59 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_60'] = x__artifact_findings__mutmut_60 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_61'] = x__artifact_findings__mutmut_61 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_62'] = x__artifact_findings__mutmut_62 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_63'] = x__artifact_findings__mutmut_63 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_64'] = x__artifact_findings__mutmut_64 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_65'] = x__artifact_findings__mutmut_65 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_66'] = x__artifact_findings__mutmut_66 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_67'] = x__artifact_findings__mutmut_67 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_68'] = x__artifact_findings__mutmut_68 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_69'] = x__artifact_findings__mutmut_69 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_70'] = x__artifact_findings__mutmut_70 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_71'] = x__artifact_findings__mutmut_71 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_72'] = x__artifact_findings__mutmut_72 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_73'] = x__artifact_findings__mutmut_73 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_74'] = x__artifact_findings__mutmut_74 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_75'] = x__artifact_findings__mutmut_75 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_76'] = x__artifact_findings__mutmut_76 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_77'] = x__artifact_findings__mutmut_77 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_78'] = x__artifact_findings__mutmut_78 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_79'] = x__artifact_findings__mutmut_79 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_80'] = x__artifact_findings__mutmut_80 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_81'] = x__artifact_findings__mutmut_81 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_82'] = x__artifact_findings__mutmut_82 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_83'] = x__artifact_findings__mutmut_83 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_84'] = x__artifact_findings__mutmut_84 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_85'] = x__artifact_findings__mutmut_85 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_86'] = x__artifact_findings__mutmut_86 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_87'] = x__artifact_findings__mutmut_87 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_88'] = x__artifact_findings__mutmut_88 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_89'] = x__artifact_findings__mutmut_89 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_90'] = x__artifact_findings__mutmut_90 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_91'] = x__artifact_findings__mutmut_91 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_92'] = x__artifact_findings__mutmut_92 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_93'] = x__artifact_findings__mutmut_93 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_94'] = x__artifact_findings__mutmut_94 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_95'] = x__artifact_findings__mutmut_95 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_96'] = x__artifact_findings__mutmut_96 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_97'] = x__artifact_findings__mutmut_97 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_98'] = x__artifact_findings__mutmut_98 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_99'] = x__artifact_findings__mutmut_99 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_100'] = x__artifact_findings__mutmut_100 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_101'] = x__artifact_findings__mutmut_101 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_102'] = x__artifact_findings__mutmut_102 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_103'] = x__artifact_findings__mutmut_103 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_104'] = x__artifact_findings__mutmut_104 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_105'] = x__artifact_findings__mutmut_105 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_106'] = x__artifact_findings__mutmut_106 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_107'] = x__artifact_findings__mutmut_107 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_108'] = x__artifact_findings__mutmut_108 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_109'] = x__artifact_findings__mutmut_109 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_110'] = x__artifact_findings__mutmut_110 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_111'] = x__artifact_findings__mutmut_111 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_112'] = x__artifact_findings__mutmut_112 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_113'] = x__artifact_findings__mutmut_113 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_114'] = x__artifact_findings__mutmut_114 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_115'] = x__artifact_findings__mutmut_115 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_116'] = x__artifact_findings__mutmut_116 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_117'] = x__artifact_findings__mutmut_117 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_118'] = x__artifact_findings__mutmut_118 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_119'] = x__artifact_findings__mutmut_119 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_120'] = x__artifact_findings__mutmut_120 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_121'] = x__artifact_findings__mutmut_121 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_122'] = x__artifact_findings__mutmut_122 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_123'] = x__artifact_findings__mutmut_123 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_124'] = x__artifact_findings__mutmut_124 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_125'] = x__artifact_findings__mutmut_125 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_126'] = x__artifact_findings__mutmut_126 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_127'] = x__artifact_findings__mutmut_127 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_128'] = x__artifact_findings__mutmut_128 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_129'] = x__artifact_findings__mutmut_129 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_130'] = x__artifact_findings__mutmut_130 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_131'] = x__artifact_findings__mutmut_131 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_132'] = x__artifact_findings__mutmut_132 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_133'] = x__artifact_findings__mutmut_133 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_134'] = x__artifact_findings__mutmut_134 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_135'] = x__artifact_findings__mutmut_135 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_136'] = x__artifact_findings__mutmut_136 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_137'] = x__artifact_findings__mutmut_137 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_138'] = x__artifact_findings__mutmut_138 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_139'] = x__artifact_findings__mutmut_139 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_140'] = x__artifact_findings__mutmut_140 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_141'] = x__artifact_findings__mutmut_141 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_142'] = x__artifact_findings__mutmut_142 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_143'] = x__artifact_findings__mutmut_143 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_144'] = x__artifact_findings__mutmut_144 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_145'] = x__artifact_findings__mutmut_145 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_146'] = x__artifact_findings__mutmut_146 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_147'] = x__artifact_findings__mutmut_147 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_148'] = x__artifact_findings__mutmut_148 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_149'] = x__artifact_findings__mutmut_149 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_150'] = x__artifact_findings__mutmut_150 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_151'] = x__artifact_findings__mutmut_151 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_152'] = x__artifact_findings__mutmut_152 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_153'] = x__artifact_findings__mutmut_153 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_154'] = x__artifact_findings__mutmut_154 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_155'] = x__artifact_findings__mutmut_155 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_156'] = x__artifact_findings__mutmut_156 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_157'] = x__artifact_findings__mutmut_157 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_158'] = x__artifact_findings__mutmut_158 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_159'] = x__artifact_findings__mutmut_159 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_160'] = x__artifact_findings__mutmut_160 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_161'] = x__artifact_findings__mutmut_161 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_162'] = x__artifact_findings__mutmut_162 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_163'] = x__artifact_findings__mutmut_163 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_164'] = x__artifact_findings__mutmut_164 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_165'] = x__artifact_findings__mutmut_165 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_166'] = x__artifact_findings__mutmut_166 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_167'] = x__artifact_findings__mutmut_167 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_168'] = x__artifact_findings__mutmut_168 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_169'] = x__artifact_findings__mutmut_169 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_170'] = x__artifact_findings__mutmut_170 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_171'] = x__artifact_findings__mutmut_171 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_172'] = x__artifact_findings__mutmut_172 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_173'] = x__artifact_findings__mutmut_173 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_174'] = x__artifact_findings__mutmut_174 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_175'] = x__artifact_findings__mutmut_175 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_176'] = x__artifact_findings__mutmut_176 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_177'] = x__artifact_findings__mutmut_177 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_178'] = x__artifact_findings__mutmut_178 # type: ignore # mutmut generated
mutants_x__artifact_findings__mutmut['x__artifact_findings__mutmut_179'] = x__artifact_findings__mutmut_179 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_validate_runtime_evidence__mutmut)
def validate_runtime_evidence(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_orig(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_1(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = None
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_2(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path and documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_3(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = None
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_4(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None and documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_5(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is not None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_6(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is not None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_7(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                None,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_8(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                None,
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_9(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                None,
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_10(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                None,
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_11(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                None,
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_12(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_13(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_14(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_15(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_16(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_17(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "XX/XX",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_18(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "XXmissing-evidenceXX",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_19(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "MISSING-EVIDENCE",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_20(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "XXruntime evidence document is requiredXX",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_21(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "RUNTIME EVIDENCE DOCUMENT IS REQUIRED",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_22(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "XXconfigure and retain a runtime evidence receiptXX",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_23(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "CONFIGURE AND RETAIN A RUNTIME EVIDENCE RECEIPT",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_24(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = None
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_25(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        None,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_26(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=None,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_27(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_28(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_29(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = None
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_30(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(None)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_31(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(None)
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_32(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(None, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_33(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=None))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_34(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_35(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, ))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_36(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(None)
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_37(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(None, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_38(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=None))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_39(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_40(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, ))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_41(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) and max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_42(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) and not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_43(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_44(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds <= 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_45(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 1:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_46(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            None
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_47(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                None,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_48(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                None,
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_49(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                None,
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_50(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                None,
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_51(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                None,
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_52(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_53(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_54(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_55(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_56(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_57(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "XX/max_age_secondsXX",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_58(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/MAX_AGE_SECONDS",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_59(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "XXinvalid-max-ageXX",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_60(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "INVALID-MAX-AGE",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_61(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "XXmax_age_seconds must be a non-negative integerXX",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_62(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "MAX_AGE_SECONDS MUST BE A NON-NEGATIVE INTEGER",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_63(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "XXconfigure a bounded non-negative freshness windowXX",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_64(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "CONFIGURE A BOUNDED NON-NEGATIVE FRESHNESS WINDOW",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_65(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            None
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_66(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(None, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_67(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=None, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_68(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=None, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_69(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=None)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_70(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_71(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_72(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_73(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, )
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_74(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = None
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_75(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" - hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_76(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "XXsha256:XX" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_77(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "SHA256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_78(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(None).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_79(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(None)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_80(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = None
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_81(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get(None)
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_82(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("XXcontract_digestXX")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_83(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("CONTRACT_DIGEST")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_84(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) and _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_85(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_86(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(None) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_87(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is not None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_88(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            None
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_89(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                None,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_90(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                None,
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_91(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                None,
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_92(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                None,
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_93(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                None,
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_94(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_95(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_96(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_97(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_98(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_99(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "XX/contract_digestXX",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_100(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/CONTRACT_DIGEST",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_101(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "XXinvalid-contract-digestXX",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_102(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "INVALID-CONTRACT-DIGEST",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_103(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "XXcontract digest must be sha256 followed by 64 lowercase hexadecimal charactersXX",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_104(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "CONTRACT DIGEST MUST BE SHA256 FOLLOWED BY 64 LOWERCASE HEXADECIMAL CHARACTERS",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_105(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "XXhash the selected canonical contract bytes with SHA-256XX",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_106(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with sha-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_107(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "HASH THE SELECTED CANONICAL CONTRACT BYTES WITH SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_108(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest == expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_109(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            None
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_110(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                None,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_111(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                None,
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_112(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                None,
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_113(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                None,
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_114(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                None,
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_115(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_116(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_117(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_118(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_119(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_120(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "XX/contract_digestXX",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_121(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/CONTRACT_DIGEST",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_122(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "XXcontract-digest-mismatchXX",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_123(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "CONTRACT-DIGEST-MISMATCH",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_124(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "XXevidence is bound to different contract bytesXX",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_125(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "EVIDENCE IS BOUND TO DIFFERENT CONTRACT BYTES",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_126(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "XXcollect evidence using the exact selected canonical contractXX",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_127(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "COLLECT EVIDENCE USING THE EXACT SELECTED CANONICAL CONTRACT",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_128(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(None)
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_129(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(None, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_130(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=None, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_131(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=None))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_132(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_133(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_134(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, ))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_135(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(None)
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_136(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(None, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_137(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=None, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_138(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=None))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_139(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_140(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_141(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, ))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_142(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(None)
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_143(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(None, source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_144(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=None))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_145(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(source=source))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_146(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, ))
    return sort_findings(findings)


def x_validate_runtime_evidence__mutmut_147(
    documents: ContractDocuments,
    *,
    expected_identity: Mapping[str, object],
    required_checks: tuple[str, ...],
    now: datetime,
    max_age_seconds: int,
) -> tuple[ContractFinding, ...]:
    """Validate a receipt against canonical contract bytes and independent identity."""
    source = documents.evidence_path or documents.contract_path
    evidence = documents.evidence
    if evidence is None or documents.evidence_path is None:
        return (
            _finding(
                source,
                "/",
                "missing-evidence",
                "runtime evidence document is required",
                "configure and retain a runtime evidence receipt",
            ),
        )
    validated_required_checks, required_check_findings = _normalise_required_checks(
        required_checks,
        source=source,
    )
    findings = list(required_check_findings)
    findings.extend(_expected_identity_findings(expected_identity, source=source))
    findings.extend(_receipt_identity_findings(evidence, source=source))
    if isinstance(max_age_seconds, bool) or not isinstance(max_age_seconds, int) or max_age_seconds < 0:
        findings.append(
            _finding(
                source,
                "/max_age_seconds",
                "invalid-max-age",
                "max_age_seconds must be a non-negative integer",
                "configure a bounded non-negative freshness window",
            )
        )
    else:
        findings.extend(
            _freshness_findings(evidence, source=source, now=now, max_age_seconds=max_age_seconds)
        )

    expected_contract_digest = (
        "sha256:" + hashlib.sha256(canonical_json_bytes(documents.contract)).hexdigest()
    )
    actual_contract_digest = evidence.get("contract_digest")
    if not isinstance(actual_contract_digest, str) or _SHA256_RE.fullmatch(actual_contract_digest) is None:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "invalid-contract-digest",
                "contract digest must be sha256 followed by 64 lowercase hexadecimal characters",
                "hash the selected canonical contract bytes with SHA-256",
            )
        )
    elif actual_contract_digest != expected_contract_digest:
        findings.append(
            _finding(
                source,
                "/contract_digest",
                "contract-digest-mismatch",
                "evidence is bound to different contract bytes",
                "collect evidence using the exact selected canonical contract",
            )
        )

    findings.extend(_identity_findings(evidence, expected_identity=expected_identity, source=source))
    findings.extend(_check_findings(evidence, required_checks=validated_required_checks, source=source))
    findings.extend(_artifact_findings(evidence, source=source))
    return sort_findings(None)

mutants_x_validate_runtime_evidence__mutmut['_mutmut_orig'] = x_validate_runtime_evidence__mutmut_orig # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_1'] = x_validate_runtime_evidence__mutmut_1 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_2'] = x_validate_runtime_evidence__mutmut_2 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_3'] = x_validate_runtime_evidence__mutmut_3 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_4'] = x_validate_runtime_evidence__mutmut_4 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_5'] = x_validate_runtime_evidence__mutmut_5 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_6'] = x_validate_runtime_evidence__mutmut_6 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_7'] = x_validate_runtime_evidence__mutmut_7 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_8'] = x_validate_runtime_evidence__mutmut_8 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_9'] = x_validate_runtime_evidence__mutmut_9 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_10'] = x_validate_runtime_evidence__mutmut_10 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_11'] = x_validate_runtime_evidence__mutmut_11 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_12'] = x_validate_runtime_evidence__mutmut_12 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_13'] = x_validate_runtime_evidence__mutmut_13 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_14'] = x_validate_runtime_evidence__mutmut_14 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_15'] = x_validate_runtime_evidence__mutmut_15 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_16'] = x_validate_runtime_evidence__mutmut_16 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_17'] = x_validate_runtime_evidence__mutmut_17 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_18'] = x_validate_runtime_evidence__mutmut_18 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_19'] = x_validate_runtime_evidence__mutmut_19 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_20'] = x_validate_runtime_evidence__mutmut_20 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_21'] = x_validate_runtime_evidence__mutmut_21 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_22'] = x_validate_runtime_evidence__mutmut_22 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_23'] = x_validate_runtime_evidence__mutmut_23 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_24'] = x_validate_runtime_evidence__mutmut_24 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_25'] = x_validate_runtime_evidence__mutmut_25 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_26'] = x_validate_runtime_evidence__mutmut_26 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_27'] = x_validate_runtime_evidence__mutmut_27 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_28'] = x_validate_runtime_evidence__mutmut_28 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_29'] = x_validate_runtime_evidence__mutmut_29 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_30'] = x_validate_runtime_evidence__mutmut_30 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_31'] = x_validate_runtime_evidence__mutmut_31 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_32'] = x_validate_runtime_evidence__mutmut_32 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_33'] = x_validate_runtime_evidence__mutmut_33 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_34'] = x_validate_runtime_evidence__mutmut_34 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_35'] = x_validate_runtime_evidence__mutmut_35 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_36'] = x_validate_runtime_evidence__mutmut_36 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_37'] = x_validate_runtime_evidence__mutmut_37 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_38'] = x_validate_runtime_evidence__mutmut_38 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_39'] = x_validate_runtime_evidence__mutmut_39 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_40'] = x_validate_runtime_evidence__mutmut_40 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_41'] = x_validate_runtime_evidence__mutmut_41 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_42'] = x_validate_runtime_evidence__mutmut_42 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_43'] = x_validate_runtime_evidence__mutmut_43 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_44'] = x_validate_runtime_evidence__mutmut_44 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_45'] = x_validate_runtime_evidence__mutmut_45 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_46'] = x_validate_runtime_evidence__mutmut_46 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_47'] = x_validate_runtime_evidence__mutmut_47 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_48'] = x_validate_runtime_evidence__mutmut_48 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_49'] = x_validate_runtime_evidence__mutmut_49 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_50'] = x_validate_runtime_evidence__mutmut_50 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_51'] = x_validate_runtime_evidence__mutmut_51 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_52'] = x_validate_runtime_evidence__mutmut_52 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_53'] = x_validate_runtime_evidence__mutmut_53 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_54'] = x_validate_runtime_evidence__mutmut_54 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_55'] = x_validate_runtime_evidence__mutmut_55 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_56'] = x_validate_runtime_evidence__mutmut_56 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_57'] = x_validate_runtime_evidence__mutmut_57 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_58'] = x_validate_runtime_evidence__mutmut_58 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_59'] = x_validate_runtime_evidence__mutmut_59 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_60'] = x_validate_runtime_evidence__mutmut_60 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_61'] = x_validate_runtime_evidence__mutmut_61 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_62'] = x_validate_runtime_evidence__mutmut_62 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_63'] = x_validate_runtime_evidence__mutmut_63 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_64'] = x_validate_runtime_evidence__mutmut_64 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_65'] = x_validate_runtime_evidence__mutmut_65 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_66'] = x_validate_runtime_evidence__mutmut_66 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_67'] = x_validate_runtime_evidence__mutmut_67 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_68'] = x_validate_runtime_evidence__mutmut_68 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_69'] = x_validate_runtime_evidence__mutmut_69 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_70'] = x_validate_runtime_evidence__mutmut_70 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_71'] = x_validate_runtime_evidence__mutmut_71 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_72'] = x_validate_runtime_evidence__mutmut_72 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_73'] = x_validate_runtime_evidence__mutmut_73 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_74'] = x_validate_runtime_evidence__mutmut_74 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_75'] = x_validate_runtime_evidence__mutmut_75 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_76'] = x_validate_runtime_evidence__mutmut_76 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_77'] = x_validate_runtime_evidence__mutmut_77 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_78'] = x_validate_runtime_evidence__mutmut_78 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_79'] = x_validate_runtime_evidence__mutmut_79 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_80'] = x_validate_runtime_evidence__mutmut_80 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_81'] = x_validate_runtime_evidence__mutmut_81 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_82'] = x_validate_runtime_evidence__mutmut_82 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_83'] = x_validate_runtime_evidence__mutmut_83 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_84'] = x_validate_runtime_evidence__mutmut_84 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_85'] = x_validate_runtime_evidence__mutmut_85 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_86'] = x_validate_runtime_evidence__mutmut_86 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_87'] = x_validate_runtime_evidence__mutmut_87 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_88'] = x_validate_runtime_evidence__mutmut_88 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_89'] = x_validate_runtime_evidence__mutmut_89 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_90'] = x_validate_runtime_evidence__mutmut_90 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_91'] = x_validate_runtime_evidence__mutmut_91 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_92'] = x_validate_runtime_evidence__mutmut_92 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_93'] = x_validate_runtime_evidence__mutmut_93 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_94'] = x_validate_runtime_evidence__mutmut_94 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_95'] = x_validate_runtime_evidence__mutmut_95 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_96'] = x_validate_runtime_evidence__mutmut_96 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_97'] = x_validate_runtime_evidence__mutmut_97 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_98'] = x_validate_runtime_evidence__mutmut_98 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_99'] = x_validate_runtime_evidence__mutmut_99 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_100'] = x_validate_runtime_evidence__mutmut_100 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_101'] = x_validate_runtime_evidence__mutmut_101 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_102'] = x_validate_runtime_evidence__mutmut_102 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_103'] = x_validate_runtime_evidence__mutmut_103 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_104'] = x_validate_runtime_evidence__mutmut_104 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_105'] = x_validate_runtime_evidence__mutmut_105 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_106'] = x_validate_runtime_evidence__mutmut_106 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_107'] = x_validate_runtime_evidence__mutmut_107 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_108'] = x_validate_runtime_evidence__mutmut_108 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_109'] = x_validate_runtime_evidence__mutmut_109 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_110'] = x_validate_runtime_evidence__mutmut_110 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_111'] = x_validate_runtime_evidence__mutmut_111 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_112'] = x_validate_runtime_evidence__mutmut_112 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_113'] = x_validate_runtime_evidence__mutmut_113 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_114'] = x_validate_runtime_evidence__mutmut_114 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_115'] = x_validate_runtime_evidence__mutmut_115 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_116'] = x_validate_runtime_evidence__mutmut_116 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_117'] = x_validate_runtime_evidence__mutmut_117 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_118'] = x_validate_runtime_evidence__mutmut_118 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_119'] = x_validate_runtime_evidence__mutmut_119 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_120'] = x_validate_runtime_evidence__mutmut_120 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_121'] = x_validate_runtime_evidence__mutmut_121 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_122'] = x_validate_runtime_evidence__mutmut_122 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_123'] = x_validate_runtime_evidence__mutmut_123 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_124'] = x_validate_runtime_evidence__mutmut_124 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_125'] = x_validate_runtime_evidence__mutmut_125 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_126'] = x_validate_runtime_evidence__mutmut_126 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_127'] = x_validate_runtime_evidence__mutmut_127 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_128'] = x_validate_runtime_evidence__mutmut_128 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_129'] = x_validate_runtime_evidence__mutmut_129 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_130'] = x_validate_runtime_evidence__mutmut_130 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_131'] = x_validate_runtime_evidence__mutmut_131 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_132'] = x_validate_runtime_evidence__mutmut_132 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_133'] = x_validate_runtime_evidence__mutmut_133 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_134'] = x_validate_runtime_evidence__mutmut_134 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_135'] = x_validate_runtime_evidence__mutmut_135 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_136'] = x_validate_runtime_evidence__mutmut_136 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_137'] = x_validate_runtime_evidence__mutmut_137 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_138'] = x_validate_runtime_evidence__mutmut_138 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_139'] = x_validate_runtime_evidence__mutmut_139 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_140'] = x_validate_runtime_evidence__mutmut_140 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_141'] = x_validate_runtime_evidence__mutmut_141 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_142'] = x_validate_runtime_evidence__mutmut_142 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_143'] = x_validate_runtime_evidence__mutmut_143 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_144'] = x_validate_runtime_evidence__mutmut_144 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_145'] = x_validate_runtime_evidence__mutmut_145 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_146'] = x_validate_runtime_evidence__mutmut_146 # type: ignore # mutmut generated
mutants_x_validate_runtime_evidence__mutmut['x_validate_runtime_evidence__mutmut_147'] = x_validate_runtime_evidence__mutmut_147 # type: ignore # mutmut generated
mutants_xǁRuntimeEvidenceContractǁ__init____mutmut: MutantDict = {}  # type: ignore
mutants_xǁRuntimeEvidenceContractǁvalidate_documents__mutmut: MutantDict = {}  # type: ignore
mutants_xǁRuntimeEvidenceContractǁrun__mutmut: MutantDict = {}  # type: ignore


class RuntimeEvidenceContract(RuntimeContractRule):
    """Hard-adoption CORE rule for fresh, digest-bound runtime receipts."""

    name = "runtime-evidence-contract"
    remediation = REMEDIATION
    requires_evidence = True

    @_mutmut_mutated(mutants_xǁRuntimeEvidenceContractǁ__init____mutmut)
    def __init__(self, config: Mapping[str, object], *, repo_root: Path | None = None) -> None:
        super().__init__(config, repo_root=repo_root)
        nested_identity = config.get("expected_identity")
        if isinstance(nested_identity, Mapping):
            self.expected_identity = dict(cast(Mapping[str, object], nested_identity))
        else:
            self.expected_identity = {field: config.get(f"expected_{field}") for field in _IDENTITY_FIELDS}
        self.required_checks, self._required_check_findings = _normalise_required_checks(
            config.get("required_checks", ()),
            source=self._repo_root / "pyproject.toml",
        )
        raw_max_age = config.get("max_age_seconds", 300)
        self.max_age_seconds = cast(int, raw_max_age)

    def xǁRuntimeEvidenceContractǁ__init____mutmut_orig(self, config: Mapping[str, object], *, repo_root: Path | None = None) -> None:
        super().__init__(config, repo_root=repo_root)
        nested_identity = config.get("expected_identity")
        if isinstance(nested_identity, Mapping):
            self.expected_identity = dict(cast(Mapping[str, object], nested_identity))
        else:
            self.expected_identity = {field: config.get(f"expected_{field}") for field in _IDENTITY_FIELDS}
        self.required_checks, self._required_check_findings = _normalise_required_checks(
            config.get("required_checks", ()),
            source=self._repo_root / "pyproject.toml",
        )
        raw_max_age = config.get("max_age_seconds", 300)
        self.max_age_seconds = cast(int, raw_max_age)

    def xǁRuntimeEvidenceContractǁ__init____mutmut_1(self, config: Mapping[str, object], *, repo_root: Path | None = None) -> None:
        super().__init__(None, repo_root=repo_root)
        nested_identity = config.get("expected_identity")
        if isinstance(nested_identity, Mapping):
            self.expected_identity = dict(cast(Mapping[str, object], nested_identity))
        else:
            self.expected_identity = {field: config.get(f"expected_{field}") for field in _IDENTITY_FIELDS}
        self.required_checks, self._required_check_findings = _normalise_required_checks(
            config.get("required_checks", ()),
            source=self._repo_root / "pyproject.toml",
        )
        raw_max_age = config.get("max_age_seconds", 300)
        self.max_age_seconds = cast(int, raw_max_age)

    def xǁRuntimeEvidenceContractǁ__init____mutmut_2(self, config: Mapping[str, object], *, repo_root: Path | None = None) -> None:
        super().__init__(config, repo_root=None)
        nested_identity = config.get("expected_identity")
        if isinstance(nested_identity, Mapping):
            self.expected_identity = dict(cast(Mapping[str, object], nested_identity))
        else:
            self.expected_identity = {field: config.get(f"expected_{field}") for field in _IDENTITY_FIELDS}
        self.required_checks, self._required_check_findings = _normalise_required_checks(
            config.get("required_checks", ()),
            source=self._repo_root / "pyproject.toml",
        )
        raw_max_age = config.get("max_age_seconds", 300)
        self.max_age_seconds = cast(int, raw_max_age)

    def xǁRuntimeEvidenceContractǁ__init____mutmut_3(self, config: Mapping[str, object], *, repo_root: Path | None = None) -> None:
        super().__init__(repo_root=repo_root)
        nested_identity = config.get("expected_identity")
        if isinstance(nested_identity, Mapping):
            self.expected_identity = dict(cast(Mapping[str, object], nested_identity))
        else:
            self.expected_identity = {field: config.get(f"expected_{field}") for field in _IDENTITY_FIELDS}
        self.required_checks, self._required_check_findings = _normalise_required_checks(
            config.get("required_checks", ()),
            source=self._repo_root / "pyproject.toml",
        )
        raw_max_age = config.get("max_age_seconds", 300)
        self.max_age_seconds = cast(int, raw_max_age)

    def xǁRuntimeEvidenceContractǁ__init____mutmut_4(self, config: Mapping[str, object], *, repo_root: Path | None = None) -> None:
        super().__init__(config, )
        nested_identity = config.get("expected_identity")
        if isinstance(nested_identity, Mapping):
            self.expected_identity = dict(cast(Mapping[str, object], nested_identity))
        else:
            self.expected_identity = {field: config.get(f"expected_{field}") for field in _IDENTITY_FIELDS}
        self.required_checks, self._required_check_findings = _normalise_required_checks(
            config.get("required_checks", ()),
            source=self._repo_root / "pyproject.toml",
        )
        raw_max_age = config.get("max_age_seconds", 300)
        self.max_age_seconds = cast(int, raw_max_age)

    def xǁRuntimeEvidenceContractǁ__init____mutmut_5(self, config: Mapping[str, object], *, repo_root: Path | None = None) -> None:
        super().__init__(config, repo_root=repo_root)
        nested_identity = None
        if isinstance(nested_identity, Mapping):
            self.expected_identity = dict(cast(Mapping[str, object], nested_identity))
        else:
            self.expected_identity = {field: config.get(f"expected_{field}") for field in _IDENTITY_FIELDS}
        self.required_checks, self._required_check_findings = _normalise_required_checks(
            config.get("required_checks", ()),
            source=self._repo_root / "pyproject.toml",
        )
        raw_max_age = config.get("max_age_seconds", 300)
        self.max_age_seconds = cast(int, raw_max_age)

    def xǁRuntimeEvidenceContractǁ__init____mutmut_6(self, config: Mapping[str, object], *, repo_root: Path | None = None) -> None:
        super().__init__(config, repo_root=repo_root)
        nested_identity = config.get(None)
        if isinstance(nested_identity, Mapping):
            self.expected_identity = dict(cast(Mapping[str, object], nested_identity))
        else:
            self.expected_identity = {field: config.get(f"expected_{field}") for field in _IDENTITY_FIELDS}
        self.required_checks, self._required_check_findings = _normalise_required_checks(
            config.get("required_checks", ()),
            source=self._repo_root / "pyproject.toml",
        )
        raw_max_age = config.get("max_age_seconds", 300)
        self.max_age_seconds = cast(int, raw_max_age)

    def xǁRuntimeEvidenceContractǁ__init____mutmut_7(self, config: Mapping[str, object], *, repo_root: Path | None = None) -> None:
        super().__init__(config, repo_root=repo_root)
        nested_identity = config.get("XXexpected_identityXX")
        if isinstance(nested_identity, Mapping):
            self.expected_identity = dict(cast(Mapping[str, object], nested_identity))
        else:
            self.expected_identity = {field: config.get(f"expected_{field}") for field in _IDENTITY_FIELDS}
        self.required_checks, self._required_check_findings = _normalise_required_checks(
            config.get("required_checks", ()),
            source=self._repo_root / "pyproject.toml",
        )
        raw_max_age = config.get("max_age_seconds", 300)
        self.max_age_seconds = cast(int, raw_max_age)

    def xǁRuntimeEvidenceContractǁ__init____mutmut_8(self, config: Mapping[str, object], *, repo_root: Path | None = None) -> None:
        super().__init__(config, repo_root=repo_root)
        nested_identity = config.get("EXPECTED_IDENTITY")
        if isinstance(nested_identity, Mapping):
            self.expected_identity = dict(cast(Mapping[str, object], nested_identity))
        else:
            self.expected_identity = {field: config.get(f"expected_{field}") for field in _IDENTITY_FIELDS}
        self.required_checks, self._required_check_findings = _normalise_required_checks(
            config.get("required_checks", ()),
            source=self._repo_root / "pyproject.toml",
        )
        raw_max_age = config.get("max_age_seconds", 300)
        self.max_age_seconds = cast(int, raw_max_age)

    def xǁRuntimeEvidenceContractǁ__init____mutmut_9(self, config: Mapping[str, object], *, repo_root: Path | None = None) -> None:
        super().__init__(config, repo_root=repo_root)
        nested_identity = config.get("expected_identity")
        if isinstance(nested_identity, Mapping):
            self.expected_identity = None
        else:
            self.expected_identity = {field: config.get(f"expected_{field}") for field in _IDENTITY_FIELDS}
        self.required_checks, self._required_check_findings = _normalise_required_checks(
            config.get("required_checks", ()),
            source=self._repo_root / "pyproject.toml",
        )
        raw_max_age = config.get("max_age_seconds", 300)
        self.max_age_seconds = cast(int, raw_max_age)

    def xǁRuntimeEvidenceContractǁ__init____mutmut_10(self, config: Mapping[str, object], *, repo_root: Path | None = None) -> None:
        super().__init__(config, repo_root=repo_root)
        nested_identity = config.get("expected_identity")
        if isinstance(nested_identity, Mapping):
            self.expected_identity = dict(None)
        else:
            self.expected_identity = {field: config.get(f"expected_{field}") for field in _IDENTITY_FIELDS}
        self.required_checks, self._required_check_findings = _normalise_required_checks(
            config.get("required_checks", ()),
            source=self._repo_root / "pyproject.toml",
        )
        raw_max_age = config.get("max_age_seconds", 300)
        self.max_age_seconds = cast(int, raw_max_age)

    def xǁRuntimeEvidenceContractǁ__init____mutmut_11(self, config: Mapping[str, object], *, repo_root: Path | None = None) -> None:
        super().__init__(config, repo_root=repo_root)
        nested_identity = config.get("expected_identity")
        if isinstance(nested_identity, Mapping):
            self.expected_identity = dict(cast(None, nested_identity))
        else:
            self.expected_identity = {field: config.get(f"expected_{field}") for field in _IDENTITY_FIELDS}
        self.required_checks, self._required_check_findings = _normalise_required_checks(
            config.get("required_checks", ()),
            source=self._repo_root / "pyproject.toml",
        )
        raw_max_age = config.get("max_age_seconds", 300)
        self.max_age_seconds = cast(int, raw_max_age)

    def xǁRuntimeEvidenceContractǁ__init____mutmut_12(self, config: Mapping[str, object], *, repo_root: Path | None = None) -> None:
        super().__init__(config, repo_root=repo_root)
        nested_identity = config.get("expected_identity")
        if isinstance(nested_identity, Mapping):
            self.expected_identity = dict(cast(Mapping[str, object], None))
        else:
            self.expected_identity = {field: config.get(f"expected_{field}") for field in _IDENTITY_FIELDS}
        self.required_checks, self._required_check_findings = _normalise_required_checks(
            config.get("required_checks", ()),
            source=self._repo_root / "pyproject.toml",
        )
        raw_max_age = config.get("max_age_seconds", 300)
        self.max_age_seconds = cast(int, raw_max_age)

    def xǁRuntimeEvidenceContractǁ__init____mutmut_13(self, config: Mapping[str, object], *, repo_root: Path | None = None) -> None:
        super().__init__(config, repo_root=repo_root)
        nested_identity = config.get("expected_identity")
        if isinstance(nested_identity, Mapping):
            self.expected_identity = dict(cast(nested_identity))
        else:
            self.expected_identity = {field: config.get(f"expected_{field}") for field in _IDENTITY_FIELDS}
        self.required_checks, self._required_check_findings = _normalise_required_checks(
            config.get("required_checks", ()),
            source=self._repo_root / "pyproject.toml",
        )
        raw_max_age = config.get("max_age_seconds", 300)
        self.max_age_seconds = cast(int, raw_max_age)

    def xǁRuntimeEvidenceContractǁ__init____mutmut_14(self, config: Mapping[str, object], *, repo_root: Path | None = None) -> None:
        super().__init__(config, repo_root=repo_root)
        nested_identity = config.get("expected_identity")
        if isinstance(nested_identity, Mapping):
            self.expected_identity = dict(cast(Mapping[str, object], ))
        else:
            self.expected_identity = {field: config.get(f"expected_{field}") for field in _IDENTITY_FIELDS}
        self.required_checks, self._required_check_findings = _normalise_required_checks(
            config.get("required_checks", ()),
            source=self._repo_root / "pyproject.toml",
        )
        raw_max_age = config.get("max_age_seconds", 300)
        self.max_age_seconds = cast(int, raw_max_age)

    def xǁRuntimeEvidenceContractǁ__init____mutmut_15(self, config: Mapping[str, object], *, repo_root: Path | None = None) -> None:
        super().__init__(config, repo_root=repo_root)
        nested_identity = config.get("expected_identity")
        if isinstance(nested_identity, Mapping):
            self.expected_identity = dict(cast(Mapping[str, object], nested_identity))
        else:
            self.expected_identity = None
        self.required_checks, self._required_check_findings = _normalise_required_checks(
            config.get("required_checks", ()),
            source=self._repo_root / "pyproject.toml",
        )
        raw_max_age = config.get("max_age_seconds", 300)
        self.max_age_seconds = cast(int, raw_max_age)

    def xǁRuntimeEvidenceContractǁ__init____mutmut_16(self, config: Mapping[str, object], *, repo_root: Path | None = None) -> None:
        super().__init__(config, repo_root=repo_root)
        nested_identity = config.get("expected_identity")
        if isinstance(nested_identity, Mapping):
            self.expected_identity = dict(cast(Mapping[str, object], nested_identity))
        else:
            self.expected_identity = {field: config.get(None) for field in _IDENTITY_FIELDS}
        self.required_checks, self._required_check_findings = _normalise_required_checks(
            config.get("required_checks", ()),
            source=self._repo_root / "pyproject.toml",
        )
        raw_max_age = config.get("max_age_seconds", 300)
        self.max_age_seconds = cast(int, raw_max_age)

    def xǁRuntimeEvidenceContractǁ__init____mutmut_17(self, config: Mapping[str, object], *, repo_root: Path | None = None) -> None:
        super().__init__(config, repo_root=repo_root)
        nested_identity = config.get("expected_identity")
        if isinstance(nested_identity, Mapping):
            self.expected_identity = dict(cast(Mapping[str, object], nested_identity))
        else:
            self.expected_identity = {field: config.get(f"expected_{field}") for field in _IDENTITY_FIELDS}
        self.required_checks, self._required_check_findings = None
        raw_max_age = config.get("max_age_seconds", 300)
        self.max_age_seconds = cast(int, raw_max_age)

    def xǁRuntimeEvidenceContractǁ__init____mutmut_18(self, config: Mapping[str, object], *, repo_root: Path | None = None) -> None:
        super().__init__(config, repo_root=repo_root)
        nested_identity = config.get("expected_identity")
        if isinstance(nested_identity, Mapping):
            self.expected_identity = dict(cast(Mapping[str, object], nested_identity))
        else:
            self.expected_identity = {field: config.get(f"expected_{field}") for field in _IDENTITY_FIELDS}
        self.required_checks, self._required_check_findings = _normalise_required_checks(
            None,
            source=self._repo_root / "pyproject.toml",
        )
        raw_max_age = config.get("max_age_seconds", 300)
        self.max_age_seconds = cast(int, raw_max_age)

    def xǁRuntimeEvidenceContractǁ__init____mutmut_19(self, config: Mapping[str, object], *, repo_root: Path | None = None) -> None:
        super().__init__(config, repo_root=repo_root)
        nested_identity = config.get("expected_identity")
        if isinstance(nested_identity, Mapping):
            self.expected_identity = dict(cast(Mapping[str, object], nested_identity))
        else:
            self.expected_identity = {field: config.get(f"expected_{field}") for field in _IDENTITY_FIELDS}
        self.required_checks, self._required_check_findings = _normalise_required_checks(
            config.get("required_checks", ()),
            source=None,
        )
        raw_max_age = config.get("max_age_seconds", 300)
        self.max_age_seconds = cast(int, raw_max_age)

    def xǁRuntimeEvidenceContractǁ__init____mutmut_20(self, config: Mapping[str, object], *, repo_root: Path | None = None) -> None:
        super().__init__(config, repo_root=repo_root)
        nested_identity = config.get("expected_identity")
        if isinstance(nested_identity, Mapping):
            self.expected_identity = dict(cast(Mapping[str, object], nested_identity))
        else:
            self.expected_identity = {field: config.get(f"expected_{field}") for field in _IDENTITY_FIELDS}
        self.required_checks, self._required_check_findings = _normalise_required_checks(
            source=self._repo_root / "pyproject.toml",
        )
        raw_max_age = config.get("max_age_seconds", 300)
        self.max_age_seconds = cast(int, raw_max_age)

    def xǁRuntimeEvidenceContractǁ__init____mutmut_21(self, config: Mapping[str, object], *, repo_root: Path | None = None) -> None:
        super().__init__(config, repo_root=repo_root)
        nested_identity = config.get("expected_identity")
        if isinstance(nested_identity, Mapping):
            self.expected_identity = dict(cast(Mapping[str, object], nested_identity))
        else:
            self.expected_identity = {field: config.get(f"expected_{field}") for field in _IDENTITY_FIELDS}
        self.required_checks, self._required_check_findings = _normalise_required_checks(
            config.get("required_checks", ()),
            )
        raw_max_age = config.get("max_age_seconds", 300)
        self.max_age_seconds = cast(int, raw_max_age)

    def xǁRuntimeEvidenceContractǁ__init____mutmut_22(self, config: Mapping[str, object], *, repo_root: Path | None = None) -> None:
        super().__init__(config, repo_root=repo_root)
        nested_identity = config.get("expected_identity")
        if isinstance(nested_identity, Mapping):
            self.expected_identity = dict(cast(Mapping[str, object], nested_identity))
        else:
            self.expected_identity = {field: config.get(f"expected_{field}") for field in _IDENTITY_FIELDS}
        self.required_checks, self._required_check_findings = _normalise_required_checks(
            config.get(None, ()),
            source=self._repo_root / "pyproject.toml",
        )
        raw_max_age = config.get("max_age_seconds", 300)
        self.max_age_seconds = cast(int, raw_max_age)

    def xǁRuntimeEvidenceContractǁ__init____mutmut_23(self, config: Mapping[str, object], *, repo_root: Path | None = None) -> None:
        super().__init__(config, repo_root=repo_root)
        nested_identity = config.get("expected_identity")
        if isinstance(nested_identity, Mapping):
            self.expected_identity = dict(cast(Mapping[str, object], nested_identity))
        else:
            self.expected_identity = {field: config.get(f"expected_{field}") for field in _IDENTITY_FIELDS}
        self.required_checks, self._required_check_findings = _normalise_required_checks(
            config.get("required_checks", None),
            source=self._repo_root / "pyproject.toml",
        )
        raw_max_age = config.get("max_age_seconds", 300)
        self.max_age_seconds = cast(int, raw_max_age)

    def xǁRuntimeEvidenceContractǁ__init____mutmut_24(self, config: Mapping[str, object], *, repo_root: Path | None = None) -> None:
        super().__init__(config, repo_root=repo_root)
        nested_identity = config.get("expected_identity")
        if isinstance(nested_identity, Mapping):
            self.expected_identity = dict(cast(Mapping[str, object], nested_identity))
        else:
            self.expected_identity = {field: config.get(f"expected_{field}") for field in _IDENTITY_FIELDS}
        self.required_checks, self._required_check_findings = _normalise_required_checks(
            config.get(()),
            source=self._repo_root / "pyproject.toml",
        )
        raw_max_age = config.get("max_age_seconds", 300)
        self.max_age_seconds = cast(int, raw_max_age)

    def xǁRuntimeEvidenceContractǁ__init____mutmut_25(self, config: Mapping[str, object], *, repo_root: Path | None = None) -> None:
        super().__init__(config, repo_root=repo_root)
        nested_identity = config.get("expected_identity")
        if isinstance(nested_identity, Mapping):
            self.expected_identity = dict(cast(Mapping[str, object], nested_identity))
        else:
            self.expected_identity = {field: config.get(f"expected_{field}") for field in _IDENTITY_FIELDS}
        self.required_checks, self._required_check_findings = _normalise_required_checks(
            config.get("required_checks", ),
            source=self._repo_root / "pyproject.toml",
        )
        raw_max_age = config.get("max_age_seconds", 300)
        self.max_age_seconds = cast(int, raw_max_age)

    def xǁRuntimeEvidenceContractǁ__init____mutmut_26(self, config: Mapping[str, object], *, repo_root: Path | None = None) -> None:
        super().__init__(config, repo_root=repo_root)
        nested_identity = config.get("expected_identity")
        if isinstance(nested_identity, Mapping):
            self.expected_identity = dict(cast(Mapping[str, object], nested_identity))
        else:
            self.expected_identity = {field: config.get(f"expected_{field}") for field in _IDENTITY_FIELDS}
        self.required_checks, self._required_check_findings = _normalise_required_checks(
            config.get("XXrequired_checksXX", ()),
            source=self._repo_root / "pyproject.toml",
        )
        raw_max_age = config.get("max_age_seconds", 300)
        self.max_age_seconds = cast(int, raw_max_age)

    def xǁRuntimeEvidenceContractǁ__init____mutmut_27(self, config: Mapping[str, object], *, repo_root: Path | None = None) -> None:
        super().__init__(config, repo_root=repo_root)
        nested_identity = config.get("expected_identity")
        if isinstance(nested_identity, Mapping):
            self.expected_identity = dict(cast(Mapping[str, object], nested_identity))
        else:
            self.expected_identity = {field: config.get(f"expected_{field}") for field in _IDENTITY_FIELDS}
        self.required_checks, self._required_check_findings = _normalise_required_checks(
            config.get("REQUIRED_CHECKS", ()),
            source=self._repo_root / "pyproject.toml",
        )
        raw_max_age = config.get("max_age_seconds", 300)
        self.max_age_seconds = cast(int, raw_max_age)

    def xǁRuntimeEvidenceContractǁ__init____mutmut_28(self, config: Mapping[str, object], *, repo_root: Path | None = None) -> None:
        super().__init__(config, repo_root=repo_root)
        nested_identity = config.get("expected_identity")
        if isinstance(nested_identity, Mapping):
            self.expected_identity = dict(cast(Mapping[str, object], nested_identity))
        else:
            self.expected_identity = {field: config.get(f"expected_{field}") for field in _IDENTITY_FIELDS}
        self.required_checks, self._required_check_findings = _normalise_required_checks(
            config.get("required_checks", ()),
            source=self._repo_root * "pyproject.toml",
        )
        raw_max_age = config.get("max_age_seconds", 300)
        self.max_age_seconds = cast(int, raw_max_age)

    def xǁRuntimeEvidenceContractǁ__init____mutmut_29(self, config: Mapping[str, object], *, repo_root: Path | None = None) -> None:
        super().__init__(config, repo_root=repo_root)
        nested_identity = config.get("expected_identity")
        if isinstance(nested_identity, Mapping):
            self.expected_identity = dict(cast(Mapping[str, object], nested_identity))
        else:
            self.expected_identity = {field: config.get(f"expected_{field}") for field in _IDENTITY_FIELDS}
        self.required_checks, self._required_check_findings = _normalise_required_checks(
            config.get("required_checks", ()),
            source=self._repo_root / "XXpyproject.tomlXX",
        )
        raw_max_age = config.get("max_age_seconds", 300)
        self.max_age_seconds = cast(int, raw_max_age)

    def xǁRuntimeEvidenceContractǁ__init____mutmut_30(self, config: Mapping[str, object], *, repo_root: Path | None = None) -> None:
        super().__init__(config, repo_root=repo_root)
        nested_identity = config.get("expected_identity")
        if isinstance(nested_identity, Mapping):
            self.expected_identity = dict(cast(Mapping[str, object], nested_identity))
        else:
            self.expected_identity = {field: config.get(f"expected_{field}") for field in _IDENTITY_FIELDS}
        self.required_checks, self._required_check_findings = _normalise_required_checks(
            config.get("required_checks", ()),
            source=self._repo_root / "PYPROJECT.TOML",
        )
        raw_max_age = config.get("max_age_seconds", 300)
        self.max_age_seconds = cast(int, raw_max_age)

    def xǁRuntimeEvidenceContractǁ__init____mutmut_31(self, config: Mapping[str, object], *, repo_root: Path | None = None) -> None:
        super().__init__(config, repo_root=repo_root)
        nested_identity = config.get("expected_identity")
        if isinstance(nested_identity, Mapping):
            self.expected_identity = dict(cast(Mapping[str, object], nested_identity))
        else:
            self.expected_identity = {field: config.get(f"expected_{field}") for field in _IDENTITY_FIELDS}
        self.required_checks, self._required_check_findings = _normalise_required_checks(
            config.get("required_checks", ()),
            source=self._repo_root / "pyproject.toml",
        )
        raw_max_age = None
        self.max_age_seconds = cast(int, raw_max_age)

    def xǁRuntimeEvidenceContractǁ__init____mutmut_32(self, config: Mapping[str, object], *, repo_root: Path | None = None) -> None:
        super().__init__(config, repo_root=repo_root)
        nested_identity = config.get("expected_identity")
        if isinstance(nested_identity, Mapping):
            self.expected_identity = dict(cast(Mapping[str, object], nested_identity))
        else:
            self.expected_identity = {field: config.get(f"expected_{field}") for field in _IDENTITY_FIELDS}
        self.required_checks, self._required_check_findings = _normalise_required_checks(
            config.get("required_checks", ()),
            source=self._repo_root / "pyproject.toml",
        )
        raw_max_age = config.get(None, 300)
        self.max_age_seconds = cast(int, raw_max_age)

    def xǁRuntimeEvidenceContractǁ__init____mutmut_33(self, config: Mapping[str, object], *, repo_root: Path | None = None) -> None:
        super().__init__(config, repo_root=repo_root)
        nested_identity = config.get("expected_identity")
        if isinstance(nested_identity, Mapping):
            self.expected_identity = dict(cast(Mapping[str, object], nested_identity))
        else:
            self.expected_identity = {field: config.get(f"expected_{field}") for field in _IDENTITY_FIELDS}
        self.required_checks, self._required_check_findings = _normalise_required_checks(
            config.get("required_checks", ()),
            source=self._repo_root / "pyproject.toml",
        )
        raw_max_age = config.get("max_age_seconds", None)
        self.max_age_seconds = cast(int, raw_max_age)

    def xǁRuntimeEvidenceContractǁ__init____mutmut_34(self, config: Mapping[str, object], *, repo_root: Path | None = None) -> None:
        super().__init__(config, repo_root=repo_root)
        nested_identity = config.get("expected_identity")
        if isinstance(nested_identity, Mapping):
            self.expected_identity = dict(cast(Mapping[str, object], nested_identity))
        else:
            self.expected_identity = {field: config.get(f"expected_{field}") for field in _IDENTITY_FIELDS}
        self.required_checks, self._required_check_findings = _normalise_required_checks(
            config.get("required_checks", ()),
            source=self._repo_root / "pyproject.toml",
        )
        raw_max_age = config.get(300)
        self.max_age_seconds = cast(int, raw_max_age)

    def xǁRuntimeEvidenceContractǁ__init____mutmut_35(self, config: Mapping[str, object], *, repo_root: Path | None = None) -> None:
        super().__init__(config, repo_root=repo_root)
        nested_identity = config.get("expected_identity")
        if isinstance(nested_identity, Mapping):
            self.expected_identity = dict(cast(Mapping[str, object], nested_identity))
        else:
            self.expected_identity = {field: config.get(f"expected_{field}") for field in _IDENTITY_FIELDS}
        self.required_checks, self._required_check_findings = _normalise_required_checks(
            config.get("required_checks", ()),
            source=self._repo_root / "pyproject.toml",
        )
        raw_max_age = config.get("max_age_seconds", )
        self.max_age_seconds = cast(int, raw_max_age)

    def xǁRuntimeEvidenceContractǁ__init____mutmut_36(self, config: Mapping[str, object], *, repo_root: Path | None = None) -> None:
        super().__init__(config, repo_root=repo_root)
        nested_identity = config.get("expected_identity")
        if isinstance(nested_identity, Mapping):
            self.expected_identity = dict(cast(Mapping[str, object], nested_identity))
        else:
            self.expected_identity = {field: config.get(f"expected_{field}") for field in _IDENTITY_FIELDS}
        self.required_checks, self._required_check_findings = _normalise_required_checks(
            config.get("required_checks", ()),
            source=self._repo_root / "pyproject.toml",
        )
        raw_max_age = config.get("XXmax_age_secondsXX", 300)
        self.max_age_seconds = cast(int, raw_max_age)

    def xǁRuntimeEvidenceContractǁ__init____mutmut_37(self, config: Mapping[str, object], *, repo_root: Path | None = None) -> None:
        super().__init__(config, repo_root=repo_root)
        nested_identity = config.get("expected_identity")
        if isinstance(nested_identity, Mapping):
            self.expected_identity = dict(cast(Mapping[str, object], nested_identity))
        else:
            self.expected_identity = {field: config.get(f"expected_{field}") for field in _IDENTITY_FIELDS}
        self.required_checks, self._required_check_findings = _normalise_required_checks(
            config.get("required_checks", ()),
            source=self._repo_root / "pyproject.toml",
        )
        raw_max_age = config.get("MAX_AGE_SECONDS", 300)
        self.max_age_seconds = cast(int, raw_max_age)

    def xǁRuntimeEvidenceContractǁ__init____mutmut_38(self, config: Mapping[str, object], *, repo_root: Path | None = None) -> None:
        super().__init__(config, repo_root=repo_root)
        nested_identity = config.get("expected_identity")
        if isinstance(nested_identity, Mapping):
            self.expected_identity = dict(cast(Mapping[str, object], nested_identity))
        else:
            self.expected_identity = {field: config.get(f"expected_{field}") for field in _IDENTITY_FIELDS}
        self.required_checks, self._required_check_findings = _normalise_required_checks(
            config.get("required_checks", ()),
            source=self._repo_root / "pyproject.toml",
        )
        raw_max_age = config.get("max_age_seconds", 301)
        self.max_age_seconds = cast(int, raw_max_age)

    def xǁRuntimeEvidenceContractǁ__init____mutmut_39(self, config: Mapping[str, object], *, repo_root: Path | None = None) -> None:
        super().__init__(config, repo_root=repo_root)
        nested_identity = config.get("expected_identity")
        if isinstance(nested_identity, Mapping):
            self.expected_identity = dict(cast(Mapping[str, object], nested_identity))
        else:
            self.expected_identity = {field: config.get(f"expected_{field}") for field in _IDENTITY_FIELDS}
        self.required_checks, self._required_check_findings = _normalise_required_checks(
            config.get("required_checks", ()),
            source=self._repo_root / "pyproject.toml",
        )
        raw_max_age = config.get("max_age_seconds", 300)
        self.max_age_seconds = None

    def xǁRuntimeEvidenceContractǁ__init____mutmut_40(self, config: Mapping[str, object], *, repo_root: Path | None = None) -> None:
        super().__init__(config, repo_root=repo_root)
        nested_identity = config.get("expected_identity")
        if isinstance(nested_identity, Mapping):
            self.expected_identity = dict(cast(Mapping[str, object], nested_identity))
        else:
            self.expected_identity = {field: config.get(f"expected_{field}") for field in _IDENTITY_FIELDS}
        self.required_checks, self._required_check_findings = _normalise_required_checks(
            config.get("required_checks", ()),
            source=self._repo_root / "pyproject.toml",
        )
        raw_max_age = config.get("max_age_seconds", 300)
        self.max_age_seconds = cast(None, raw_max_age)

    def xǁRuntimeEvidenceContractǁ__init____mutmut_41(self, config: Mapping[str, object], *, repo_root: Path | None = None) -> None:
        super().__init__(config, repo_root=repo_root)
        nested_identity = config.get("expected_identity")
        if isinstance(nested_identity, Mapping):
            self.expected_identity = dict(cast(Mapping[str, object], nested_identity))
        else:
            self.expected_identity = {field: config.get(f"expected_{field}") for field in _IDENTITY_FIELDS}
        self.required_checks, self._required_check_findings = _normalise_required_checks(
            config.get("required_checks", ()),
            source=self._repo_root / "pyproject.toml",
        )
        raw_max_age = config.get("max_age_seconds", 300)
        self.max_age_seconds = cast(int, None)

    def xǁRuntimeEvidenceContractǁ__init____mutmut_42(self, config: Mapping[str, object], *, repo_root: Path | None = None) -> None:
        super().__init__(config, repo_root=repo_root)
        nested_identity = config.get("expected_identity")
        if isinstance(nested_identity, Mapping):
            self.expected_identity = dict(cast(Mapping[str, object], nested_identity))
        else:
            self.expected_identity = {field: config.get(f"expected_{field}") for field in _IDENTITY_FIELDS}
        self.required_checks, self._required_check_findings = _normalise_required_checks(
            config.get("required_checks", ()),
            source=self._repo_root / "pyproject.toml",
        )
        raw_max_age = config.get("max_age_seconds", 300)
        self.max_age_seconds = cast(raw_max_age)

    def xǁRuntimeEvidenceContractǁ__init____mutmut_43(self, config: Mapping[str, object], *, repo_root: Path | None = None) -> None:
        super().__init__(config, repo_root=repo_root)
        nested_identity = config.get("expected_identity")
        if isinstance(nested_identity, Mapping):
            self.expected_identity = dict(cast(Mapping[str, object], nested_identity))
        else:
            self.expected_identity = {field: config.get(f"expected_{field}") for field in _IDENTITY_FIELDS}
        self.required_checks, self._required_check_findings = _normalise_required_checks(
            config.get("required_checks", ()),
            source=self._repo_root / "pyproject.toml",
        )
        raw_max_age = config.get("max_age_seconds", 300)
        self.max_age_seconds = cast(int, )

    def validate_configuration(self) -> tuple[ContractFinding, ...]:
        return self._required_check_findings

    @_mutmut_mutated(mutants_xǁRuntimeEvidenceContractǁvalidate_documents__mutmut)
    def validate_documents(self, documents: ContractDocuments) -> tuple[ContractFinding, ...]:
        return validate_runtime_evidence(
            documents,
            expected_identity=self.expected_identity,
            required_checks=self.required_checks,
            now=datetime.now(UTC),
            max_age_seconds=self.max_age_seconds,
        )

    def xǁRuntimeEvidenceContractǁvalidate_documents__mutmut_orig(self, documents: ContractDocuments) -> tuple[ContractFinding, ...]:
        return validate_runtime_evidence(
            documents,
            expected_identity=self.expected_identity,
            required_checks=self.required_checks,
            now=datetime.now(UTC),
            max_age_seconds=self.max_age_seconds,
        )

    def xǁRuntimeEvidenceContractǁvalidate_documents__mutmut_1(self, documents: ContractDocuments) -> tuple[ContractFinding, ...]:
        return validate_runtime_evidence(
            None,
            expected_identity=self.expected_identity,
            required_checks=self.required_checks,
            now=datetime.now(UTC),
            max_age_seconds=self.max_age_seconds,
        )

    def xǁRuntimeEvidenceContractǁvalidate_documents__mutmut_2(self, documents: ContractDocuments) -> tuple[ContractFinding, ...]:
        return validate_runtime_evidence(
            documents,
            expected_identity=None,
            required_checks=self.required_checks,
            now=datetime.now(UTC),
            max_age_seconds=self.max_age_seconds,
        )

    def xǁRuntimeEvidenceContractǁvalidate_documents__mutmut_3(self, documents: ContractDocuments) -> tuple[ContractFinding, ...]:
        return validate_runtime_evidence(
            documents,
            expected_identity=self.expected_identity,
            required_checks=None,
            now=datetime.now(UTC),
            max_age_seconds=self.max_age_seconds,
        )

    def xǁRuntimeEvidenceContractǁvalidate_documents__mutmut_4(self, documents: ContractDocuments) -> tuple[ContractFinding, ...]:
        return validate_runtime_evidence(
            documents,
            expected_identity=self.expected_identity,
            required_checks=self.required_checks,
            now=None,
            max_age_seconds=self.max_age_seconds,
        )

    def xǁRuntimeEvidenceContractǁvalidate_documents__mutmut_5(self, documents: ContractDocuments) -> tuple[ContractFinding, ...]:
        return validate_runtime_evidence(
            documents,
            expected_identity=self.expected_identity,
            required_checks=self.required_checks,
            now=datetime.now(UTC),
            max_age_seconds=None,
        )

    def xǁRuntimeEvidenceContractǁvalidate_documents__mutmut_6(self, documents: ContractDocuments) -> tuple[ContractFinding, ...]:
        return validate_runtime_evidence(
            expected_identity=self.expected_identity,
            required_checks=self.required_checks,
            now=datetime.now(UTC),
            max_age_seconds=self.max_age_seconds,
        )

    def xǁRuntimeEvidenceContractǁvalidate_documents__mutmut_7(self, documents: ContractDocuments) -> tuple[ContractFinding, ...]:
        return validate_runtime_evidence(
            documents,
            required_checks=self.required_checks,
            now=datetime.now(UTC),
            max_age_seconds=self.max_age_seconds,
        )

    def xǁRuntimeEvidenceContractǁvalidate_documents__mutmut_8(self, documents: ContractDocuments) -> tuple[ContractFinding, ...]:
        return validate_runtime_evidence(
            documents,
            expected_identity=self.expected_identity,
            now=datetime.now(UTC),
            max_age_seconds=self.max_age_seconds,
        )

    def xǁRuntimeEvidenceContractǁvalidate_documents__mutmut_9(self, documents: ContractDocuments) -> tuple[ContractFinding, ...]:
        return validate_runtime_evidence(
            documents,
            expected_identity=self.expected_identity,
            required_checks=self.required_checks,
            max_age_seconds=self.max_age_seconds,
        )

    def xǁRuntimeEvidenceContractǁvalidate_documents__mutmut_10(self, documents: ContractDocuments) -> tuple[ContractFinding, ...]:
        return validate_runtime_evidence(
            documents,
            expected_identity=self.expected_identity,
            required_checks=self.required_checks,
            now=datetime.now(UTC),
            )

    def xǁRuntimeEvidenceContractǁvalidate_documents__mutmut_11(self, documents: ContractDocuments) -> tuple[ContractFinding, ...]:
        return validate_runtime_evidence(
            documents,
            expected_identity=self.expected_identity,
            required_checks=self.required_checks,
            now=datetime.now(None),
            max_age_seconds=self.max_age_seconds,
        )

    @_mutmut_mutated(mutants_xǁRuntimeEvidenceContractǁrun__mutmut)
    def run(self) -> int:
        """Emit each validated receipt defect into the structured check ledger."""
        findings = self.collect_findings()
        for finding in findings:
            report_finding(
                finding.code,
                str(finding.source.relative_to(self._repo_root)),
                finding.message,
            )
        if not findings:
            return 0
        render_findings(findings)
        return 1

    def xǁRuntimeEvidenceContractǁrun__mutmut_orig(self) -> int:
        """Emit each validated receipt defect into the structured check ledger."""
        findings = self.collect_findings()
        for finding in findings:
            report_finding(
                finding.code,
                str(finding.source.relative_to(self._repo_root)),
                finding.message,
            )
        if not findings:
            return 0
        render_findings(findings)
        return 1

    def xǁRuntimeEvidenceContractǁrun__mutmut_1(self) -> int:
        """Emit each validated receipt defect into the structured check ledger."""
        findings = None
        for finding in findings:
            report_finding(
                finding.code,
                str(finding.source.relative_to(self._repo_root)),
                finding.message,
            )
        if not findings:
            return 0
        render_findings(findings)
        return 1

    def xǁRuntimeEvidenceContractǁrun__mutmut_2(self) -> int:
        """Emit each validated receipt defect into the structured check ledger."""
        findings = self.collect_findings()
        for finding in findings:
            report_finding(
                None,
                str(finding.source.relative_to(self._repo_root)),
                finding.message,
            )
        if not findings:
            return 0
        render_findings(findings)
        return 1

    def xǁRuntimeEvidenceContractǁrun__mutmut_3(self) -> int:
        """Emit each validated receipt defect into the structured check ledger."""
        findings = self.collect_findings()
        for finding in findings:
            report_finding(
                finding.code,
                None,
                finding.message,
            )
        if not findings:
            return 0
        render_findings(findings)
        return 1

    def xǁRuntimeEvidenceContractǁrun__mutmut_4(self) -> int:
        """Emit each validated receipt defect into the structured check ledger."""
        findings = self.collect_findings()
        for finding in findings:
            report_finding(
                finding.code,
                str(finding.source.relative_to(self._repo_root)),
                None,
            )
        if not findings:
            return 0
        render_findings(findings)
        return 1

    def xǁRuntimeEvidenceContractǁrun__mutmut_5(self) -> int:
        """Emit each validated receipt defect into the structured check ledger."""
        findings = self.collect_findings()
        for finding in findings:
            report_finding(
                str(finding.source.relative_to(self._repo_root)),
                finding.message,
            )
        if not findings:
            return 0
        render_findings(findings)
        return 1

    def xǁRuntimeEvidenceContractǁrun__mutmut_6(self) -> int:
        """Emit each validated receipt defect into the structured check ledger."""
        findings = self.collect_findings()
        for finding in findings:
            report_finding(
                finding.code,
                finding.message,
            )
        if not findings:
            return 0
        render_findings(findings)
        return 1

    def xǁRuntimeEvidenceContractǁrun__mutmut_7(self) -> int:
        """Emit each validated receipt defect into the structured check ledger."""
        findings = self.collect_findings()
        for finding in findings:
            report_finding(
                finding.code,
                str(finding.source.relative_to(self._repo_root)),
                )
        if not findings:
            return 0
        render_findings(findings)
        return 1

    def xǁRuntimeEvidenceContractǁrun__mutmut_8(self) -> int:
        """Emit each validated receipt defect into the structured check ledger."""
        findings = self.collect_findings()
        for finding in findings:
            report_finding(
                finding.code,
                str(None),
                finding.message,
            )
        if not findings:
            return 0
        render_findings(findings)
        return 1

    def xǁRuntimeEvidenceContractǁrun__mutmut_9(self) -> int:
        """Emit each validated receipt defect into the structured check ledger."""
        findings = self.collect_findings()
        for finding in findings:
            report_finding(
                finding.code,
                str(finding.source.relative_to(None)),
                finding.message,
            )
        if not findings:
            return 0
        render_findings(findings)
        return 1

    def xǁRuntimeEvidenceContractǁrun__mutmut_10(self) -> int:
        """Emit each validated receipt defect into the structured check ledger."""
        findings = self.collect_findings()
        for finding in findings:
            report_finding(
                finding.code,
                str(finding.source.relative_to(self._repo_root)),
                finding.message,
            )
        if findings:
            return 0
        render_findings(findings)
        return 1

    def xǁRuntimeEvidenceContractǁrun__mutmut_11(self) -> int:
        """Emit each validated receipt defect into the structured check ledger."""
        findings = self.collect_findings()
        for finding in findings:
            report_finding(
                finding.code,
                str(finding.source.relative_to(self._repo_root)),
                finding.message,
            )
        if not findings:
            return 1
        render_findings(findings)
        return 1

    def xǁRuntimeEvidenceContractǁrun__mutmut_12(self) -> int:
        """Emit each validated receipt defect into the structured check ledger."""
        findings = self.collect_findings()
        for finding in findings:
            report_finding(
                finding.code,
                str(finding.source.relative_to(self._repo_root)),
                finding.message,
            )
        if not findings:
            return 0
        render_findings(None)
        return 1

    def xǁRuntimeEvidenceContractǁrun__mutmut_13(self) -> int:
        """Emit each validated receipt defect into the structured check ledger."""
        findings = self.collect_findings()
        for finding in findings:
            report_finding(
                finding.code,
                str(finding.source.relative_to(self._repo_root)),
                finding.message,
            )
        if not findings:
            return 0
        render_findings(findings)
        return 2

mutants_xǁRuntimeEvidenceContractǁ__init____mutmut['_mutmut_orig'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁ__init____mutmut_orig # type: ignore # mutmut generated
mutants_xǁRuntimeEvidenceContractǁ__init____mutmut['xǁRuntimeEvidenceContractǁ__init____mutmut_1'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁ__init____mutmut_1 # type: ignore # mutmut generated
mutants_xǁRuntimeEvidenceContractǁ__init____mutmut['xǁRuntimeEvidenceContractǁ__init____mutmut_2'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁ__init____mutmut_2 # type: ignore # mutmut generated
mutants_xǁRuntimeEvidenceContractǁ__init____mutmut['xǁRuntimeEvidenceContractǁ__init____mutmut_3'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁ__init____mutmut_3 # type: ignore # mutmut generated
mutants_xǁRuntimeEvidenceContractǁ__init____mutmut['xǁRuntimeEvidenceContractǁ__init____mutmut_4'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁ__init____mutmut_4 # type: ignore # mutmut generated
mutants_xǁRuntimeEvidenceContractǁ__init____mutmut['xǁRuntimeEvidenceContractǁ__init____mutmut_5'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁ__init____mutmut_5 # type: ignore # mutmut generated
mutants_xǁRuntimeEvidenceContractǁ__init____mutmut['xǁRuntimeEvidenceContractǁ__init____mutmut_6'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁ__init____mutmut_6 # type: ignore # mutmut generated
mutants_xǁRuntimeEvidenceContractǁ__init____mutmut['xǁRuntimeEvidenceContractǁ__init____mutmut_7'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁ__init____mutmut_7 # type: ignore # mutmut generated
mutants_xǁRuntimeEvidenceContractǁ__init____mutmut['xǁRuntimeEvidenceContractǁ__init____mutmut_8'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁ__init____mutmut_8 # type: ignore # mutmut generated
mutants_xǁRuntimeEvidenceContractǁ__init____mutmut['xǁRuntimeEvidenceContractǁ__init____mutmut_9'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁ__init____mutmut_9 # type: ignore # mutmut generated
mutants_xǁRuntimeEvidenceContractǁ__init____mutmut['xǁRuntimeEvidenceContractǁ__init____mutmut_10'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁ__init____mutmut_10 # type: ignore # mutmut generated
mutants_xǁRuntimeEvidenceContractǁ__init____mutmut['xǁRuntimeEvidenceContractǁ__init____mutmut_11'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁ__init____mutmut_11 # type: ignore # mutmut generated
mutants_xǁRuntimeEvidenceContractǁ__init____mutmut['xǁRuntimeEvidenceContractǁ__init____mutmut_12'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁ__init____mutmut_12 # type: ignore # mutmut generated
mutants_xǁRuntimeEvidenceContractǁ__init____mutmut['xǁRuntimeEvidenceContractǁ__init____mutmut_13'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁ__init____mutmut_13 # type: ignore # mutmut generated
mutants_xǁRuntimeEvidenceContractǁ__init____mutmut['xǁRuntimeEvidenceContractǁ__init____mutmut_14'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁ__init____mutmut_14 # type: ignore # mutmut generated
mutants_xǁRuntimeEvidenceContractǁ__init____mutmut['xǁRuntimeEvidenceContractǁ__init____mutmut_15'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁ__init____mutmut_15 # type: ignore # mutmut generated
mutants_xǁRuntimeEvidenceContractǁ__init____mutmut['xǁRuntimeEvidenceContractǁ__init____mutmut_16'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁ__init____mutmut_16 # type: ignore # mutmut generated
mutants_xǁRuntimeEvidenceContractǁ__init____mutmut['xǁRuntimeEvidenceContractǁ__init____mutmut_17'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁ__init____mutmut_17 # type: ignore # mutmut generated
mutants_xǁRuntimeEvidenceContractǁ__init____mutmut['xǁRuntimeEvidenceContractǁ__init____mutmut_18'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁ__init____mutmut_18 # type: ignore # mutmut generated
mutants_xǁRuntimeEvidenceContractǁ__init____mutmut['xǁRuntimeEvidenceContractǁ__init____mutmut_19'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁ__init____mutmut_19 # type: ignore # mutmut generated
mutants_xǁRuntimeEvidenceContractǁ__init____mutmut['xǁRuntimeEvidenceContractǁ__init____mutmut_20'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁ__init____mutmut_20 # type: ignore # mutmut generated
mutants_xǁRuntimeEvidenceContractǁ__init____mutmut['xǁRuntimeEvidenceContractǁ__init____mutmut_21'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁ__init____mutmut_21 # type: ignore # mutmut generated
mutants_xǁRuntimeEvidenceContractǁ__init____mutmut['xǁRuntimeEvidenceContractǁ__init____mutmut_22'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁ__init____mutmut_22 # type: ignore # mutmut generated
mutants_xǁRuntimeEvidenceContractǁ__init____mutmut['xǁRuntimeEvidenceContractǁ__init____mutmut_23'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁ__init____mutmut_23 # type: ignore # mutmut generated
mutants_xǁRuntimeEvidenceContractǁ__init____mutmut['xǁRuntimeEvidenceContractǁ__init____mutmut_24'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁ__init____mutmut_24 # type: ignore # mutmut generated
mutants_xǁRuntimeEvidenceContractǁ__init____mutmut['xǁRuntimeEvidenceContractǁ__init____mutmut_25'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁ__init____mutmut_25 # type: ignore # mutmut generated
mutants_xǁRuntimeEvidenceContractǁ__init____mutmut['xǁRuntimeEvidenceContractǁ__init____mutmut_26'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁ__init____mutmut_26 # type: ignore # mutmut generated
mutants_xǁRuntimeEvidenceContractǁ__init____mutmut['xǁRuntimeEvidenceContractǁ__init____mutmut_27'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁ__init____mutmut_27 # type: ignore # mutmut generated
mutants_xǁRuntimeEvidenceContractǁ__init____mutmut['xǁRuntimeEvidenceContractǁ__init____mutmut_28'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁ__init____mutmut_28 # type: ignore # mutmut generated
mutants_xǁRuntimeEvidenceContractǁ__init____mutmut['xǁRuntimeEvidenceContractǁ__init____mutmut_29'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁ__init____mutmut_29 # type: ignore # mutmut generated
mutants_xǁRuntimeEvidenceContractǁ__init____mutmut['xǁRuntimeEvidenceContractǁ__init____mutmut_30'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁ__init____mutmut_30 # type: ignore # mutmut generated
mutants_xǁRuntimeEvidenceContractǁ__init____mutmut['xǁRuntimeEvidenceContractǁ__init____mutmut_31'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁ__init____mutmut_31 # type: ignore # mutmut generated
mutants_xǁRuntimeEvidenceContractǁ__init____mutmut['xǁRuntimeEvidenceContractǁ__init____mutmut_32'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁ__init____mutmut_32 # type: ignore # mutmut generated
mutants_xǁRuntimeEvidenceContractǁ__init____mutmut['xǁRuntimeEvidenceContractǁ__init____mutmut_33'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁ__init____mutmut_33 # type: ignore # mutmut generated
mutants_xǁRuntimeEvidenceContractǁ__init____mutmut['xǁRuntimeEvidenceContractǁ__init____mutmut_34'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁ__init____mutmut_34 # type: ignore # mutmut generated
mutants_xǁRuntimeEvidenceContractǁ__init____mutmut['xǁRuntimeEvidenceContractǁ__init____mutmut_35'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁ__init____mutmut_35 # type: ignore # mutmut generated
mutants_xǁRuntimeEvidenceContractǁ__init____mutmut['xǁRuntimeEvidenceContractǁ__init____mutmut_36'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁ__init____mutmut_36 # type: ignore # mutmut generated
mutants_xǁRuntimeEvidenceContractǁ__init____mutmut['xǁRuntimeEvidenceContractǁ__init____mutmut_37'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁ__init____mutmut_37 # type: ignore # mutmut generated
mutants_xǁRuntimeEvidenceContractǁ__init____mutmut['xǁRuntimeEvidenceContractǁ__init____mutmut_38'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁ__init____mutmut_38 # type: ignore # mutmut generated
mutants_xǁRuntimeEvidenceContractǁ__init____mutmut['xǁRuntimeEvidenceContractǁ__init____mutmut_39'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁ__init____mutmut_39 # type: ignore # mutmut generated
mutants_xǁRuntimeEvidenceContractǁ__init____mutmut['xǁRuntimeEvidenceContractǁ__init____mutmut_40'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁ__init____mutmut_40 # type: ignore # mutmut generated
mutants_xǁRuntimeEvidenceContractǁ__init____mutmut['xǁRuntimeEvidenceContractǁ__init____mutmut_41'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁ__init____mutmut_41 # type: ignore # mutmut generated
mutants_xǁRuntimeEvidenceContractǁ__init____mutmut['xǁRuntimeEvidenceContractǁ__init____mutmut_42'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁ__init____mutmut_42 # type: ignore # mutmut generated
mutants_xǁRuntimeEvidenceContractǁ__init____mutmut['xǁRuntimeEvidenceContractǁ__init____mutmut_43'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁ__init____mutmut_43 # type: ignore # mutmut generated

mutants_xǁRuntimeEvidenceContractǁvalidate_documents__mutmut['_mutmut_orig'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁvalidate_documents__mutmut_orig # type: ignore # mutmut generated
mutants_xǁRuntimeEvidenceContractǁvalidate_documents__mutmut['xǁRuntimeEvidenceContractǁvalidate_documents__mutmut_1'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁvalidate_documents__mutmut_1 # type: ignore # mutmut generated
mutants_xǁRuntimeEvidenceContractǁvalidate_documents__mutmut['xǁRuntimeEvidenceContractǁvalidate_documents__mutmut_2'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁvalidate_documents__mutmut_2 # type: ignore # mutmut generated
mutants_xǁRuntimeEvidenceContractǁvalidate_documents__mutmut['xǁRuntimeEvidenceContractǁvalidate_documents__mutmut_3'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁvalidate_documents__mutmut_3 # type: ignore # mutmut generated
mutants_xǁRuntimeEvidenceContractǁvalidate_documents__mutmut['xǁRuntimeEvidenceContractǁvalidate_documents__mutmut_4'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁvalidate_documents__mutmut_4 # type: ignore # mutmut generated
mutants_xǁRuntimeEvidenceContractǁvalidate_documents__mutmut['xǁRuntimeEvidenceContractǁvalidate_documents__mutmut_5'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁvalidate_documents__mutmut_5 # type: ignore # mutmut generated
mutants_xǁRuntimeEvidenceContractǁvalidate_documents__mutmut['xǁRuntimeEvidenceContractǁvalidate_documents__mutmut_6'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁvalidate_documents__mutmut_6 # type: ignore # mutmut generated
mutants_xǁRuntimeEvidenceContractǁvalidate_documents__mutmut['xǁRuntimeEvidenceContractǁvalidate_documents__mutmut_7'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁvalidate_documents__mutmut_7 # type: ignore # mutmut generated
mutants_xǁRuntimeEvidenceContractǁvalidate_documents__mutmut['xǁRuntimeEvidenceContractǁvalidate_documents__mutmut_8'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁvalidate_documents__mutmut_8 # type: ignore # mutmut generated
mutants_xǁRuntimeEvidenceContractǁvalidate_documents__mutmut['xǁRuntimeEvidenceContractǁvalidate_documents__mutmut_9'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁvalidate_documents__mutmut_9 # type: ignore # mutmut generated
mutants_xǁRuntimeEvidenceContractǁvalidate_documents__mutmut['xǁRuntimeEvidenceContractǁvalidate_documents__mutmut_10'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁvalidate_documents__mutmut_10 # type: ignore # mutmut generated
mutants_xǁRuntimeEvidenceContractǁvalidate_documents__mutmut['xǁRuntimeEvidenceContractǁvalidate_documents__mutmut_11'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁvalidate_documents__mutmut_11 # type: ignore # mutmut generated

mutants_xǁRuntimeEvidenceContractǁrun__mutmut['_mutmut_orig'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁrun__mutmut_orig # type: ignore # mutmut generated
mutants_xǁRuntimeEvidenceContractǁrun__mutmut['xǁRuntimeEvidenceContractǁrun__mutmut_1'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁrun__mutmut_1 # type: ignore # mutmut generated
mutants_xǁRuntimeEvidenceContractǁrun__mutmut['xǁRuntimeEvidenceContractǁrun__mutmut_2'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁrun__mutmut_2 # type: ignore # mutmut generated
mutants_xǁRuntimeEvidenceContractǁrun__mutmut['xǁRuntimeEvidenceContractǁrun__mutmut_3'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁrun__mutmut_3 # type: ignore # mutmut generated
mutants_xǁRuntimeEvidenceContractǁrun__mutmut['xǁRuntimeEvidenceContractǁrun__mutmut_4'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁrun__mutmut_4 # type: ignore # mutmut generated
mutants_xǁRuntimeEvidenceContractǁrun__mutmut['xǁRuntimeEvidenceContractǁrun__mutmut_5'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁrun__mutmut_5 # type: ignore # mutmut generated
mutants_xǁRuntimeEvidenceContractǁrun__mutmut['xǁRuntimeEvidenceContractǁrun__mutmut_6'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁrun__mutmut_6 # type: ignore # mutmut generated
mutants_xǁRuntimeEvidenceContractǁrun__mutmut['xǁRuntimeEvidenceContractǁrun__mutmut_7'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁrun__mutmut_7 # type: ignore # mutmut generated
mutants_xǁRuntimeEvidenceContractǁrun__mutmut['xǁRuntimeEvidenceContractǁrun__mutmut_8'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁrun__mutmut_8 # type: ignore # mutmut generated
mutants_xǁRuntimeEvidenceContractǁrun__mutmut['xǁRuntimeEvidenceContractǁrun__mutmut_9'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁrun__mutmut_9 # type: ignore # mutmut generated
mutants_xǁRuntimeEvidenceContractǁrun__mutmut['xǁRuntimeEvidenceContractǁrun__mutmut_10'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁrun__mutmut_10 # type: ignore # mutmut generated
mutants_xǁRuntimeEvidenceContractǁrun__mutmut['xǁRuntimeEvidenceContractǁrun__mutmut_11'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁrun__mutmut_11 # type: ignore # mutmut generated
mutants_xǁRuntimeEvidenceContractǁrun__mutmut['xǁRuntimeEvidenceContractǁrun__mutmut_12'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁrun__mutmut_12 # type: ignore # mutmut generated
mutants_xǁRuntimeEvidenceContractǁrun__mutmut['xǁRuntimeEvidenceContractǁrun__mutmut_13'] = RuntimeEvidenceContract.xǁRuntimeEvidenceContractǁrun__mutmut_13 # type: ignore # mutmut generated
mutants_x_build__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_build__mutmut)
def build(
    config: Mapping[str, Any],
    *,
    repo_root: Path | None = None,
) -> RuntimeEvidenceContract:
    """Bind this CORE check to one consumer configuration block."""
    return RuntimeEvidenceContract(cast(Mapping[str, object], config), repo_root=repo_root)


def x_build__mutmut_orig(
    config: Mapping[str, Any],
    *,
    repo_root: Path | None = None,
) -> RuntimeEvidenceContract:
    """Bind this CORE check to one consumer configuration block."""
    return RuntimeEvidenceContract(cast(Mapping[str, object], config), repo_root=repo_root)


def x_build__mutmut_1(
    config: Mapping[str, Any],
    *,
    repo_root: Path | None = None,
) -> RuntimeEvidenceContract:
    """Bind this CORE check to one consumer configuration block."""
    return RuntimeEvidenceContract(None, repo_root=repo_root)


def x_build__mutmut_2(
    config: Mapping[str, Any],
    *,
    repo_root: Path | None = None,
) -> RuntimeEvidenceContract:
    """Bind this CORE check to one consumer configuration block."""
    return RuntimeEvidenceContract(cast(Mapping[str, object], config), repo_root=None)


def x_build__mutmut_3(
    config: Mapping[str, Any],
    *,
    repo_root: Path | None = None,
) -> RuntimeEvidenceContract:
    """Bind this CORE check to one consumer configuration block."""
    return RuntimeEvidenceContract(repo_root=repo_root)


def x_build__mutmut_4(
    config: Mapping[str, Any],
    *,
    repo_root: Path | None = None,
) -> RuntimeEvidenceContract:
    """Bind this CORE check to one consumer configuration block."""
    return RuntimeEvidenceContract(cast(Mapping[str, object], config), )


def x_build__mutmut_5(
    config: Mapping[str, Any],
    *,
    repo_root: Path | None = None,
) -> RuntimeEvidenceContract:
    """Bind this CORE check to one consumer configuration block."""
    return RuntimeEvidenceContract(cast(None, config), repo_root=repo_root)


def x_build__mutmut_6(
    config: Mapping[str, Any],
    *,
    repo_root: Path | None = None,
) -> RuntimeEvidenceContract:
    """Bind this CORE check to one consumer configuration block."""
    return RuntimeEvidenceContract(cast(Mapping[str, object], None), repo_root=repo_root)


def x_build__mutmut_7(
    config: Mapping[str, Any],
    *,
    repo_root: Path | None = None,
) -> RuntimeEvidenceContract:
    """Bind this CORE check to one consumer configuration block."""
    return RuntimeEvidenceContract(cast(config), repo_root=repo_root)


def x_build__mutmut_8(
    config: Mapping[str, Any],
    *,
    repo_root: Path | None = None,
) -> RuntimeEvidenceContract:
    """Bind this CORE check to one consumer configuration block."""
    return RuntimeEvidenceContract(cast(Mapping[str, object], ), repo_root=repo_root)

mutants_x_build__mutmut['_mutmut_orig'] = x_build__mutmut_orig # type: ignore # mutmut generated
mutants_x_build__mutmut['x_build__mutmut_1'] = x_build__mutmut_1 # type: ignore # mutmut generated
mutants_x_build__mutmut['x_build__mutmut_2'] = x_build__mutmut_2 # type: ignore # mutmut generated
mutants_x_build__mutmut['x_build__mutmut_3'] = x_build__mutmut_3 # type: ignore # mutmut generated
mutants_x_build__mutmut['x_build__mutmut_4'] = x_build__mutmut_4 # type: ignore # mutmut generated
mutants_x_build__mutmut['x_build__mutmut_5'] = x_build__mutmut_5 # type: ignore # mutmut generated
mutants_x_build__mutmut['x_build__mutmut_6'] = x_build__mutmut_6 # type: ignore # mutmut generated
mutants_x_build__mutmut['x_build__mutmut_7'] = x_build__mutmut_7 # type: ignore # mutmut generated
mutants_x_build__mutmut['x_build__mutmut_8'] = x_build__mutmut_8 # type: ignore # mutmut generated
mutants_x_main__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_main__mutmut)
def main(argv: list[str] | None = None) -> int:
    """CLI entry compatible with the shared CORE-check runner."""
    return run_core_check(RuntimeEvidenceContract, argv)


def x_main__mutmut_orig(argv: list[str] | None = None) -> int:
    """CLI entry compatible with the shared CORE-check runner."""
    return run_core_check(RuntimeEvidenceContract, argv)


def x_main__mutmut_1(argv: list[str] | None = None) -> int:
    """CLI entry compatible with the shared CORE-check runner."""
    return run_core_check(None, argv)


def x_main__mutmut_2(argv: list[str] | None = None) -> int:
    """CLI entry compatible with the shared CORE-check runner."""
    return run_core_check(RuntimeEvidenceContract, None)


def x_main__mutmut_3(argv: list[str] | None = None) -> int:
    """CLI entry compatible with the shared CORE-check runner."""
    return run_core_check(argv)


def x_main__mutmut_4(argv: list[str] | None = None) -> int:
    """CLI entry compatible with the shared CORE-check runner."""
    return run_core_check(RuntimeEvidenceContract, )

mutants_x_main__mutmut['_mutmut_orig'] = x_main__mutmut_orig # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_1'] = x_main__mutmut_1 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_2'] = x_main__mutmut_2 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_3'] = x_main__mutmut_3 # type: ignore # mutmut generated
mutants_x_main__mutmut['x_main__mutmut_4'] = x_main__mutmut_4 # type: ignore # mutmut generated


if __name__ == "__main__":
    import sys

    sys.exit(main())


__all__ = ["RuntimeEvidenceContract", "build", "main", "validate_runtime_evidence"]
