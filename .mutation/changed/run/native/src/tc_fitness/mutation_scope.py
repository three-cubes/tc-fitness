"""Immutable Git inputs and conservative changed-function dependency closure."""

from __future__ import annotations

import ast
import copy
import hashlib
import io
import re
import tarfile
import tomllib
from dataclasses import dataclass
from pathlib import Path, PurePosixPath
from typing import IO, Any, cast

from tc_fitness.check_contract_execution import payload_digest
from tc_fitness.runner import run_bounded_process


from mutmut.mutation.trampoline import wrap_in_trampoline as _mutmut_mutated, MutantDict
mutants_xǁMutationErrorǁ__init____mutmut: MutantDict = {}  # type: ignore


class MutationError(ValueError):
    """Mutation evidence cannot establish the required candidate claim."""

    @_mutmut_mutated(mutants_xǁMutationErrorǁ__init____mutmut)
    def __init__(self, message: str, code: str = "invalid-evidence") -> None:
        super().__init__(message)
        self.code = code

    def xǁMutationErrorǁ__init____mutmut_orig(self, message: str, code: str = "invalid-evidence") -> None:
        super().__init__(message)
        self.code = code

    def xǁMutationErrorǁ__init____mutmut_1(self, message: str, code: str = "XXinvalid-evidenceXX") -> None:
        super().__init__(message)
        self.code = code

    def xǁMutationErrorǁ__init____mutmut_2(self, message: str, code: str = "INVALID-EVIDENCE") -> None:
        super().__init__(message)
        self.code = code

    def xǁMutationErrorǁ__init____mutmut_3(self, message: str, code: str = "invalid-evidence") -> None:
        super().__init__(None)
        self.code = code

    def xǁMutationErrorǁ__init____mutmut_4(self, message: str, code: str = "invalid-evidence") -> None:
        super().__init__(message)
        self.code = None

mutants_xǁMutationErrorǁ__init____mutmut['_mutmut_orig'] = MutationError.xǁMutationErrorǁ__init____mutmut_orig # type: ignore # mutmut generated
mutants_xǁMutationErrorǁ__init____mutmut['xǁMutationErrorǁ__init____mutmut_1'] = MutationError.xǁMutationErrorǁ__init____mutmut_1 # type: ignore # mutmut generated
mutants_xǁMutationErrorǁ__init____mutmut['xǁMutationErrorǁ__init____mutmut_2'] = MutationError.xǁMutationErrorǁ__init____mutmut_2 # type: ignore # mutmut generated
mutants_xǁMutationErrorǁ__init____mutmut['xǁMutationErrorǁ__init____mutmut_3'] = MutationError.xǁMutationErrorǁ__init____mutmut_3 # type: ignore # mutmut generated
mutants_xǁMutationErrorǁ__init____mutmut['xǁMutationErrorǁ__init____mutmut_4'] = MutationError.xǁMutationErrorǁ__init____mutmut_4 # type: ignore # mutmut generated
mutants_x_digest_bytes__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_digest_bytes__mutmut)
def digest_bytes(value: bytes) -> str:
    return "sha256:" + hashlib.sha256(value).hexdigest()


def x_digest_bytes__mutmut_orig(value: bytes) -> str:
    return "sha256:" + hashlib.sha256(value).hexdigest()


def x_digest_bytes__mutmut_1(value: bytes) -> str:
    return "sha256:" - hashlib.sha256(value).hexdigest()


def x_digest_bytes__mutmut_2(value: bytes) -> str:
    return "XXsha256:XX" + hashlib.sha256(value).hexdigest()


def x_digest_bytes__mutmut_3(value: bytes) -> str:
    return "SHA256:" + hashlib.sha256(value).hexdigest()


def x_digest_bytes__mutmut_4(value: bytes) -> str:
    return "sha256:" + hashlib.sha256(None).hexdigest()

mutants_x_digest_bytes__mutmut['_mutmut_orig'] = x_digest_bytes__mutmut_orig # type: ignore # mutmut generated
mutants_x_digest_bytes__mutmut['x_digest_bytes__mutmut_1'] = x_digest_bytes__mutmut_1 # type: ignore # mutmut generated
mutants_x_digest_bytes__mutmut['x_digest_bytes__mutmut_2'] = x_digest_bytes__mutmut_2 # type: ignore # mutmut generated
mutants_x_digest_bytes__mutmut['x_digest_bytes__mutmut_3'] = x_digest_bytes__mutmut_3 # type: ignore # mutmut generated
mutants_x_digest_bytes__mutmut['x_digest_bytes__mutmut_4'] = x_digest_bytes__mutmut_4 # type: ignore # mutmut generated
mutants_x_git_bytes__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_git_bytes__mutmut)
def git_bytes(root: Path, *args: str) -> bytes:
    result = run_bounded_process(["git", *args], cwd=root)
    if result.returncode:
        raise MutationError(f"Git identity unavailable: {' '.join(args[:2])}")
    return result.stdout


def x_git_bytes__mutmut_orig(root: Path, *args: str) -> bytes:
    result = run_bounded_process(["git", *args], cwd=root)
    if result.returncode:
        raise MutationError(f"Git identity unavailable: {' '.join(args[:2])}")
    return result.stdout


def x_git_bytes__mutmut_1(root: Path, *args: str) -> bytes:
    result = None
    if result.returncode:
        raise MutationError(f"Git identity unavailable: {' '.join(args[:2])}")
    return result.stdout


def x_git_bytes__mutmut_2(root: Path, *args: str) -> bytes:
    result = run_bounded_process(None, cwd=root)
    if result.returncode:
        raise MutationError(f"Git identity unavailable: {' '.join(args[:2])}")
    return result.stdout


def x_git_bytes__mutmut_3(root: Path, *args: str) -> bytes:
    result = run_bounded_process(["git", *args], cwd=None)
    if result.returncode:
        raise MutationError(f"Git identity unavailable: {' '.join(args[:2])}")
    return result.stdout


def x_git_bytes__mutmut_4(root: Path, *args: str) -> bytes:
    result = run_bounded_process(cwd=root)
    if result.returncode:
        raise MutationError(f"Git identity unavailable: {' '.join(args[:2])}")
    return result.stdout


def x_git_bytes__mutmut_5(root: Path, *args: str) -> bytes:
    result = run_bounded_process(["git", *args], )
    if result.returncode:
        raise MutationError(f"Git identity unavailable: {' '.join(args[:2])}")
    return result.stdout


def x_git_bytes__mutmut_6(root: Path, *args: str) -> bytes:
    result = run_bounded_process(["XXgitXX", *args], cwd=root)
    if result.returncode:
        raise MutationError(f"Git identity unavailable: {' '.join(args[:2])}")
    return result.stdout


def x_git_bytes__mutmut_7(root: Path, *args: str) -> bytes:
    result = run_bounded_process(["GIT", *args], cwd=root)
    if result.returncode:
        raise MutationError(f"Git identity unavailable: {' '.join(args[:2])}")
    return result.stdout


def x_git_bytes__mutmut_8(root: Path, *args: str) -> bytes:
    result = run_bounded_process(["git", *args], cwd=root)
    if result.returncode:
        raise MutationError(None)
    return result.stdout


def x_git_bytes__mutmut_9(root: Path, *args: str) -> bytes:
    result = run_bounded_process(["git", *args], cwd=root)
    if result.returncode:
        raise MutationError(f"Git identity unavailable: {' '.join(None)}")
    return result.stdout


def x_git_bytes__mutmut_10(root: Path, *args: str) -> bytes:
    result = run_bounded_process(["git", *args], cwd=root)
    if result.returncode:
        raise MutationError(f"Git identity unavailable: {'XX XX'.join(args[:2])}")
    return result.stdout


def x_git_bytes__mutmut_11(root: Path, *args: str) -> bytes:
    result = run_bounded_process(["git", *args], cwd=root)
    if result.returncode:
        raise MutationError(f"Git identity unavailable: {' '.join(args[:3])}")
    return result.stdout

mutants_x_git_bytes__mutmut['_mutmut_orig'] = x_git_bytes__mutmut_orig # type: ignore # mutmut generated
mutants_x_git_bytes__mutmut['x_git_bytes__mutmut_1'] = x_git_bytes__mutmut_1 # type: ignore # mutmut generated
mutants_x_git_bytes__mutmut['x_git_bytes__mutmut_2'] = x_git_bytes__mutmut_2 # type: ignore # mutmut generated
mutants_x_git_bytes__mutmut['x_git_bytes__mutmut_3'] = x_git_bytes__mutmut_3 # type: ignore # mutmut generated
mutants_x_git_bytes__mutmut['x_git_bytes__mutmut_4'] = x_git_bytes__mutmut_4 # type: ignore # mutmut generated
mutants_x_git_bytes__mutmut['x_git_bytes__mutmut_5'] = x_git_bytes__mutmut_5 # type: ignore # mutmut generated
mutants_x_git_bytes__mutmut['x_git_bytes__mutmut_6'] = x_git_bytes__mutmut_6 # type: ignore # mutmut generated
mutants_x_git_bytes__mutmut['x_git_bytes__mutmut_7'] = x_git_bytes__mutmut_7 # type: ignore # mutmut generated
mutants_x_git_bytes__mutmut['x_git_bytes__mutmut_8'] = x_git_bytes__mutmut_8 # type: ignore # mutmut generated
mutants_x_git_bytes__mutmut['x_git_bytes__mutmut_9'] = x_git_bytes__mutmut_9 # type: ignore # mutmut generated
mutants_x_git_bytes__mutmut['x_git_bytes__mutmut_10'] = x_git_bytes__mutmut_10 # type: ignore # mutmut generated
mutants_x_git_bytes__mutmut['x_git_bytes__mutmut_11'] = x_git_bytes__mutmut_11 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_candidate_archives__mutmut)
def candidate_archives(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_orig(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_1(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(None, commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_2(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", None) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_3(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_4(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", ) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_5(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"XX[0-9a-f]{40}(?:[0-9a-f]{24})?XX", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_6(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9A-F]{40}(?:[0-9A-F]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_7(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is not None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_8(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError(None)
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_9(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("XXbase and head must be full immutable commit SHAsXX")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_10(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit shas")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_11(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("BASE AND HEAD MUST BE FULL IMMUTABLE COMMIT SHAS")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_12(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(None, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_13(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, None, f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_14(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", None).decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_15(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes("rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_16(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_17(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", ).decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_18(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "XXrev-parseXX", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_19(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "REV-PARSE", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_20(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() == commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_21(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError(None)
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_22(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("XXcommit identity mismatchXX")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_23(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("COMMIT IDENTITY MISMATCH")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_24(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(None, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_25(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, None, "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_26(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", None).decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_27(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes("rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_28(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_29(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", ).decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_30(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "XXrev-parseXX", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_31(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "REV-PARSE", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_32(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "XXHEADXX").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_33(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "head").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_34(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() == head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_35(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError(None)
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_36(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("XXhead does not identify the current checkoutXX")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_37(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("HEAD DOES NOT IDENTIFY THE CURRENT CHECKOUT")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_38(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(None, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_39(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, None, "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_40(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", None, base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_41(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", None, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_42(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, None)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_43(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes("merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_44(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_45(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_46(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_47(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, )
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_48(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "XXmerge-baseXX", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_49(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "MERGE-BASE", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_50(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "XX--is-ancestorXX", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_51(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--IS-ANCESTOR", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_52(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(None, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_53(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, None, "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_54(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", None, "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_55(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", None):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_56(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes("status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_57(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_58(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_59(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", ):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_60(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "XXstatusXX", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_61(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "STATUS", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_62(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "XX--porcelainXX", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_63(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--PORCELAIN", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_64(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "XX--untracked-files=noXX"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_65(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--UNTRACKED-FILES=NO"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_66(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError(None)
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_67(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("XXcandidate checkout has tracked modificationsXX")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_68(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("CANDIDATE CHECKOUT HAS TRACKED MODIFICATIONS")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_69(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = None
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_70(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(None, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_71(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, None, base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_72(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", None), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_73(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes("archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_74(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_75(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", ), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_76(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "XXarchiveXX", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_77(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "ARCHIVE", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_78(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(None, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_79(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, None, head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_80(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", None))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_81(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes("archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_82(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_83(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", ))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_84(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "XXarchiveXX", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_85(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "ARCHIVE", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_86(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip(None, archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_87(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), None, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_88(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=None):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_89(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip(archives, strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_90(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), strict=True):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_91(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, ):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_92(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=False):
        _validate_archive(root, commit, archive)
    return archives


def x_candidate_archives__mutmut_93(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(None, commit, archive)
    return archives


def x_candidate_archives__mutmut_94(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, None, archive)
    return archives


def x_candidate_archives__mutmut_95(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, None)
    return archives


def x_candidate_archives__mutmut_96(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(commit, archive)
    return archives


def x_candidate_archives__mutmut_97(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, archive)
    return archives


def x_candidate_archives__mutmut_98(root: Path, base: str, head: str) -> tuple[bytes, bytes]:
    """Resolve immutable identities and reject any different or dirty checkout."""
    for commit in (base, head):
        if re.fullmatch(r"[0-9a-f]{40}(?:[0-9a-f]{24})?", commit) is None:
            raise MutationError("base and head must be full immutable commit SHAs")
        if git_bytes(root, "rev-parse", f"{commit}^{{commit}}").decode().strip() != commit:
            raise MutationError("commit identity mismatch")
    if git_bytes(root, "rev-parse", "HEAD").decode().strip() != head:
        raise MutationError("head does not identify the current checkout")
    git_bytes(root, "merge-base", "--is-ancestor", base, head)
    if git_bytes(root, "status", "--porcelain", "--untracked-files=no"):
        raise MutationError("candidate checkout has tracked modifications")
    archives = (git_bytes(root, "archive", base), git_bytes(root, "archive", head))
    for commit, archive in zip((base, head), archives, strict=True):
        _validate_archive(root, commit, )
    return archives

mutants_x_candidate_archives__mutmut['_mutmut_orig'] = x_candidate_archives__mutmut_orig # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_1'] = x_candidate_archives__mutmut_1 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_2'] = x_candidate_archives__mutmut_2 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_3'] = x_candidate_archives__mutmut_3 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_4'] = x_candidate_archives__mutmut_4 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_5'] = x_candidate_archives__mutmut_5 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_6'] = x_candidate_archives__mutmut_6 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_7'] = x_candidate_archives__mutmut_7 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_8'] = x_candidate_archives__mutmut_8 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_9'] = x_candidate_archives__mutmut_9 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_10'] = x_candidate_archives__mutmut_10 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_11'] = x_candidate_archives__mutmut_11 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_12'] = x_candidate_archives__mutmut_12 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_13'] = x_candidate_archives__mutmut_13 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_14'] = x_candidate_archives__mutmut_14 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_15'] = x_candidate_archives__mutmut_15 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_16'] = x_candidate_archives__mutmut_16 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_17'] = x_candidate_archives__mutmut_17 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_18'] = x_candidate_archives__mutmut_18 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_19'] = x_candidate_archives__mutmut_19 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_20'] = x_candidate_archives__mutmut_20 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_21'] = x_candidate_archives__mutmut_21 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_22'] = x_candidate_archives__mutmut_22 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_23'] = x_candidate_archives__mutmut_23 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_24'] = x_candidate_archives__mutmut_24 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_25'] = x_candidate_archives__mutmut_25 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_26'] = x_candidate_archives__mutmut_26 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_27'] = x_candidate_archives__mutmut_27 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_28'] = x_candidate_archives__mutmut_28 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_29'] = x_candidate_archives__mutmut_29 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_30'] = x_candidate_archives__mutmut_30 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_31'] = x_candidate_archives__mutmut_31 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_32'] = x_candidate_archives__mutmut_32 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_33'] = x_candidate_archives__mutmut_33 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_34'] = x_candidate_archives__mutmut_34 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_35'] = x_candidate_archives__mutmut_35 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_36'] = x_candidate_archives__mutmut_36 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_37'] = x_candidate_archives__mutmut_37 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_38'] = x_candidate_archives__mutmut_38 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_39'] = x_candidate_archives__mutmut_39 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_40'] = x_candidate_archives__mutmut_40 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_41'] = x_candidate_archives__mutmut_41 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_42'] = x_candidate_archives__mutmut_42 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_43'] = x_candidate_archives__mutmut_43 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_44'] = x_candidate_archives__mutmut_44 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_45'] = x_candidate_archives__mutmut_45 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_46'] = x_candidate_archives__mutmut_46 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_47'] = x_candidate_archives__mutmut_47 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_48'] = x_candidate_archives__mutmut_48 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_49'] = x_candidate_archives__mutmut_49 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_50'] = x_candidate_archives__mutmut_50 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_51'] = x_candidate_archives__mutmut_51 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_52'] = x_candidate_archives__mutmut_52 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_53'] = x_candidate_archives__mutmut_53 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_54'] = x_candidate_archives__mutmut_54 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_55'] = x_candidate_archives__mutmut_55 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_56'] = x_candidate_archives__mutmut_56 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_57'] = x_candidate_archives__mutmut_57 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_58'] = x_candidate_archives__mutmut_58 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_59'] = x_candidate_archives__mutmut_59 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_60'] = x_candidate_archives__mutmut_60 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_61'] = x_candidate_archives__mutmut_61 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_62'] = x_candidate_archives__mutmut_62 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_63'] = x_candidate_archives__mutmut_63 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_64'] = x_candidate_archives__mutmut_64 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_65'] = x_candidate_archives__mutmut_65 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_66'] = x_candidate_archives__mutmut_66 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_67'] = x_candidate_archives__mutmut_67 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_68'] = x_candidate_archives__mutmut_68 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_69'] = x_candidate_archives__mutmut_69 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_70'] = x_candidate_archives__mutmut_70 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_71'] = x_candidate_archives__mutmut_71 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_72'] = x_candidate_archives__mutmut_72 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_73'] = x_candidate_archives__mutmut_73 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_74'] = x_candidate_archives__mutmut_74 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_75'] = x_candidate_archives__mutmut_75 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_76'] = x_candidate_archives__mutmut_76 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_77'] = x_candidate_archives__mutmut_77 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_78'] = x_candidate_archives__mutmut_78 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_79'] = x_candidate_archives__mutmut_79 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_80'] = x_candidate_archives__mutmut_80 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_81'] = x_candidate_archives__mutmut_81 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_82'] = x_candidate_archives__mutmut_82 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_83'] = x_candidate_archives__mutmut_83 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_84'] = x_candidate_archives__mutmut_84 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_85'] = x_candidate_archives__mutmut_85 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_86'] = x_candidate_archives__mutmut_86 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_87'] = x_candidate_archives__mutmut_87 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_88'] = x_candidate_archives__mutmut_88 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_89'] = x_candidate_archives__mutmut_89 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_90'] = x_candidate_archives__mutmut_90 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_91'] = x_candidate_archives__mutmut_91 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_92'] = x_candidate_archives__mutmut_92 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_93'] = x_candidate_archives__mutmut_93 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_94'] = x_candidate_archives__mutmut_94 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_95'] = x_candidate_archives__mutmut_95 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_96'] = x_candidate_archives__mutmut_96 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_97'] = x_candidate_archives__mutmut_97 # type: ignore # mutmut generated
mutants_x_candidate_archives__mutmut['x_candidate_archives__mutmut_98'] = x_candidate_archives__mutmut_98 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__validate_archive__mutmut)
def _validate_archive(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, "ls-tree", "-r", "-z", commit).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.split(b"\t", 1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode("ascii") + b"\0" + raw
        algorithm = "sha1" if len(objects[path]) == 40 else "sha256"
        actual = hashlib.new(algorithm, blob, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_orig(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, "ls-tree", "-r", "-z", commit).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.split(b"\t", 1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode("ascii") + b"\0" + raw
        algorithm = "sha1" if len(objects[path]) == 40 else "sha256"
        actual = hashlib.new(algorithm, blob, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_1(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = None
    objects = {}
    for record in git_bytes(root, "ls-tree", "-r", "-z", commit).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.split(b"\t", 1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode("ascii") + b"\0" + raw
        algorithm = "sha1" if len(objects[path]) == 40 else "sha256"
        actual = hashlib.new(algorithm, blob, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_2(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(None)
    objects = {}
    for record in git_bytes(root, "ls-tree", "-r", "-z", commit).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.split(b"\t", 1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode("ascii") + b"\0" + raw
        algorithm = "sha1" if len(objects[path]) == 40 else "sha256"
        actual = hashlib.new(algorithm, blob, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_3(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = None
    for record in git_bytes(root, "ls-tree", "-r", "-z", commit).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.split(b"\t", 1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode("ascii") + b"\0" + raw
        algorithm = "sha1" if len(objects[path]) == 40 else "sha256"
        actual = hashlib.new(algorithm, blob, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_4(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, "ls-tree", "-r", "-z", commit).split(None):
        if not record:
            continue
        metadata, raw_path = record.split(b"\t", 1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode("ascii") + b"\0" + raw
        algorithm = "sha1" if len(objects[path]) == 40 else "sha256"
        actual = hashlib.new(algorithm, blob, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_5(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(None, "ls-tree", "-r", "-z", commit).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.split(b"\t", 1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode("ascii") + b"\0" + raw
        algorithm = "sha1" if len(objects[path]) == 40 else "sha256"
        actual = hashlib.new(algorithm, blob, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_6(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, None, "-r", "-z", commit).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.split(b"\t", 1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode("ascii") + b"\0" + raw
        algorithm = "sha1" if len(objects[path]) == 40 else "sha256"
        actual = hashlib.new(algorithm, blob, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_7(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, "ls-tree", None, "-z", commit).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.split(b"\t", 1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode("ascii") + b"\0" + raw
        algorithm = "sha1" if len(objects[path]) == 40 else "sha256"
        actual = hashlib.new(algorithm, blob, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_8(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, "ls-tree", "-r", None, commit).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.split(b"\t", 1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode("ascii") + b"\0" + raw
        algorithm = "sha1" if len(objects[path]) == 40 else "sha256"
        actual = hashlib.new(algorithm, blob, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_9(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, "ls-tree", "-r", "-z", None).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.split(b"\t", 1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode("ascii") + b"\0" + raw
        algorithm = "sha1" if len(objects[path]) == 40 else "sha256"
        actual = hashlib.new(algorithm, blob, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_10(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes("ls-tree", "-r", "-z", commit).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.split(b"\t", 1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode("ascii") + b"\0" + raw
        algorithm = "sha1" if len(objects[path]) == 40 else "sha256"
        actual = hashlib.new(algorithm, blob, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_11(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, "-r", "-z", commit).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.split(b"\t", 1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode("ascii") + b"\0" + raw
        algorithm = "sha1" if len(objects[path]) == 40 else "sha256"
        actual = hashlib.new(algorithm, blob, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_12(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, "ls-tree", "-z", commit).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.split(b"\t", 1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode("ascii") + b"\0" + raw
        algorithm = "sha1" if len(objects[path]) == 40 else "sha256"
        actual = hashlib.new(algorithm, blob, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_13(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, "ls-tree", "-r", commit).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.split(b"\t", 1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode("ascii") + b"\0" + raw
        algorithm = "sha1" if len(objects[path]) == 40 else "sha256"
        actual = hashlib.new(algorithm, blob, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_14(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, "ls-tree", "-r", "-z", ).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.split(b"\t", 1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode("ascii") + b"\0" + raw
        algorithm = "sha1" if len(objects[path]) == 40 else "sha256"
        actual = hashlib.new(algorithm, blob, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_15(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, "XXls-treeXX", "-r", "-z", commit).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.split(b"\t", 1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode("ascii") + b"\0" + raw
        algorithm = "sha1" if len(objects[path]) == 40 else "sha256"
        actual = hashlib.new(algorithm, blob, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_16(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, "LS-TREE", "-r", "-z", commit).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.split(b"\t", 1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode("ascii") + b"\0" + raw
        algorithm = "sha1" if len(objects[path]) == 40 else "sha256"
        actual = hashlib.new(algorithm, blob, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_17(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, "ls-tree", "XX-rXX", "-z", commit).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.split(b"\t", 1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode("ascii") + b"\0" + raw
        algorithm = "sha1" if len(objects[path]) == 40 else "sha256"
        actual = hashlib.new(algorithm, blob, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_18(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, "ls-tree", "-R", "-z", commit).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.split(b"\t", 1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode("ascii") + b"\0" + raw
        algorithm = "sha1" if len(objects[path]) == 40 else "sha256"
        actual = hashlib.new(algorithm, blob, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_19(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, "ls-tree", "-r", "XX-zXX", commit).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.split(b"\t", 1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode("ascii") + b"\0" + raw
        algorithm = "sha1" if len(objects[path]) == 40 else "sha256"
        actual = hashlib.new(algorithm, blob, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_20(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, "ls-tree", "-r", "-Z", commit).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.split(b"\t", 1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode("ascii") + b"\0" + raw
        algorithm = "sha1" if len(objects[path]) == 40 else "sha256"
        actual = hashlib.new(algorithm, blob, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_21(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, "ls-tree", "-r", "-z", commit).split(b"XX\0XX"):
        if not record:
            continue
        metadata, raw_path = record.split(b"\t", 1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode("ascii") + b"\0" + raw
        algorithm = "sha1" if len(objects[path]) == 40 else "sha256"
        actual = hashlib.new(algorithm, blob, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_22(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, "ls-tree", "-r", "-z", commit).split(b"\0"):
        if record:
            continue
        metadata, raw_path = record.split(b"\t", 1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode("ascii") + b"\0" + raw
        algorithm = "sha1" if len(objects[path]) == 40 else "sha256"
        actual = hashlib.new(algorithm, blob, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_23(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, "ls-tree", "-r", "-z", commit).split(b"\0"):
        if not record:
            break
        metadata, raw_path = record.split(b"\t", 1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode("ascii") + b"\0" + raw
        algorithm = "sha1" if len(objects[path]) == 40 else "sha256"
        actual = hashlib.new(algorithm, blob, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_24(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, "ls-tree", "-r", "-z", commit).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = None
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode("ascii") + b"\0" + raw
        algorithm = "sha1" if len(objects[path]) == 40 else "sha256"
        actual = hashlib.new(algorithm, blob, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_25(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, "ls-tree", "-r", "-z", commit).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.split(None, 1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode("ascii") + b"\0" + raw
        algorithm = "sha1" if len(objects[path]) == 40 else "sha256"
        actual = hashlib.new(algorithm, blob, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_26(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, "ls-tree", "-r", "-z", commit).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.split(b"\t", None)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode("ascii") + b"\0" + raw
        algorithm = "sha1" if len(objects[path]) == 40 else "sha256"
        actual = hashlib.new(algorithm, blob, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_27(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, "ls-tree", "-r", "-z", commit).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.split(1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode("ascii") + b"\0" + raw
        algorithm = "sha1" if len(objects[path]) == 40 else "sha256"
        actual = hashlib.new(algorithm, blob, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_28(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, "ls-tree", "-r", "-z", commit).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.split(b"\t", )
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode("ascii") + b"\0" + raw
        algorithm = "sha1" if len(objects[path]) == 40 else "sha256"
        actual = hashlib.new(algorithm, blob, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_29(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, "ls-tree", "-r", "-z", commit).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.rsplit(b"\t", 1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode("ascii") + b"\0" + raw
        algorithm = "sha1" if len(objects[path]) == 40 else "sha256"
        actual = hashlib.new(algorithm, blob, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_30(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, "ls-tree", "-r", "-z", commit).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.split(b"XX\tXX", 1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode("ascii") + b"\0" + raw
        algorithm = "sha1" if len(objects[path]) == 40 else "sha256"
        actual = hashlib.new(algorithm, blob, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_31(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, "ls-tree", "-r", "-z", commit).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.split(b"\t", 2)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode("ascii") + b"\0" + raw
        algorithm = "sha1" if len(objects[path]) == 40 else "sha256"
        actual = hashlib.new(algorithm, blob, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_32(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, "ls-tree", "-r", "-z", commit).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.split(b"\t", 1)
        _mode, _kind, object_id = None
        objects[raw_path.decode("utf-8")] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode("ascii") + b"\0" + raw
        algorithm = "sha1" if len(objects[path]) == 40 else "sha256"
        actual = hashlib.new(algorithm, blob, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_33(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, "ls-tree", "-r", "-z", commit).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.split(b"\t", 1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = None
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode("ascii") + b"\0" + raw
        algorithm = "sha1" if len(objects[path]) == 40 else "sha256"
        actual = hashlib.new(algorithm, blob, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_34(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, "ls-tree", "-r", "-z", commit).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.split(b"\t", 1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode(None)] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode("ascii") + b"\0" + raw
        algorithm = "sha1" if len(objects[path]) == 40 else "sha256"
        actual = hashlib.new(algorithm, blob, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_35(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, "ls-tree", "-r", "-z", commit).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.split(b"\t", 1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("XXutf-8XX")] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode("ascii") + b"\0" + raw
        algorithm = "sha1" if len(objects[path]) == 40 else "sha256"
        actual = hashlib.new(algorithm, blob, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_36(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, "ls-tree", "-r", "-z", commit).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.split(b"\t", 1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("UTF-8")] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode("ascii") + b"\0" + raw
        algorithm = "sha1" if len(objects[path]) == 40 else "sha256"
        actual = hashlib.new(algorithm, blob, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_37(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, "ls-tree", "-r", "-z", commit).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.split(b"\t", 1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode(None)
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode("ascii") + b"\0" + raw
        algorithm = "sha1" if len(objects[path]) == 40 else "sha256"
        actual = hashlib.new(algorithm, blob, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_38(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, "ls-tree", "-r", "-z", commit).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.split(b"\t", 1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode("XXasciiXX")
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode("ascii") + b"\0" + raw
        algorithm = "sha1" if len(objects[path]) == 40 else "sha256"
        actual = hashlib.new(algorithm, blob, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_39(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, "ls-tree", "-r", "-z", commit).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.split(b"\t", 1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode("ASCII")
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode("ascii") + b"\0" + raw
        algorithm = "sha1" if len(objects[path]) == 40 else "sha256"
        actual = hashlib.new(algorithm, blob, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_40(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, "ls-tree", "-r", "-z", commit).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.split(b"\t", 1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode("ascii")
    if files.keys() == objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode("ascii") + b"\0" + raw
        algorithm = "sha1" if len(objects[path]) == 40 else "sha256"
        actual = hashlib.new(algorithm, blob, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_41(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, "ls-tree", "-r", "-z", commit).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.split(b"\t", 1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError(None)
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode("ascii") + b"\0" + raw
        algorithm = "sha1" if len(objects[path]) == 40 else "sha256"
        actual = hashlib.new(algorithm, blob, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_42(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, "ls-tree", "-r", "-z", commit).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.split(b"\t", 1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError("XXcandidate archive omits tracked inputs; export-ignore is forbiddenXX")
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode("ascii") + b"\0" + raw
        algorithm = "sha1" if len(objects[path]) == 40 else "sha256"
        actual = hashlib.new(algorithm, blob, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_43(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, "ls-tree", "-r", "-z", commit).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.split(b"\t", 1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError("CANDIDATE ARCHIVE OMITS TRACKED INPUTS; EXPORT-IGNORE IS FORBIDDEN")
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode("ascii") + b"\0" + raw
        algorithm = "sha1" if len(objects[path]) == 40 else "sha256"
        actual = hashlib.new(algorithm, blob, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_44(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, "ls-tree", "-r", "-z", commit).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.split(b"\t", 1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = None
        algorithm = "sha1" if len(objects[path]) == 40 else "sha256"
        actual = hashlib.new(algorithm, blob, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_45(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, "ls-tree", "-r", "-z", commit).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.split(b"\t", 1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode("ascii") + b"\0" - raw
        algorithm = "sha1" if len(objects[path]) == 40 else "sha256"
        actual = hashlib.new(algorithm, blob, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_46(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, "ls-tree", "-r", "-z", commit).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.split(b"\t", 1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode("ascii") - b"\0" + raw
        algorithm = "sha1" if len(objects[path]) == 40 else "sha256"
        actual = hashlib.new(algorithm, blob, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_47(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, "ls-tree", "-r", "-z", commit).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.split(b"\t", 1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"blob " - str(len(raw)).encode("ascii") + b"\0" + raw
        algorithm = "sha1" if len(objects[path]) == 40 else "sha256"
        actual = hashlib.new(algorithm, blob, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_48(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, "ls-tree", "-r", "-z", commit).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.split(b"\t", 1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"XXblob XX" + str(len(raw)).encode("ascii") + b"\0" + raw
        algorithm = "sha1" if len(objects[path]) == 40 else "sha256"
        actual = hashlib.new(algorithm, blob, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_49(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, "ls-tree", "-r", "-z", commit).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.split(b"\t", 1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"BLOB " + str(len(raw)).encode("ascii") + b"\0" + raw
        algorithm = "sha1" if len(objects[path]) == 40 else "sha256"
        actual = hashlib.new(algorithm, blob, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_50(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, "ls-tree", "-r", "-z", commit).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.split(b"\t", 1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode(None) + b"\0" + raw
        algorithm = "sha1" if len(objects[path]) == 40 else "sha256"
        actual = hashlib.new(algorithm, blob, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_51(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, "ls-tree", "-r", "-z", commit).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.split(b"\t", 1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"blob " + str(None).encode("ascii") + b"\0" + raw
        algorithm = "sha1" if len(objects[path]) == 40 else "sha256"
        actual = hashlib.new(algorithm, blob, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_52(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, "ls-tree", "-r", "-z", commit).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.split(b"\t", 1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode("XXasciiXX") + b"\0" + raw
        algorithm = "sha1" if len(objects[path]) == 40 else "sha256"
        actual = hashlib.new(algorithm, blob, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_53(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, "ls-tree", "-r", "-z", commit).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.split(b"\t", 1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode("ASCII") + b"\0" + raw
        algorithm = "sha1" if len(objects[path]) == 40 else "sha256"
        actual = hashlib.new(algorithm, blob, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_54(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, "ls-tree", "-r", "-z", commit).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.split(b"\t", 1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode("ascii") + b"XX\0XX" + raw
        algorithm = "sha1" if len(objects[path]) == 40 else "sha256"
        actual = hashlib.new(algorithm, blob, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_55(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, "ls-tree", "-r", "-z", commit).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.split(b"\t", 1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode("ascii") + b"\0" + raw
        algorithm = None
        actual = hashlib.new(algorithm, blob, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_56(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, "ls-tree", "-r", "-z", commit).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.split(b"\t", 1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode("ascii") + b"\0" + raw
        algorithm = "XXsha1XX" if len(objects[path]) == 40 else "sha256"
        actual = hashlib.new(algorithm, blob, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_57(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, "ls-tree", "-r", "-z", commit).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.split(b"\t", 1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode("ascii") + b"\0" + raw
        algorithm = "SHA1" if len(objects[path]) == 40 else "sha256"
        actual = hashlib.new(algorithm, blob, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_58(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, "ls-tree", "-r", "-z", commit).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.split(b"\t", 1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode("ascii") + b"\0" + raw
        algorithm = "sha1" if len(objects[path]) != 40 else "sha256"
        actual = hashlib.new(algorithm, blob, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_59(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, "ls-tree", "-r", "-z", commit).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.split(b"\t", 1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode("ascii") + b"\0" + raw
        algorithm = "sha1" if len(objects[path]) == 41 else "sha256"
        actual = hashlib.new(algorithm, blob, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_60(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, "ls-tree", "-r", "-z", commit).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.split(b"\t", 1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode("ascii") + b"\0" + raw
        algorithm = "sha1" if len(objects[path]) == 40 else "XXsha256XX"
        actual = hashlib.new(algorithm, blob, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_61(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, "ls-tree", "-r", "-z", commit).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.split(b"\t", 1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode("ascii") + b"\0" + raw
        algorithm = "sha1" if len(objects[path]) == 40 else "SHA256"
        actual = hashlib.new(algorithm, blob, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_62(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, "ls-tree", "-r", "-z", commit).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.split(b"\t", 1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode("ascii") + b"\0" + raw
        algorithm = "sha1" if len(objects[path]) == 40 else "sha256"
        actual = None
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_63(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, "ls-tree", "-r", "-z", commit).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.split(b"\t", 1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode("ascii") + b"\0" + raw
        algorithm = "sha1" if len(objects[path]) == 40 else "sha256"
        actual = hashlib.new(None, blob, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_64(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, "ls-tree", "-r", "-z", commit).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.split(b"\t", 1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode("ascii") + b"\0" + raw
        algorithm = "sha1" if len(objects[path]) == 40 else "sha256"
        actual = hashlib.new(algorithm, None, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_65(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, "ls-tree", "-r", "-z", commit).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.split(b"\t", 1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode("ascii") + b"\0" + raw
        algorithm = "sha1" if len(objects[path]) == 40 else "sha256"
        actual = hashlib.new(algorithm, blob, usedforsecurity=None).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_66(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, "ls-tree", "-r", "-z", commit).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.split(b"\t", 1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode("ascii") + b"\0" + raw
        algorithm = "sha1" if len(objects[path]) == 40 else "sha256"
        actual = hashlib.new(blob, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_67(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, "ls-tree", "-r", "-z", commit).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.split(b"\t", 1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode("ascii") + b"\0" + raw
        algorithm = "sha1" if len(objects[path]) == 40 else "sha256"
        actual = hashlib.new(algorithm, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_68(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, "ls-tree", "-r", "-z", commit).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.split(b"\t", 1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode("ascii") + b"\0" + raw
        algorithm = "sha1" if len(objects[path]) == 40 else "sha256"
        actual = hashlib.new(algorithm, blob, ).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_69(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, "ls-tree", "-r", "-z", commit).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.split(b"\t", 1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode("ascii") + b"\0" + raw
        algorithm = "sha1" if len(objects[path]) == 40 else "sha256"
        actual = hashlib.new(algorithm, blob, usedforsecurity=True).hexdigest()
        if actual != objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_70(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, "ls-tree", "-r", "-z", commit).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.split(b"\t", 1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode("ascii") + b"\0" + raw
        algorithm = "sha1" if len(objects[path]) == 40 else "sha256"
        actual = hashlib.new(algorithm, blob, usedforsecurity=False).hexdigest()
        if actual == objects[path]:
            raise MutationError("candidate archive rewrites tracked inputs; export-subst is forbidden")


def x__validate_archive__mutmut_71(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, "ls-tree", "-r", "-z", commit).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.split(b"\t", 1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode("ascii") + b"\0" + raw
        algorithm = "sha1" if len(objects[path]) == 40 else "sha256"
        actual = hashlib.new(algorithm, blob, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError(None)


def x__validate_archive__mutmut_72(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, "ls-tree", "-r", "-z", commit).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.split(b"\t", 1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode("ascii") + b"\0" + raw
        algorithm = "sha1" if len(objects[path]) == 40 else "sha256"
        actual = hashlib.new(algorithm, blob, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError("XXcandidate archive rewrites tracked inputs; export-subst is forbiddenXX")


def x__validate_archive__mutmut_73(root: Path, commit: str, archive: bytes) -> None:
    """Git export attributes must not omit or rewrite tracked candidate bytes."""
    files = archive_files(archive)
    objects = {}
    for record in git_bytes(root, "ls-tree", "-r", "-z", commit).split(b"\0"):
        if not record:
            continue
        metadata, raw_path = record.split(b"\t", 1)
        _mode, _kind, object_id = metadata.split()
        objects[raw_path.decode("utf-8")] = object_id.decode("ascii")
    if files.keys() != objects.keys():
        raise MutationError("candidate archive omits tracked inputs; export-ignore is forbidden")
    for path, raw in files.items():
        blob = b"blob " + str(len(raw)).encode("ascii") + b"\0" + raw
        algorithm = "sha1" if len(objects[path]) == 40 else "sha256"
        actual = hashlib.new(algorithm, blob, usedforsecurity=False).hexdigest()
        if actual != objects[path]:
            raise MutationError("CANDIDATE ARCHIVE REWRITES TRACKED INPUTS; EXPORT-SUBST IS FORBIDDEN")

mutants_x__validate_archive__mutmut['_mutmut_orig'] = x__validate_archive__mutmut_orig # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_1'] = x__validate_archive__mutmut_1 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_2'] = x__validate_archive__mutmut_2 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_3'] = x__validate_archive__mutmut_3 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_4'] = x__validate_archive__mutmut_4 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_5'] = x__validate_archive__mutmut_5 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_6'] = x__validate_archive__mutmut_6 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_7'] = x__validate_archive__mutmut_7 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_8'] = x__validate_archive__mutmut_8 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_9'] = x__validate_archive__mutmut_9 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_10'] = x__validate_archive__mutmut_10 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_11'] = x__validate_archive__mutmut_11 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_12'] = x__validate_archive__mutmut_12 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_13'] = x__validate_archive__mutmut_13 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_14'] = x__validate_archive__mutmut_14 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_15'] = x__validate_archive__mutmut_15 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_16'] = x__validate_archive__mutmut_16 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_17'] = x__validate_archive__mutmut_17 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_18'] = x__validate_archive__mutmut_18 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_19'] = x__validate_archive__mutmut_19 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_20'] = x__validate_archive__mutmut_20 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_21'] = x__validate_archive__mutmut_21 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_22'] = x__validate_archive__mutmut_22 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_23'] = x__validate_archive__mutmut_23 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_24'] = x__validate_archive__mutmut_24 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_25'] = x__validate_archive__mutmut_25 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_26'] = x__validate_archive__mutmut_26 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_27'] = x__validate_archive__mutmut_27 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_28'] = x__validate_archive__mutmut_28 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_29'] = x__validate_archive__mutmut_29 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_30'] = x__validate_archive__mutmut_30 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_31'] = x__validate_archive__mutmut_31 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_32'] = x__validate_archive__mutmut_32 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_33'] = x__validate_archive__mutmut_33 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_34'] = x__validate_archive__mutmut_34 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_35'] = x__validate_archive__mutmut_35 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_36'] = x__validate_archive__mutmut_36 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_37'] = x__validate_archive__mutmut_37 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_38'] = x__validate_archive__mutmut_38 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_39'] = x__validate_archive__mutmut_39 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_40'] = x__validate_archive__mutmut_40 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_41'] = x__validate_archive__mutmut_41 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_42'] = x__validate_archive__mutmut_42 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_43'] = x__validate_archive__mutmut_43 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_44'] = x__validate_archive__mutmut_44 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_45'] = x__validate_archive__mutmut_45 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_46'] = x__validate_archive__mutmut_46 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_47'] = x__validate_archive__mutmut_47 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_48'] = x__validate_archive__mutmut_48 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_49'] = x__validate_archive__mutmut_49 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_50'] = x__validate_archive__mutmut_50 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_51'] = x__validate_archive__mutmut_51 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_52'] = x__validate_archive__mutmut_52 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_53'] = x__validate_archive__mutmut_53 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_54'] = x__validate_archive__mutmut_54 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_55'] = x__validate_archive__mutmut_55 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_56'] = x__validate_archive__mutmut_56 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_57'] = x__validate_archive__mutmut_57 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_58'] = x__validate_archive__mutmut_58 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_59'] = x__validate_archive__mutmut_59 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_60'] = x__validate_archive__mutmut_60 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_61'] = x__validate_archive__mutmut_61 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_62'] = x__validate_archive__mutmut_62 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_63'] = x__validate_archive__mutmut_63 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_64'] = x__validate_archive__mutmut_64 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_65'] = x__validate_archive__mutmut_65 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_66'] = x__validate_archive__mutmut_66 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_67'] = x__validate_archive__mutmut_67 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_68'] = x__validate_archive__mutmut_68 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_69'] = x__validate_archive__mutmut_69 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_70'] = x__validate_archive__mutmut_70 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_71'] = x__validate_archive__mutmut_71 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_72'] = x__validate_archive__mutmut_72 # type: ignore # mutmut generated
mutants_x__validate_archive__mutmut['x__validate_archive__mutmut_73'] = x__validate_archive__mutmut_73 # type: ignore # mutmut generated
mutants_x_archive_files__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_archive_files__mutmut)
def archive_files(archive: bytes) -> dict[str, bytes]:
    files: dict[str, bytes] = {}
    with tarfile.open(fileobj=io.BytesIO(archive)) as stream:
        for member in stream.getmembers():
            path = PurePosixPath(member.name)
            if path.is_absolute() or ".." in path.parts or "\\" in member.name:
                raise MutationError("unsafe path in candidate archive")
            if member.isdir():
                continue
            if not member.isfile():
                raise MutationError("candidate mutation snapshots require regular tracked files")
            # ``member.isfile()`` guarantees an extractable stream in tarfile.
            source = cast(IO[bytes], stream.extractfile(member))
            files[path.as_posix()] = source.read()
    return files


def x_archive_files__mutmut_orig(archive: bytes) -> dict[str, bytes]:
    files: dict[str, bytes] = {}
    with tarfile.open(fileobj=io.BytesIO(archive)) as stream:
        for member in stream.getmembers():
            path = PurePosixPath(member.name)
            if path.is_absolute() or ".." in path.parts or "\\" in member.name:
                raise MutationError("unsafe path in candidate archive")
            if member.isdir():
                continue
            if not member.isfile():
                raise MutationError("candidate mutation snapshots require regular tracked files")
            # ``member.isfile()`` guarantees an extractable stream in tarfile.
            source = cast(IO[bytes], stream.extractfile(member))
            files[path.as_posix()] = source.read()
    return files


def x_archive_files__mutmut_1(archive: bytes) -> dict[str, bytes]:
    files: dict[str, bytes] = None
    with tarfile.open(fileobj=io.BytesIO(archive)) as stream:
        for member in stream.getmembers():
            path = PurePosixPath(member.name)
            if path.is_absolute() or ".." in path.parts or "\\" in member.name:
                raise MutationError("unsafe path in candidate archive")
            if member.isdir():
                continue
            if not member.isfile():
                raise MutationError("candidate mutation snapshots require regular tracked files")
            # ``member.isfile()`` guarantees an extractable stream in tarfile.
            source = cast(IO[bytes], stream.extractfile(member))
            files[path.as_posix()] = source.read()
    return files


def x_archive_files__mutmut_2(archive: bytes) -> dict[str, bytes]:
    files: dict[str, bytes] = {}
    with tarfile.open(fileobj=None) as stream:
        for member in stream.getmembers():
            path = PurePosixPath(member.name)
            if path.is_absolute() or ".." in path.parts or "\\" in member.name:
                raise MutationError("unsafe path in candidate archive")
            if member.isdir():
                continue
            if not member.isfile():
                raise MutationError("candidate mutation snapshots require regular tracked files")
            # ``member.isfile()`` guarantees an extractable stream in tarfile.
            source = cast(IO[bytes], stream.extractfile(member))
            files[path.as_posix()] = source.read()
    return files


def x_archive_files__mutmut_3(archive: bytes) -> dict[str, bytes]:
    files: dict[str, bytes] = {}
    with tarfile.open(fileobj=io.BytesIO(None)) as stream:
        for member in stream.getmembers():
            path = PurePosixPath(member.name)
            if path.is_absolute() or ".." in path.parts or "\\" in member.name:
                raise MutationError("unsafe path in candidate archive")
            if member.isdir():
                continue
            if not member.isfile():
                raise MutationError("candidate mutation snapshots require regular tracked files")
            # ``member.isfile()`` guarantees an extractable stream in tarfile.
            source = cast(IO[bytes], stream.extractfile(member))
            files[path.as_posix()] = source.read()
    return files


def x_archive_files__mutmut_4(archive: bytes) -> dict[str, bytes]:
    files: dict[str, bytes] = {}
    with tarfile.open(fileobj=io.BytesIO(archive)) as stream:
        for member in stream.getmembers():
            path = None
            if path.is_absolute() or ".." in path.parts or "\\" in member.name:
                raise MutationError("unsafe path in candidate archive")
            if member.isdir():
                continue
            if not member.isfile():
                raise MutationError("candidate mutation snapshots require regular tracked files")
            # ``member.isfile()`` guarantees an extractable stream in tarfile.
            source = cast(IO[bytes], stream.extractfile(member))
            files[path.as_posix()] = source.read()
    return files


def x_archive_files__mutmut_5(archive: bytes) -> dict[str, bytes]:
    files: dict[str, bytes] = {}
    with tarfile.open(fileobj=io.BytesIO(archive)) as stream:
        for member in stream.getmembers():
            path = PurePosixPath(None)
            if path.is_absolute() or ".." in path.parts or "\\" in member.name:
                raise MutationError("unsafe path in candidate archive")
            if member.isdir():
                continue
            if not member.isfile():
                raise MutationError("candidate mutation snapshots require regular tracked files")
            # ``member.isfile()`` guarantees an extractable stream in tarfile.
            source = cast(IO[bytes], stream.extractfile(member))
            files[path.as_posix()] = source.read()
    return files


def x_archive_files__mutmut_6(archive: bytes) -> dict[str, bytes]:
    files: dict[str, bytes] = {}
    with tarfile.open(fileobj=io.BytesIO(archive)) as stream:
        for member in stream.getmembers():
            path = PurePosixPath(member.name)
            if path.is_absolute() or ".." in path.parts and "\\" in member.name:
                raise MutationError("unsafe path in candidate archive")
            if member.isdir():
                continue
            if not member.isfile():
                raise MutationError("candidate mutation snapshots require regular tracked files")
            # ``member.isfile()`` guarantees an extractable stream in tarfile.
            source = cast(IO[bytes], stream.extractfile(member))
            files[path.as_posix()] = source.read()
    return files


def x_archive_files__mutmut_7(archive: bytes) -> dict[str, bytes]:
    files: dict[str, bytes] = {}
    with tarfile.open(fileobj=io.BytesIO(archive)) as stream:
        for member in stream.getmembers():
            path = PurePosixPath(member.name)
            if path.is_absolute() and ".." in path.parts or "\\" in member.name:
                raise MutationError("unsafe path in candidate archive")
            if member.isdir():
                continue
            if not member.isfile():
                raise MutationError("candidate mutation snapshots require regular tracked files")
            # ``member.isfile()`` guarantees an extractable stream in tarfile.
            source = cast(IO[bytes], stream.extractfile(member))
            files[path.as_posix()] = source.read()
    return files


def x_archive_files__mutmut_8(archive: bytes) -> dict[str, bytes]:
    files: dict[str, bytes] = {}
    with tarfile.open(fileobj=io.BytesIO(archive)) as stream:
        for member in stream.getmembers():
            path = PurePosixPath(member.name)
            if path.is_absolute() or "XX..XX" in path.parts or "\\" in member.name:
                raise MutationError("unsafe path in candidate archive")
            if member.isdir():
                continue
            if not member.isfile():
                raise MutationError("candidate mutation snapshots require regular tracked files")
            # ``member.isfile()`` guarantees an extractable stream in tarfile.
            source = cast(IO[bytes], stream.extractfile(member))
            files[path.as_posix()] = source.read()
    return files


def x_archive_files__mutmut_9(archive: bytes) -> dict[str, bytes]:
    files: dict[str, bytes] = {}
    with tarfile.open(fileobj=io.BytesIO(archive)) as stream:
        for member in stream.getmembers():
            path = PurePosixPath(member.name)
            if path.is_absolute() or ".." not in path.parts or "\\" in member.name:
                raise MutationError("unsafe path in candidate archive")
            if member.isdir():
                continue
            if not member.isfile():
                raise MutationError("candidate mutation snapshots require regular tracked files")
            # ``member.isfile()`` guarantees an extractable stream in tarfile.
            source = cast(IO[bytes], stream.extractfile(member))
            files[path.as_posix()] = source.read()
    return files


def x_archive_files__mutmut_10(archive: bytes) -> dict[str, bytes]:
    files: dict[str, bytes] = {}
    with tarfile.open(fileobj=io.BytesIO(archive)) as stream:
        for member in stream.getmembers():
            path = PurePosixPath(member.name)
            if path.is_absolute() or ".." in path.parts or "XX\\XX" in member.name:
                raise MutationError("unsafe path in candidate archive")
            if member.isdir():
                continue
            if not member.isfile():
                raise MutationError("candidate mutation snapshots require regular tracked files")
            # ``member.isfile()`` guarantees an extractable stream in tarfile.
            source = cast(IO[bytes], stream.extractfile(member))
            files[path.as_posix()] = source.read()
    return files


def x_archive_files__mutmut_11(archive: bytes) -> dict[str, bytes]:
    files: dict[str, bytes] = {}
    with tarfile.open(fileobj=io.BytesIO(archive)) as stream:
        for member in stream.getmembers():
            path = PurePosixPath(member.name)
            if path.is_absolute() or ".." in path.parts or "\\" not in member.name:
                raise MutationError("unsafe path in candidate archive")
            if member.isdir():
                continue
            if not member.isfile():
                raise MutationError("candidate mutation snapshots require regular tracked files")
            # ``member.isfile()`` guarantees an extractable stream in tarfile.
            source = cast(IO[bytes], stream.extractfile(member))
            files[path.as_posix()] = source.read()
    return files


def x_archive_files__mutmut_12(archive: bytes) -> dict[str, bytes]:
    files: dict[str, bytes] = {}
    with tarfile.open(fileobj=io.BytesIO(archive)) as stream:
        for member in stream.getmembers():
            path = PurePosixPath(member.name)
            if path.is_absolute() or ".." in path.parts or "\\" in member.name:
                raise MutationError(None)
            if member.isdir():
                continue
            if not member.isfile():
                raise MutationError("candidate mutation snapshots require regular tracked files")
            # ``member.isfile()`` guarantees an extractable stream in tarfile.
            source = cast(IO[bytes], stream.extractfile(member))
            files[path.as_posix()] = source.read()
    return files


def x_archive_files__mutmut_13(archive: bytes) -> dict[str, bytes]:
    files: dict[str, bytes] = {}
    with tarfile.open(fileobj=io.BytesIO(archive)) as stream:
        for member in stream.getmembers():
            path = PurePosixPath(member.name)
            if path.is_absolute() or ".." in path.parts or "\\" in member.name:
                raise MutationError("XXunsafe path in candidate archiveXX")
            if member.isdir():
                continue
            if not member.isfile():
                raise MutationError("candidate mutation snapshots require regular tracked files")
            # ``member.isfile()`` guarantees an extractable stream in tarfile.
            source = cast(IO[bytes], stream.extractfile(member))
            files[path.as_posix()] = source.read()
    return files


def x_archive_files__mutmut_14(archive: bytes) -> dict[str, bytes]:
    files: dict[str, bytes] = {}
    with tarfile.open(fileobj=io.BytesIO(archive)) as stream:
        for member in stream.getmembers():
            path = PurePosixPath(member.name)
            if path.is_absolute() or ".." in path.parts or "\\" in member.name:
                raise MutationError("UNSAFE PATH IN CANDIDATE ARCHIVE")
            if member.isdir():
                continue
            if not member.isfile():
                raise MutationError("candidate mutation snapshots require regular tracked files")
            # ``member.isfile()`` guarantees an extractable stream in tarfile.
            source = cast(IO[bytes], stream.extractfile(member))
            files[path.as_posix()] = source.read()
    return files


def x_archive_files__mutmut_15(archive: bytes) -> dict[str, bytes]:
    files: dict[str, bytes] = {}
    with tarfile.open(fileobj=io.BytesIO(archive)) as stream:
        for member in stream.getmembers():
            path = PurePosixPath(member.name)
            if path.is_absolute() or ".." in path.parts or "\\" in member.name:
                raise MutationError("unsafe path in candidate archive")
            if member.isdir():
                break
            if not member.isfile():
                raise MutationError("candidate mutation snapshots require regular tracked files")
            # ``member.isfile()`` guarantees an extractable stream in tarfile.
            source = cast(IO[bytes], stream.extractfile(member))
            files[path.as_posix()] = source.read()
    return files


def x_archive_files__mutmut_16(archive: bytes) -> dict[str, bytes]:
    files: dict[str, bytes] = {}
    with tarfile.open(fileobj=io.BytesIO(archive)) as stream:
        for member in stream.getmembers():
            path = PurePosixPath(member.name)
            if path.is_absolute() or ".." in path.parts or "\\" in member.name:
                raise MutationError("unsafe path in candidate archive")
            if member.isdir():
                continue
            if member.isfile():
                raise MutationError("candidate mutation snapshots require regular tracked files")
            # ``member.isfile()`` guarantees an extractable stream in tarfile.
            source = cast(IO[bytes], stream.extractfile(member))
            files[path.as_posix()] = source.read()
    return files


def x_archive_files__mutmut_17(archive: bytes) -> dict[str, bytes]:
    files: dict[str, bytes] = {}
    with tarfile.open(fileobj=io.BytesIO(archive)) as stream:
        for member in stream.getmembers():
            path = PurePosixPath(member.name)
            if path.is_absolute() or ".." in path.parts or "\\" in member.name:
                raise MutationError("unsafe path in candidate archive")
            if member.isdir():
                continue
            if not member.isfile():
                raise MutationError(None)
            # ``member.isfile()`` guarantees an extractable stream in tarfile.
            source = cast(IO[bytes], stream.extractfile(member))
            files[path.as_posix()] = source.read()
    return files


def x_archive_files__mutmut_18(archive: bytes) -> dict[str, bytes]:
    files: dict[str, bytes] = {}
    with tarfile.open(fileobj=io.BytesIO(archive)) as stream:
        for member in stream.getmembers():
            path = PurePosixPath(member.name)
            if path.is_absolute() or ".." in path.parts or "\\" in member.name:
                raise MutationError("unsafe path in candidate archive")
            if member.isdir():
                continue
            if not member.isfile():
                raise MutationError("XXcandidate mutation snapshots require regular tracked filesXX")
            # ``member.isfile()`` guarantees an extractable stream in tarfile.
            source = cast(IO[bytes], stream.extractfile(member))
            files[path.as_posix()] = source.read()
    return files


def x_archive_files__mutmut_19(archive: bytes) -> dict[str, bytes]:
    files: dict[str, bytes] = {}
    with tarfile.open(fileobj=io.BytesIO(archive)) as stream:
        for member in stream.getmembers():
            path = PurePosixPath(member.name)
            if path.is_absolute() or ".." in path.parts or "\\" in member.name:
                raise MutationError("unsafe path in candidate archive")
            if member.isdir():
                continue
            if not member.isfile():
                raise MutationError("CANDIDATE MUTATION SNAPSHOTS REQUIRE REGULAR TRACKED FILES")
            # ``member.isfile()`` guarantees an extractable stream in tarfile.
            source = cast(IO[bytes], stream.extractfile(member))
            files[path.as_posix()] = source.read()
    return files


def x_archive_files__mutmut_20(archive: bytes) -> dict[str, bytes]:
    files: dict[str, bytes] = {}
    with tarfile.open(fileobj=io.BytesIO(archive)) as stream:
        for member in stream.getmembers():
            path = PurePosixPath(member.name)
            if path.is_absolute() or ".." in path.parts or "\\" in member.name:
                raise MutationError("unsafe path in candidate archive")
            if member.isdir():
                continue
            if not member.isfile():
                raise MutationError("candidate mutation snapshots require regular tracked files")
            # ``member.isfile()`` guarantees an extractable stream in tarfile.
            source = None
            files[path.as_posix()] = source.read()
    return files


def x_archive_files__mutmut_21(archive: bytes) -> dict[str, bytes]:
    files: dict[str, bytes] = {}
    with tarfile.open(fileobj=io.BytesIO(archive)) as stream:
        for member in stream.getmembers():
            path = PurePosixPath(member.name)
            if path.is_absolute() or ".." in path.parts or "\\" in member.name:
                raise MutationError("unsafe path in candidate archive")
            if member.isdir():
                continue
            if not member.isfile():
                raise MutationError("candidate mutation snapshots require regular tracked files")
            # ``member.isfile()`` guarantees an extractable stream in tarfile.
            source = cast(None, stream.extractfile(member))
            files[path.as_posix()] = source.read()
    return files


def x_archive_files__mutmut_22(archive: bytes) -> dict[str, bytes]:
    files: dict[str, bytes] = {}
    with tarfile.open(fileobj=io.BytesIO(archive)) as stream:
        for member in stream.getmembers():
            path = PurePosixPath(member.name)
            if path.is_absolute() or ".." in path.parts or "\\" in member.name:
                raise MutationError("unsafe path in candidate archive")
            if member.isdir():
                continue
            if not member.isfile():
                raise MutationError("candidate mutation snapshots require regular tracked files")
            # ``member.isfile()`` guarantees an extractable stream in tarfile.
            source = cast(IO[bytes], None)
            files[path.as_posix()] = source.read()
    return files


def x_archive_files__mutmut_23(archive: bytes) -> dict[str, bytes]:
    files: dict[str, bytes] = {}
    with tarfile.open(fileobj=io.BytesIO(archive)) as stream:
        for member in stream.getmembers():
            path = PurePosixPath(member.name)
            if path.is_absolute() or ".." in path.parts or "\\" in member.name:
                raise MutationError("unsafe path in candidate archive")
            if member.isdir():
                continue
            if not member.isfile():
                raise MutationError("candidate mutation snapshots require regular tracked files")
            # ``member.isfile()`` guarantees an extractable stream in tarfile.
            source = cast(stream.extractfile(member))
            files[path.as_posix()] = source.read()
    return files


def x_archive_files__mutmut_24(archive: bytes) -> dict[str, bytes]:
    files: dict[str, bytes] = {}
    with tarfile.open(fileobj=io.BytesIO(archive)) as stream:
        for member in stream.getmembers():
            path = PurePosixPath(member.name)
            if path.is_absolute() or ".." in path.parts or "\\" in member.name:
                raise MutationError("unsafe path in candidate archive")
            if member.isdir():
                continue
            if not member.isfile():
                raise MutationError("candidate mutation snapshots require regular tracked files")
            # ``member.isfile()`` guarantees an extractable stream in tarfile.
            source = cast(IO[bytes], )
            files[path.as_posix()] = source.read()
    return files


def x_archive_files__mutmut_25(archive: bytes) -> dict[str, bytes]:
    files: dict[str, bytes] = {}
    with tarfile.open(fileobj=io.BytesIO(archive)) as stream:
        for member in stream.getmembers():
            path = PurePosixPath(member.name)
            if path.is_absolute() or ".." in path.parts or "\\" in member.name:
                raise MutationError("unsafe path in candidate archive")
            if member.isdir():
                continue
            if not member.isfile():
                raise MutationError("candidate mutation snapshots require regular tracked files")
            # ``member.isfile()`` guarantees an extractable stream in tarfile.
            source = cast(IO[bytes], stream.extractfile(None))
            files[path.as_posix()] = source.read()
    return files


def x_archive_files__mutmut_26(archive: bytes) -> dict[str, bytes]:
    files: dict[str, bytes] = {}
    with tarfile.open(fileobj=io.BytesIO(archive)) as stream:
        for member in stream.getmembers():
            path = PurePosixPath(member.name)
            if path.is_absolute() or ".." in path.parts or "\\" in member.name:
                raise MutationError("unsafe path in candidate archive")
            if member.isdir():
                continue
            if not member.isfile():
                raise MutationError("candidate mutation snapshots require regular tracked files")
            # ``member.isfile()`` guarantees an extractable stream in tarfile.
            source = cast(IO[bytes], stream.extractfile(member))
            files[path.as_posix()] = None
    return files

mutants_x_archive_files__mutmut['_mutmut_orig'] = x_archive_files__mutmut_orig # type: ignore # mutmut generated
mutants_x_archive_files__mutmut['x_archive_files__mutmut_1'] = x_archive_files__mutmut_1 # type: ignore # mutmut generated
mutants_x_archive_files__mutmut['x_archive_files__mutmut_2'] = x_archive_files__mutmut_2 # type: ignore # mutmut generated
mutants_x_archive_files__mutmut['x_archive_files__mutmut_3'] = x_archive_files__mutmut_3 # type: ignore # mutmut generated
mutants_x_archive_files__mutmut['x_archive_files__mutmut_4'] = x_archive_files__mutmut_4 # type: ignore # mutmut generated
mutants_x_archive_files__mutmut['x_archive_files__mutmut_5'] = x_archive_files__mutmut_5 # type: ignore # mutmut generated
mutants_x_archive_files__mutmut['x_archive_files__mutmut_6'] = x_archive_files__mutmut_6 # type: ignore # mutmut generated
mutants_x_archive_files__mutmut['x_archive_files__mutmut_7'] = x_archive_files__mutmut_7 # type: ignore # mutmut generated
mutants_x_archive_files__mutmut['x_archive_files__mutmut_8'] = x_archive_files__mutmut_8 # type: ignore # mutmut generated
mutants_x_archive_files__mutmut['x_archive_files__mutmut_9'] = x_archive_files__mutmut_9 # type: ignore # mutmut generated
mutants_x_archive_files__mutmut['x_archive_files__mutmut_10'] = x_archive_files__mutmut_10 # type: ignore # mutmut generated
mutants_x_archive_files__mutmut['x_archive_files__mutmut_11'] = x_archive_files__mutmut_11 # type: ignore # mutmut generated
mutants_x_archive_files__mutmut['x_archive_files__mutmut_12'] = x_archive_files__mutmut_12 # type: ignore # mutmut generated
mutants_x_archive_files__mutmut['x_archive_files__mutmut_13'] = x_archive_files__mutmut_13 # type: ignore # mutmut generated
mutants_x_archive_files__mutmut['x_archive_files__mutmut_14'] = x_archive_files__mutmut_14 # type: ignore # mutmut generated
mutants_x_archive_files__mutmut['x_archive_files__mutmut_15'] = x_archive_files__mutmut_15 # type: ignore # mutmut generated
mutants_x_archive_files__mutmut['x_archive_files__mutmut_16'] = x_archive_files__mutmut_16 # type: ignore # mutmut generated
mutants_x_archive_files__mutmut['x_archive_files__mutmut_17'] = x_archive_files__mutmut_17 # type: ignore # mutmut generated
mutants_x_archive_files__mutmut['x_archive_files__mutmut_18'] = x_archive_files__mutmut_18 # type: ignore # mutmut generated
mutants_x_archive_files__mutmut['x_archive_files__mutmut_19'] = x_archive_files__mutmut_19 # type: ignore # mutmut generated
mutants_x_archive_files__mutmut['x_archive_files__mutmut_20'] = x_archive_files__mutmut_20 # type: ignore # mutmut generated
mutants_x_archive_files__mutmut['x_archive_files__mutmut_21'] = x_archive_files__mutmut_21 # type: ignore # mutmut generated
mutants_x_archive_files__mutmut['x_archive_files__mutmut_22'] = x_archive_files__mutmut_22 # type: ignore # mutmut generated
mutants_x_archive_files__mutmut['x_archive_files__mutmut_23'] = x_archive_files__mutmut_23 # type: ignore # mutmut generated
mutants_x_archive_files__mutmut['x_archive_files__mutmut_24'] = x_archive_files__mutmut_24 # type: ignore # mutmut generated
mutants_x_archive_files__mutmut['x_archive_files__mutmut_25'] = x_archive_files__mutmut_25 # type: ignore # mutmut generated
mutants_x_archive_files__mutmut['x_archive_files__mutmut_26'] = x_archive_files__mutmut_26 # type: ignore # mutmut generated
mutants_x_archive_executables__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_archive_executables__mutmut)
def archive_executables(archive: bytes) -> list[str]:
    """Retain Git's executable bit alongside the already validated archive paths."""
    with tarfile.open(fileobj=io.BytesIO(archive)) as stream:
        return sorted(
            member.name for member in stream.getmembers() if member.isfile() and member.mode & 0o111
        )


def x_archive_executables__mutmut_orig(archive: bytes) -> list[str]:
    """Retain Git's executable bit alongside the already validated archive paths."""
    with tarfile.open(fileobj=io.BytesIO(archive)) as stream:
        return sorted(
            member.name for member in stream.getmembers() if member.isfile() and member.mode & 0o111
        )


def x_archive_executables__mutmut_1(archive: bytes) -> list[str]:
    """Retain Git's executable bit alongside the already validated archive paths."""
    with tarfile.open(fileobj=None) as stream:
        return sorted(
            member.name for member in stream.getmembers() if member.isfile() and member.mode & 0o111
        )


def x_archive_executables__mutmut_2(archive: bytes) -> list[str]:
    """Retain Git's executable bit alongside the already validated archive paths."""
    with tarfile.open(fileobj=io.BytesIO(None)) as stream:
        return sorted(
            member.name for member in stream.getmembers() if member.isfile() and member.mode & 0o111
        )


def x_archive_executables__mutmut_3(archive: bytes) -> list[str]:
    """Retain Git's executable bit alongside the already validated archive paths."""
    with tarfile.open(fileobj=io.BytesIO(archive)) as stream:
        return sorted(
            None
        )


def x_archive_executables__mutmut_4(archive: bytes) -> list[str]:
    """Retain Git's executable bit alongside the already validated archive paths."""
    with tarfile.open(fileobj=io.BytesIO(archive)) as stream:
        return sorted(
            member.name for member in stream.getmembers() if member.isfile() or member.mode & 0o111
        )


def x_archive_executables__mutmut_5(archive: bytes) -> list[str]:
    """Retain Git's executable bit alongside the already validated archive paths."""
    with tarfile.open(fileobj=io.BytesIO(archive)) as stream:
        return sorted(
            member.name for member in stream.getmembers() if member.isfile() and member.mode | 0o111
        )


def x_archive_executables__mutmut_6(archive: bytes) -> list[str]:
    """Retain Git's executable bit alongside the already validated archive paths."""
    with tarfile.open(fileobj=io.BytesIO(archive)) as stream:
        return sorted(
            member.name for member in stream.getmembers() if member.isfile() and member.mode & 74
        )

mutants_x_archive_executables__mutmut['_mutmut_orig'] = x_archive_executables__mutmut_orig # type: ignore # mutmut generated
mutants_x_archive_executables__mutmut['x_archive_executables__mutmut_1'] = x_archive_executables__mutmut_1 # type: ignore # mutmut generated
mutants_x_archive_executables__mutmut['x_archive_executables__mutmut_2'] = x_archive_executables__mutmut_2 # type: ignore # mutmut generated
mutants_x_archive_executables__mutmut['x_archive_executables__mutmut_3'] = x_archive_executables__mutmut_3 # type: ignore # mutmut generated
mutants_x_archive_executables__mutmut['x_archive_executables__mutmut_4'] = x_archive_executables__mutmut_4 # type: ignore # mutmut generated
mutants_x_archive_executables__mutmut['x_archive_executables__mutmut_5'] = x_archive_executables__mutmut_5 # type: ignore # mutmut generated
mutants_x_archive_executables__mutmut['x_archive_executables__mutmut_6'] = x_archive_executables__mutmut_6 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_read_policy__mutmut)
def read_policy(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_orig(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_1(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = None
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_2(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(None)
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_3(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["XXmutation.tomlXX"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_4(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["MUTATION.TOML"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_5(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError(None) from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_6(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("XXtracked mutation.toml policy is requiredXX") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_7(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("TRACKED MUTATION.TOML POLICY IS REQUIRED") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_8(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(None) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_9(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) == {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_10(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"XXschemaXX", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_11(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"SCHEMA", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_12(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "XXsource_rootsXX", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_13(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "SOURCE_ROOTS", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_14(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "XXtestsXX", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_15(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "TESTS", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_16(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "XXtimeout_secondsXX", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_17(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "TIMEOUT_SECONDS", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_18(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "XXmax_mutantsXX"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_19(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "MAX_MUTANTS"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_20(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError(None)
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_21(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("XXmutation policy requires exact fields; exemptions are not supportedXX")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_22(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("MUTATION POLICY REQUIRES EXACT FIELDS; EXEMPTIONS ARE NOT SUPPORTED")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_23(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["XXschemaXX"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_24(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["SCHEMA"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_25(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] == "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_26(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "XXtc.fitness/mutation-policy/v1XX":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_27(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "TC.FITNESS/MUTATION-POLICY/V1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_28(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError(None)
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_29(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("XXunknown mutation policy schemaXX")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_30(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("UNKNOWN MUTATION POLICY SCHEMA")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_31(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("XXsource_rootsXX", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_32(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("SOURCE_ROOTS", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_33(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "XXtestsXX"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_34(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "TESTS"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_35(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = None
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_36(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths and len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_37(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) and not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_38(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_39(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_40(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) == len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_41(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(None)
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_42(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path and any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_43(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts and "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_44(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute() and ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_45(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-") and PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_46(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path and path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_47(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str) and not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_48(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_49(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_50(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith(None)
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_51(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("XX-XX")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_52(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(None).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_53(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or "XX..XX" in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_54(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." not in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_55(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(None).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_56(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "XX\\XX" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_57(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" not in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_58(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(None)
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_59(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char not in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_60(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "XX*?\n\rXX")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_61(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(None)
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_62(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_63(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(None):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_64(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path and name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_65(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name != path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_66(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(None) for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_67(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") - "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_68(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip(None) + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_69(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.lstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_70(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("XX/XX") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_71(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "XX/XX") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_72(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(None)
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_73(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("XXtimeout_secondsXX", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_74(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("TIMEOUT_SECONDS", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_75(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 901), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_76(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("XXmax_mutantsXX", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_77(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("MAX_MUTANTS", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_78(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100001)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_79(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int and not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_80(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(None) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_81(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_82(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_83(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 2 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_84(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 < value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_85(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] < ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_86(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(None)
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_87(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        None
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_88(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") and test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_89(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test and root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_90(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root != test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_91(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(None) or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_92(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test - "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_93(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "XX/XX") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_94(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(None)
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_95(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root - "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_96(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "XX/XX")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_97(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip(None) for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_98(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.lstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_99(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("XX/XX") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_100(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["XXsource_rootsXX"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_101(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["SOURCE_ROOTS"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_102(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip(None) for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_103(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.lstrip("/") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_104(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("XX/XX") for path in value["tests"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_105(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["XXtestsXX"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_106(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["TESTS"])
    ):
        raise MutationError("production roots and test paths must be disjoint")
    return value


def x_read_policy__mutmut_107(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError(None)
    return value


def x_read_policy__mutmut_108(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("XXproduction roots and test paths must be disjointXX")
    return value


def x_read_policy__mutmut_109(files: dict[str, bytes]) -> dict[str, Any]:
    try:
        value = tomllib.loads(files["mutation.toml"].decode())
    except (KeyError, ValueError) as exc:
        raise MutationError("tracked mutation.toml policy is required") from exc
    if set(value) != {"schema", "source_roots", "tests", "timeout_seconds", "max_mutants"}:
        raise MutationError("mutation policy requires exact fields; exemptions are not supported")
    if value["schema"] != "tc.fitness/mutation-policy/v1":
        raise MutationError("unknown mutation policy schema")
    for key in ("source_roots", "tests"):
        paths = value[key]
        if not isinstance(paths, list) or not paths or len(paths) != len(set(paths)):
            raise MutationError(f"{key} must be a nonempty unique path list")
        for path in paths:
            if (
                not isinstance(path, str)
                or not path
                or path.startswith("-")
                or PurePosixPath(path).is_absolute()
                or ".." in PurePosixPath(path).parts
                or "\\" in path
                or any(char in path for char in "*?\n\r")
            ):
                raise MutationError(f"{key} must contain literal repository-relative paths")
            if not any(name == path or name.startswith(path.rstrip("/") + "/") for name in files):
                raise MutationError(f"policy path has no tracked inputs: {path}")
    for key, ceiling in (("timeout_seconds", 900), ("max_mutants", 100000)):
        if type(value[key]) is not int or not 1 <= value[key] <= ceiling:
            raise MutationError(f"{key} must be bounded between 1 and {ceiling}")
    if any(
        root == test or root.startswith(test + "/") or test.startswith(root + "/")
        for root in (path.rstrip("/") for path in value["source_roots"])
        for test in (path.rstrip("/") for path in value["tests"])
    ):
        raise MutationError("PRODUCTION ROOTS AND TEST PATHS MUST BE DISJOINT")
    return value

mutants_x_read_policy__mutmut['_mutmut_orig'] = x_read_policy__mutmut_orig # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_1'] = x_read_policy__mutmut_1 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_2'] = x_read_policy__mutmut_2 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_3'] = x_read_policy__mutmut_3 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_4'] = x_read_policy__mutmut_4 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_5'] = x_read_policy__mutmut_5 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_6'] = x_read_policy__mutmut_6 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_7'] = x_read_policy__mutmut_7 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_8'] = x_read_policy__mutmut_8 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_9'] = x_read_policy__mutmut_9 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_10'] = x_read_policy__mutmut_10 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_11'] = x_read_policy__mutmut_11 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_12'] = x_read_policy__mutmut_12 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_13'] = x_read_policy__mutmut_13 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_14'] = x_read_policy__mutmut_14 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_15'] = x_read_policy__mutmut_15 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_16'] = x_read_policy__mutmut_16 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_17'] = x_read_policy__mutmut_17 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_18'] = x_read_policy__mutmut_18 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_19'] = x_read_policy__mutmut_19 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_20'] = x_read_policy__mutmut_20 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_21'] = x_read_policy__mutmut_21 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_22'] = x_read_policy__mutmut_22 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_23'] = x_read_policy__mutmut_23 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_24'] = x_read_policy__mutmut_24 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_25'] = x_read_policy__mutmut_25 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_26'] = x_read_policy__mutmut_26 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_27'] = x_read_policy__mutmut_27 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_28'] = x_read_policy__mutmut_28 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_29'] = x_read_policy__mutmut_29 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_30'] = x_read_policy__mutmut_30 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_31'] = x_read_policy__mutmut_31 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_32'] = x_read_policy__mutmut_32 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_33'] = x_read_policy__mutmut_33 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_34'] = x_read_policy__mutmut_34 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_35'] = x_read_policy__mutmut_35 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_36'] = x_read_policy__mutmut_36 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_37'] = x_read_policy__mutmut_37 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_38'] = x_read_policy__mutmut_38 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_39'] = x_read_policy__mutmut_39 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_40'] = x_read_policy__mutmut_40 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_41'] = x_read_policy__mutmut_41 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_42'] = x_read_policy__mutmut_42 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_43'] = x_read_policy__mutmut_43 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_44'] = x_read_policy__mutmut_44 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_45'] = x_read_policy__mutmut_45 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_46'] = x_read_policy__mutmut_46 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_47'] = x_read_policy__mutmut_47 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_48'] = x_read_policy__mutmut_48 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_49'] = x_read_policy__mutmut_49 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_50'] = x_read_policy__mutmut_50 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_51'] = x_read_policy__mutmut_51 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_52'] = x_read_policy__mutmut_52 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_53'] = x_read_policy__mutmut_53 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_54'] = x_read_policy__mutmut_54 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_55'] = x_read_policy__mutmut_55 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_56'] = x_read_policy__mutmut_56 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_57'] = x_read_policy__mutmut_57 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_58'] = x_read_policy__mutmut_58 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_59'] = x_read_policy__mutmut_59 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_60'] = x_read_policy__mutmut_60 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_61'] = x_read_policy__mutmut_61 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_62'] = x_read_policy__mutmut_62 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_63'] = x_read_policy__mutmut_63 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_64'] = x_read_policy__mutmut_64 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_65'] = x_read_policy__mutmut_65 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_66'] = x_read_policy__mutmut_66 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_67'] = x_read_policy__mutmut_67 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_68'] = x_read_policy__mutmut_68 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_69'] = x_read_policy__mutmut_69 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_70'] = x_read_policy__mutmut_70 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_71'] = x_read_policy__mutmut_71 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_72'] = x_read_policy__mutmut_72 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_73'] = x_read_policy__mutmut_73 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_74'] = x_read_policy__mutmut_74 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_75'] = x_read_policy__mutmut_75 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_76'] = x_read_policy__mutmut_76 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_77'] = x_read_policy__mutmut_77 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_78'] = x_read_policy__mutmut_78 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_79'] = x_read_policy__mutmut_79 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_80'] = x_read_policy__mutmut_80 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_81'] = x_read_policy__mutmut_81 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_82'] = x_read_policy__mutmut_82 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_83'] = x_read_policy__mutmut_83 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_84'] = x_read_policy__mutmut_84 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_85'] = x_read_policy__mutmut_85 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_86'] = x_read_policy__mutmut_86 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_87'] = x_read_policy__mutmut_87 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_88'] = x_read_policy__mutmut_88 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_89'] = x_read_policy__mutmut_89 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_90'] = x_read_policy__mutmut_90 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_91'] = x_read_policy__mutmut_91 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_92'] = x_read_policy__mutmut_92 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_93'] = x_read_policy__mutmut_93 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_94'] = x_read_policy__mutmut_94 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_95'] = x_read_policy__mutmut_95 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_96'] = x_read_policy__mutmut_96 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_97'] = x_read_policy__mutmut_97 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_98'] = x_read_policy__mutmut_98 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_99'] = x_read_policy__mutmut_99 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_100'] = x_read_policy__mutmut_100 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_101'] = x_read_policy__mutmut_101 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_102'] = x_read_policy__mutmut_102 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_103'] = x_read_policy__mutmut_103 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_104'] = x_read_policy__mutmut_104 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_105'] = x_read_policy__mutmut_105 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_106'] = x_read_policy__mutmut_106 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_107'] = x_read_policy__mutmut_107 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_108'] = x_read_policy__mutmut_108 # type: ignore # mutmut generated
mutants_x_read_policy__mutmut['x_read_policy__mutmut_109'] = x_read_policy__mutmut_109 # type: ignore # mutmut generated


@dataclass(frozen=True)
class Function:
    name: str
    path: str
    node: ast.FunctionDef | ast.AsyncFunctionDef
    selector: str
mutants_x_definition_digest__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_definition_digest__mutmut)
def definition_digest(node: ast.FunctionDef | ast.AsyncFunctionDef) -> str:
    """Bind the executable definition, independently of generated trampoline names."""
    normalised = copy.deepcopy(node)
    normalised.name = "subject"
    normalised.decorator_list = []
    return payload_digest(ast.dump(normalised))


def x_definition_digest__mutmut_orig(node: ast.FunctionDef | ast.AsyncFunctionDef) -> str:
    """Bind the executable definition, independently of generated trampoline names."""
    normalised = copy.deepcopy(node)
    normalised.name = "subject"
    normalised.decorator_list = []
    return payload_digest(ast.dump(normalised))


def x_definition_digest__mutmut_1(node: ast.FunctionDef | ast.AsyncFunctionDef) -> str:
    """Bind the executable definition, independently of generated trampoline names."""
    normalised = None
    normalised.name = "subject"
    normalised.decorator_list = []
    return payload_digest(ast.dump(normalised))


def x_definition_digest__mutmut_2(node: ast.FunctionDef | ast.AsyncFunctionDef) -> str:
    """Bind the executable definition, independently of generated trampoline names."""
    normalised = copy.deepcopy(None)
    normalised.name = "subject"
    normalised.decorator_list = []
    return payload_digest(ast.dump(normalised))


def x_definition_digest__mutmut_3(node: ast.FunctionDef | ast.AsyncFunctionDef) -> str:
    """Bind the executable definition, independently of generated trampoline names."""
    normalised = copy.copy(node)
    normalised.name = "subject"
    normalised.decorator_list = []
    return payload_digest(ast.dump(normalised))


def x_definition_digest__mutmut_4(node: ast.FunctionDef | ast.AsyncFunctionDef) -> str:
    """Bind the executable definition, independently of generated trampoline names."""
    normalised = copy.deepcopy(node)
    normalised.name = None
    normalised.decorator_list = []
    return payload_digest(ast.dump(normalised))


def x_definition_digest__mutmut_5(node: ast.FunctionDef | ast.AsyncFunctionDef) -> str:
    """Bind the executable definition, independently of generated trampoline names."""
    normalised = copy.deepcopy(node)
    normalised.name = "XXsubjectXX"
    normalised.decorator_list = []
    return payload_digest(ast.dump(normalised))


def x_definition_digest__mutmut_6(node: ast.FunctionDef | ast.AsyncFunctionDef) -> str:
    """Bind the executable definition, independently of generated trampoline names."""
    normalised = copy.deepcopy(node)
    normalised.name = "SUBJECT"
    normalised.decorator_list = []
    return payload_digest(ast.dump(normalised))


def x_definition_digest__mutmut_7(node: ast.FunctionDef | ast.AsyncFunctionDef) -> str:
    """Bind the executable definition, independently of generated trampoline names."""
    normalised = copy.deepcopy(node)
    normalised.name = "subject"
    normalised.decorator_list = None
    return payload_digest(ast.dump(normalised))


def x_definition_digest__mutmut_8(node: ast.FunctionDef | ast.AsyncFunctionDef) -> str:
    """Bind the executable definition, independently of generated trampoline names."""
    normalised = copy.deepcopy(node)
    normalised.name = "subject"
    normalised.decorator_list = []
    return payload_digest(None)


def x_definition_digest__mutmut_9(node: ast.FunctionDef | ast.AsyncFunctionDef) -> str:
    """Bind the executable definition, independently of generated trampoline names."""
    normalised = copy.deepcopy(node)
    normalised.name = "subject"
    normalised.decorator_list = []
    return payload_digest(ast.dump(None))

mutants_x_definition_digest__mutmut['_mutmut_orig'] = x_definition_digest__mutmut_orig # type: ignore # mutmut generated
mutants_x_definition_digest__mutmut['x_definition_digest__mutmut_1'] = x_definition_digest__mutmut_1 # type: ignore # mutmut generated
mutants_x_definition_digest__mutmut['x_definition_digest__mutmut_2'] = x_definition_digest__mutmut_2 # type: ignore # mutmut generated
mutants_x_definition_digest__mutmut['x_definition_digest__mutmut_3'] = x_definition_digest__mutmut_3 # type: ignore # mutmut generated
mutants_x_definition_digest__mutmut['x_definition_digest__mutmut_4'] = x_definition_digest__mutmut_4 # type: ignore # mutmut generated
mutants_x_definition_digest__mutmut['x_definition_digest__mutmut_5'] = x_definition_digest__mutmut_5 # type: ignore # mutmut generated
mutants_x_definition_digest__mutmut['x_definition_digest__mutmut_6'] = x_definition_digest__mutmut_6 # type: ignore # mutmut generated
mutants_x_definition_digest__mutmut['x_definition_digest__mutmut_7'] = x_definition_digest__mutmut_7 # type: ignore # mutmut generated
mutants_x_definition_digest__mutmut['x_definition_digest__mutmut_8'] = x_definition_digest__mutmut_8 # type: ignore # mutmut generated
mutants_x_definition_digest__mutmut['x_definition_digest__mutmut_9'] = x_definition_digest__mutmut_9 # type: ignore # mutmut generated
mutants_x__functions__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__functions__mutmut)
def _functions(
    files: dict[str, bytes], roots: list[str]
) -> tuple[dict[str, Function], dict[str, ast.Module]]:
    functions: dict[str, Function] = {}
    modules: dict[str, ast.Module] = {}
    for path, raw in sorted(files.items()):
        if not path.endswith(".py") or not any(path.startswith(root.rstrip("/") + "/") for root in roots):
            continue
        if re.search(rb"pragma\s*:\s*no\s+mutate", raw, re.IGNORECASE):
            raise MutationError(f"mutation suppression is forbidden: {path}")
        try:
            tree = ast.parse(raw, filename=path)
        except (SyntaxError, ValueError) as exc:
            raise MutationError(f"production Python cannot be parsed: {path}") from exc
        module = path.removesuffix(".py").replace("/", ".").removeprefix("src.")
        module = module.removesuffix(".__init__")
        modules[module] = tree
        for node in tree.body:
            candidates: list[tuple[ast.stmt, str | None]] = [(node, None)]
            if isinstance(node, ast.ClassDef):
                candidates = [(child, node.name) for child in node.body]
            for child, owner in candidates:
                if isinstance(child, ast.FunctionDef | ast.AsyncFunctionDef):
                    suffix = f"{owner}.{child.name}" if owner else child.name
                    name = f"{module}.{suffix}"
                    mangled = f"xǁ{owner}ǁ{child.name}" if owner else f"x_{child.name}"
                    functions[name] = Function(name, path, child, f"{module}.{mangled}__mutmut_*")
    return functions, modules


def x__functions__mutmut_orig(
    files: dict[str, bytes], roots: list[str]
) -> tuple[dict[str, Function], dict[str, ast.Module]]:
    functions: dict[str, Function] = {}
    modules: dict[str, ast.Module] = {}
    for path, raw in sorted(files.items()):
        if not path.endswith(".py") or not any(path.startswith(root.rstrip("/") + "/") for root in roots):
            continue
        if re.search(rb"pragma\s*:\s*no\s+mutate", raw, re.IGNORECASE):
            raise MutationError(f"mutation suppression is forbidden: {path}")
        try:
            tree = ast.parse(raw, filename=path)
        except (SyntaxError, ValueError) as exc:
            raise MutationError(f"production Python cannot be parsed: {path}") from exc
        module = path.removesuffix(".py").replace("/", ".").removeprefix("src.")
        module = module.removesuffix(".__init__")
        modules[module] = tree
        for node in tree.body:
            candidates: list[tuple[ast.stmt, str | None]] = [(node, None)]
            if isinstance(node, ast.ClassDef):
                candidates = [(child, node.name) for child in node.body]
            for child, owner in candidates:
                if isinstance(child, ast.FunctionDef | ast.AsyncFunctionDef):
                    suffix = f"{owner}.{child.name}" if owner else child.name
                    name = f"{module}.{suffix}"
                    mangled = f"xǁ{owner}ǁ{child.name}" if owner else f"x_{child.name}"
                    functions[name] = Function(name, path, child, f"{module}.{mangled}__mutmut_*")
    return functions, modules


def x__functions__mutmut_1(
    files: dict[str, bytes], roots: list[str]
) -> tuple[dict[str, Function], dict[str, ast.Module]]:
    functions: dict[str, Function] = None
    modules: dict[str, ast.Module] = {}
    for path, raw in sorted(files.items()):
        if not path.endswith(".py") or not any(path.startswith(root.rstrip("/") + "/") for root in roots):
            continue
        if re.search(rb"pragma\s*:\s*no\s+mutate", raw, re.IGNORECASE):
            raise MutationError(f"mutation suppression is forbidden: {path}")
        try:
            tree = ast.parse(raw, filename=path)
        except (SyntaxError, ValueError) as exc:
            raise MutationError(f"production Python cannot be parsed: {path}") from exc
        module = path.removesuffix(".py").replace("/", ".").removeprefix("src.")
        module = module.removesuffix(".__init__")
        modules[module] = tree
        for node in tree.body:
            candidates: list[tuple[ast.stmt, str | None]] = [(node, None)]
            if isinstance(node, ast.ClassDef):
                candidates = [(child, node.name) for child in node.body]
            for child, owner in candidates:
                if isinstance(child, ast.FunctionDef | ast.AsyncFunctionDef):
                    suffix = f"{owner}.{child.name}" if owner else child.name
                    name = f"{module}.{suffix}"
                    mangled = f"xǁ{owner}ǁ{child.name}" if owner else f"x_{child.name}"
                    functions[name] = Function(name, path, child, f"{module}.{mangled}__mutmut_*")
    return functions, modules


def x__functions__mutmut_2(
    files: dict[str, bytes], roots: list[str]
) -> tuple[dict[str, Function], dict[str, ast.Module]]:
    functions: dict[str, Function] = {}
    modules: dict[str, ast.Module] = None
    for path, raw in sorted(files.items()):
        if not path.endswith(".py") or not any(path.startswith(root.rstrip("/") + "/") for root in roots):
            continue
        if re.search(rb"pragma\s*:\s*no\s+mutate", raw, re.IGNORECASE):
            raise MutationError(f"mutation suppression is forbidden: {path}")
        try:
            tree = ast.parse(raw, filename=path)
        except (SyntaxError, ValueError) as exc:
            raise MutationError(f"production Python cannot be parsed: {path}") from exc
        module = path.removesuffix(".py").replace("/", ".").removeprefix("src.")
        module = module.removesuffix(".__init__")
        modules[module] = tree
        for node in tree.body:
            candidates: list[tuple[ast.stmt, str | None]] = [(node, None)]
            if isinstance(node, ast.ClassDef):
                candidates = [(child, node.name) for child in node.body]
            for child, owner in candidates:
                if isinstance(child, ast.FunctionDef | ast.AsyncFunctionDef):
                    suffix = f"{owner}.{child.name}" if owner else child.name
                    name = f"{module}.{suffix}"
                    mangled = f"xǁ{owner}ǁ{child.name}" if owner else f"x_{child.name}"
                    functions[name] = Function(name, path, child, f"{module}.{mangled}__mutmut_*")
    return functions, modules


def x__functions__mutmut_3(
    files: dict[str, bytes], roots: list[str]
) -> tuple[dict[str, Function], dict[str, ast.Module]]:
    functions: dict[str, Function] = {}
    modules: dict[str, ast.Module] = {}
    for path, raw in sorted(None):
        if not path.endswith(".py") or not any(path.startswith(root.rstrip("/") + "/") for root in roots):
            continue
        if re.search(rb"pragma\s*:\s*no\s+mutate", raw, re.IGNORECASE):
            raise MutationError(f"mutation suppression is forbidden: {path}")
        try:
            tree = ast.parse(raw, filename=path)
        except (SyntaxError, ValueError) as exc:
            raise MutationError(f"production Python cannot be parsed: {path}") from exc
        module = path.removesuffix(".py").replace("/", ".").removeprefix("src.")
        module = module.removesuffix(".__init__")
        modules[module] = tree
        for node in tree.body:
            candidates: list[tuple[ast.stmt, str | None]] = [(node, None)]
            if isinstance(node, ast.ClassDef):
                candidates = [(child, node.name) for child in node.body]
            for child, owner in candidates:
                if isinstance(child, ast.FunctionDef | ast.AsyncFunctionDef):
                    suffix = f"{owner}.{child.name}" if owner else child.name
                    name = f"{module}.{suffix}"
                    mangled = f"xǁ{owner}ǁ{child.name}" if owner else f"x_{child.name}"
                    functions[name] = Function(name, path, child, f"{module}.{mangled}__mutmut_*")
    return functions, modules


def x__functions__mutmut_4(
    files: dict[str, bytes], roots: list[str]
) -> tuple[dict[str, Function], dict[str, ast.Module]]:
    functions: dict[str, Function] = {}
    modules: dict[str, ast.Module] = {}
    for path, raw in sorted(files.items()):
        if not path.endswith(".py") and not any(path.startswith(root.rstrip("/") + "/") for root in roots):
            continue
        if re.search(rb"pragma\s*:\s*no\s+mutate", raw, re.IGNORECASE):
            raise MutationError(f"mutation suppression is forbidden: {path}")
        try:
            tree = ast.parse(raw, filename=path)
        except (SyntaxError, ValueError) as exc:
            raise MutationError(f"production Python cannot be parsed: {path}") from exc
        module = path.removesuffix(".py").replace("/", ".").removeprefix("src.")
        module = module.removesuffix(".__init__")
        modules[module] = tree
        for node in tree.body:
            candidates: list[tuple[ast.stmt, str | None]] = [(node, None)]
            if isinstance(node, ast.ClassDef):
                candidates = [(child, node.name) for child in node.body]
            for child, owner in candidates:
                if isinstance(child, ast.FunctionDef | ast.AsyncFunctionDef):
                    suffix = f"{owner}.{child.name}" if owner else child.name
                    name = f"{module}.{suffix}"
                    mangled = f"xǁ{owner}ǁ{child.name}" if owner else f"x_{child.name}"
                    functions[name] = Function(name, path, child, f"{module}.{mangled}__mutmut_*")
    return functions, modules


def x__functions__mutmut_5(
    files: dict[str, bytes], roots: list[str]
) -> tuple[dict[str, Function], dict[str, ast.Module]]:
    functions: dict[str, Function] = {}
    modules: dict[str, ast.Module] = {}
    for path, raw in sorted(files.items()):
        if path.endswith(".py") or not any(path.startswith(root.rstrip("/") + "/") for root in roots):
            continue
        if re.search(rb"pragma\s*:\s*no\s+mutate", raw, re.IGNORECASE):
            raise MutationError(f"mutation suppression is forbidden: {path}")
        try:
            tree = ast.parse(raw, filename=path)
        except (SyntaxError, ValueError) as exc:
            raise MutationError(f"production Python cannot be parsed: {path}") from exc
        module = path.removesuffix(".py").replace("/", ".").removeprefix("src.")
        module = module.removesuffix(".__init__")
        modules[module] = tree
        for node in tree.body:
            candidates: list[tuple[ast.stmt, str | None]] = [(node, None)]
            if isinstance(node, ast.ClassDef):
                candidates = [(child, node.name) for child in node.body]
            for child, owner in candidates:
                if isinstance(child, ast.FunctionDef | ast.AsyncFunctionDef):
                    suffix = f"{owner}.{child.name}" if owner else child.name
                    name = f"{module}.{suffix}"
                    mangled = f"xǁ{owner}ǁ{child.name}" if owner else f"x_{child.name}"
                    functions[name] = Function(name, path, child, f"{module}.{mangled}__mutmut_*")
    return functions, modules


def x__functions__mutmut_6(
    files: dict[str, bytes], roots: list[str]
) -> tuple[dict[str, Function], dict[str, ast.Module]]:
    functions: dict[str, Function] = {}
    modules: dict[str, ast.Module] = {}
    for path, raw in sorted(files.items()):
        if not path.endswith(None) or not any(path.startswith(root.rstrip("/") + "/") for root in roots):
            continue
        if re.search(rb"pragma\s*:\s*no\s+mutate", raw, re.IGNORECASE):
            raise MutationError(f"mutation suppression is forbidden: {path}")
        try:
            tree = ast.parse(raw, filename=path)
        except (SyntaxError, ValueError) as exc:
            raise MutationError(f"production Python cannot be parsed: {path}") from exc
        module = path.removesuffix(".py").replace("/", ".").removeprefix("src.")
        module = module.removesuffix(".__init__")
        modules[module] = tree
        for node in tree.body:
            candidates: list[tuple[ast.stmt, str | None]] = [(node, None)]
            if isinstance(node, ast.ClassDef):
                candidates = [(child, node.name) for child in node.body]
            for child, owner in candidates:
                if isinstance(child, ast.FunctionDef | ast.AsyncFunctionDef):
                    suffix = f"{owner}.{child.name}" if owner else child.name
                    name = f"{module}.{suffix}"
                    mangled = f"xǁ{owner}ǁ{child.name}" if owner else f"x_{child.name}"
                    functions[name] = Function(name, path, child, f"{module}.{mangled}__mutmut_*")
    return functions, modules


def x__functions__mutmut_7(
    files: dict[str, bytes], roots: list[str]
) -> tuple[dict[str, Function], dict[str, ast.Module]]:
    functions: dict[str, Function] = {}
    modules: dict[str, ast.Module] = {}
    for path, raw in sorted(files.items()):
        if not path.endswith("XX.pyXX") or not any(path.startswith(root.rstrip("/") + "/") for root in roots):
            continue
        if re.search(rb"pragma\s*:\s*no\s+mutate", raw, re.IGNORECASE):
            raise MutationError(f"mutation suppression is forbidden: {path}")
        try:
            tree = ast.parse(raw, filename=path)
        except (SyntaxError, ValueError) as exc:
            raise MutationError(f"production Python cannot be parsed: {path}") from exc
        module = path.removesuffix(".py").replace("/", ".").removeprefix("src.")
        module = module.removesuffix(".__init__")
        modules[module] = tree
        for node in tree.body:
            candidates: list[tuple[ast.stmt, str | None]] = [(node, None)]
            if isinstance(node, ast.ClassDef):
                candidates = [(child, node.name) for child in node.body]
            for child, owner in candidates:
                if isinstance(child, ast.FunctionDef | ast.AsyncFunctionDef):
                    suffix = f"{owner}.{child.name}" if owner else child.name
                    name = f"{module}.{suffix}"
                    mangled = f"xǁ{owner}ǁ{child.name}" if owner else f"x_{child.name}"
                    functions[name] = Function(name, path, child, f"{module}.{mangled}__mutmut_*")
    return functions, modules


def x__functions__mutmut_8(
    files: dict[str, bytes], roots: list[str]
) -> tuple[dict[str, Function], dict[str, ast.Module]]:
    functions: dict[str, Function] = {}
    modules: dict[str, ast.Module] = {}
    for path, raw in sorted(files.items()):
        if not path.endswith(".PY") or not any(path.startswith(root.rstrip("/") + "/") for root in roots):
            continue
        if re.search(rb"pragma\s*:\s*no\s+mutate", raw, re.IGNORECASE):
            raise MutationError(f"mutation suppression is forbidden: {path}")
        try:
            tree = ast.parse(raw, filename=path)
        except (SyntaxError, ValueError) as exc:
            raise MutationError(f"production Python cannot be parsed: {path}") from exc
        module = path.removesuffix(".py").replace("/", ".").removeprefix("src.")
        module = module.removesuffix(".__init__")
        modules[module] = tree
        for node in tree.body:
            candidates: list[tuple[ast.stmt, str | None]] = [(node, None)]
            if isinstance(node, ast.ClassDef):
                candidates = [(child, node.name) for child in node.body]
            for child, owner in candidates:
                if isinstance(child, ast.FunctionDef | ast.AsyncFunctionDef):
                    suffix = f"{owner}.{child.name}" if owner else child.name
                    name = f"{module}.{suffix}"
                    mangled = f"xǁ{owner}ǁ{child.name}" if owner else f"x_{child.name}"
                    functions[name] = Function(name, path, child, f"{module}.{mangled}__mutmut_*")
    return functions, modules


def x__functions__mutmut_9(
    files: dict[str, bytes], roots: list[str]
) -> tuple[dict[str, Function], dict[str, ast.Module]]:
    functions: dict[str, Function] = {}
    modules: dict[str, ast.Module] = {}
    for path, raw in sorted(files.items()):
        if not path.endswith(".py") or any(path.startswith(root.rstrip("/") + "/") for root in roots):
            continue
        if re.search(rb"pragma\s*:\s*no\s+mutate", raw, re.IGNORECASE):
            raise MutationError(f"mutation suppression is forbidden: {path}")
        try:
            tree = ast.parse(raw, filename=path)
        except (SyntaxError, ValueError) as exc:
            raise MutationError(f"production Python cannot be parsed: {path}") from exc
        module = path.removesuffix(".py").replace("/", ".").removeprefix("src.")
        module = module.removesuffix(".__init__")
        modules[module] = tree
        for node in tree.body:
            candidates: list[tuple[ast.stmt, str | None]] = [(node, None)]
            if isinstance(node, ast.ClassDef):
                candidates = [(child, node.name) for child in node.body]
            for child, owner in candidates:
                if isinstance(child, ast.FunctionDef | ast.AsyncFunctionDef):
                    suffix = f"{owner}.{child.name}" if owner else child.name
                    name = f"{module}.{suffix}"
                    mangled = f"xǁ{owner}ǁ{child.name}" if owner else f"x_{child.name}"
                    functions[name] = Function(name, path, child, f"{module}.{mangled}__mutmut_*")
    return functions, modules


def x__functions__mutmut_10(
    files: dict[str, bytes], roots: list[str]
) -> tuple[dict[str, Function], dict[str, ast.Module]]:
    functions: dict[str, Function] = {}
    modules: dict[str, ast.Module] = {}
    for path, raw in sorted(files.items()):
        if not path.endswith(".py") or not any(None):
            continue
        if re.search(rb"pragma\s*:\s*no\s+mutate", raw, re.IGNORECASE):
            raise MutationError(f"mutation suppression is forbidden: {path}")
        try:
            tree = ast.parse(raw, filename=path)
        except (SyntaxError, ValueError) as exc:
            raise MutationError(f"production Python cannot be parsed: {path}") from exc
        module = path.removesuffix(".py").replace("/", ".").removeprefix("src.")
        module = module.removesuffix(".__init__")
        modules[module] = tree
        for node in tree.body:
            candidates: list[tuple[ast.stmt, str | None]] = [(node, None)]
            if isinstance(node, ast.ClassDef):
                candidates = [(child, node.name) for child in node.body]
            for child, owner in candidates:
                if isinstance(child, ast.FunctionDef | ast.AsyncFunctionDef):
                    suffix = f"{owner}.{child.name}" if owner else child.name
                    name = f"{module}.{suffix}"
                    mangled = f"xǁ{owner}ǁ{child.name}" if owner else f"x_{child.name}"
                    functions[name] = Function(name, path, child, f"{module}.{mangled}__mutmut_*")
    return functions, modules


def x__functions__mutmut_11(
    files: dict[str, bytes], roots: list[str]
) -> tuple[dict[str, Function], dict[str, ast.Module]]:
    functions: dict[str, Function] = {}
    modules: dict[str, ast.Module] = {}
    for path, raw in sorted(files.items()):
        if not path.endswith(".py") or not any(path.startswith(None) for root in roots):
            continue
        if re.search(rb"pragma\s*:\s*no\s+mutate", raw, re.IGNORECASE):
            raise MutationError(f"mutation suppression is forbidden: {path}")
        try:
            tree = ast.parse(raw, filename=path)
        except (SyntaxError, ValueError) as exc:
            raise MutationError(f"production Python cannot be parsed: {path}") from exc
        module = path.removesuffix(".py").replace("/", ".").removeprefix("src.")
        module = module.removesuffix(".__init__")
        modules[module] = tree
        for node in tree.body:
            candidates: list[tuple[ast.stmt, str | None]] = [(node, None)]
            if isinstance(node, ast.ClassDef):
                candidates = [(child, node.name) for child in node.body]
            for child, owner in candidates:
                if isinstance(child, ast.FunctionDef | ast.AsyncFunctionDef):
                    suffix = f"{owner}.{child.name}" if owner else child.name
                    name = f"{module}.{suffix}"
                    mangled = f"xǁ{owner}ǁ{child.name}" if owner else f"x_{child.name}"
                    functions[name] = Function(name, path, child, f"{module}.{mangled}__mutmut_*")
    return functions, modules


def x__functions__mutmut_12(
    files: dict[str, bytes], roots: list[str]
) -> tuple[dict[str, Function], dict[str, ast.Module]]:
    functions: dict[str, Function] = {}
    modules: dict[str, ast.Module] = {}
    for path, raw in sorted(files.items()):
        if not path.endswith(".py") or not any(path.startswith(root.rstrip("/") - "/") for root in roots):
            continue
        if re.search(rb"pragma\s*:\s*no\s+mutate", raw, re.IGNORECASE):
            raise MutationError(f"mutation suppression is forbidden: {path}")
        try:
            tree = ast.parse(raw, filename=path)
        except (SyntaxError, ValueError) as exc:
            raise MutationError(f"production Python cannot be parsed: {path}") from exc
        module = path.removesuffix(".py").replace("/", ".").removeprefix("src.")
        module = module.removesuffix(".__init__")
        modules[module] = tree
        for node in tree.body:
            candidates: list[tuple[ast.stmt, str | None]] = [(node, None)]
            if isinstance(node, ast.ClassDef):
                candidates = [(child, node.name) for child in node.body]
            for child, owner in candidates:
                if isinstance(child, ast.FunctionDef | ast.AsyncFunctionDef):
                    suffix = f"{owner}.{child.name}" if owner else child.name
                    name = f"{module}.{suffix}"
                    mangled = f"xǁ{owner}ǁ{child.name}" if owner else f"x_{child.name}"
                    functions[name] = Function(name, path, child, f"{module}.{mangled}__mutmut_*")
    return functions, modules


def x__functions__mutmut_13(
    files: dict[str, bytes], roots: list[str]
) -> tuple[dict[str, Function], dict[str, ast.Module]]:
    functions: dict[str, Function] = {}
    modules: dict[str, ast.Module] = {}
    for path, raw in sorted(files.items()):
        if not path.endswith(".py") or not any(path.startswith(root.rstrip(None) + "/") for root in roots):
            continue
        if re.search(rb"pragma\s*:\s*no\s+mutate", raw, re.IGNORECASE):
            raise MutationError(f"mutation suppression is forbidden: {path}")
        try:
            tree = ast.parse(raw, filename=path)
        except (SyntaxError, ValueError) as exc:
            raise MutationError(f"production Python cannot be parsed: {path}") from exc
        module = path.removesuffix(".py").replace("/", ".").removeprefix("src.")
        module = module.removesuffix(".__init__")
        modules[module] = tree
        for node in tree.body:
            candidates: list[tuple[ast.stmt, str | None]] = [(node, None)]
            if isinstance(node, ast.ClassDef):
                candidates = [(child, node.name) for child in node.body]
            for child, owner in candidates:
                if isinstance(child, ast.FunctionDef | ast.AsyncFunctionDef):
                    suffix = f"{owner}.{child.name}" if owner else child.name
                    name = f"{module}.{suffix}"
                    mangled = f"xǁ{owner}ǁ{child.name}" if owner else f"x_{child.name}"
                    functions[name] = Function(name, path, child, f"{module}.{mangled}__mutmut_*")
    return functions, modules


def x__functions__mutmut_14(
    files: dict[str, bytes], roots: list[str]
) -> tuple[dict[str, Function], dict[str, ast.Module]]:
    functions: dict[str, Function] = {}
    modules: dict[str, ast.Module] = {}
    for path, raw in sorted(files.items()):
        if not path.endswith(".py") or not any(path.startswith(root.lstrip("/") + "/") for root in roots):
            continue
        if re.search(rb"pragma\s*:\s*no\s+mutate", raw, re.IGNORECASE):
            raise MutationError(f"mutation suppression is forbidden: {path}")
        try:
            tree = ast.parse(raw, filename=path)
        except (SyntaxError, ValueError) as exc:
            raise MutationError(f"production Python cannot be parsed: {path}") from exc
        module = path.removesuffix(".py").replace("/", ".").removeprefix("src.")
        module = module.removesuffix(".__init__")
        modules[module] = tree
        for node in tree.body:
            candidates: list[tuple[ast.stmt, str | None]] = [(node, None)]
            if isinstance(node, ast.ClassDef):
                candidates = [(child, node.name) for child in node.body]
            for child, owner in candidates:
                if isinstance(child, ast.FunctionDef | ast.AsyncFunctionDef):
                    suffix = f"{owner}.{child.name}" if owner else child.name
                    name = f"{module}.{suffix}"
                    mangled = f"xǁ{owner}ǁ{child.name}" if owner else f"x_{child.name}"
                    functions[name] = Function(name, path, child, f"{module}.{mangled}__mutmut_*")
    return functions, modules


def x__functions__mutmut_15(
    files: dict[str, bytes], roots: list[str]
) -> tuple[dict[str, Function], dict[str, ast.Module]]:
    functions: dict[str, Function] = {}
    modules: dict[str, ast.Module] = {}
    for path, raw in sorted(files.items()):
        if not path.endswith(".py") or not any(path.startswith(root.rstrip("XX/XX") + "/") for root in roots):
            continue
        if re.search(rb"pragma\s*:\s*no\s+mutate", raw, re.IGNORECASE):
            raise MutationError(f"mutation suppression is forbidden: {path}")
        try:
            tree = ast.parse(raw, filename=path)
        except (SyntaxError, ValueError) as exc:
            raise MutationError(f"production Python cannot be parsed: {path}") from exc
        module = path.removesuffix(".py").replace("/", ".").removeprefix("src.")
        module = module.removesuffix(".__init__")
        modules[module] = tree
        for node in tree.body:
            candidates: list[tuple[ast.stmt, str | None]] = [(node, None)]
            if isinstance(node, ast.ClassDef):
                candidates = [(child, node.name) for child in node.body]
            for child, owner in candidates:
                if isinstance(child, ast.FunctionDef | ast.AsyncFunctionDef):
                    suffix = f"{owner}.{child.name}" if owner else child.name
                    name = f"{module}.{suffix}"
                    mangled = f"xǁ{owner}ǁ{child.name}" if owner else f"x_{child.name}"
                    functions[name] = Function(name, path, child, f"{module}.{mangled}__mutmut_*")
    return functions, modules


def x__functions__mutmut_16(
    files: dict[str, bytes], roots: list[str]
) -> tuple[dict[str, Function], dict[str, ast.Module]]:
    functions: dict[str, Function] = {}
    modules: dict[str, ast.Module] = {}
    for path, raw in sorted(files.items()):
        if not path.endswith(".py") or not any(path.startswith(root.rstrip("/") + "XX/XX") for root in roots):
            continue
        if re.search(rb"pragma\s*:\s*no\s+mutate", raw, re.IGNORECASE):
            raise MutationError(f"mutation suppression is forbidden: {path}")
        try:
            tree = ast.parse(raw, filename=path)
        except (SyntaxError, ValueError) as exc:
            raise MutationError(f"production Python cannot be parsed: {path}") from exc
        module = path.removesuffix(".py").replace("/", ".").removeprefix("src.")
        module = module.removesuffix(".__init__")
        modules[module] = tree
        for node in tree.body:
            candidates: list[tuple[ast.stmt, str | None]] = [(node, None)]
            if isinstance(node, ast.ClassDef):
                candidates = [(child, node.name) for child in node.body]
            for child, owner in candidates:
                if isinstance(child, ast.FunctionDef | ast.AsyncFunctionDef):
                    suffix = f"{owner}.{child.name}" if owner else child.name
                    name = f"{module}.{suffix}"
                    mangled = f"xǁ{owner}ǁ{child.name}" if owner else f"x_{child.name}"
                    functions[name] = Function(name, path, child, f"{module}.{mangled}__mutmut_*")
    return functions, modules


def x__functions__mutmut_17(
    files: dict[str, bytes], roots: list[str]
) -> tuple[dict[str, Function], dict[str, ast.Module]]:
    functions: dict[str, Function] = {}
    modules: dict[str, ast.Module] = {}
    for path, raw in sorted(files.items()):
        if not path.endswith(".py") or not any(path.startswith(root.rstrip("/") + "/") for root in roots):
            break
        if re.search(rb"pragma\s*:\s*no\s+mutate", raw, re.IGNORECASE):
            raise MutationError(f"mutation suppression is forbidden: {path}")
        try:
            tree = ast.parse(raw, filename=path)
        except (SyntaxError, ValueError) as exc:
            raise MutationError(f"production Python cannot be parsed: {path}") from exc
        module = path.removesuffix(".py").replace("/", ".").removeprefix("src.")
        module = module.removesuffix(".__init__")
        modules[module] = tree
        for node in tree.body:
            candidates: list[tuple[ast.stmt, str | None]] = [(node, None)]
            if isinstance(node, ast.ClassDef):
                candidates = [(child, node.name) for child in node.body]
            for child, owner in candidates:
                if isinstance(child, ast.FunctionDef | ast.AsyncFunctionDef):
                    suffix = f"{owner}.{child.name}" if owner else child.name
                    name = f"{module}.{suffix}"
                    mangled = f"xǁ{owner}ǁ{child.name}" if owner else f"x_{child.name}"
                    functions[name] = Function(name, path, child, f"{module}.{mangled}__mutmut_*")
    return functions, modules


def x__functions__mutmut_18(
    files: dict[str, bytes], roots: list[str]
) -> tuple[dict[str, Function], dict[str, ast.Module]]:
    functions: dict[str, Function] = {}
    modules: dict[str, ast.Module] = {}
    for path, raw in sorted(files.items()):
        if not path.endswith(".py") or not any(path.startswith(root.rstrip("/") + "/") for root in roots):
            continue
        if re.search(None, raw, re.IGNORECASE):
            raise MutationError(f"mutation suppression is forbidden: {path}")
        try:
            tree = ast.parse(raw, filename=path)
        except (SyntaxError, ValueError) as exc:
            raise MutationError(f"production Python cannot be parsed: {path}") from exc
        module = path.removesuffix(".py").replace("/", ".").removeprefix("src.")
        module = module.removesuffix(".__init__")
        modules[module] = tree
        for node in tree.body:
            candidates: list[tuple[ast.stmt, str | None]] = [(node, None)]
            if isinstance(node, ast.ClassDef):
                candidates = [(child, node.name) for child in node.body]
            for child, owner in candidates:
                if isinstance(child, ast.FunctionDef | ast.AsyncFunctionDef):
                    suffix = f"{owner}.{child.name}" if owner else child.name
                    name = f"{module}.{suffix}"
                    mangled = f"xǁ{owner}ǁ{child.name}" if owner else f"x_{child.name}"
                    functions[name] = Function(name, path, child, f"{module}.{mangled}__mutmut_*")
    return functions, modules


def x__functions__mutmut_19(
    files: dict[str, bytes], roots: list[str]
) -> tuple[dict[str, Function], dict[str, ast.Module]]:
    functions: dict[str, Function] = {}
    modules: dict[str, ast.Module] = {}
    for path, raw in sorted(files.items()):
        if not path.endswith(".py") or not any(path.startswith(root.rstrip("/") + "/") for root in roots):
            continue
        if re.search(rb"pragma\s*:\s*no\s+mutate", None, re.IGNORECASE):
            raise MutationError(f"mutation suppression is forbidden: {path}")
        try:
            tree = ast.parse(raw, filename=path)
        except (SyntaxError, ValueError) as exc:
            raise MutationError(f"production Python cannot be parsed: {path}") from exc
        module = path.removesuffix(".py").replace("/", ".").removeprefix("src.")
        module = module.removesuffix(".__init__")
        modules[module] = tree
        for node in tree.body:
            candidates: list[tuple[ast.stmt, str | None]] = [(node, None)]
            if isinstance(node, ast.ClassDef):
                candidates = [(child, node.name) for child in node.body]
            for child, owner in candidates:
                if isinstance(child, ast.FunctionDef | ast.AsyncFunctionDef):
                    suffix = f"{owner}.{child.name}" if owner else child.name
                    name = f"{module}.{suffix}"
                    mangled = f"xǁ{owner}ǁ{child.name}" if owner else f"x_{child.name}"
                    functions[name] = Function(name, path, child, f"{module}.{mangled}__mutmut_*")
    return functions, modules


def x__functions__mutmut_20(
    files: dict[str, bytes], roots: list[str]
) -> tuple[dict[str, Function], dict[str, ast.Module]]:
    functions: dict[str, Function] = {}
    modules: dict[str, ast.Module] = {}
    for path, raw in sorted(files.items()):
        if not path.endswith(".py") or not any(path.startswith(root.rstrip("/") + "/") for root in roots):
            continue
        if re.search(rb"pragma\s*:\s*no\s+mutate", raw, None):
            raise MutationError(f"mutation suppression is forbidden: {path}")
        try:
            tree = ast.parse(raw, filename=path)
        except (SyntaxError, ValueError) as exc:
            raise MutationError(f"production Python cannot be parsed: {path}") from exc
        module = path.removesuffix(".py").replace("/", ".").removeprefix("src.")
        module = module.removesuffix(".__init__")
        modules[module] = tree
        for node in tree.body:
            candidates: list[tuple[ast.stmt, str | None]] = [(node, None)]
            if isinstance(node, ast.ClassDef):
                candidates = [(child, node.name) for child in node.body]
            for child, owner in candidates:
                if isinstance(child, ast.FunctionDef | ast.AsyncFunctionDef):
                    suffix = f"{owner}.{child.name}" if owner else child.name
                    name = f"{module}.{suffix}"
                    mangled = f"xǁ{owner}ǁ{child.name}" if owner else f"x_{child.name}"
                    functions[name] = Function(name, path, child, f"{module}.{mangled}__mutmut_*")
    return functions, modules


def x__functions__mutmut_21(
    files: dict[str, bytes], roots: list[str]
) -> tuple[dict[str, Function], dict[str, ast.Module]]:
    functions: dict[str, Function] = {}
    modules: dict[str, ast.Module] = {}
    for path, raw in sorted(files.items()):
        if not path.endswith(".py") or not any(path.startswith(root.rstrip("/") + "/") for root in roots):
            continue
        if re.search(raw, re.IGNORECASE):
            raise MutationError(f"mutation suppression is forbidden: {path}")
        try:
            tree = ast.parse(raw, filename=path)
        except (SyntaxError, ValueError) as exc:
            raise MutationError(f"production Python cannot be parsed: {path}") from exc
        module = path.removesuffix(".py").replace("/", ".").removeprefix("src.")
        module = module.removesuffix(".__init__")
        modules[module] = tree
        for node in tree.body:
            candidates: list[tuple[ast.stmt, str | None]] = [(node, None)]
            if isinstance(node, ast.ClassDef):
                candidates = [(child, node.name) for child in node.body]
            for child, owner in candidates:
                if isinstance(child, ast.FunctionDef | ast.AsyncFunctionDef):
                    suffix = f"{owner}.{child.name}" if owner else child.name
                    name = f"{module}.{suffix}"
                    mangled = f"xǁ{owner}ǁ{child.name}" if owner else f"x_{child.name}"
                    functions[name] = Function(name, path, child, f"{module}.{mangled}__mutmut_*")
    return functions, modules


def x__functions__mutmut_22(
    files: dict[str, bytes], roots: list[str]
) -> tuple[dict[str, Function], dict[str, ast.Module]]:
    functions: dict[str, Function] = {}
    modules: dict[str, ast.Module] = {}
    for path, raw in sorted(files.items()):
        if not path.endswith(".py") or not any(path.startswith(root.rstrip("/") + "/") for root in roots):
            continue
        if re.search(rb"pragma\s*:\s*no\s+mutate", re.IGNORECASE):
            raise MutationError(f"mutation suppression is forbidden: {path}")
        try:
            tree = ast.parse(raw, filename=path)
        except (SyntaxError, ValueError) as exc:
            raise MutationError(f"production Python cannot be parsed: {path}") from exc
        module = path.removesuffix(".py").replace("/", ".").removeprefix("src.")
        module = module.removesuffix(".__init__")
        modules[module] = tree
        for node in tree.body:
            candidates: list[tuple[ast.stmt, str | None]] = [(node, None)]
            if isinstance(node, ast.ClassDef):
                candidates = [(child, node.name) for child in node.body]
            for child, owner in candidates:
                if isinstance(child, ast.FunctionDef | ast.AsyncFunctionDef):
                    suffix = f"{owner}.{child.name}" if owner else child.name
                    name = f"{module}.{suffix}"
                    mangled = f"xǁ{owner}ǁ{child.name}" if owner else f"x_{child.name}"
                    functions[name] = Function(name, path, child, f"{module}.{mangled}__mutmut_*")
    return functions, modules


def x__functions__mutmut_23(
    files: dict[str, bytes], roots: list[str]
) -> tuple[dict[str, Function], dict[str, ast.Module]]:
    functions: dict[str, Function] = {}
    modules: dict[str, ast.Module] = {}
    for path, raw in sorted(files.items()):
        if not path.endswith(".py") or not any(path.startswith(root.rstrip("/") + "/") for root in roots):
            continue
        if re.search(rb"pragma\s*:\s*no\s+mutate", raw, ):
            raise MutationError(f"mutation suppression is forbidden: {path}")
        try:
            tree = ast.parse(raw, filename=path)
        except (SyntaxError, ValueError) as exc:
            raise MutationError(f"production Python cannot be parsed: {path}") from exc
        module = path.removesuffix(".py").replace("/", ".").removeprefix("src.")
        module = module.removesuffix(".__init__")
        modules[module] = tree
        for node in tree.body:
            candidates: list[tuple[ast.stmt, str | None]] = [(node, None)]
            if isinstance(node, ast.ClassDef):
                candidates = [(child, node.name) for child in node.body]
            for child, owner in candidates:
                if isinstance(child, ast.FunctionDef | ast.AsyncFunctionDef):
                    suffix = f"{owner}.{child.name}" if owner else child.name
                    name = f"{module}.{suffix}"
                    mangled = f"xǁ{owner}ǁ{child.name}" if owner else f"x_{child.name}"
                    functions[name] = Function(name, path, child, f"{module}.{mangled}__mutmut_*")
    return functions, modules


def x__functions__mutmut_24(
    files: dict[str, bytes], roots: list[str]
) -> tuple[dict[str, Function], dict[str, ast.Module]]:
    functions: dict[str, Function] = {}
    modules: dict[str, ast.Module] = {}
    for path, raw in sorted(files.items()):
        if not path.endswith(".py") or not any(path.startswith(root.rstrip("/") + "/") for root in roots):
            continue
        if re.search(rb"XXpragma\s*:\s*no\s+mutateXX", raw, re.IGNORECASE):
            raise MutationError(f"mutation suppression is forbidden: {path}")
        try:
            tree = ast.parse(raw, filename=path)
        except (SyntaxError, ValueError) as exc:
            raise MutationError(f"production Python cannot be parsed: {path}") from exc
        module = path.removesuffix(".py").replace("/", ".").removeprefix("src.")
        module = module.removesuffix(".__init__")
        modules[module] = tree
        for node in tree.body:
            candidates: list[tuple[ast.stmt, str | None]] = [(node, None)]
            if isinstance(node, ast.ClassDef):
                candidates = [(child, node.name) for child in node.body]
            for child, owner in candidates:
                if isinstance(child, ast.FunctionDef | ast.AsyncFunctionDef):
                    suffix = f"{owner}.{child.name}" if owner else child.name
                    name = f"{module}.{suffix}"
                    mangled = f"xǁ{owner}ǁ{child.name}" if owner else f"x_{child.name}"
                    functions[name] = Function(name, path, child, f"{module}.{mangled}__mutmut_*")
    return functions, modules


def x__functions__mutmut_25(
    files: dict[str, bytes], roots: list[str]
) -> tuple[dict[str, Function], dict[str, ast.Module]]:
    functions: dict[str, Function] = {}
    modules: dict[str, ast.Module] = {}
    for path, raw in sorted(files.items()):
        if not path.endswith(".py") or not any(path.startswith(root.rstrip("/") + "/") for root in roots):
            continue
        if re.search(rb"PRAGMA\s*:\s*NO\s+MUTATE", raw, re.IGNORECASE):
            raise MutationError(f"mutation suppression is forbidden: {path}")
        try:
            tree = ast.parse(raw, filename=path)
        except (SyntaxError, ValueError) as exc:
            raise MutationError(f"production Python cannot be parsed: {path}") from exc
        module = path.removesuffix(".py").replace("/", ".").removeprefix("src.")
        module = module.removesuffix(".__init__")
        modules[module] = tree
        for node in tree.body:
            candidates: list[tuple[ast.stmt, str | None]] = [(node, None)]
            if isinstance(node, ast.ClassDef):
                candidates = [(child, node.name) for child in node.body]
            for child, owner in candidates:
                if isinstance(child, ast.FunctionDef | ast.AsyncFunctionDef):
                    suffix = f"{owner}.{child.name}" if owner else child.name
                    name = f"{module}.{suffix}"
                    mangled = f"xǁ{owner}ǁ{child.name}" if owner else f"x_{child.name}"
                    functions[name] = Function(name, path, child, f"{module}.{mangled}__mutmut_*")
    return functions, modules


def x__functions__mutmut_26(
    files: dict[str, bytes], roots: list[str]
) -> tuple[dict[str, Function], dict[str, ast.Module]]:
    functions: dict[str, Function] = {}
    modules: dict[str, ast.Module] = {}
    for path, raw in sorted(files.items()):
        if not path.endswith(".py") or not any(path.startswith(root.rstrip("/") + "/") for root in roots):
            continue
        if re.search(rb"pragma\s*:\s*no\s+mutate", raw, re.IGNORECASE):
            raise MutationError(None)
        try:
            tree = ast.parse(raw, filename=path)
        except (SyntaxError, ValueError) as exc:
            raise MutationError(f"production Python cannot be parsed: {path}") from exc
        module = path.removesuffix(".py").replace("/", ".").removeprefix("src.")
        module = module.removesuffix(".__init__")
        modules[module] = tree
        for node in tree.body:
            candidates: list[tuple[ast.stmt, str | None]] = [(node, None)]
            if isinstance(node, ast.ClassDef):
                candidates = [(child, node.name) for child in node.body]
            for child, owner in candidates:
                if isinstance(child, ast.FunctionDef | ast.AsyncFunctionDef):
                    suffix = f"{owner}.{child.name}" if owner else child.name
                    name = f"{module}.{suffix}"
                    mangled = f"xǁ{owner}ǁ{child.name}" if owner else f"x_{child.name}"
                    functions[name] = Function(name, path, child, f"{module}.{mangled}__mutmut_*")
    return functions, modules


def x__functions__mutmut_27(
    files: dict[str, bytes], roots: list[str]
) -> tuple[dict[str, Function], dict[str, ast.Module]]:
    functions: dict[str, Function] = {}
    modules: dict[str, ast.Module] = {}
    for path, raw in sorted(files.items()):
        if not path.endswith(".py") or not any(path.startswith(root.rstrip("/") + "/") for root in roots):
            continue
        if re.search(rb"pragma\s*:\s*no\s+mutate", raw, re.IGNORECASE):
            raise MutationError(f"mutation suppression is forbidden: {path}")
        try:
            tree = None
        except (SyntaxError, ValueError) as exc:
            raise MutationError(f"production Python cannot be parsed: {path}") from exc
        module = path.removesuffix(".py").replace("/", ".").removeprefix("src.")
        module = module.removesuffix(".__init__")
        modules[module] = tree
        for node in tree.body:
            candidates: list[tuple[ast.stmt, str | None]] = [(node, None)]
            if isinstance(node, ast.ClassDef):
                candidates = [(child, node.name) for child in node.body]
            for child, owner in candidates:
                if isinstance(child, ast.FunctionDef | ast.AsyncFunctionDef):
                    suffix = f"{owner}.{child.name}" if owner else child.name
                    name = f"{module}.{suffix}"
                    mangled = f"xǁ{owner}ǁ{child.name}" if owner else f"x_{child.name}"
                    functions[name] = Function(name, path, child, f"{module}.{mangled}__mutmut_*")
    return functions, modules


def x__functions__mutmut_28(
    files: dict[str, bytes], roots: list[str]
) -> tuple[dict[str, Function], dict[str, ast.Module]]:
    functions: dict[str, Function] = {}
    modules: dict[str, ast.Module] = {}
    for path, raw in sorted(files.items()):
        if not path.endswith(".py") or not any(path.startswith(root.rstrip("/") + "/") for root in roots):
            continue
        if re.search(rb"pragma\s*:\s*no\s+mutate", raw, re.IGNORECASE):
            raise MutationError(f"mutation suppression is forbidden: {path}")
        try:
            tree = ast.parse(None, filename=path)
        except (SyntaxError, ValueError) as exc:
            raise MutationError(f"production Python cannot be parsed: {path}") from exc
        module = path.removesuffix(".py").replace("/", ".").removeprefix("src.")
        module = module.removesuffix(".__init__")
        modules[module] = tree
        for node in tree.body:
            candidates: list[tuple[ast.stmt, str | None]] = [(node, None)]
            if isinstance(node, ast.ClassDef):
                candidates = [(child, node.name) for child in node.body]
            for child, owner in candidates:
                if isinstance(child, ast.FunctionDef | ast.AsyncFunctionDef):
                    suffix = f"{owner}.{child.name}" if owner else child.name
                    name = f"{module}.{suffix}"
                    mangled = f"xǁ{owner}ǁ{child.name}" if owner else f"x_{child.name}"
                    functions[name] = Function(name, path, child, f"{module}.{mangled}__mutmut_*")
    return functions, modules


def x__functions__mutmut_29(
    files: dict[str, bytes], roots: list[str]
) -> tuple[dict[str, Function], dict[str, ast.Module]]:
    functions: dict[str, Function] = {}
    modules: dict[str, ast.Module] = {}
    for path, raw in sorted(files.items()):
        if not path.endswith(".py") or not any(path.startswith(root.rstrip("/") + "/") for root in roots):
            continue
        if re.search(rb"pragma\s*:\s*no\s+mutate", raw, re.IGNORECASE):
            raise MutationError(f"mutation suppression is forbidden: {path}")
        try:
            tree = ast.parse(raw, filename=None)
        except (SyntaxError, ValueError) as exc:
            raise MutationError(f"production Python cannot be parsed: {path}") from exc
        module = path.removesuffix(".py").replace("/", ".").removeprefix("src.")
        module = module.removesuffix(".__init__")
        modules[module] = tree
        for node in tree.body:
            candidates: list[tuple[ast.stmt, str | None]] = [(node, None)]
            if isinstance(node, ast.ClassDef):
                candidates = [(child, node.name) for child in node.body]
            for child, owner in candidates:
                if isinstance(child, ast.FunctionDef | ast.AsyncFunctionDef):
                    suffix = f"{owner}.{child.name}" if owner else child.name
                    name = f"{module}.{suffix}"
                    mangled = f"xǁ{owner}ǁ{child.name}" if owner else f"x_{child.name}"
                    functions[name] = Function(name, path, child, f"{module}.{mangled}__mutmut_*")
    return functions, modules


def x__functions__mutmut_30(
    files: dict[str, bytes], roots: list[str]
) -> tuple[dict[str, Function], dict[str, ast.Module]]:
    functions: dict[str, Function] = {}
    modules: dict[str, ast.Module] = {}
    for path, raw in sorted(files.items()):
        if not path.endswith(".py") or not any(path.startswith(root.rstrip("/") + "/") for root in roots):
            continue
        if re.search(rb"pragma\s*:\s*no\s+mutate", raw, re.IGNORECASE):
            raise MutationError(f"mutation suppression is forbidden: {path}")
        try:
            tree = ast.parse(filename=path)
        except (SyntaxError, ValueError) as exc:
            raise MutationError(f"production Python cannot be parsed: {path}") from exc
        module = path.removesuffix(".py").replace("/", ".").removeprefix("src.")
        module = module.removesuffix(".__init__")
        modules[module] = tree
        for node in tree.body:
            candidates: list[tuple[ast.stmt, str | None]] = [(node, None)]
            if isinstance(node, ast.ClassDef):
                candidates = [(child, node.name) for child in node.body]
            for child, owner in candidates:
                if isinstance(child, ast.FunctionDef | ast.AsyncFunctionDef):
                    suffix = f"{owner}.{child.name}" if owner else child.name
                    name = f"{module}.{suffix}"
                    mangled = f"xǁ{owner}ǁ{child.name}" if owner else f"x_{child.name}"
                    functions[name] = Function(name, path, child, f"{module}.{mangled}__mutmut_*")
    return functions, modules


def x__functions__mutmut_31(
    files: dict[str, bytes], roots: list[str]
) -> tuple[dict[str, Function], dict[str, ast.Module]]:
    functions: dict[str, Function] = {}
    modules: dict[str, ast.Module] = {}
    for path, raw in sorted(files.items()):
        if not path.endswith(".py") or not any(path.startswith(root.rstrip("/") + "/") for root in roots):
            continue
        if re.search(rb"pragma\s*:\s*no\s+mutate", raw, re.IGNORECASE):
            raise MutationError(f"mutation suppression is forbidden: {path}")
        try:
            tree = ast.parse(raw, )
        except (SyntaxError, ValueError) as exc:
            raise MutationError(f"production Python cannot be parsed: {path}") from exc
        module = path.removesuffix(".py").replace("/", ".").removeprefix("src.")
        module = module.removesuffix(".__init__")
        modules[module] = tree
        for node in tree.body:
            candidates: list[tuple[ast.stmt, str | None]] = [(node, None)]
            if isinstance(node, ast.ClassDef):
                candidates = [(child, node.name) for child in node.body]
            for child, owner in candidates:
                if isinstance(child, ast.FunctionDef | ast.AsyncFunctionDef):
                    suffix = f"{owner}.{child.name}" if owner else child.name
                    name = f"{module}.{suffix}"
                    mangled = f"xǁ{owner}ǁ{child.name}" if owner else f"x_{child.name}"
                    functions[name] = Function(name, path, child, f"{module}.{mangled}__mutmut_*")
    return functions, modules


def x__functions__mutmut_32(
    files: dict[str, bytes], roots: list[str]
) -> tuple[dict[str, Function], dict[str, ast.Module]]:
    functions: dict[str, Function] = {}
    modules: dict[str, ast.Module] = {}
    for path, raw in sorted(files.items()):
        if not path.endswith(".py") or not any(path.startswith(root.rstrip("/") + "/") for root in roots):
            continue
        if re.search(rb"pragma\s*:\s*no\s+mutate", raw, re.IGNORECASE):
            raise MutationError(f"mutation suppression is forbidden: {path}")
        try:
            tree = ast.parse(raw, filename=path)
        except (SyntaxError, ValueError) as exc:
            raise MutationError(None) from exc
        module = path.removesuffix(".py").replace("/", ".").removeprefix("src.")
        module = module.removesuffix(".__init__")
        modules[module] = tree
        for node in tree.body:
            candidates: list[tuple[ast.stmt, str | None]] = [(node, None)]
            if isinstance(node, ast.ClassDef):
                candidates = [(child, node.name) for child in node.body]
            for child, owner in candidates:
                if isinstance(child, ast.FunctionDef | ast.AsyncFunctionDef):
                    suffix = f"{owner}.{child.name}" if owner else child.name
                    name = f"{module}.{suffix}"
                    mangled = f"xǁ{owner}ǁ{child.name}" if owner else f"x_{child.name}"
                    functions[name] = Function(name, path, child, f"{module}.{mangled}__mutmut_*")
    return functions, modules


def x__functions__mutmut_33(
    files: dict[str, bytes], roots: list[str]
) -> tuple[dict[str, Function], dict[str, ast.Module]]:
    functions: dict[str, Function] = {}
    modules: dict[str, ast.Module] = {}
    for path, raw in sorted(files.items()):
        if not path.endswith(".py") or not any(path.startswith(root.rstrip("/") + "/") for root in roots):
            continue
        if re.search(rb"pragma\s*:\s*no\s+mutate", raw, re.IGNORECASE):
            raise MutationError(f"mutation suppression is forbidden: {path}")
        try:
            tree = ast.parse(raw, filename=path)
        except (SyntaxError, ValueError) as exc:
            raise MutationError(f"production Python cannot be parsed: {path}") from exc
        module = None
        module = module.removesuffix(".__init__")
        modules[module] = tree
        for node in tree.body:
            candidates: list[tuple[ast.stmt, str | None]] = [(node, None)]
            if isinstance(node, ast.ClassDef):
                candidates = [(child, node.name) for child in node.body]
            for child, owner in candidates:
                if isinstance(child, ast.FunctionDef | ast.AsyncFunctionDef):
                    suffix = f"{owner}.{child.name}" if owner else child.name
                    name = f"{module}.{suffix}"
                    mangled = f"xǁ{owner}ǁ{child.name}" if owner else f"x_{child.name}"
                    functions[name] = Function(name, path, child, f"{module}.{mangled}__mutmut_*")
    return functions, modules


def x__functions__mutmut_34(
    files: dict[str, bytes], roots: list[str]
) -> tuple[dict[str, Function], dict[str, ast.Module]]:
    functions: dict[str, Function] = {}
    modules: dict[str, ast.Module] = {}
    for path, raw in sorted(files.items()):
        if not path.endswith(".py") or not any(path.startswith(root.rstrip("/") + "/") for root in roots):
            continue
        if re.search(rb"pragma\s*:\s*no\s+mutate", raw, re.IGNORECASE):
            raise MutationError(f"mutation suppression is forbidden: {path}")
        try:
            tree = ast.parse(raw, filename=path)
        except (SyntaxError, ValueError) as exc:
            raise MutationError(f"production Python cannot be parsed: {path}") from exc
        module = path.removesuffix(".py").replace("/", ".").removeprefix(None)
        module = module.removesuffix(".__init__")
        modules[module] = tree
        for node in tree.body:
            candidates: list[tuple[ast.stmt, str | None]] = [(node, None)]
            if isinstance(node, ast.ClassDef):
                candidates = [(child, node.name) for child in node.body]
            for child, owner in candidates:
                if isinstance(child, ast.FunctionDef | ast.AsyncFunctionDef):
                    suffix = f"{owner}.{child.name}" if owner else child.name
                    name = f"{module}.{suffix}"
                    mangled = f"xǁ{owner}ǁ{child.name}" if owner else f"x_{child.name}"
                    functions[name] = Function(name, path, child, f"{module}.{mangled}__mutmut_*")
    return functions, modules


def x__functions__mutmut_35(
    files: dict[str, bytes], roots: list[str]
) -> tuple[dict[str, Function], dict[str, ast.Module]]:
    functions: dict[str, Function] = {}
    modules: dict[str, ast.Module] = {}
    for path, raw in sorted(files.items()):
        if not path.endswith(".py") or not any(path.startswith(root.rstrip("/") + "/") for root in roots):
            continue
        if re.search(rb"pragma\s*:\s*no\s+mutate", raw, re.IGNORECASE):
            raise MutationError(f"mutation suppression is forbidden: {path}")
        try:
            tree = ast.parse(raw, filename=path)
        except (SyntaxError, ValueError) as exc:
            raise MutationError(f"production Python cannot be parsed: {path}") from exc
        module = path.removesuffix(".py").replace("/", ".").removesuffix("src.")
        module = module.removesuffix(".__init__")
        modules[module] = tree
        for node in tree.body:
            candidates: list[tuple[ast.stmt, str | None]] = [(node, None)]
            if isinstance(node, ast.ClassDef):
                candidates = [(child, node.name) for child in node.body]
            for child, owner in candidates:
                if isinstance(child, ast.FunctionDef | ast.AsyncFunctionDef):
                    suffix = f"{owner}.{child.name}" if owner else child.name
                    name = f"{module}.{suffix}"
                    mangled = f"xǁ{owner}ǁ{child.name}" if owner else f"x_{child.name}"
                    functions[name] = Function(name, path, child, f"{module}.{mangled}__mutmut_*")
    return functions, modules


def x__functions__mutmut_36(
    files: dict[str, bytes], roots: list[str]
) -> tuple[dict[str, Function], dict[str, ast.Module]]:
    functions: dict[str, Function] = {}
    modules: dict[str, ast.Module] = {}
    for path, raw in sorted(files.items()):
        if not path.endswith(".py") or not any(path.startswith(root.rstrip("/") + "/") for root in roots):
            continue
        if re.search(rb"pragma\s*:\s*no\s+mutate", raw, re.IGNORECASE):
            raise MutationError(f"mutation suppression is forbidden: {path}")
        try:
            tree = ast.parse(raw, filename=path)
        except (SyntaxError, ValueError) as exc:
            raise MutationError(f"production Python cannot be parsed: {path}") from exc
        module = path.removesuffix(".py").replace(None, ".").removeprefix("src.")
        module = module.removesuffix(".__init__")
        modules[module] = tree
        for node in tree.body:
            candidates: list[tuple[ast.stmt, str | None]] = [(node, None)]
            if isinstance(node, ast.ClassDef):
                candidates = [(child, node.name) for child in node.body]
            for child, owner in candidates:
                if isinstance(child, ast.FunctionDef | ast.AsyncFunctionDef):
                    suffix = f"{owner}.{child.name}" if owner else child.name
                    name = f"{module}.{suffix}"
                    mangled = f"xǁ{owner}ǁ{child.name}" if owner else f"x_{child.name}"
                    functions[name] = Function(name, path, child, f"{module}.{mangled}__mutmut_*")
    return functions, modules


def x__functions__mutmut_37(
    files: dict[str, bytes], roots: list[str]
) -> tuple[dict[str, Function], dict[str, ast.Module]]:
    functions: dict[str, Function] = {}
    modules: dict[str, ast.Module] = {}
    for path, raw in sorted(files.items()):
        if not path.endswith(".py") or not any(path.startswith(root.rstrip("/") + "/") for root in roots):
            continue
        if re.search(rb"pragma\s*:\s*no\s+mutate", raw, re.IGNORECASE):
            raise MutationError(f"mutation suppression is forbidden: {path}")
        try:
            tree = ast.parse(raw, filename=path)
        except (SyntaxError, ValueError) as exc:
            raise MutationError(f"production Python cannot be parsed: {path}") from exc
        module = path.removesuffix(".py").replace("/", None).removeprefix("src.")
        module = module.removesuffix(".__init__")
        modules[module] = tree
        for node in tree.body:
            candidates: list[tuple[ast.stmt, str | None]] = [(node, None)]
            if isinstance(node, ast.ClassDef):
                candidates = [(child, node.name) for child in node.body]
            for child, owner in candidates:
                if isinstance(child, ast.FunctionDef | ast.AsyncFunctionDef):
                    suffix = f"{owner}.{child.name}" if owner else child.name
                    name = f"{module}.{suffix}"
                    mangled = f"xǁ{owner}ǁ{child.name}" if owner else f"x_{child.name}"
                    functions[name] = Function(name, path, child, f"{module}.{mangled}__mutmut_*")
    return functions, modules


def x__functions__mutmut_38(
    files: dict[str, bytes], roots: list[str]
) -> tuple[dict[str, Function], dict[str, ast.Module]]:
    functions: dict[str, Function] = {}
    modules: dict[str, ast.Module] = {}
    for path, raw in sorted(files.items()):
        if not path.endswith(".py") or not any(path.startswith(root.rstrip("/") + "/") for root in roots):
            continue
        if re.search(rb"pragma\s*:\s*no\s+mutate", raw, re.IGNORECASE):
            raise MutationError(f"mutation suppression is forbidden: {path}")
        try:
            tree = ast.parse(raw, filename=path)
        except (SyntaxError, ValueError) as exc:
            raise MutationError(f"production Python cannot be parsed: {path}") from exc
        module = path.removesuffix(".py").replace(".").removeprefix("src.")
        module = module.removesuffix(".__init__")
        modules[module] = tree
        for node in tree.body:
            candidates: list[tuple[ast.stmt, str | None]] = [(node, None)]
            if isinstance(node, ast.ClassDef):
                candidates = [(child, node.name) for child in node.body]
            for child, owner in candidates:
                if isinstance(child, ast.FunctionDef | ast.AsyncFunctionDef):
                    suffix = f"{owner}.{child.name}" if owner else child.name
                    name = f"{module}.{suffix}"
                    mangled = f"xǁ{owner}ǁ{child.name}" if owner else f"x_{child.name}"
                    functions[name] = Function(name, path, child, f"{module}.{mangled}__mutmut_*")
    return functions, modules


def x__functions__mutmut_39(
    files: dict[str, bytes], roots: list[str]
) -> tuple[dict[str, Function], dict[str, ast.Module]]:
    functions: dict[str, Function] = {}
    modules: dict[str, ast.Module] = {}
    for path, raw in sorted(files.items()):
        if not path.endswith(".py") or not any(path.startswith(root.rstrip("/") + "/") for root in roots):
            continue
        if re.search(rb"pragma\s*:\s*no\s+mutate", raw, re.IGNORECASE):
            raise MutationError(f"mutation suppression is forbidden: {path}")
        try:
            tree = ast.parse(raw, filename=path)
        except (SyntaxError, ValueError) as exc:
            raise MutationError(f"production Python cannot be parsed: {path}") from exc
        module = path.removesuffix(".py").replace("/", ).removeprefix("src.")
        module = module.removesuffix(".__init__")
        modules[module] = tree
        for node in tree.body:
            candidates: list[tuple[ast.stmt, str | None]] = [(node, None)]
            if isinstance(node, ast.ClassDef):
                candidates = [(child, node.name) for child in node.body]
            for child, owner in candidates:
                if isinstance(child, ast.FunctionDef | ast.AsyncFunctionDef):
                    suffix = f"{owner}.{child.name}" if owner else child.name
                    name = f"{module}.{suffix}"
                    mangled = f"xǁ{owner}ǁ{child.name}" if owner else f"x_{child.name}"
                    functions[name] = Function(name, path, child, f"{module}.{mangled}__mutmut_*")
    return functions, modules


def x__functions__mutmut_40(
    files: dict[str, bytes], roots: list[str]
) -> tuple[dict[str, Function], dict[str, ast.Module]]:
    functions: dict[str, Function] = {}
    modules: dict[str, ast.Module] = {}
    for path, raw in sorted(files.items()):
        if not path.endswith(".py") or not any(path.startswith(root.rstrip("/") + "/") for root in roots):
            continue
        if re.search(rb"pragma\s*:\s*no\s+mutate", raw, re.IGNORECASE):
            raise MutationError(f"mutation suppression is forbidden: {path}")
        try:
            tree = ast.parse(raw, filename=path)
        except (SyntaxError, ValueError) as exc:
            raise MutationError(f"production Python cannot be parsed: {path}") from exc
        module = path.removesuffix(None).replace("/", ".").removeprefix("src.")
        module = module.removesuffix(".__init__")
        modules[module] = tree
        for node in tree.body:
            candidates: list[tuple[ast.stmt, str | None]] = [(node, None)]
            if isinstance(node, ast.ClassDef):
                candidates = [(child, node.name) for child in node.body]
            for child, owner in candidates:
                if isinstance(child, ast.FunctionDef | ast.AsyncFunctionDef):
                    suffix = f"{owner}.{child.name}" if owner else child.name
                    name = f"{module}.{suffix}"
                    mangled = f"xǁ{owner}ǁ{child.name}" if owner else f"x_{child.name}"
                    functions[name] = Function(name, path, child, f"{module}.{mangled}__mutmut_*")
    return functions, modules


def x__functions__mutmut_41(
    files: dict[str, bytes], roots: list[str]
) -> tuple[dict[str, Function], dict[str, ast.Module]]:
    functions: dict[str, Function] = {}
    modules: dict[str, ast.Module] = {}
    for path, raw in sorted(files.items()):
        if not path.endswith(".py") or not any(path.startswith(root.rstrip("/") + "/") for root in roots):
            continue
        if re.search(rb"pragma\s*:\s*no\s+mutate", raw, re.IGNORECASE):
            raise MutationError(f"mutation suppression is forbidden: {path}")
        try:
            tree = ast.parse(raw, filename=path)
        except (SyntaxError, ValueError) as exc:
            raise MutationError(f"production Python cannot be parsed: {path}") from exc
        module = path.removeprefix(".py").replace("/", ".").removeprefix("src.")
        module = module.removesuffix(".__init__")
        modules[module] = tree
        for node in tree.body:
            candidates: list[tuple[ast.stmt, str | None]] = [(node, None)]
            if isinstance(node, ast.ClassDef):
                candidates = [(child, node.name) for child in node.body]
            for child, owner in candidates:
                if isinstance(child, ast.FunctionDef | ast.AsyncFunctionDef):
                    suffix = f"{owner}.{child.name}" if owner else child.name
                    name = f"{module}.{suffix}"
                    mangled = f"xǁ{owner}ǁ{child.name}" if owner else f"x_{child.name}"
                    functions[name] = Function(name, path, child, f"{module}.{mangled}__mutmut_*")
    return functions, modules


def x__functions__mutmut_42(
    files: dict[str, bytes], roots: list[str]
) -> tuple[dict[str, Function], dict[str, ast.Module]]:
    functions: dict[str, Function] = {}
    modules: dict[str, ast.Module] = {}
    for path, raw in sorted(files.items()):
        if not path.endswith(".py") or not any(path.startswith(root.rstrip("/") + "/") for root in roots):
            continue
        if re.search(rb"pragma\s*:\s*no\s+mutate", raw, re.IGNORECASE):
            raise MutationError(f"mutation suppression is forbidden: {path}")
        try:
            tree = ast.parse(raw, filename=path)
        except (SyntaxError, ValueError) as exc:
            raise MutationError(f"production Python cannot be parsed: {path}") from exc
        module = path.removesuffix("XX.pyXX").replace("/", ".").removeprefix("src.")
        module = module.removesuffix(".__init__")
        modules[module] = tree
        for node in tree.body:
            candidates: list[tuple[ast.stmt, str | None]] = [(node, None)]
            if isinstance(node, ast.ClassDef):
                candidates = [(child, node.name) for child in node.body]
            for child, owner in candidates:
                if isinstance(child, ast.FunctionDef | ast.AsyncFunctionDef):
                    suffix = f"{owner}.{child.name}" if owner else child.name
                    name = f"{module}.{suffix}"
                    mangled = f"xǁ{owner}ǁ{child.name}" if owner else f"x_{child.name}"
                    functions[name] = Function(name, path, child, f"{module}.{mangled}__mutmut_*")
    return functions, modules


def x__functions__mutmut_43(
    files: dict[str, bytes], roots: list[str]
) -> tuple[dict[str, Function], dict[str, ast.Module]]:
    functions: dict[str, Function] = {}
    modules: dict[str, ast.Module] = {}
    for path, raw in sorted(files.items()):
        if not path.endswith(".py") or not any(path.startswith(root.rstrip("/") + "/") for root in roots):
            continue
        if re.search(rb"pragma\s*:\s*no\s+mutate", raw, re.IGNORECASE):
            raise MutationError(f"mutation suppression is forbidden: {path}")
        try:
            tree = ast.parse(raw, filename=path)
        except (SyntaxError, ValueError) as exc:
            raise MutationError(f"production Python cannot be parsed: {path}") from exc
        module = path.removesuffix(".PY").replace("/", ".").removeprefix("src.")
        module = module.removesuffix(".__init__")
        modules[module] = tree
        for node in tree.body:
            candidates: list[tuple[ast.stmt, str | None]] = [(node, None)]
            if isinstance(node, ast.ClassDef):
                candidates = [(child, node.name) for child in node.body]
            for child, owner in candidates:
                if isinstance(child, ast.FunctionDef | ast.AsyncFunctionDef):
                    suffix = f"{owner}.{child.name}" if owner else child.name
                    name = f"{module}.{suffix}"
                    mangled = f"xǁ{owner}ǁ{child.name}" if owner else f"x_{child.name}"
                    functions[name] = Function(name, path, child, f"{module}.{mangled}__mutmut_*")
    return functions, modules


def x__functions__mutmut_44(
    files: dict[str, bytes], roots: list[str]
) -> tuple[dict[str, Function], dict[str, ast.Module]]:
    functions: dict[str, Function] = {}
    modules: dict[str, ast.Module] = {}
    for path, raw in sorted(files.items()):
        if not path.endswith(".py") or not any(path.startswith(root.rstrip("/") + "/") for root in roots):
            continue
        if re.search(rb"pragma\s*:\s*no\s+mutate", raw, re.IGNORECASE):
            raise MutationError(f"mutation suppression is forbidden: {path}")
        try:
            tree = ast.parse(raw, filename=path)
        except (SyntaxError, ValueError) as exc:
            raise MutationError(f"production Python cannot be parsed: {path}") from exc
        module = path.removesuffix(".py").replace("XX/XX", ".").removeprefix("src.")
        module = module.removesuffix(".__init__")
        modules[module] = tree
        for node in tree.body:
            candidates: list[tuple[ast.stmt, str | None]] = [(node, None)]
            if isinstance(node, ast.ClassDef):
                candidates = [(child, node.name) for child in node.body]
            for child, owner in candidates:
                if isinstance(child, ast.FunctionDef | ast.AsyncFunctionDef):
                    suffix = f"{owner}.{child.name}" if owner else child.name
                    name = f"{module}.{suffix}"
                    mangled = f"xǁ{owner}ǁ{child.name}" if owner else f"x_{child.name}"
                    functions[name] = Function(name, path, child, f"{module}.{mangled}__mutmut_*")
    return functions, modules


def x__functions__mutmut_45(
    files: dict[str, bytes], roots: list[str]
) -> tuple[dict[str, Function], dict[str, ast.Module]]:
    functions: dict[str, Function] = {}
    modules: dict[str, ast.Module] = {}
    for path, raw in sorted(files.items()):
        if not path.endswith(".py") or not any(path.startswith(root.rstrip("/") + "/") for root in roots):
            continue
        if re.search(rb"pragma\s*:\s*no\s+mutate", raw, re.IGNORECASE):
            raise MutationError(f"mutation suppression is forbidden: {path}")
        try:
            tree = ast.parse(raw, filename=path)
        except (SyntaxError, ValueError) as exc:
            raise MutationError(f"production Python cannot be parsed: {path}") from exc
        module = path.removesuffix(".py").replace("/", "XX.XX").removeprefix("src.")
        module = module.removesuffix(".__init__")
        modules[module] = tree
        for node in tree.body:
            candidates: list[tuple[ast.stmt, str | None]] = [(node, None)]
            if isinstance(node, ast.ClassDef):
                candidates = [(child, node.name) for child in node.body]
            for child, owner in candidates:
                if isinstance(child, ast.FunctionDef | ast.AsyncFunctionDef):
                    suffix = f"{owner}.{child.name}" if owner else child.name
                    name = f"{module}.{suffix}"
                    mangled = f"xǁ{owner}ǁ{child.name}" if owner else f"x_{child.name}"
                    functions[name] = Function(name, path, child, f"{module}.{mangled}__mutmut_*")
    return functions, modules


def x__functions__mutmut_46(
    files: dict[str, bytes], roots: list[str]
) -> tuple[dict[str, Function], dict[str, ast.Module]]:
    functions: dict[str, Function] = {}
    modules: dict[str, ast.Module] = {}
    for path, raw in sorted(files.items()):
        if not path.endswith(".py") or not any(path.startswith(root.rstrip("/") + "/") for root in roots):
            continue
        if re.search(rb"pragma\s*:\s*no\s+mutate", raw, re.IGNORECASE):
            raise MutationError(f"mutation suppression is forbidden: {path}")
        try:
            tree = ast.parse(raw, filename=path)
        except (SyntaxError, ValueError) as exc:
            raise MutationError(f"production Python cannot be parsed: {path}") from exc
        module = path.removesuffix(".py").replace("/", ".").removeprefix("XXsrc.XX")
        module = module.removesuffix(".__init__")
        modules[module] = tree
        for node in tree.body:
            candidates: list[tuple[ast.stmt, str | None]] = [(node, None)]
            if isinstance(node, ast.ClassDef):
                candidates = [(child, node.name) for child in node.body]
            for child, owner in candidates:
                if isinstance(child, ast.FunctionDef | ast.AsyncFunctionDef):
                    suffix = f"{owner}.{child.name}" if owner else child.name
                    name = f"{module}.{suffix}"
                    mangled = f"xǁ{owner}ǁ{child.name}" if owner else f"x_{child.name}"
                    functions[name] = Function(name, path, child, f"{module}.{mangled}__mutmut_*")
    return functions, modules


def x__functions__mutmut_47(
    files: dict[str, bytes], roots: list[str]
) -> tuple[dict[str, Function], dict[str, ast.Module]]:
    functions: dict[str, Function] = {}
    modules: dict[str, ast.Module] = {}
    for path, raw in sorted(files.items()):
        if not path.endswith(".py") or not any(path.startswith(root.rstrip("/") + "/") for root in roots):
            continue
        if re.search(rb"pragma\s*:\s*no\s+mutate", raw, re.IGNORECASE):
            raise MutationError(f"mutation suppression is forbidden: {path}")
        try:
            tree = ast.parse(raw, filename=path)
        except (SyntaxError, ValueError) as exc:
            raise MutationError(f"production Python cannot be parsed: {path}") from exc
        module = path.removesuffix(".py").replace("/", ".").removeprefix("SRC.")
        module = module.removesuffix(".__init__")
        modules[module] = tree
        for node in tree.body:
            candidates: list[tuple[ast.stmt, str | None]] = [(node, None)]
            if isinstance(node, ast.ClassDef):
                candidates = [(child, node.name) for child in node.body]
            for child, owner in candidates:
                if isinstance(child, ast.FunctionDef | ast.AsyncFunctionDef):
                    suffix = f"{owner}.{child.name}" if owner else child.name
                    name = f"{module}.{suffix}"
                    mangled = f"xǁ{owner}ǁ{child.name}" if owner else f"x_{child.name}"
                    functions[name] = Function(name, path, child, f"{module}.{mangled}__mutmut_*")
    return functions, modules


def x__functions__mutmut_48(
    files: dict[str, bytes], roots: list[str]
) -> tuple[dict[str, Function], dict[str, ast.Module]]:
    functions: dict[str, Function] = {}
    modules: dict[str, ast.Module] = {}
    for path, raw in sorted(files.items()):
        if not path.endswith(".py") or not any(path.startswith(root.rstrip("/") + "/") for root in roots):
            continue
        if re.search(rb"pragma\s*:\s*no\s+mutate", raw, re.IGNORECASE):
            raise MutationError(f"mutation suppression is forbidden: {path}")
        try:
            tree = ast.parse(raw, filename=path)
        except (SyntaxError, ValueError) as exc:
            raise MutationError(f"production Python cannot be parsed: {path}") from exc
        module = path.removesuffix(".py").replace("/", ".").removeprefix("src.")
        module = None
        modules[module] = tree
        for node in tree.body:
            candidates: list[tuple[ast.stmt, str | None]] = [(node, None)]
            if isinstance(node, ast.ClassDef):
                candidates = [(child, node.name) for child in node.body]
            for child, owner in candidates:
                if isinstance(child, ast.FunctionDef | ast.AsyncFunctionDef):
                    suffix = f"{owner}.{child.name}" if owner else child.name
                    name = f"{module}.{suffix}"
                    mangled = f"xǁ{owner}ǁ{child.name}" if owner else f"x_{child.name}"
                    functions[name] = Function(name, path, child, f"{module}.{mangled}__mutmut_*")
    return functions, modules


def x__functions__mutmut_49(
    files: dict[str, bytes], roots: list[str]
) -> tuple[dict[str, Function], dict[str, ast.Module]]:
    functions: dict[str, Function] = {}
    modules: dict[str, ast.Module] = {}
    for path, raw in sorted(files.items()):
        if not path.endswith(".py") or not any(path.startswith(root.rstrip("/") + "/") for root in roots):
            continue
        if re.search(rb"pragma\s*:\s*no\s+mutate", raw, re.IGNORECASE):
            raise MutationError(f"mutation suppression is forbidden: {path}")
        try:
            tree = ast.parse(raw, filename=path)
        except (SyntaxError, ValueError) as exc:
            raise MutationError(f"production Python cannot be parsed: {path}") from exc
        module = path.removesuffix(".py").replace("/", ".").removeprefix("src.")
        module = module.removesuffix(None)
        modules[module] = tree
        for node in tree.body:
            candidates: list[tuple[ast.stmt, str | None]] = [(node, None)]
            if isinstance(node, ast.ClassDef):
                candidates = [(child, node.name) for child in node.body]
            for child, owner in candidates:
                if isinstance(child, ast.FunctionDef | ast.AsyncFunctionDef):
                    suffix = f"{owner}.{child.name}" if owner else child.name
                    name = f"{module}.{suffix}"
                    mangled = f"xǁ{owner}ǁ{child.name}" if owner else f"x_{child.name}"
                    functions[name] = Function(name, path, child, f"{module}.{mangled}__mutmut_*")
    return functions, modules


def x__functions__mutmut_50(
    files: dict[str, bytes], roots: list[str]
) -> tuple[dict[str, Function], dict[str, ast.Module]]:
    functions: dict[str, Function] = {}
    modules: dict[str, ast.Module] = {}
    for path, raw in sorted(files.items()):
        if not path.endswith(".py") or not any(path.startswith(root.rstrip("/") + "/") for root in roots):
            continue
        if re.search(rb"pragma\s*:\s*no\s+mutate", raw, re.IGNORECASE):
            raise MutationError(f"mutation suppression is forbidden: {path}")
        try:
            tree = ast.parse(raw, filename=path)
        except (SyntaxError, ValueError) as exc:
            raise MutationError(f"production Python cannot be parsed: {path}") from exc
        module = path.removesuffix(".py").replace("/", ".").removeprefix("src.")
        module = module.removeprefix(".__init__")
        modules[module] = tree
        for node in tree.body:
            candidates: list[tuple[ast.stmt, str | None]] = [(node, None)]
            if isinstance(node, ast.ClassDef):
                candidates = [(child, node.name) for child in node.body]
            for child, owner in candidates:
                if isinstance(child, ast.FunctionDef | ast.AsyncFunctionDef):
                    suffix = f"{owner}.{child.name}" if owner else child.name
                    name = f"{module}.{suffix}"
                    mangled = f"xǁ{owner}ǁ{child.name}" if owner else f"x_{child.name}"
                    functions[name] = Function(name, path, child, f"{module}.{mangled}__mutmut_*")
    return functions, modules


def x__functions__mutmut_51(
    files: dict[str, bytes], roots: list[str]
) -> tuple[dict[str, Function], dict[str, ast.Module]]:
    functions: dict[str, Function] = {}
    modules: dict[str, ast.Module] = {}
    for path, raw in sorted(files.items()):
        if not path.endswith(".py") or not any(path.startswith(root.rstrip("/") + "/") for root in roots):
            continue
        if re.search(rb"pragma\s*:\s*no\s+mutate", raw, re.IGNORECASE):
            raise MutationError(f"mutation suppression is forbidden: {path}")
        try:
            tree = ast.parse(raw, filename=path)
        except (SyntaxError, ValueError) as exc:
            raise MutationError(f"production Python cannot be parsed: {path}") from exc
        module = path.removesuffix(".py").replace("/", ".").removeprefix("src.")
        module = module.removesuffix("XX.__init__XX")
        modules[module] = tree
        for node in tree.body:
            candidates: list[tuple[ast.stmt, str | None]] = [(node, None)]
            if isinstance(node, ast.ClassDef):
                candidates = [(child, node.name) for child in node.body]
            for child, owner in candidates:
                if isinstance(child, ast.FunctionDef | ast.AsyncFunctionDef):
                    suffix = f"{owner}.{child.name}" if owner else child.name
                    name = f"{module}.{suffix}"
                    mangled = f"xǁ{owner}ǁ{child.name}" if owner else f"x_{child.name}"
                    functions[name] = Function(name, path, child, f"{module}.{mangled}__mutmut_*")
    return functions, modules


def x__functions__mutmut_52(
    files: dict[str, bytes], roots: list[str]
) -> tuple[dict[str, Function], dict[str, ast.Module]]:
    functions: dict[str, Function] = {}
    modules: dict[str, ast.Module] = {}
    for path, raw in sorted(files.items()):
        if not path.endswith(".py") or not any(path.startswith(root.rstrip("/") + "/") for root in roots):
            continue
        if re.search(rb"pragma\s*:\s*no\s+mutate", raw, re.IGNORECASE):
            raise MutationError(f"mutation suppression is forbidden: {path}")
        try:
            tree = ast.parse(raw, filename=path)
        except (SyntaxError, ValueError) as exc:
            raise MutationError(f"production Python cannot be parsed: {path}") from exc
        module = path.removesuffix(".py").replace("/", ".").removeprefix("src.")
        module = module.removesuffix(".__INIT__")
        modules[module] = tree
        for node in tree.body:
            candidates: list[tuple[ast.stmt, str | None]] = [(node, None)]
            if isinstance(node, ast.ClassDef):
                candidates = [(child, node.name) for child in node.body]
            for child, owner in candidates:
                if isinstance(child, ast.FunctionDef | ast.AsyncFunctionDef):
                    suffix = f"{owner}.{child.name}" if owner else child.name
                    name = f"{module}.{suffix}"
                    mangled = f"xǁ{owner}ǁ{child.name}" if owner else f"x_{child.name}"
                    functions[name] = Function(name, path, child, f"{module}.{mangled}__mutmut_*")
    return functions, modules


def x__functions__mutmut_53(
    files: dict[str, bytes], roots: list[str]
) -> tuple[dict[str, Function], dict[str, ast.Module]]:
    functions: dict[str, Function] = {}
    modules: dict[str, ast.Module] = {}
    for path, raw in sorted(files.items()):
        if not path.endswith(".py") or not any(path.startswith(root.rstrip("/") + "/") for root in roots):
            continue
        if re.search(rb"pragma\s*:\s*no\s+mutate", raw, re.IGNORECASE):
            raise MutationError(f"mutation suppression is forbidden: {path}")
        try:
            tree = ast.parse(raw, filename=path)
        except (SyntaxError, ValueError) as exc:
            raise MutationError(f"production Python cannot be parsed: {path}") from exc
        module = path.removesuffix(".py").replace("/", ".").removeprefix("src.")
        module = module.removesuffix(".__init__")
        modules[module] = None
        for node in tree.body:
            candidates: list[tuple[ast.stmt, str | None]] = [(node, None)]
            if isinstance(node, ast.ClassDef):
                candidates = [(child, node.name) for child in node.body]
            for child, owner in candidates:
                if isinstance(child, ast.FunctionDef | ast.AsyncFunctionDef):
                    suffix = f"{owner}.{child.name}" if owner else child.name
                    name = f"{module}.{suffix}"
                    mangled = f"xǁ{owner}ǁ{child.name}" if owner else f"x_{child.name}"
                    functions[name] = Function(name, path, child, f"{module}.{mangled}__mutmut_*")
    return functions, modules


def x__functions__mutmut_54(
    files: dict[str, bytes], roots: list[str]
) -> tuple[dict[str, Function], dict[str, ast.Module]]:
    functions: dict[str, Function] = {}
    modules: dict[str, ast.Module] = {}
    for path, raw in sorted(files.items()):
        if not path.endswith(".py") or not any(path.startswith(root.rstrip("/") + "/") for root in roots):
            continue
        if re.search(rb"pragma\s*:\s*no\s+mutate", raw, re.IGNORECASE):
            raise MutationError(f"mutation suppression is forbidden: {path}")
        try:
            tree = ast.parse(raw, filename=path)
        except (SyntaxError, ValueError) as exc:
            raise MutationError(f"production Python cannot be parsed: {path}") from exc
        module = path.removesuffix(".py").replace("/", ".").removeprefix("src.")
        module = module.removesuffix(".__init__")
        modules[module] = tree
        for node in tree.body:
            candidates: list[tuple[ast.stmt, str | None]] = None
            if isinstance(node, ast.ClassDef):
                candidates = [(child, node.name) for child in node.body]
            for child, owner in candidates:
                if isinstance(child, ast.FunctionDef | ast.AsyncFunctionDef):
                    suffix = f"{owner}.{child.name}" if owner else child.name
                    name = f"{module}.{suffix}"
                    mangled = f"xǁ{owner}ǁ{child.name}" if owner else f"x_{child.name}"
                    functions[name] = Function(name, path, child, f"{module}.{mangled}__mutmut_*")
    return functions, modules


def x__functions__mutmut_55(
    files: dict[str, bytes], roots: list[str]
) -> tuple[dict[str, Function], dict[str, ast.Module]]:
    functions: dict[str, Function] = {}
    modules: dict[str, ast.Module] = {}
    for path, raw in sorted(files.items()):
        if not path.endswith(".py") or not any(path.startswith(root.rstrip("/") + "/") for root in roots):
            continue
        if re.search(rb"pragma\s*:\s*no\s+mutate", raw, re.IGNORECASE):
            raise MutationError(f"mutation suppression is forbidden: {path}")
        try:
            tree = ast.parse(raw, filename=path)
        except (SyntaxError, ValueError) as exc:
            raise MutationError(f"production Python cannot be parsed: {path}") from exc
        module = path.removesuffix(".py").replace("/", ".").removeprefix("src.")
        module = module.removesuffix(".__init__")
        modules[module] = tree
        for node in tree.body:
            candidates: list[tuple[ast.stmt, str | None]] = [(node, None)]
            if isinstance(node, ast.ClassDef):
                candidates = None
            for child, owner in candidates:
                if isinstance(child, ast.FunctionDef | ast.AsyncFunctionDef):
                    suffix = f"{owner}.{child.name}" if owner else child.name
                    name = f"{module}.{suffix}"
                    mangled = f"xǁ{owner}ǁ{child.name}" if owner else f"x_{child.name}"
                    functions[name] = Function(name, path, child, f"{module}.{mangled}__mutmut_*")
    return functions, modules


def x__functions__mutmut_56(
    files: dict[str, bytes], roots: list[str]
) -> tuple[dict[str, Function], dict[str, ast.Module]]:
    functions: dict[str, Function] = {}
    modules: dict[str, ast.Module] = {}
    for path, raw in sorted(files.items()):
        if not path.endswith(".py") or not any(path.startswith(root.rstrip("/") + "/") for root in roots):
            continue
        if re.search(rb"pragma\s*:\s*no\s+mutate", raw, re.IGNORECASE):
            raise MutationError(f"mutation suppression is forbidden: {path}")
        try:
            tree = ast.parse(raw, filename=path)
        except (SyntaxError, ValueError) as exc:
            raise MutationError(f"production Python cannot be parsed: {path}") from exc
        module = path.removesuffix(".py").replace("/", ".").removeprefix("src.")
        module = module.removesuffix(".__init__")
        modules[module] = tree
        for node in tree.body:
            candidates: list[tuple[ast.stmt, str | None]] = [(node, None)]
            if isinstance(node, ast.ClassDef):
                candidates = [(child, node.name) for child in node.body]
            for child, owner in candidates:
                if isinstance(child, ast.FunctionDef | ast.AsyncFunctionDef):
                    suffix = None
                    name = f"{module}.{suffix}"
                    mangled = f"xǁ{owner}ǁ{child.name}" if owner else f"x_{child.name}"
                    functions[name] = Function(name, path, child, f"{module}.{mangled}__mutmut_*")
    return functions, modules


def x__functions__mutmut_57(
    files: dict[str, bytes], roots: list[str]
) -> tuple[dict[str, Function], dict[str, ast.Module]]:
    functions: dict[str, Function] = {}
    modules: dict[str, ast.Module] = {}
    for path, raw in sorted(files.items()):
        if not path.endswith(".py") or not any(path.startswith(root.rstrip("/") + "/") for root in roots):
            continue
        if re.search(rb"pragma\s*:\s*no\s+mutate", raw, re.IGNORECASE):
            raise MutationError(f"mutation suppression is forbidden: {path}")
        try:
            tree = ast.parse(raw, filename=path)
        except (SyntaxError, ValueError) as exc:
            raise MutationError(f"production Python cannot be parsed: {path}") from exc
        module = path.removesuffix(".py").replace("/", ".").removeprefix("src.")
        module = module.removesuffix(".__init__")
        modules[module] = tree
        for node in tree.body:
            candidates: list[tuple[ast.stmt, str | None]] = [(node, None)]
            if isinstance(node, ast.ClassDef):
                candidates = [(child, node.name) for child in node.body]
            for child, owner in candidates:
                if isinstance(child, ast.FunctionDef | ast.AsyncFunctionDef):
                    suffix = f"{owner}.{child.name}" if owner else child.name
                    name = None
                    mangled = f"xǁ{owner}ǁ{child.name}" if owner else f"x_{child.name}"
                    functions[name] = Function(name, path, child, f"{module}.{mangled}__mutmut_*")
    return functions, modules


def x__functions__mutmut_58(
    files: dict[str, bytes], roots: list[str]
) -> tuple[dict[str, Function], dict[str, ast.Module]]:
    functions: dict[str, Function] = {}
    modules: dict[str, ast.Module] = {}
    for path, raw in sorted(files.items()):
        if not path.endswith(".py") or not any(path.startswith(root.rstrip("/") + "/") for root in roots):
            continue
        if re.search(rb"pragma\s*:\s*no\s+mutate", raw, re.IGNORECASE):
            raise MutationError(f"mutation suppression is forbidden: {path}")
        try:
            tree = ast.parse(raw, filename=path)
        except (SyntaxError, ValueError) as exc:
            raise MutationError(f"production Python cannot be parsed: {path}") from exc
        module = path.removesuffix(".py").replace("/", ".").removeprefix("src.")
        module = module.removesuffix(".__init__")
        modules[module] = tree
        for node in tree.body:
            candidates: list[tuple[ast.stmt, str | None]] = [(node, None)]
            if isinstance(node, ast.ClassDef):
                candidates = [(child, node.name) for child in node.body]
            for child, owner in candidates:
                if isinstance(child, ast.FunctionDef | ast.AsyncFunctionDef):
                    suffix = f"{owner}.{child.name}" if owner else child.name
                    name = f"{module}.{suffix}"
                    mangled = None
                    functions[name] = Function(name, path, child, f"{module}.{mangled}__mutmut_*")
    return functions, modules


def x__functions__mutmut_59(
    files: dict[str, bytes], roots: list[str]
) -> tuple[dict[str, Function], dict[str, ast.Module]]:
    functions: dict[str, Function] = {}
    modules: dict[str, ast.Module] = {}
    for path, raw in sorted(files.items()):
        if not path.endswith(".py") or not any(path.startswith(root.rstrip("/") + "/") for root in roots):
            continue
        if re.search(rb"pragma\s*:\s*no\s+mutate", raw, re.IGNORECASE):
            raise MutationError(f"mutation suppression is forbidden: {path}")
        try:
            tree = ast.parse(raw, filename=path)
        except (SyntaxError, ValueError) as exc:
            raise MutationError(f"production Python cannot be parsed: {path}") from exc
        module = path.removesuffix(".py").replace("/", ".").removeprefix("src.")
        module = module.removesuffix(".__init__")
        modules[module] = tree
        for node in tree.body:
            candidates: list[tuple[ast.stmt, str | None]] = [(node, None)]
            if isinstance(node, ast.ClassDef):
                candidates = [(child, node.name) for child in node.body]
            for child, owner in candidates:
                if isinstance(child, ast.FunctionDef | ast.AsyncFunctionDef):
                    suffix = f"{owner}.{child.name}" if owner else child.name
                    name = f"{module}.{suffix}"
                    mangled = f"xǁ{owner}ǁ{child.name}" if owner else f"x_{child.name}"
                    functions[name] = None
    return functions, modules


def x__functions__mutmut_60(
    files: dict[str, bytes], roots: list[str]
) -> tuple[dict[str, Function], dict[str, ast.Module]]:
    functions: dict[str, Function] = {}
    modules: dict[str, ast.Module] = {}
    for path, raw in sorted(files.items()):
        if not path.endswith(".py") or not any(path.startswith(root.rstrip("/") + "/") for root in roots):
            continue
        if re.search(rb"pragma\s*:\s*no\s+mutate", raw, re.IGNORECASE):
            raise MutationError(f"mutation suppression is forbidden: {path}")
        try:
            tree = ast.parse(raw, filename=path)
        except (SyntaxError, ValueError) as exc:
            raise MutationError(f"production Python cannot be parsed: {path}") from exc
        module = path.removesuffix(".py").replace("/", ".").removeprefix("src.")
        module = module.removesuffix(".__init__")
        modules[module] = tree
        for node in tree.body:
            candidates: list[tuple[ast.stmt, str | None]] = [(node, None)]
            if isinstance(node, ast.ClassDef):
                candidates = [(child, node.name) for child in node.body]
            for child, owner in candidates:
                if isinstance(child, ast.FunctionDef | ast.AsyncFunctionDef):
                    suffix = f"{owner}.{child.name}" if owner else child.name
                    name = f"{module}.{suffix}"
                    mangled = f"xǁ{owner}ǁ{child.name}" if owner else f"x_{child.name}"
                    functions[name] = Function(None, path, child, f"{module}.{mangled}__mutmut_*")
    return functions, modules


def x__functions__mutmut_61(
    files: dict[str, bytes], roots: list[str]
) -> tuple[dict[str, Function], dict[str, ast.Module]]:
    functions: dict[str, Function] = {}
    modules: dict[str, ast.Module] = {}
    for path, raw in sorted(files.items()):
        if not path.endswith(".py") or not any(path.startswith(root.rstrip("/") + "/") for root in roots):
            continue
        if re.search(rb"pragma\s*:\s*no\s+mutate", raw, re.IGNORECASE):
            raise MutationError(f"mutation suppression is forbidden: {path}")
        try:
            tree = ast.parse(raw, filename=path)
        except (SyntaxError, ValueError) as exc:
            raise MutationError(f"production Python cannot be parsed: {path}") from exc
        module = path.removesuffix(".py").replace("/", ".").removeprefix("src.")
        module = module.removesuffix(".__init__")
        modules[module] = tree
        for node in tree.body:
            candidates: list[tuple[ast.stmt, str | None]] = [(node, None)]
            if isinstance(node, ast.ClassDef):
                candidates = [(child, node.name) for child in node.body]
            for child, owner in candidates:
                if isinstance(child, ast.FunctionDef | ast.AsyncFunctionDef):
                    suffix = f"{owner}.{child.name}" if owner else child.name
                    name = f"{module}.{suffix}"
                    mangled = f"xǁ{owner}ǁ{child.name}" if owner else f"x_{child.name}"
                    functions[name] = Function(name, None, child, f"{module}.{mangled}__mutmut_*")
    return functions, modules


def x__functions__mutmut_62(
    files: dict[str, bytes], roots: list[str]
) -> tuple[dict[str, Function], dict[str, ast.Module]]:
    functions: dict[str, Function] = {}
    modules: dict[str, ast.Module] = {}
    for path, raw in sorted(files.items()):
        if not path.endswith(".py") or not any(path.startswith(root.rstrip("/") + "/") for root in roots):
            continue
        if re.search(rb"pragma\s*:\s*no\s+mutate", raw, re.IGNORECASE):
            raise MutationError(f"mutation suppression is forbidden: {path}")
        try:
            tree = ast.parse(raw, filename=path)
        except (SyntaxError, ValueError) as exc:
            raise MutationError(f"production Python cannot be parsed: {path}") from exc
        module = path.removesuffix(".py").replace("/", ".").removeprefix("src.")
        module = module.removesuffix(".__init__")
        modules[module] = tree
        for node in tree.body:
            candidates: list[tuple[ast.stmt, str | None]] = [(node, None)]
            if isinstance(node, ast.ClassDef):
                candidates = [(child, node.name) for child in node.body]
            for child, owner in candidates:
                if isinstance(child, ast.FunctionDef | ast.AsyncFunctionDef):
                    suffix = f"{owner}.{child.name}" if owner else child.name
                    name = f"{module}.{suffix}"
                    mangled = f"xǁ{owner}ǁ{child.name}" if owner else f"x_{child.name}"
                    functions[name] = Function(name, path, None, f"{module}.{mangled}__mutmut_*")
    return functions, modules


def x__functions__mutmut_63(
    files: dict[str, bytes], roots: list[str]
) -> tuple[dict[str, Function], dict[str, ast.Module]]:
    functions: dict[str, Function] = {}
    modules: dict[str, ast.Module] = {}
    for path, raw in sorted(files.items()):
        if not path.endswith(".py") or not any(path.startswith(root.rstrip("/") + "/") for root in roots):
            continue
        if re.search(rb"pragma\s*:\s*no\s+mutate", raw, re.IGNORECASE):
            raise MutationError(f"mutation suppression is forbidden: {path}")
        try:
            tree = ast.parse(raw, filename=path)
        except (SyntaxError, ValueError) as exc:
            raise MutationError(f"production Python cannot be parsed: {path}") from exc
        module = path.removesuffix(".py").replace("/", ".").removeprefix("src.")
        module = module.removesuffix(".__init__")
        modules[module] = tree
        for node in tree.body:
            candidates: list[tuple[ast.stmt, str | None]] = [(node, None)]
            if isinstance(node, ast.ClassDef):
                candidates = [(child, node.name) for child in node.body]
            for child, owner in candidates:
                if isinstance(child, ast.FunctionDef | ast.AsyncFunctionDef):
                    suffix = f"{owner}.{child.name}" if owner else child.name
                    name = f"{module}.{suffix}"
                    mangled = f"xǁ{owner}ǁ{child.name}" if owner else f"x_{child.name}"
                    functions[name] = Function(name, path, child, None)
    return functions, modules


def x__functions__mutmut_64(
    files: dict[str, bytes], roots: list[str]
) -> tuple[dict[str, Function], dict[str, ast.Module]]:
    functions: dict[str, Function] = {}
    modules: dict[str, ast.Module] = {}
    for path, raw in sorted(files.items()):
        if not path.endswith(".py") or not any(path.startswith(root.rstrip("/") + "/") for root in roots):
            continue
        if re.search(rb"pragma\s*:\s*no\s+mutate", raw, re.IGNORECASE):
            raise MutationError(f"mutation suppression is forbidden: {path}")
        try:
            tree = ast.parse(raw, filename=path)
        except (SyntaxError, ValueError) as exc:
            raise MutationError(f"production Python cannot be parsed: {path}") from exc
        module = path.removesuffix(".py").replace("/", ".").removeprefix("src.")
        module = module.removesuffix(".__init__")
        modules[module] = tree
        for node in tree.body:
            candidates: list[tuple[ast.stmt, str | None]] = [(node, None)]
            if isinstance(node, ast.ClassDef):
                candidates = [(child, node.name) for child in node.body]
            for child, owner in candidates:
                if isinstance(child, ast.FunctionDef | ast.AsyncFunctionDef):
                    suffix = f"{owner}.{child.name}" if owner else child.name
                    name = f"{module}.{suffix}"
                    mangled = f"xǁ{owner}ǁ{child.name}" if owner else f"x_{child.name}"
                    functions[name] = Function(path, child, f"{module}.{mangled}__mutmut_*")
    return functions, modules


def x__functions__mutmut_65(
    files: dict[str, bytes], roots: list[str]
) -> tuple[dict[str, Function], dict[str, ast.Module]]:
    functions: dict[str, Function] = {}
    modules: dict[str, ast.Module] = {}
    for path, raw in sorted(files.items()):
        if not path.endswith(".py") or not any(path.startswith(root.rstrip("/") + "/") for root in roots):
            continue
        if re.search(rb"pragma\s*:\s*no\s+mutate", raw, re.IGNORECASE):
            raise MutationError(f"mutation suppression is forbidden: {path}")
        try:
            tree = ast.parse(raw, filename=path)
        except (SyntaxError, ValueError) as exc:
            raise MutationError(f"production Python cannot be parsed: {path}") from exc
        module = path.removesuffix(".py").replace("/", ".").removeprefix("src.")
        module = module.removesuffix(".__init__")
        modules[module] = tree
        for node in tree.body:
            candidates: list[tuple[ast.stmt, str | None]] = [(node, None)]
            if isinstance(node, ast.ClassDef):
                candidates = [(child, node.name) for child in node.body]
            for child, owner in candidates:
                if isinstance(child, ast.FunctionDef | ast.AsyncFunctionDef):
                    suffix = f"{owner}.{child.name}" if owner else child.name
                    name = f"{module}.{suffix}"
                    mangled = f"xǁ{owner}ǁ{child.name}" if owner else f"x_{child.name}"
                    functions[name] = Function(name, child, f"{module}.{mangled}__mutmut_*")
    return functions, modules


def x__functions__mutmut_66(
    files: dict[str, bytes], roots: list[str]
) -> tuple[dict[str, Function], dict[str, ast.Module]]:
    functions: dict[str, Function] = {}
    modules: dict[str, ast.Module] = {}
    for path, raw in sorted(files.items()):
        if not path.endswith(".py") or not any(path.startswith(root.rstrip("/") + "/") for root in roots):
            continue
        if re.search(rb"pragma\s*:\s*no\s+mutate", raw, re.IGNORECASE):
            raise MutationError(f"mutation suppression is forbidden: {path}")
        try:
            tree = ast.parse(raw, filename=path)
        except (SyntaxError, ValueError) as exc:
            raise MutationError(f"production Python cannot be parsed: {path}") from exc
        module = path.removesuffix(".py").replace("/", ".").removeprefix("src.")
        module = module.removesuffix(".__init__")
        modules[module] = tree
        for node in tree.body:
            candidates: list[tuple[ast.stmt, str | None]] = [(node, None)]
            if isinstance(node, ast.ClassDef):
                candidates = [(child, node.name) for child in node.body]
            for child, owner in candidates:
                if isinstance(child, ast.FunctionDef | ast.AsyncFunctionDef):
                    suffix = f"{owner}.{child.name}" if owner else child.name
                    name = f"{module}.{suffix}"
                    mangled = f"xǁ{owner}ǁ{child.name}" if owner else f"x_{child.name}"
                    functions[name] = Function(name, path, f"{module}.{mangled}__mutmut_*")
    return functions, modules


def x__functions__mutmut_67(
    files: dict[str, bytes], roots: list[str]
) -> tuple[dict[str, Function], dict[str, ast.Module]]:
    functions: dict[str, Function] = {}
    modules: dict[str, ast.Module] = {}
    for path, raw in sorted(files.items()):
        if not path.endswith(".py") or not any(path.startswith(root.rstrip("/") + "/") for root in roots):
            continue
        if re.search(rb"pragma\s*:\s*no\s+mutate", raw, re.IGNORECASE):
            raise MutationError(f"mutation suppression is forbidden: {path}")
        try:
            tree = ast.parse(raw, filename=path)
        except (SyntaxError, ValueError) as exc:
            raise MutationError(f"production Python cannot be parsed: {path}") from exc
        module = path.removesuffix(".py").replace("/", ".").removeprefix("src.")
        module = module.removesuffix(".__init__")
        modules[module] = tree
        for node in tree.body:
            candidates: list[tuple[ast.stmt, str | None]] = [(node, None)]
            if isinstance(node, ast.ClassDef):
                candidates = [(child, node.name) for child in node.body]
            for child, owner in candidates:
                if isinstance(child, ast.FunctionDef | ast.AsyncFunctionDef):
                    suffix = f"{owner}.{child.name}" if owner else child.name
                    name = f"{module}.{suffix}"
                    mangled = f"xǁ{owner}ǁ{child.name}" if owner else f"x_{child.name}"
                    functions[name] = Function(name, path, child, )
    return functions, modules

mutants_x__functions__mutmut['_mutmut_orig'] = x__functions__mutmut_orig # type: ignore # mutmut generated
mutants_x__functions__mutmut['x__functions__mutmut_1'] = x__functions__mutmut_1 # type: ignore # mutmut generated
mutants_x__functions__mutmut['x__functions__mutmut_2'] = x__functions__mutmut_2 # type: ignore # mutmut generated
mutants_x__functions__mutmut['x__functions__mutmut_3'] = x__functions__mutmut_3 # type: ignore # mutmut generated
mutants_x__functions__mutmut['x__functions__mutmut_4'] = x__functions__mutmut_4 # type: ignore # mutmut generated
mutants_x__functions__mutmut['x__functions__mutmut_5'] = x__functions__mutmut_5 # type: ignore # mutmut generated
mutants_x__functions__mutmut['x__functions__mutmut_6'] = x__functions__mutmut_6 # type: ignore # mutmut generated
mutants_x__functions__mutmut['x__functions__mutmut_7'] = x__functions__mutmut_7 # type: ignore # mutmut generated
mutants_x__functions__mutmut['x__functions__mutmut_8'] = x__functions__mutmut_8 # type: ignore # mutmut generated
mutants_x__functions__mutmut['x__functions__mutmut_9'] = x__functions__mutmut_9 # type: ignore # mutmut generated
mutants_x__functions__mutmut['x__functions__mutmut_10'] = x__functions__mutmut_10 # type: ignore # mutmut generated
mutants_x__functions__mutmut['x__functions__mutmut_11'] = x__functions__mutmut_11 # type: ignore # mutmut generated
mutants_x__functions__mutmut['x__functions__mutmut_12'] = x__functions__mutmut_12 # type: ignore # mutmut generated
mutants_x__functions__mutmut['x__functions__mutmut_13'] = x__functions__mutmut_13 # type: ignore # mutmut generated
mutants_x__functions__mutmut['x__functions__mutmut_14'] = x__functions__mutmut_14 # type: ignore # mutmut generated
mutants_x__functions__mutmut['x__functions__mutmut_15'] = x__functions__mutmut_15 # type: ignore # mutmut generated
mutants_x__functions__mutmut['x__functions__mutmut_16'] = x__functions__mutmut_16 # type: ignore # mutmut generated
mutants_x__functions__mutmut['x__functions__mutmut_17'] = x__functions__mutmut_17 # type: ignore # mutmut generated
mutants_x__functions__mutmut['x__functions__mutmut_18'] = x__functions__mutmut_18 # type: ignore # mutmut generated
mutants_x__functions__mutmut['x__functions__mutmut_19'] = x__functions__mutmut_19 # type: ignore # mutmut generated
mutants_x__functions__mutmut['x__functions__mutmut_20'] = x__functions__mutmut_20 # type: ignore # mutmut generated
mutants_x__functions__mutmut['x__functions__mutmut_21'] = x__functions__mutmut_21 # type: ignore # mutmut generated
mutants_x__functions__mutmut['x__functions__mutmut_22'] = x__functions__mutmut_22 # type: ignore # mutmut generated
mutants_x__functions__mutmut['x__functions__mutmut_23'] = x__functions__mutmut_23 # type: ignore # mutmut generated
mutants_x__functions__mutmut['x__functions__mutmut_24'] = x__functions__mutmut_24 # type: ignore # mutmut generated
mutants_x__functions__mutmut['x__functions__mutmut_25'] = x__functions__mutmut_25 # type: ignore # mutmut generated
mutants_x__functions__mutmut['x__functions__mutmut_26'] = x__functions__mutmut_26 # type: ignore # mutmut generated
mutants_x__functions__mutmut['x__functions__mutmut_27'] = x__functions__mutmut_27 # type: ignore # mutmut generated
mutants_x__functions__mutmut['x__functions__mutmut_28'] = x__functions__mutmut_28 # type: ignore # mutmut generated
mutants_x__functions__mutmut['x__functions__mutmut_29'] = x__functions__mutmut_29 # type: ignore # mutmut generated
mutants_x__functions__mutmut['x__functions__mutmut_30'] = x__functions__mutmut_30 # type: ignore # mutmut generated
mutants_x__functions__mutmut['x__functions__mutmut_31'] = x__functions__mutmut_31 # type: ignore # mutmut generated
mutants_x__functions__mutmut['x__functions__mutmut_32'] = x__functions__mutmut_32 # type: ignore # mutmut generated
mutants_x__functions__mutmut['x__functions__mutmut_33'] = x__functions__mutmut_33 # type: ignore # mutmut generated
mutants_x__functions__mutmut['x__functions__mutmut_34'] = x__functions__mutmut_34 # type: ignore # mutmut generated
mutants_x__functions__mutmut['x__functions__mutmut_35'] = x__functions__mutmut_35 # type: ignore # mutmut generated
mutants_x__functions__mutmut['x__functions__mutmut_36'] = x__functions__mutmut_36 # type: ignore # mutmut generated
mutants_x__functions__mutmut['x__functions__mutmut_37'] = x__functions__mutmut_37 # type: ignore # mutmut generated
mutants_x__functions__mutmut['x__functions__mutmut_38'] = x__functions__mutmut_38 # type: ignore # mutmut generated
mutants_x__functions__mutmut['x__functions__mutmut_39'] = x__functions__mutmut_39 # type: ignore # mutmut generated
mutants_x__functions__mutmut['x__functions__mutmut_40'] = x__functions__mutmut_40 # type: ignore # mutmut generated
mutants_x__functions__mutmut['x__functions__mutmut_41'] = x__functions__mutmut_41 # type: ignore # mutmut generated
mutants_x__functions__mutmut['x__functions__mutmut_42'] = x__functions__mutmut_42 # type: ignore # mutmut generated
mutants_x__functions__mutmut['x__functions__mutmut_43'] = x__functions__mutmut_43 # type: ignore # mutmut generated
mutants_x__functions__mutmut['x__functions__mutmut_44'] = x__functions__mutmut_44 # type: ignore # mutmut generated
mutants_x__functions__mutmut['x__functions__mutmut_45'] = x__functions__mutmut_45 # type: ignore # mutmut generated
mutants_x__functions__mutmut['x__functions__mutmut_46'] = x__functions__mutmut_46 # type: ignore # mutmut generated
mutants_x__functions__mutmut['x__functions__mutmut_47'] = x__functions__mutmut_47 # type: ignore # mutmut generated
mutants_x__functions__mutmut['x__functions__mutmut_48'] = x__functions__mutmut_48 # type: ignore # mutmut generated
mutants_x__functions__mutmut['x__functions__mutmut_49'] = x__functions__mutmut_49 # type: ignore # mutmut generated
mutants_x__functions__mutmut['x__functions__mutmut_50'] = x__functions__mutmut_50 # type: ignore # mutmut generated
mutants_x__functions__mutmut['x__functions__mutmut_51'] = x__functions__mutmut_51 # type: ignore # mutmut generated
mutants_x__functions__mutmut['x__functions__mutmut_52'] = x__functions__mutmut_52 # type: ignore # mutmut generated
mutants_x__functions__mutmut['x__functions__mutmut_53'] = x__functions__mutmut_53 # type: ignore # mutmut generated
mutants_x__functions__mutmut['x__functions__mutmut_54'] = x__functions__mutmut_54 # type: ignore # mutmut generated
mutants_x__functions__mutmut['x__functions__mutmut_55'] = x__functions__mutmut_55 # type: ignore # mutmut generated
mutants_x__functions__mutmut['x__functions__mutmut_56'] = x__functions__mutmut_56 # type: ignore # mutmut generated
mutants_x__functions__mutmut['x__functions__mutmut_57'] = x__functions__mutmut_57 # type: ignore # mutmut generated
mutants_x__functions__mutmut['x__functions__mutmut_58'] = x__functions__mutmut_58 # type: ignore # mutmut generated
mutants_x__functions__mutmut['x__functions__mutmut_59'] = x__functions__mutmut_59 # type: ignore # mutmut generated
mutants_x__functions__mutmut['x__functions__mutmut_60'] = x__functions__mutmut_60 # type: ignore # mutmut generated
mutants_x__functions__mutmut['x__functions__mutmut_61'] = x__functions__mutmut_61 # type: ignore # mutmut generated
mutants_x__functions__mutmut['x__functions__mutmut_62'] = x__functions__mutmut_62 # type: ignore # mutmut generated
mutants_x__functions__mutmut['x__functions__mutmut_63'] = x__functions__mutmut_63 # type: ignore # mutmut generated
mutants_x__functions__mutmut['x__functions__mutmut_64'] = x__functions__mutmut_64 # type: ignore # mutmut generated
mutants_x__functions__mutmut['x__functions__mutmut_65'] = x__functions__mutmut_65 # type: ignore # mutmut generated
mutants_x__functions__mutmut['x__functions__mutmut_66'] = x__functions__mutmut_66 # type: ignore # mutmut generated
mutants_x__functions__mutmut['x__functions__mutmut_67'] = x__functions__mutmut_67 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_select_scope__mutmut)
def select_scope(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_orig(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_1(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = True
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_2(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = None
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_3(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(None, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_4(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, None)
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_5(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_6(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, )
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_7(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["XXsource_rootsXX"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_8(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["SOURCE_ROOTS"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_9(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = None
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_10(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(None, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_11(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, None)
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_12(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_13(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, )
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_14(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["XXsource_rootsXX"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_15(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["SOURCE_ROOTS"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_16(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = None
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_17(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old and ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_18(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_19(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(None) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_20(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) == ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_21(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(None)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_22(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = None
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_23(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) & set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_24(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(None) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_25(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(None)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_26(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(None)
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_27(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(None, 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_28(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", None)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_29(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_30(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", )[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_31(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.split(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_32(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit("XX.xXX", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_33(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".X", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_34(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 2)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_35(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[1] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_36(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() + functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_37(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = None
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_38(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=None,
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_39(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=None,
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_40(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_41(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_42(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_43(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = None
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_44(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(None)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_45(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = None
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_46(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=None,
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_47(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=None,
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_48(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_49(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_50(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_51(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None and ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_52(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is not None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_53(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(None) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_54(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) == ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_55(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(None):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_56(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(None)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_57(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = None
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_58(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(None, 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_59(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", None)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_60(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_61(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", )[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_62(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.split(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_63(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit("XX.xXX", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_64(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".X", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_65(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 2)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_66(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[1]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_67(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module not in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_68(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(None)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_69(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = None
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_70(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = None
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_71(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(None)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_72(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) or node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_73(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports | changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_74(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(None)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_75(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = None
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_76(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(None) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_77(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(None)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_78(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = None
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_79(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(None)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_80(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = None
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_81(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = None
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_82(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = None
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_83(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(None, 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_84(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", None)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_85(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_86(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", )[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_87(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.split(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_88(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit("XX.xXX", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_89(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".X", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_90(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 2)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_91(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[1]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_92(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = None
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_93(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = None
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_94(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) or node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_95(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = None
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_96(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = None
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_97(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." - prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_98(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) - "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_99(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(None) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_100(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = "XX.XX".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_101(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(None)[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_102(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split("XX.XX")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_103(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: +node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_104(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "XX.XX" + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_105(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    None
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_106(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname and alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_107(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." - alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_108(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix - "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_109(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "XX.XX" + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_110(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update(None)
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_111(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname and alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_112(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = None
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_113(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = None
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_114(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = None
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_115(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name) or statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_116(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign) or isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_117(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = None
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_118(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = None
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_119(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(None)
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_120(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(None))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_121(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = None
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_122(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = None
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_123(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = None
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_124(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(None, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_125(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, None)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_126(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_127(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, )
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_128(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." - expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_129(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module - "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_130(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "XX.XX" + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_131(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = None
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_132(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved and candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_133(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate != resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_134(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(None)
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_135(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved - ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_136(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + "XX.XX")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_137(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(None)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_138(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values or expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_139(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id not in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_140(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_141(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(None)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_142(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(None)
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_143(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(None))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_144(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases or not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_145(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id not in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_146(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_147(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = None
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_148(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(None)[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_149(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split("XX.XX")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_150(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[1]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_151(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        None
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_152(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(None)
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_153(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package - ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_154(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + "XX.XX")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_155(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id not in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_156(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"XX__import__XX", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_157(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__IMPORT__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_158(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "XXgetattrXX", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_159(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "GETATTR", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_160(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "XXglobalsXX", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_161(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "GLOBALS", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_162(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "XXlocalsXX", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_163(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "LOCALS", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_164(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "XXevalXX", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_165(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "EVAL", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_166(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "XXexecXX"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_167(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "EXEC"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_168(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(None)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_169(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    None
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_170(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith(None)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_171(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." - expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_172(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("XX.XX" + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_173(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr != "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_174(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "XXimport_moduleXX":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_175(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "IMPORT_MODULE":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_176(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(None)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_177(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies + selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_178(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(None)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_179(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(None)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_180(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "XXmodeXX": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_181(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "MODE": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_182(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "XXbroadXX" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_183(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "BROAD" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_184(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "XXchangedXX",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_185(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "CHANGED",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_186(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "XXrequiredXX": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_187(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "REQUIRED": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_188(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(None) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_189(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(None),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_190(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed and changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_191(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "XXchanged_functionsXX": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_192(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "CHANGED_FUNCTIONS": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_193(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(None),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_194(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "XXfunctionsXX": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_195(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "FUNCTIONS": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_196(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "XXnameXX": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_197(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "NAME": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_198(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "XXpathXX": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_199(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "PATH": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_200(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "XXselectorXX": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_201(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "SELECTOR": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_202(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "XXdefinition_digestXX": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_203(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "DEFINITION_DIGEST": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_204(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(None),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_205(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(None)
        ],
        "policy_digest": payload_digest(policy),
    }


def x_select_scope__mutmut_206(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "XXpolicy_digestXX": payload_digest(policy),
    }


def x_select_scope__mutmut_207(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "POLICY_DIGEST": payload_digest(policy),
    }


def x_select_scope__mutmut_208(
    base_files: dict[str, bytes], head_files: dict[str, bytes], policy: dict[str, Any], *, broad: bool = False
) -> dict[str, Any]:
    """Select changed functions and transitive statically referenced production functions.

    Module-level changes include every function in the affected module.
    Attribute dispatch is conservatively matched by method name; dynamic import
    causes whole-package closure rather than silently omitting dependencies.
    """
    functions, modules = _functions(head_files, policy["source_roots"])
    old, old_modules = _functions(base_files, policy["source_roots"])
    changed = {
        name
        for name, item in functions.items()
        if name not in old or ast.dump(item.node) != ast.dump(old[name].node)
    }
    changed_modules = set(old_modules) ^ set(modules)
    changed_modules.update(old[name].selector.rsplit(".x", 1)[0] for name in old.keys() - functions.keys())
    for name, tree in modules.items():
        outside = ast.Module(
            body=[node for node in tree.body if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)],
            type_ignores=[],
        )
        previous = old_modules.get(name)
        before = (
            ast.Module(
                body=[
                    node
                    for node in previous.body
                    if not isinstance(node, ast.FunctionDef | ast.AsyncFunctionDef)
                ],
                type_ignores=[],
            )
            if previous
            else None
        )
        if before is None or ast.dump(outside) != ast.dump(before):
            changed_modules.add(name)
    for name, item in functions.items():
        module = item.selector.rsplit(".x", 1)[0]
        if module in changed_modules:
            changed.add(name)
        tree = modules[module]
        imports = {alias.name for node in tree.body if isinstance(node, ast.Import) for alias in node.names}
        imports.update(node.module for node in tree.body if isinstance(node, ast.ImportFrom) and node.module)
        if imports & changed_modules:
            changed.add(name)
    selected = set(functions) if broad else set(changed)
    queue = list(selected)
    while queue:
        name = queue.pop()
        item = functions[name]
        module = item.selector.rsplit(".x", 1)[0]
        tree = modules[module]
        aliases: dict[str, str] = {}
        for node in tree.body:
            if isinstance(node, ast.ImportFrom) and node.module:
                prefix = node.module
                if node.level:
                    prefix = ".".join(module.split(".")[: -node.level]) + "." + prefix
                aliases.update(
                    {alias.asname or alias.name: prefix + "." + alias.name for alias in node.names}
                )
            elif isinstance(node, ast.Import):
                aliases.update({alias.asname or alias.name: alias.name for alias in node.names})
        dependencies: set[str] = set()
        global_values: dict[str, ast.expr] = {}
        for statement in tree.body:
            if isinstance(statement, ast.Assign):
                for target in statement.targets:
                    if isinstance(target, ast.Name):
                        global_values[target.id] = statement.value
            elif (
                isinstance(statement, ast.AnnAssign)
                and isinstance(statement.target, ast.Name)
                and statement.value
            ):
                global_values[statement.target.id] = statement.value
        pending = list(ast.walk(item.node))
        visited_globals: set[str] = set()
        while pending:
            expression = pending.pop()
            if isinstance(expression, ast.Name):
                resolved = aliases.get(expression.id, module + "." + expression.id)
                direct = {
                    candidate
                    for candidate in functions
                    if candidate == resolved or candidate.startswith(resolved + ".")
                }
                dependencies.update(direct)
                if expression.id in global_values and expression.id not in visited_globals:
                    visited_globals.add(expression.id)
                    pending.extend(ast.walk(global_values[expression.id]))
                if expression.id in aliases and not direct:
                    package = resolved.split(".")[0]
                    dependencies.update(
                        candidate for candidate in functions if candidate.startswith(package + ".")
                    )
                if expression.id in {"__import__", "getattr", "globals", "locals", "eval", "exec"}:
                    dependencies.update(functions)
            if isinstance(expression, ast.Attribute):
                dependencies.update(
                    candidate for candidate in functions if candidate.endswith("." + expression.attr)
                )
                if expression.attr == "import_module":
                    dependencies.update(functions)
        for dependency in dependencies - selected:
            selected.add(dependency)
            queue.append(dependency)
    return {
        "mode": "broad" if broad else "changed",
        "required": bool(functions) if broad else bool(changed or changed_modules),
        "changed_functions": sorted(changed),
        "functions": [
            {
                "name": name,
                "path": functions[name].path,
                "selector": functions[name].selector,
                "definition_digest": definition_digest(functions[name].node),
            }
            for name in sorted(selected)
        ],
        "policy_digest": payload_digest(None),
    }

mutants_x_select_scope__mutmut['_mutmut_orig'] = x_select_scope__mutmut_orig # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_1'] = x_select_scope__mutmut_1 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_2'] = x_select_scope__mutmut_2 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_3'] = x_select_scope__mutmut_3 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_4'] = x_select_scope__mutmut_4 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_5'] = x_select_scope__mutmut_5 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_6'] = x_select_scope__mutmut_6 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_7'] = x_select_scope__mutmut_7 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_8'] = x_select_scope__mutmut_8 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_9'] = x_select_scope__mutmut_9 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_10'] = x_select_scope__mutmut_10 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_11'] = x_select_scope__mutmut_11 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_12'] = x_select_scope__mutmut_12 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_13'] = x_select_scope__mutmut_13 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_14'] = x_select_scope__mutmut_14 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_15'] = x_select_scope__mutmut_15 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_16'] = x_select_scope__mutmut_16 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_17'] = x_select_scope__mutmut_17 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_18'] = x_select_scope__mutmut_18 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_19'] = x_select_scope__mutmut_19 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_20'] = x_select_scope__mutmut_20 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_21'] = x_select_scope__mutmut_21 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_22'] = x_select_scope__mutmut_22 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_23'] = x_select_scope__mutmut_23 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_24'] = x_select_scope__mutmut_24 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_25'] = x_select_scope__mutmut_25 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_26'] = x_select_scope__mutmut_26 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_27'] = x_select_scope__mutmut_27 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_28'] = x_select_scope__mutmut_28 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_29'] = x_select_scope__mutmut_29 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_30'] = x_select_scope__mutmut_30 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_31'] = x_select_scope__mutmut_31 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_32'] = x_select_scope__mutmut_32 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_33'] = x_select_scope__mutmut_33 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_34'] = x_select_scope__mutmut_34 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_35'] = x_select_scope__mutmut_35 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_36'] = x_select_scope__mutmut_36 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_37'] = x_select_scope__mutmut_37 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_38'] = x_select_scope__mutmut_38 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_39'] = x_select_scope__mutmut_39 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_40'] = x_select_scope__mutmut_40 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_41'] = x_select_scope__mutmut_41 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_42'] = x_select_scope__mutmut_42 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_43'] = x_select_scope__mutmut_43 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_44'] = x_select_scope__mutmut_44 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_45'] = x_select_scope__mutmut_45 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_46'] = x_select_scope__mutmut_46 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_47'] = x_select_scope__mutmut_47 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_48'] = x_select_scope__mutmut_48 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_49'] = x_select_scope__mutmut_49 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_50'] = x_select_scope__mutmut_50 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_51'] = x_select_scope__mutmut_51 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_52'] = x_select_scope__mutmut_52 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_53'] = x_select_scope__mutmut_53 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_54'] = x_select_scope__mutmut_54 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_55'] = x_select_scope__mutmut_55 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_56'] = x_select_scope__mutmut_56 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_57'] = x_select_scope__mutmut_57 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_58'] = x_select_scope__mutmut_58 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_59'] = x_select_scope__mutmut_59 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_60'] = x_select_scope__mutmut_60 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_61'] = x_select_scope__mutmut_61 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_62'] = x_select_scope__mutmut_62 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_63'] = x_select_scope__mutmut_63 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_64'] = x_select_scope__mutmut_64 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_65'] = x_select_scope__mutmut_65 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_66'] = x_select_scope__mutmut_66 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_67'] = x_select_scope__mutmut_67 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_68'] = x_select_scope__mutmut_68 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_69'] = x_select_scope__mutmut_69 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_70'] = x_select_scope__mutmut_70 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_71'] = x_select_scope__mutmut_71 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_72'] = x_select_scope__mutmut_72 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_73'] = x_select_scope__mutmut_73 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_74'] = x_select_scope__mutmut_74 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_75'] = x_select_scope__mutmut_75 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_76'] = x_select_scope__mutmut_76 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_77'] = x_select_scope__mutmut_77 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_78'] = x_select_scope__mutmut_78 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_79'] = x_select_scope__mutmut_79 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_80'] = x_select_scope__mutmut_80 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_81'] = x_select_scope__mutmut_81 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_82'] = x_select_scope__mutmut_82 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_83'] = x_select_scope__mutmut_83 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_84'] = x_select_scope__mutmut_84 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_85'] = x_select_scope__mutmut_85 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_86'] = x_select_scope__mutmut_86 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_87'] = x_select_scope__mutmut_87 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_88'] = x_select_scope__mutmut_88 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_89'] = x_select_scope__mutmut_89 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_90'] = x_select_scope__mutmut_90 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_91'] = x_select_scope__mutmut_91 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_92'] = x_select_scope__mutmut_92 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_93'] = x_select_scope__mutmut_93 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_94'] = x_select_scope__mutmut_94 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_95'] = x_select_scope__mutmut_95 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_96'] = x_select_scope__mutmut_96 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_97'] = x_select_scope__mutmut_97 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_98'] = x_select_scope__mutmut_98 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_99'] = x_select_scope__mutmut_99 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_100'] = x_select_scope__mutmut_100 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_101'] = x_select_scope__mutmut_101 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_102'] = x_select_scope__mutmut_102 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_103'] = x_select_scope__mutmut_103 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_104'] = x_select_scope__mutmut_104 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_105'] = x_select_scope__mutmut_105 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_106'] = x_select_scope__mutmut_106 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_107'] = x_select_scope__mutmut_107 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_108'] = x_select_scope__mutmut_108 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_109'] = x_select_scope__mutmut_109 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_110'] = x_select_scope__mutmut_110 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_111'] = x_select_scope__mutmut_111 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_112'] = x_select_scope__mutmut_112 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_113'] = x_select_scope__mutmut_113 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_114'] = x_select_scope__mutmut_114 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_115'] = x_select_scope__mutmut_115 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_116'] = x_select_scope__mutmut_116 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_117'] = x_select_scope__mutmut_117 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_118'] = x_select_scope__mutmut_118 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_119'] = x_select_scope__mutmut_119 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_120'] = x_select_scope__mutmut_120 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_121'] = x_select_scope__mutmut_121 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_122'] = x_select_scope__mutmut_122 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_123'] = x_select_scope__mutmut_123 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_124'] = x_select_scope__mutmut_124 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_125'] = x_select_scope__mutmut_125 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_126'] = x_select_scope__mutmut_126 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_127'] = x_select_scope__mutmut_127 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_128'] = x_select_scope__mutmut_128 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_129'] = x_select_scope__mutmut_129 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_130'] = x_select_scope__mutmut_130 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_131'] = x_select_scope__mutmut_131 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_132'] = x_select_scope__mutmut_132 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_133'] = x_select_scope__mutmut_133 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_134'] = x_select_scope__mutmut_134 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_135'] = x_select_scope__mutmut_135 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_136'] = x_select_scope__mutmut_136 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_137'] = x_select_scope__mutmut_137 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_138'] = x_select_scope__mutmut_138 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_139'] = x_select_scope__mutmut_139 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_140'] = x_select_scope__mutmut_140 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_141'] = x_select_scope__mutmut_141 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_142'] = x_select_scope__mutmut_142 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_143'] = x_select_scope__mutmut_143 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_144'] = x_select_scope__mutmut_144 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_145'] = x_select_scope__mutmut_145 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_146'] = x_select_scope__mutmut_146 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_147'] = x_select_scope__mutmut_147 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_148'] = x_select_scope__mutmut_148 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_149'] = x_select_scope__mutmut_149 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_150'] = x_select_scope__mutmut_150 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_151'] = x_select_scope__mutmut_151 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_152'] = x_select_scope__mutmut_152 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_153'] = x_select_scope__mutmut_153 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_154'] = x_select_scope__mutmut_154 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_155'] = x_select_scope__mutmut_155 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_156'] = x_select_scope__mutmut_156 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_157'] = x_select_scope__mutmut_157 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_158'] = x_select_scope__mutmut_158 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_159'] = x_select_scope__mutmut_159 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_160'] = x_select_scope__mutmut_160 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_161'] = x_select_scope__mutmut_161 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_162'] = x_select_scope__mutmut_162 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_163'] = x_select_scope__mutmut_163 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_164'] = x_select_scope__mutmut_164 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_165'] = x_select_scope__mutmut_165 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_166'] = x_select_scope__mutmut_166 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_167'] = x_select_scope__mutmut_167 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_168'] = x_select_scope__mutmut_168 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_169'] = x_select_scope__mutmut_169 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_170'] = x_select_scope__mutmut_170 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_171'] = x_select_scope__mutmut_171 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_172'] = x_select_scope__mutmut_172 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_173'] = x_select_scope__mutmut_173 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_174'] = x_select_scope__mutmut_174 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_175'] = x_select_scope__mutmut_175 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_176'] = x_select_scope__mutmut_176 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_177'] = x_select_scope__mutmut_177 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_178'] = x_select_scope__mutmut_178 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_179'] = x_select_scope__mutmut_179 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_180'] = x_select_scope__mutmut_180 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_181'] = x_select_scope__mutmut_181 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_182'] = x_select_scope__mutmut_182 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_183'] = x_select_scope__mutmut_183 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_184'] = x_select_scope__mutmut_184 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_185'] = x_select_scope__mutmut_185 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_186'] = x_select_scope__mutmut_186 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_187'] = x_select_scope__mutmut_187 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_188'] = x_select_scope__mutmut_188 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_189'] = x_select_scope__mutmut_189 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_190'] = x_select_scope__mutmut_190 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_191'] = x_select_scope__mutmut_191 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_192'] = x_select_scope__mutmut_192 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_193'] = x_select_scope__mutmut_193 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_194'] = x_select_scope__mutmut_194 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_195'] = x_select_scope__mutmut_195 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_196'] = x_select_scope__mutmut_196 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_197'] = x_select_scope__mutmut_197 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_198'] = x_select_scope__mutmut_198 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_199'] = x_select_scope__mutmut_199 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_200'] = x_select_scope__mutmut_200 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_201'] = x_select_scope__mutmut_201 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_202'] = x_select_scope__mutmut_202 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_203'] = x_select_scope__mutmut_203 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_204'] = x_select_scope__mutmut_204 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_205'] = x_select_scope__mutmut_205 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_206'] = x_select_scope__mutmut_206 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_207'] = x_select_scope__mutmut_207 # type: ignore # mutmut generated
mutants_x_select_scope__mutmut['x_select_scope__mutmut_208'] = x_select_scope__mutmut_208 # type: ignore # mutmut generated
